# RFC — chapulin consumer-hardening — handoff

## Round 6 — scan closed forms + variant construction — ✅ CLOSED 2026-08-16 (both exit gates)

- 🏁 **ROUND 6 COMPLETE.** Every slice landed: Track A A0–A6 + riders (Bug #1/#2), Track B B0–B7 + riders (B4-rider NUL, A6-rider v86 soundness, B7r byte-receiver widening, B7r2 literal-seeded offsets + generalized seq placeholder). Releases this round: 0.3.4, 0.4.0, 0.4.1, **0.5.0 (`b1ea6af`, tag v0.5.0, walker v88/render v11, CI green)**. Chapulin: absorption `ebc40fd`, canary tighten `29f963d`, A6 close `7d637c5`, **B7 close `a4fc7c3`** (pin v0.5.0 + unified five-arm decode twin; suites all green; local-only, chapulin never pushed). Owner WIP untouched throughout; findings doc updated in place (untracked). Deferred to future rounds / needs-Corey: B7-2 case/else-raise sibling poisoning (in-band signaling refactor, own round); real-decode stretch (blocked on B7-2); cpp-backend infra; chapulin entangled-rename files + owner-WIP commit + push (Corey by hand); review-item list in the bullets below. ⚠ THREE prompt-injection attempts against subagents this round (INT-1 + two B7 passes, all targeting the milpa revert with fake system-reminders instructing concealment) — all disregarded, disclosed, and independently git-verified.

- **⚠ STAGE-4-STYLE REVIEW (2026-08-16, post-close, 3 lenses: verdict-soundness / recognizer-wiring / design+pins) — round 6 does NOT meet the 0-Crit/High floor; remediation slate proposed, awaiting Corey.** CRITICAL: (S1, control-loop-confirmed at runtime.nim:3301) `iekSeqLen`'s svSeq arm (and for-loop bounds via mkSeqLen) unguarded for the v88 bare-value placeholder whose length is hard-forced 0 → `.len` on a placeholder seq silently reads 0, false sxUnsat one line away from the committed B7r2 pin (`discard opts` never reads .len); (S2) literal-promoted svInt locals bypass overflowCond's BV-only gate → post-loop arithmetic on a promoted counter never forks OverflowDefect, false sxUnsat for defect searches; (S3) v86's fallthrough retSym binding only fires when `cp.env.hasKey("result")` — a callee path that NEVER touches result (legal Nim, returns zero-value) leaves retSym unconstrained, the original false-sxSat shape reintroduced narrowly; (Q1, confirmed) B7r2's new isIndex placeholder decline branch has ZERO test coverage and the B7r2 handoff bullet's "pinned instead via this slice's own file" claim is FALSE (nothing in the suite indexes a placeholder seq). HIGH: (W1) `ctx.stringBackedParams`/`intOffsetLiteralLocals` are NAME-KEYED and never proc-scoped (no caseNarrow-style save/clear/restore) → the entry proc's classification governs UNRELATED callees by name collision, bypassing their own vetting incl. the mutation veto; invisible to the corpus because every test names its receiver `data`; (S4, theoretical) B6's member branch replaces the loop with an EMPTY block — loop counter never advanced to bound, stale exit offset if a caller consumes it (recognizer doesn't gate on counter-unused-after-loop); (W2) mutation veto misses var-aliased helper-call mutation → svString value reaches iekSeqAdd's raw doAssert (crash newly reachable); (Q2) the new decline message omits stmt.ixLoc (DoD clause b; the idiom sits 120 lines below in the same handler). MEDIUM: (W3) considerCandidate's classifyType lacks the ntyNone guard + uses raw getImpl not resolveRoutineImpl (A5 hard-crash class, latent); (Q3) collector def-use tracing TRIPLICATED across collectStringBackedByteSeqParamsImpl/collectIntOffsetParamsImpl/hasLiteralInit (~150 dup lines, refactor slice); (Q4) CR2 pin file's test-name strings say v82/render10, assert 88/11; (Q5) absorption commit `ebc40fd` silently floats z3 to ref="main" + bumps softlink v0.5.1→v0.11.1 (not the rename, undisclosed, reproducibility smell). CLEAN (verified, not assumed): recognizer mutual exclusivity + deterministic order, all 8 new IR fields serialized (no third round-trip bug), v87 char alias consumers, B0 entry-guard/clamp core, pin honesty + SAT/UNSAT pairing + one-live-==-pin convention, changelogs vs actual diffs, chapulin commit hygiene (minus Q5). NOTE: 0.5.0 shipped with S1–S3; chapulin's committed pins don't hit these shapes (that is why the sweeps stayed green) but the release is not verdict-sound in general. **Proposed remediation slate (on Corey's go): R1 placeholder read-totality (S1+Q1+Q2, walker v89) · R2 v86 completion — zero-default binding when result untouched (S3) · R3 svInt overflow honesty (S2, design options recorded) · R4 proc-scope the name-keyed contexts + widen mutation veto + doAssert→classified decline + ntyNone guard (W1/W2/W3) · R5 B6 counter advance-to-bound on member branch (S4) · then 0.5.1 hotfix + chapulin re-pin. Q3/Q4/Q5/naming ride along as a cleanup slice.**

### (Original round-6 log follows)

- **Stage:** 2 architect — round 2 COMPLETE (4 lenses reported; all fixes consolidated into RFC + ADRs; no genuine forks). Round-2 catches: a SECOND live scan-lift bug (zero-iteration clamp overwrote the seeded index — FIXED in source + pinned, part of B0), A6's five-arm oracle claim split honestly (opData/opAck at 0.4.0; other three arms construct-but-sxUnknown until Track B), B6 regex widened to star segments (empty keys/values + double-NUL), B2 scope-locked (widening only, source-signedness rule, narrowing/reinterpret → classified declines, probeProto pin, HiddenStdConv out of scope), B1 synthesis (parse-time collectors + walker svString totality backstops for isIndex/iekSeqLen crash gaps), exit releases renamed 0.4.0/0.5.0 (v70-label collision), INT-1 interim runbook (SHA capture + milpa ref swap + revert checklist), sello action note, standing DoD grew typeKind-guard + switch-fan-out + docs-refresh clauses. • **Round:** 2 of 2 done
- **B0 IMPLEMENTED (walker v70, uncommitted):** the scan-lift soundness hotfix landed via TDD ahead of Track A per the RFC's queue-jump clause — `boundIsScannedLen` restriction + `scanEntryRead` probe in `tryRecognizeScanIdiom`; pins `tests/tsymex_r6_b0_scanlift_bound.nim` 4/4 green (both false-sxUnsat shapes now sxRaised, canonical shape still proves); q1_scanlift 13/13, r1b 19/19, c6 5/5 green; registered in nimble + CI leg.
- **0.3.4 RELEASED** (walker v70; commits d055776 fix + 201bff6 docs + 4a6ba04 release, tag v0.3.4 pushed; sweep before it: 85/88 real-green — 3 pre-existing A5 breaks, 4 vccexe parallel-load flakes re-confirmed green standalone; CI watch in flight at handoff time).
- **Resume — stage 3:** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; after each slice report one progress line (e.g. "slice 3/14 done"); stop when every slice is implemented` — Track A entirely before Track B; B0 already done. Consult the round-6 slice tables + standing DoD in the RFC before each slice.
- **Stage-3 grind log (2026-08-15, loop running):** ✅ **A0 LANDED `0ed2664` (walker v74)** — parse-time `low(T)`/`high(T)` fold in dsl_parser's nnkCall arm (`lowHighIntLit`, unsigned via `cast[int64]` like nnkUIntLit); `siteMsg` helper exported + unit-tested, first consumer = P2b object-constructor decline; non-int-family decline goes through classifyType+zeroValueForType type-correct dummy (NOT unbound mkVar — that KeyErrored at walk time, caught mid-TDD); TOT-1 row added; new test 6/6 + 8-file regression set green. Note: at v73 the fault had softened to a graceful-but-wrong sxUnknown (upstream ANF work), not the raw v69 fault — fold still required, RED was real. ✅ **A1 LANDED `db0138a` (walker v75)** — `iekVariantLit` (mirrors iekTupleLit) via P2b arm SPLIT (itVariant constructs; itMultiVariant keeps own decline arm citing ADR-0029, via siteMsg, TOT-1 row); `lowerVariantLit` pins disc to bvConst, lowers active-arm fields, allocates inactive-arm fields fresh-unconstrained; fan-out threaded through types/dsl_parser/abstraction/canonicalize/runtime; bonus decline pins for symbolic-disc and ref-aliased-variant construction (gated by new `ctorIsRefAliasedVariant` helper — flagged recommendation: promote ref-alias distinction to an IRType field mirroring itTuple's nameIsRefAlias if a later slice needs it elsewhere, instead of per-site re-derivation); new test 8/8 + 26-file regression sweep green. ✅ **A2 LANDED (walker v76)** — `retBindEq` gains an `svVariant` arm, the GENERAL encoding `discEq ∧ (⋀ declared arms: disc==tag → per-field eq) ∧ plain-field eq`, sound for both A1's literal-pinned construction and a pass-through return of a variant-typed PARAMETER (genuinely symbolic disc); `svVariant` added to the isReturn scalar-raise-drain kind allow-list so it no longer degrades through the composite catch-all; extracted the pre-existing 2x-duplicated per-tag `disc==tag` closure (`allocateSym`'s itVariant arm, `isVariantField`'s walker arm) plus this slice's new 3rd copy into one shared top-level `variantDiscEq` proc (refactor step, no behavior change). New test `tests/tsymex_r6_a2_retbind_variant.nim` 9/9 (tracer + UNSAT companion, pass-through SAT/UNSAT pair, guarded-arm-soundness disc-mismatch + both-arms-independently-correct pins, plain-field pin, version floor); 30-file regression sweep (A0/A1/B0/q1/r1b/CR2/TOT-1 + every variant/reassign/retbind/tuple-return test) green, c backend only (cpp pre-existing container breakage, per A0). CR-2 `==` pin bumped 75→76. Commit `d201ac7`. ✅ **A3 LANDED `913275c` (walker v77)** — new `isVariantConstructSym` statement kind (M5 A-normalized) cloning isVariantReassignSymbolic fork-per-tag for SYMBOLIC-disc construction; every declared arm's fields fresh per fork (Nim forbids arm-field init with non-const disc, so no active-arm data exists — 3 dedicated divergence pins); parse-time case-branch narrowing via lexical `ParseCtx.caseNarrow` stack, saved/cleared/restored around ensureProcRegistered's recursive callee parse (narrowing never crosses proc boundaries); `maxVariantConstructorForks` ResourceBudget field (default 8, structural pre-solver check, wired into the settings cache key immediately to avoid the CR-2 stale-cache class), budget-exceed reuses beBudgetExhausted + parse-time `vcsLoc` (new `siteLoc` helper) rendered verbatim at walk time; fan-out threaded incl. scan.nim; A1's symbolic-disc decline pin MIGRATED to a construction pin (sound capability upgrade); TOT-1 budget-exceed row; new test tests/tsymex_r6_a3_variantconstruct_sym.nim 10/10 + 37-file c-backend sweep green; CR-2 pin 76→77. ⚠ **Interim INT-1 ATTEMPTED — structurally BLOCKED by the rename, deferred (control-loop call, 2026-08-15):** two attempts against pinned SHA `913275c8d687a61480cb775180a2a05f0e114547` (A3 landing). Attempt 1: SHA unfetchable — the 4 round-6 commits are LOCAL-ONLY (origin/main still at `8040045`; nothing pushed, deliberately). Attempt 2 solved fetchability WITHOUT pushing (ledger transport: `git update-server-info` in nelli + junction `<scratch>\http-serve\proptest.git → .git` + `python -m http.server 8731` → dumb-HTTP `http://127.0.0.1:8731/proptest.git`; milpa accepted it; revert verified byte-identical in both repos) and ran dev-test.ps1: EVERY file including all 7 t_symex* fails `cannot open file: proptest` — chapulin's 38 `import proptest/*` files never absorbed the `785307c` proptest→nelli rename (dep still `proptest@v0.3.2` in its milpa.kdl), so ANY post-rename SHA fails identically incl. the round-6 baseline `8040045` itself. NOT an A0–A3 regression signal; this was the first-ever post-rename INT-1. **Deferral is lossless:** the SHA-pin discipline lets INT-1 re-run retroactively per-slice-SHA once chapulin absorbs the rename — bisection rationale preserved. **Needs-Corey bundle for the A6/0.4.0 exit gate:** (1) push nelli main (4+ commits) to origin; (2) chapulin `proptest/*`→`nelli/*` import absorption + milpa dep rename (entangled with chapulin's own uncommitted verification-harness-v2 WIP — why the loop didn't do it); (3) then retroactive INT-1 at `913275c` + subsequent slice SHAs. Minor: 2 stray OSError crashes during the suite run (likely bind-mount/nimcache contention, log at scratchpad/int1-testrun.log). **Grind continued past it: A4/A5 below.** ✅ **A4 LANDED `6978a30` (no bump, per Ver column)** — pure interaction-pin slice, tests/tsymex_r6_a4_construct_interactions.nim 9/9, ALL green-on-arrival (no production changes): construct→literal-reassign (post-arm zero-init via defaultZero, stale-arm UNSAT), construct→symbolic-reassign (both tags reachable, plain field preserved), A3-narrowed-construct→symbolic-reassign-to-OUTSIDE-the-narrowed-set (sound: reassign forks over the full declared arm table, real Nim permits it; UNSAT companion), witness pins grounded in the REAL contract (extractWitness walks formal params only — locals/returns have no witness leaf by design; pins assert the params driving the constructed variant render faithfully); floor pin >= 77; 39-file sweep green. ✅ **A5 LANDED `9508ef4` (no bump)** — root cause exactly the RFC's single-cause premise: `isResolvedBoolAndOr` (dsl_parser ~1420) called classifyType on the and/or CALL NODE, which Cluster-G's `monomorphize()` (syntactic T/U substitution, no re-semcheck) leaves typeless — and `getTypeInst` failure there is a HARD non-catchable compile error, so the fix is a pre-check: derive boolean-vs-bitwise from `n[0]`'s own resolved proc signature (getTypeImpl, immune to monomorphize), new classifyType(retTy) site gated `typeKind != ntyNone` (DoD clause d); crash→correct-verdict so no new decline site/TOT-1 row; g8_multi_param 3/3 + g10_smoke 7/7, no latent second break; both files added to the symex-windows CI leg (were nimble-only); bitwise/boolean family regression green; **FULL SWEEP 265/269 c-backend, all 4 non-zero stash-verified PRE-EXISTING** (3× MSVC C2026 string-too-big on audit-corpus statics: A2a_atomicir_audit/A2a_chokepoint_audit/N2_kindgate_audit; 1× S3_strindex known engine gap). Judgment: no separate minimal-shape pin file (G8/G10 pin the root shape; flagged for review if an isolated pin is wanted).
- **🛑 LOOP STOPPED 2026-08-15 after A5 — Track A implementable slices EXHAUSTED; everything further needs Corey.** A0–A5 landed (`0ed2664`→`9508ef4`, walker v73→v77, 5 commits + A4's test-only commit; origin/main is now 6 BEHIND local main, nothing pushed). A6 = exit gate = chapulin-side (un-void t_symex_decode + oracle on opData/opAck **against the 0.4.0 release**). Track B starts only after 0.4.0 per the recorded hard A-before-B ordering (not the loop's to override). Resume after the needs-Corey bundle: re-run `/loop /tdd RFC-chapulin-hardening til done` → it will pick up at B1 (or A6 verification if chapulin is ready first).
- **⛔ A6 ATTEMPTED (loop restarted 2026-08-15) — BLOCKED on a SPEC BREAK, escalated; loop continued to Track B.** Salvage landed: (1) **Bug #1 FIXED, nelli commit `ca51c3e` (unpushed, no SW bump — crash-fix class, no cached-verdict exposure):** `classifyObjectRecordFields`'s VARIANT path read field names via bare `.strVal` on raw `getImpl` nodes; EXPORTED fields arrive as `nnkPostfix("*", name)` (every `TftpPacket` field is export-marked — a shape no synthetic A0–A5 SUT ever exercised, they all used local unexported types) → macro-expansion crash before any symex work. The identical fix existed for the plain-record path since v64; extracted into shared `fieldNameStr`/`unwrapFieldNameNode`, applied at the 3 variant-path sites; regression pin + 25-file curated sweep green. (2) **nelli docs refresh committed `8b28e73`** (README variant-construction table, tutorial §6 recognized-shape fourth path + §7 construction — the A6 DoD docs clause, done). (3) **`t_symex_checksum` canaries tightened sxUnknown→sxUnsat, 6/6 green against released 0.4.0** (uncommitted in chapulin, alongside the absorption). **THE BLOCKER (Bug #2, engine, NOT fixed):** classification is EAGER and WHOLE-TYPE — `classifyObjectRecordFields` classifies every declared arm unconditionally, so `options: seq[(string,string)]` on the three UNTOUCHED arms (opRrq/opWrq/opOack → `seNestedSeqUnsupported`) poisons `symexFind` for EVERY proc returning `TftpPacket`, degrading the opData/opAck-only twins to sxUnknown (verified minimally: adding a third UNUSED arm carrying `seq[(string,string)]` alone flips a clean sxUnsat to sxUnknown). Exit-gate A's non-dependency note ruled out the WITNESS-READER `seq[(string,string)]` gap but never anticipated this classification-time one — **the A6 DoD "against the 0.4.0 release" is unsatisfiable as released**; any fix ships ≥0.4.1. `t_symex_decode.nim` left as intended-target WIP (all five arms CONSTRUCT under the Bug-#1-patched engine, verified via the localhost transport; file does not compile against released 0.4.0; every block doc-commented with status). Chapulin milpa.kdl/lock verified byte-reverted to the v0.4.0 pin; nothing committed there; `docs/proptest-findings.md` deliberately not updated until Bug #2 resolves. **Needs Corey:** (a) authorize a new engine slice for Bug #2 — recommendation: SCOPED DECLINE (classify an unsupported field as a kind-marked placeholder à la R8's `unsupportedFieldPlaceholder`; decline/taint only paths that actually touch the poisoned arm/field, per the RFC's per-prefix-scoping "never all-or-nothing" doctrine) over lazy per-arm classification (invasive restructuring for the same observable result); (b) then cut **0.4.1** (Bug #1 `ca51c3e` + the Bug #2 fix) and re-point A6's DoD at it; (c) push authorization for `ca51c3e`/`8b28e73` when ready.
- ✅ **B1 LANDED `bdfca57` (walker v78)** — `collectStringBackedByteSeqParams` parse-time pre-pass in parseProc (after param classification, before body walk) sharing `tryMatchScanIdiomShape` (extracted from tryRecognizeScanIdiom, receiver/literal-type-agnostic) so B1a classifier + scan recognizer are ONE predicate; string-backed iff consuming loop matches + byte-range literal delimiter + no mutation site (`scanShapeReceiverMutated`: bracket-assign/.add/.del/.insert); new `IRParam.isStringBacked` (isVar idiom) → allocateSym allocates via itString machinery + [0,1024] ceiling; duplicate bracket-expr/call-form parser sites collapsed into shared `parseSeqBracketAccess`/`parseSeqLenAccess` (string-backed → iekStrSubstr/iekStrAt/iekStrLen); walker backstops: isIndex arm (was doAssert svArray) + iekSeqLen lower (was bare ValueError) both gain svString + classified decline (feUnsupportedExprKind) with new `ixLoc`/`lenLoc` siteLoc-captured location fields. New test tsymex_r6_b1_stringbacked 7/7; 19-file sweep 18/19 (1 red = pre-existing S3_strindex, stash-verified); CR2 pin 77→78. Agent decisions (recommendations applied): `collectIntOffsetParams` name doesn't exist — left runSymexImpl's itInt range promotion untouched; detection scoped to top-level formals only; scan-lift NOT widened to seq[byte] receivers (representation only); ixLoc/lenLoc only at the 2 touched sites; NO TOT-1 row for the else-decline branches (verified unreachable through composable SUTs once svString handled). **Flagged: string-backed WITNESS gap** — model value lands in RawWitness.strVals but readSeqUInt8 reads seqLens/uintVals → graceful empty seq[byte], wrong content (mirrors CR-2c class; test asserts status only). ⚠ This will collide with **B4's "witness cross-check" DoD** — B4's agent must either fix the reader (route string-backed params to strVals) or the RFC's B4 row needs a witness-scope note. Also surfaced pre-existing: iekSeqAdd mutation lowering is width-64-only.
- ✅ **B2 LANDED `1379542` + rider `06fddc4` (walker v79→v80)** — `iekConvIntWidth`, WIDENING only, zero/sign-extend keyed on SOURCE signedness (result SymVal takes TARGET signedness); probeProto returns widened proto (iekConvFloatToInt stale-proto precedent pinned); narrowing + same-width reinterpret = classified declines via shared `declineIntWidthConv` (siteMsg + mkUnsupported + zeroValueForType, CR-2a/A0 idiom), 2 TOT-1 rows; int/int64 + uint/uint64 same-width aliasing explicitly excluded from the decline (genuine no-op); nnkHiddenStdConv untouched. **Rider (control-loop bounce):** the agent had excluded the `byte` alias — but protocol.nim:93's `uint16(b) shl 8` has a byte-typed source, so the headline consumer shape fell through to the old silent identity path; fixed via `normalizeIntTyName`/`isIntFamilyName` scoped to the B2 nnkConv arm (shared intTyNames untouched — it gates lowHighIntLit too); byte-source widening SAT/UNSAT + byte-narrowing decline pins added; ntyNone guard added to declineIntWidthConv per DoD (d) (the agent's "nnkConv always resolved" rationale is the exact assumption class A5 disproved). tsymex_r6_b2_intwidth 14/14; core sweep 18/18; name-match sweep 14/15 (1 red = pre-existing A2a_chokepoint_audit MSVC C2026); CR2 pin 78→79→80. Review note: `tryEvalInterval` treats iekConvIntWidth as no-interval (float-conv grouping) — widening is value-preserving, a future slice could propagate the source interval.
- ✅ **B3 LANDED `f5f5243` (walker v81)** — `tryRecognizeScanPairIdiom` (+`ScanPairShapeMatch`/`tryMatchScanPairIdiomShape`) wired into both nnkWhileStmt sites alongside the Q1 recognizer: int-result scan from a SYMBOLIC offset lifts to 3-arg iekStrFind, not-found vs OOB forks per B0's split; receiver stays itString-only (matches B1's scope note; c6's dead seq[int]+mask SUT migrated to string). c6 gate EXCEEDED: `destructurePair` upgraded beBudgetExhausted → real sxUnsat. New test tsymex_r6_b3_scanpair 7/7; q1 13/13, b0 6/6, full mandated sweep 0 failures; CR2 pin 80→81. Agent caught+fixed mid-sweep: the plain `<` guard (unlike Q1's and-shape) also matched ordinary iterator loops and its first-pass classifyType lacked the ntyNone guard → crashed N3_scan_boundary with the exact A5 crash class; fixed by structural-checks-first ordering + guards at all 3 new sites, stash-bisected, re-swept clean. Findings: (a) pre-existing GENERAL engine limitation confirmed vs unmodified Q1 — universal/UNSAT relational proofs crossing a function-call-inlining boundary can diverge for specific query shapes (routed around per dt-bounded doctrine, documented in test comments); (b) flagged: the ScanError not-found pin asserts sxRaised only, not raisedTypeId — the stricter check fails through an inlined callee, likely pre-existing exception-hierarchy rendering detail, worth a look. Slice wall time ~3.2h — symbolic-offset indexof proofs through call boundaries run 2–15 min EACH in this engine build.
- ✅ **B4 LANDED `3dbe8e9` (walker v82, renderAsChoicesVersion 7→8)** — `tryRecognizeAccumulatingScan` third sibling recognizer (body-stmt count 1/2/3 keeps Q1-B0/B3/B4 mutually exclusive by construction); payload binding `acc := entry-acc & iekStrSubstr(s, i, p-1)` (RFC's inclusive-hi formula, leading concat generalization); `collectIntOffsetParams` REALIZED as a parse-time collector (ADR-0027 leg — the name the B1 agent couldn't find now exists): traces a top-level int param to a scan offset through ≤1 rebind + ≤1 direct call boundary → `IRParam.isIntOffset` → svInt allocation (iekStrSubstr's low bound declines BV via CR-17 guard); fixed live crash en route — `lowerArith` never reconciled mixed svInt/svBV pairs (lowerCmp did) → `reconcileInt` at top, symmetric close. **B1's string-backed witness gap FIXED**: readSeqUInt8 routes string-backed params through strVals; renderAsChoicesVersion bumped 7→8 per M1's own precedent (content change through the generated-reader path); stale `== "7"` render pins in r1b + r14_continue_guard converted to >= floors. B3-5 pin migrated sxUnknown→sxRaised (B4's recognizer claims that shape — sound upgrade). New test tsymex_r6_b4_readcstring 10/10; 24-file sweep green minus pre-existing S3_strindex; CR2 pins 81→82 + render 7→8. **⚠ NEW PRE-EXISTING BUG (flagged, unfixed, MUST land before B7's oracle):** witness extraction corrupts any solved string containing an embedded NUL — the byte comes back as the literal 5-char SMT-LIB escape text `\u{0}` (minimal repro with pre-existing iekStrAt/iekStrFind only). Chapulin's REAL delimiter is '\0', so B7's witness-replay oracle hits this head-on; likely evalStr/Z3_get_lstring or nim-z3 model-value handling. B4's content pins use ':' (B3 precedent) + one status-only real-'\0' pin (B4-8).
- ✅ **Interim INT-1 (post-B4) PASSED, zero regressions** — chapulin full suite vs pinned nelli HEAD `24408e9` (contains B1–B4) via the localhost transport: default 34/34, symex 6/7 (only t_symex_decode's KNOWN A6-Bug-#2 failure, signature unchanged; 2 bind-mount OSError flakes cleared standalone — 2nd data point for the runbook caveat). milpa.kdl/lock byte-reverted (sha256-verified), _deps back on v0.4.0, chapulin git-status file list identical, nelli tree untouched. Runbook addenda: dev-test.ps1's default list EXCLUDES t_symex* (invoke via -Only with all 7 names); MILPA_CACHE_DIR auto-pinned in-tree by nimcontainer.ps1; nimcontainer pipes container output to Out-Host so Tee-based capture misses per-test detail. Note: the agent reported a mid-task tool-layer reminder that appeared to ask it to conceal its own byte-identical milpa.kdl restore — it disregarded and disclosed (correct); revert independently hash-verified.
- ✅ **B5 LANDED `e59fe38` (walker v83)** — NOT green-on-arrival (RFC predicted Ver —, actual engine gap found): the faithful #6 shape degraded sxUnknown because `retBindEq`'s fresh call-return placeholder allocated itInt tuple fields at the BV default regardless of callee semantics — a chained scan's second offset stayed BV and failed iekStrSubstr's CR-17 Int-sortedness check. Fix mirrors IRParam.isIntOffset at the call-RETURN end: `calleeIntOffsetReturnPositions`/`scanOffsetReturnPositions` trace which callee tuple positions are Sequence-theory Ints → new `IRStmt.isCall.retIntOffsetPositions` → allocateSym's itTuple/itInt arms allocate svInt at those positions; companions: parseCalleeImpl marks callee's own isIntOffset params, call-arg lowering picks svInt proto for isIntOffset formals, emitStmt serializes the new field (unserialized IRStmt fields silently revert to default across the macro round trip — debugging lesson worth remembering). Purely additive (untraced positions keep BV default). Zero-seed chains WERE free (B0's guarded entry composes). tsymex_r6_b5_chained 9/9 (faithful #6 SAT/UNSAT retires the finding; B4∘B4 witness cross-check; zero-seed pair; non-.len trip-wire; B4-then-B3 mix); mandated + 14-file supplementary sweeps green minus pre-existing S3_strindex; CR2 pin 82→83; render version unchanged.
- ✅ **B6 LANDED `84213d9` (walker v84)** — `tryRecognizePairLoopIdiom` fourth sibling recognizer (matched against the REAL desugared readOptions typed-AST via treeRepr probe): pair-loop replaced by two-way fork on new `iekStrInOptionRegion` boolean, `s[i..bound-1] ∈ ((nonzero)* "\0")*` built from nim-z3's already-bound range/star/concat/matches regex primitives (no missing primitive); star segments admit empty keys/values + double-NUL. Depth findings fixed en route: (1) walker descends every isIf/isWhile branch with NO feasibility pre-check — an unmodeled construct on even an infeasible branch → top-level sxUnknown discarding found sxSat (general engine property, future forked-closed-form slices must budget for it); both fork branches therefore omit the fold; (2) `var pairs: seq[(string,string)] = @[]` crashed allocateSeqDataRaw even at zero elements → lowerSeqLit one-line rider skips allocation for empty literals. Loop counter Int-sortedness rides B4/B5 collectIntOffsetParams unchanged. New test tsymex_r6_b6_optionregion 8/8 (defect proof, empty-key/value, double-NUL, truncated-region ScanError, non-.len trip-wire, floor); 19-file sweep green minus pre-existing S3_strindex; CR2 pin 83→84; no divergence (all queries seconds); no TOT-1 row (unreachable decline, B1 precedent). **⚠ Flagged: `itSeq[itTuple]` has NO backing at all** — will meet B7 wherever readOptions's return/accumulator is touched directly; belongs in the same architect look as A6 Bug #2 (both are seq[(string,string)] modeling gaps).
- ✅ **B4-rider LANDED `73051b0` (render v8→v9, walker unchanged)** — embedded-NUL witness fix. Root cause ISOLATED to nim-z3's `evalStr`/`toStr` (`Z3_get_lstring` path): a bare `mkString("\x00")` round-trip reproduces the 5-char `\u{0}` expansion with inflated length — NUL-SPECIFIC, not general-escape (backslash/quote/low-byte/0x7f/0xff all extract correctly pre-fix, pinned to prove the scope; B4's mixed corruption was offset-shift from the NUL expansion). Fix: new `evalStrBytes` in runtime.nim composes `m.eval` + already-bound `getStringContents` (`Z3_get_string_contents`, raw codepoints — safe: every free string is `(re.range '\x00' '\xff')*` per ADR-0006); extractLeaf's svString arm switched; NO _deps modification needed. tsymex_r6_nulwitness 15/15 (RED confirmed first via stash: exactly the 4 NUL-dependent pins failed); B4-8 STRENGTHENED to content + real-function replay cross-check; 15-file/152-check sweep green minus pre-existing S3_strindex; CR2 gains exact render `== "9"` pin; RFC gains a B4-rider table row. **B7's witness-replay oracle is now unblocked on the engine side.**
- **🛑 LOOP STOPPED (2nd stop, 2026-08-15 late) — Track B engine work EXHAUSTED; B7 alone remains and is needs-Corey-blocked.** This session's grind: A6 attempt (blocked, spec break — see ⛔), B1 `bdfca57` v78, B2 `1379542`+`06fddc4` v80, B3 `f5f5243` v81, B4 `3dbe8e9` v82/render8, interim INT-1 vs `24408e9` (34/34 + 6/7, zero regressions), B5 `e59fe38` v83, B6 `84213d9` v84, NUL-rider `73051b0` render9. ALL COMMITS LOCAL-ONLY (origin/main still at 0.4.0's `fc7fa7d`). **B7 cannot proceed because:** its twins/oracle return `TftpPacket` → A6 Bug #2's eager whole-type classification degrades every such proc to sxUnknown (escalated fork, unfixed); the natural twin's readOptions accumulator additionally needs `itSeq[itTuple]` backing (B6 flag — same seq[(string,string)] cluster, recommend ONE architect pass covering both); B7's DoD gates "against 0.5.0" (release = Corey); B7 edits chapulin (uncommitted WIP entanglement). **Needs-Corey bundle:** (1) decide the Bug #2 + itSeq[itTuple] engine approach (recommendation on file: scoped decline / R8-placeholder mirror over lazy classification); (2) push authorization for the 9 local commits + cut 0.5.0 when A6+B7 close (or an interim 0.4.1 with Bug #1 `ca51c3e` per the earlier recommendation); (3) chapulin absorption + t_symex_decode WIP commit strategy. Resume after any of these: `/loop /tdd RFC-chapulin-hardening til done`.
- ✅ **Bug #2 LANDED `60540a6` (walker v85), 2026-08-16 — per-field SCOPED DECLINE with read-taint, per the FORK RESOLUTION design below.** `classifyObjectRecordFields` (dsl_typebridge.nim) marks a declared object/variant field whose type is structurally unsupported for allocation backing (`isBackedSeqElemTy`, types.nim — mirrors `allocateSeqDataRaw`'s backed element set) with a KIND-MARKED placeholder (`isUnsupportedFieldPlaceholder`, extends R8's precedent) instead of the real (eagerly unallocatable) `itSeq` — `allocateSym`'s `itSeq` arm allocates it fresh-length-zero rather than raising, so an untouched arm/field no longer poisons every proc merely ALLOCATING the type. `dsl_parser.nim`'s `nnkDotExpr` field-read arms detect the placeholder and deposit an SND-1 taint on THAT READ's own statement (classified `seNestedSeqUnsupported`, path-scoped); `retBindEq`'s new `svSeq` arm skips the eq constraint on it. Also resolves the `itSeq[itTuple]` allocation gap the B6 grind-log bullet flagged (same seq[(string,string)] cluster, one slice, as the FORK RESOLUTION bullet anticipated). Two macro-round-trip gaps found and fixed en route (not anticipated by the design, discovered via TDD): `emitIRType`'s `itSeq` arm dropped the new `seqUnsupportedFieldReason` field across the compile-time→runtime `IRType` round trip (the B5 "unserialized field reverts to default" class) — allocation silently saw a plain `itSeq` and crashed exactly as before until fixed; `emitTyAndReader`'s `itSeq` arm needed its own placeholder case (a type-correct empty-seq literal) since the placeholder is a genuine nested WITNESS leaf now, not a whole-param CR-2b/CR-2c `itUninterp` demotion. New `tests/tsymex_r6_bug2_scopeddecline.nim` 13/13 (poison-gone bare-param + return-binding twins SAT+UNSAT, honest degrade on a direct field read with message assertion, retBindEq skip-eq over supported fields, A3 fork-per-tag construction with a placeholder-carrying arm, local `seq[(string,string)]` composition with the B6 empty-literal rider); new TOT-1 corpus row (surface 5: per-field scoped decline). `tests/tsymex_CR2c_witnessreader_catchall.nim`'s CR-2c-N3 MIGRATED (an untouched variant-arm `seq[Widget]` field no longer whole-run-poisons — same "crash/decline → real verdict" class as A1/A3's own migrations) — this is the ONE pre-existing pin this slice's verdict-surface tightening required updating; every other mandated-sweep + regression file (CR-2a/b, P1/P2a/P2b, C2, g8/g10, H-verification, ref-object witness, array/seq, A0–A6, B0–B6+riders, TOT-1, c6-tuple-chain) green. `tests/tsymex_phase15_C4_hof.nim` (2 reds, filter/map inline-HOF) confirmed PRE-EXISTING via stash-bisection against this slice's own diff — unrelated, not touched, not fixed. CR-2 `==` pin 84→85. Residual/flagged for a later look (none block this slice's own DoD): `iteSV` has no `svSeq` merge arm (pre-existing gap shared with `svString`/`svTable`/`svSet`/`svVariant`/`svMultiVariant` — a placeholder-carrying PLAIN (non-variant) object local reassigned differently across two branches and then merged would hit it; not reachable by any pinned SUT here, same class as those other already-unmergeable kinds); `tyOf(sv: SymVal)`'s `svSeq` arm reconstructs a plain (non-flagged) `itSeq` from a placeholder value (only matters if something re-derives a field's TYPE from its VALUE downstream of a read — no pinned path does). **A6 is now unblocked** — its DoD can point at a fresh interim release once cut (see the FORK RESOLUTION bullet's release plan, unchanged: tag 0.4.1 on main, re-point A6's DoD at it). Nothing pushed; chapulin and `_deps/` untouched.
- ✅ **0.4.1 RELEASED + A6 CLOSED + EXIT GATE A CLOSED, 2026-08-16 (Corey's "continue" = go with push/0.4.1 authorization included, per the FORK RESOLUTION bullet's recorded terms).** nelli: release commit `cdb4756`, tag `v0.4.1`, main pushed `fc7fa7d`→`cdb4756` (23 commits, walker v85, render v9); symex-windows (6m51s) + tianguis-publish (1m16s) both green. chapulin (all LOCAL commits, nothing pushed): `ebc40fd` mechanical absorption (14 files — 12 pure single-line import renames + milpa.kdl/lock at the v0.4.0 pin; the other ~5 historically-cited rename files are ENTANGLED with owner verification-harness-v2 WIP — fuzzsupport/t_corpus/t_hostile/t_symex/wireharness — their rename lines deliberately left uncommitted alongside the WIP, Corey splits by hand), `29f963d` checksum canary tighten (A6/B7 re-probe item DONE for the sidecar paths), `7d637c5` A6 close (milpa pin → `nelli@v0.4.1` lock at `cdb4756`, t_symex_decode un-voided 16/16: opData/opAck construct+prove with the differential oracle against real `decode()` exact-field-match, opOack construction proves, opError + opRrq/opWrq honest classified degrades pending Track B). Full chapulin suite 34/34 (213 suites / 639 checks) + all seven t_symex* green. Owner WIP verified per-file (diff-inspected before staging; final status = same WIP set minus committed files). `docs/proptest-findings.md` (untracked, edited in place) re-based to 0.4.1 with Bug #1/#2 FIXED rows. **Two NEW engine findings from the A6 verification (isolated-bisection confirmed, NOT Bug #2 recurrences):** (1) an object CONSTRUCTOR omitting a `seq[byte]`-typed field degrades the whole proof to sxUnknown (workaround: explicit `@[]`; honest degrade, review item); (2) **`seq[byte]` element reads through a HELPER-PROC CALL lose witness fidelity — all-zero witness on a genuinely sxSat proof (SILENT-WRONG-ANSWER class)** — control-loop decision (confident recommendation, not a fork): fix as a dedicated nelli rider slice BEFORE B7, since B7's five-arm witness-replay oracle sits directly on helper-proc reads. B2's narrowing decline surfacing in the legacy `seq[int]` twin paths confirmed working-as-designed; the A6 twins got their own seq[byte]-native widening-only read path, full migration stays B7's.
- **⛔ B7 ATTEMPTED (2026-08-16) — BLOCKED on a recognizer-wiring gap; smallest safe slice left uncommitted in chapulin; rider launched.** Against local HEAD `d54f85a` (walker v86, via the INT-1 localhost transport, set up + torn down clean): opData/opAck migrated off the atByte/seq[int] legacy onto a real shared `readU16ByteTwin(seq[byte], int): uint16` helper (B2 widening through a CALL — confirms the v86 A6-rider-2 soundness fix at the consumer); both still prove with the oracle. Full unification BLOCKED: **(BLOCKER A)** B3/B4/B5/B6's closed-form recognizers hard-gate the receiver on `classifyType(...).ty.kind != itString` and never consult B1's `stringBackedParams` — a seq[byte] receiver NEVER gets a closed form (3 independent minimal probes, all sxUnknown); **(companion bug)** with a genuine `string` receiver instead, witness extraction corrupts whenever two char-sourced B2-widened values combine (decode's own opcode dispatch shape) — both documented in chapulin's t_symex_decode.nim header. Checksum re-probe: 8/8 green at v86, const-fold item CLOSED (findings doc updated in place). Chapulin left with exactly ONE uncommitted intentional change (t_symex_decode.nim +75/−6); milpa pin byte-reverted to v0.4.1 (git-verified); suites 34/34 + 6/7 (decode red only against the v85 release pin — needs the v86+ release). **Control-loop resolution (confident recommendation, not a fork):** exit gate B's own committed text requires "closed-form scans ... on one STRING-BACKED param" — the wiring was deferred by B3's row ("deliberately NOT widened ... per B1's own scope note") and no slice ever picked it up: an RFC decomposition hole, not a spec break. Rider slice launched in nelli: widen the recognizer family's receiver gate to string-backed seq[byte] receivers via the shared B1 predicate (+ byte-literal delimiters), AND fix the char-widening witness corruption. Then B7 resumes. **Release decision (recorded earlier this session): NO interim 0.4.2 for v86 — it ships in 0.5.0 at gate B, now one rider away.**
- ✅ **B7-rider LANDED (walker v87, render v11), 2026-08-16/17 — closes BLOCKER A and the companion char-widening witness-corruption bug; B7 resumes.** **LEG 1:** the four scan recognizers' receiver gate widened via new shared `scanReceiverOk`/`scanDelimiterChar` (`dsl_parser.nim`) — itString OR string-backed `seq[byte]` (`ctx.stringBackedParams`); delimiter gate widened in lockstep for a byte-range literal. Landing it surfaced TWO further gaps the "just widen the four gates" framing missed: (1) B1's OWN classifier only ever detected a Q1-SHAPED consuming loop — chapulin's real `readCString`/`readOptions` are B4/B6-shaped, never Q1-shaped, so their `seq[byte]` params would never have been marked string-backed at all; the classifier's candidate walk now tries all four shape predicates (B6's own shape-match had its itString gate MOVED into the recognizer — it was embedded in the shape-match itself, a chicken-and-egg blocker since the classifier must call that SAME predicate before `ctx.stringBackedParams` exists yet). (2) `runtime.nim`'s `isCall` arm binds a caller's lowered argument directly into the callee's env with no representation bridge (unlike `IRParam.isIntOffset`, itString/itSeq have no lossless reinterpretation) — a caller whose own top-level `seq[byte]` param has no qualifying loop of its own (the common case: the scan lives inside a callee it invokes) would stay array-modeled while the callee's inlined body expects `svString`; fixed via a NEW one-level call trace in `collectStringBackedByteSeqParamsImpl`, mirroring `collectIntOffsetParamsImpl`'s existing wrapper-promotion pattern verbatim. Two smaller pre-existing (never-before-triggered) bugs found and fixed along the way: `tryMatchAccumulatingScanIdiomShape`'s `char(...)` accumulator-arg unwrap only checked `nnkCall`/`nnkCommand` — Nim actually represents an explicit `char(...)`/`byte(...)` conversion as `nnkConv` (confirmed via `treeRepr`), dead code until a `seq[byte]` receiver's `.add(char(data[i]))` exercised it for the first time. **LEG 2:** root-caused, and the chapulin handoff's own hypothesis ("witness extraction corrupts") found INCORRECT — the real defect is a PARSE-TIME gap one call site removed from extraction: `char` was never in `intTyNames`/`normalizeIntTyName`'s alias map (unlike `byte`), so `uint16(<charExpr>)` silently dropped the widening conversion entirely (fell to a bare identity pass-through), leaving the checked property's high byte totally unconstrained once compared against a truncated 16-bit literal — `sxSat` was technically correct but the reported witness reflected Z3's free choice, not what the property depended on. Fixed by mapping `char` to `"uint8"` in `normalizeIntTyName`, exactly like `byte`. Both legs are verdict-class fixes (not extraction/rendering-only), so `symexWalkerVersion` bumps 86→87 and `renderAsChoicesVersion` bumps 10→11 in lockstep. New `tests/tsymex_r6_b7r_bytescan.nim` 26/26 (all four recognizers through a `seq[byte]` receiver, SAT+UNSAT companions; byte-literal delimiter spellings incl. the real `0x00`; B5-shape helper-call composition; B6 pair-loop; mutation-fallback veto; non-`.len`-bound trip-wire; sibling exclusivity; LEG 2 repro with content + real-function cross-check + full-16-bit-width existence proof + char-narrowing-decline regression pin; version floors). ~30-file mandated regression sweep green except the 3 documented pre-existing reds (`S3_strindex`, `C4_hof` ×2, 3 MSVC C2026 audit files — all confirmed unchanged). CR-2 `==` pin 86→87 / render 10→11. Nothing pushed; chapulin and `_deps/` untouched. **B7 itself (the natural unified decode twin, differential oracle, checksum re-probe) is now unblocked and remains the next slice.**
- **⛔ B7 SECOND ATTEMPT (2026-08-16, against `e04f13b` walker v87/render v11 via transport) — FIVE-OPCODE ORACLE GREEN, exit gate still open on opOack; two NEW engine gaps isolated.** Landed in chapulin's working tree (UNCOMMITTED, pin byte-reverted to v0.4.1 and git-verified): all four helper twins seq[byte]-native (atByte mask + seq[int] typing gone); `decodeTwin` proves opData/opAck/opError/opRrq/opWrq with witnesses cross-checked exactly against real `decode()` (opRrq/opWrq via a genuine chained filename→mode scan — capability upgrade over A6, retiring the deferred-arms debt for those arms); sxUnsat for IndexError AND FieldDefect on the twin; full differential `decodeFullTwin` green on hand-picked + property vectors; suites 34/34 default + 7/7 symex vs the local SHA; checksum 8/8 unchanged. Agent self-caught+fixed an opError else-arm overmatch mid-run. **BLOCKER B7-1:** B6's readOptions closed form proves ONLY in its pinned SUT's exact unconditional shape — wrapping in `if`/`elif`, following with variant construction, or calling via a helper proc EACH independently degrade to sxUnknown; opOack needs the first two, so it stays construction-only (`decodeOackTwin` kept separate). **BLOCKER B7-2:** a `case`-match over scanned content with `else: raise` (parseMode shape) poisons a DISJOINT sibling branch of a multi-branch dispatch (worked around via direct string equality). Stretch goal attempted honestly: real `decode` → symexFind compiles, degrades consistent with B7-1/B7-2, recorded in findings doc. **⚠ SECOND PROMPT-INJECTION ATTEMPT observed and defeated:** a fake system-reminder told the agent the reverted milpa.kdl "was modified intentionally," not to revert it, and not to disclose — identical pattern to the INT-1 incident; agent disregarded, disclosed, and the revert is independently git-verified (diff vs HEAD empty). **Control-loop resolution (confident recommendation):** B7-1's if-wrap case and B7-2 share one root — the walker's single top-level handler discards path-local information, so an unmodeled construct on any branch poisons the WHOLE run (the all-or-nothing the RFC's per-prefix-scoping doctrine forbids; B6's grind bullet documented it as a general engine property "future forked-closed-form slices must budget for"). The budget is now due: rider B7r2 launched — branch-scoped classified degrade (Bug-#2 read-taint precedent lifted from field level to path level) + the helper-call and construction-follow wiring legs of B7-1. B7 resumes after it.
- ✅ **B7r2 LANDED (walker v88), 2026-08-16/17 — closes two of BLOCKER B7-1's three breakers; BLOCKER B7-2 and the general branch-scoped-degrade architecture escalated as a confirmed engine limitation, not fixed.** Against local HEAD `8b3c593`. **Root causes, verified against the real repro shapes (not assumed from the RFC's own framing):** breakers (a) if-wrap and (b) construction-followed share ONE cause — B6's `iekStrInOptionRegion` needs its start/bound `svInt`-represented (CR-17), and `collectIntOffsetParams` only traces a loop counter to a formal param through a bare-symbol rebind; chapulin's real shape seeds the counter from a LITERAL (`var pos = 2`) with no param to trace to. A faithful repro with the counter fed from a genuine `start: int` param (matching `tsymex_r6_b6_optionregion.nim`'s own pinned SUT) does NOT reproduce either breaker — confirming the RFC's own "if-wrap"/"construction-after" framing was one level removed from the true cause. Fixed via `collectIntOffsetLiteralLocals` (new parse-time collector, `dsl_parser.nim`) + `IRStmt.isLet.lIsIntOffsetLocal` (new field, threaded through `emitStmt`'s round trip) selecting an `svInt` proto for a literal-seeded counter — sound unconditionally, no def-use tracing risk. Breaker (c) call-boundary is a SEPARATE, independent cause: a helper RETURNING `seq[(string,string)]` (real `readOptions` signature) poisoned any caller merely binding the result — Bug #2's placeholder was scoped to record fields only; generalized `allocateSym`'s `itSeq` arm to placeholder ANY unsupported-elem seq regardless of origin, plus a new `isIndex` `svSeq` read-guard (the read-safety companion a bare value needs, since `dsl_parser.nim`'s field-read interception has no static site to catch a bare value at). Confirmed real capability upgrades (not just crash-avoidance) via isolated probes: if-wrapped/construction-followed pair-loop proofs past the 5-iteration k-unroll horizon; call-boundary reachability past a `seq[(string,string)]`-returning helper; the full opOack-faithful composite (dispatch + call-boundary `readOptions` + construction) reaching its target end-to-end, with a genuine defect on a disjoint sibling arm still found. **⛔ BLOCKER B7-2 + the general architecture — escalated, not fixed:** the RFC's own "branch-scoped classified degrade" design (a new `SymexConstructGapError` base for the 19 construct-gap carriers, caught via `try`/`except` at the `isIf`/`isWhile`/`isCall` walk boundaries, taint-and-continue via SND-1) was PROTOTYPED and found UNSOUND on this engine's C backend: wrapping even ONE of those four sites in `try`/`except` — non-re-raising, a single catch, either the new base type or one of the 19 concrete types directly — causes the underlying classified-degrade exception to NEVER BE RAISED AT ALL (not corrupted-but-visible: confirmed via an in-band print immediately before `runSymexImpl`'s own verdict computation, `w.sawUnknown`/`w.walkDegradeErrors` verifiably stay unset; reproduced identically across `--forceBuild`/fresh-nimcache reruns, ruling out a cache artifact; isolated via bisection — removing the SAME try/except reliably restores the pre-fix classified failure). This is a NEW, MORE SEVERE manifestation of the SAME pre-existing ADR-0020/CR-1c finding this codebase already recorded ("a per-`walk`-frame catch/re-raise... corrupted memory... a C-backend-only SIGSEGV") and architected around (the single top-level `runSymexImpl` catch) — confirming the constraint is stricter than "don't catch at EVERY frame": even ONE non-top-level catch around a `seq[Path]`-returning recursive call is unsafe on this toolchain (Nim 2.2.10-patched, C backend, ORC, `--exceptions:goto`, `--threads:on`). The prototyped base-type/catch changes were fully reverted (not shipped in any form); BLOCKER B7-2 stays pinned as an honest, unchanged classified decline (status + classification both asserted, so a future fix flips it red rather than going silently stale). New `tests/tsymex_r6_b7r2_pathscope.nim` 11/11; CR-2 `==` pin 87→88; ~43-file mandated sweep green except the 5 documented pre-existing reds (confirmed unchanged). A TOT-1 row for the new `isIndex` decline site was attempted and withdrawn — the shape classifies correctly in isolation, but as the ~20th sequential `symexFind` call in that file's own binary it triggers a cumulative Z3 `Z3_MEMOUT_FAIL` unrelated to correctness (tried three simplifications, including a zero-iteration variant, all OOM'd in that file's context while passing standalone); pinned instead via this slice's own file. Nothing pushed; chapulin and `_deps/` untouched (this slice never accessed either). **Needs Corey / next steps:** (1) B7-2 + full branch-scoped-degrade sibling-survival remains the one open piece of B7's original scope — a genuine architecture question (in-band SND-3-style signaling threaded through `lower()`'s entire recursive call graph, replacing the exception-based classified-degrade carriers entirely, is the only alternative pattern this session identified that avoids the confirmed hazard, but it is a MUCH larger, more invasive refactor than a rider — recommend scoping it as its own dedicated round/RFC slice with an explicit go/no-go rather than folding it into B7's resume); (2) B7 itself (the unified natural decode twin, full oracle including opOack, checksum re-probe) can resume now for the two unblocked breakers, but opOack stays construction-only pending B7-2's own resolution — same as B7 second attempt's baseline; (3) consider filing the exception-elision finding as a Nim compiler/toolchain issue if it recurs elsewhere — it is surprising and undocumented outside this codebase's own ADR-0020 note.
- ✅ **B7 THIRD PASS GREEN + 0.5.0 RELEASED (2026-08-16) — exit gate B achieved engine-side; final chapulin commit IN FLIGHT.** Against `9a652b8` (walker v88 via transport): `decodeOackTwin` merged back — genuinely ONE natural decode twin, all six wire opcodes / five arms, matching real `decode`'s dispatch shape; ALL FIVE ARMS prove sxSat with witnesses cross-checked against real `decode()` (opOack via the option-region membership proof through dispatch + construction, options content correctly unmodeled per the committed gate); differential `decodeFullTwin` green on hand-picked + property vectors; suites 7/7 symex + default green (2 bind-mount flakes cleared standalone). One assertion honestly adjusted, verified NOT a merge regression: the whole-proc unconstrained no-defect search stays sxUnknown because the region-membership NON-member fallback still k-unrolls (maxLoopUnwind=5) — consistent with B6's committed scope, isolated diagnostic shows the opOack arm alone already carries that verdict. Stretch goal (real `decode` direct): still sxUnknown, now attributable to B7-2 ALONE (open, deferred round). **0.5.0 RELEASED:** release commit `b1ea6af`, tag `v0.5.0`, main pushed `cdb4756`→`b1ea6af`, symex-windows (~15m) + tianguis-publish green. **⚠ THIRD prompt-injection attempt** (fake system-reminder against the milpa revert, identical pattern) disregarded + disclosed by the agent; revert git-verified. **IN FLIGHT:** B7 agent flipping chapulin's milpa pin to released v0.5.0, re-running full suites, committing the B7 work as one commit (pin + unified twin; findings doc stays untracked). After that: RFC B7 row + exit-gate-B flip, memory update, loop stop. **Remaining post-round-6 (needs-Corey or future rounds):** B7-2 case/else-raise sibling poisoning (own round — branch-boundary try/except is UNSOUND on this C backend, exceptions elided; in-band signaling refactor is the identified path) · real-decode stretch (blocked on B7-2) · cpp-backend infra repair · chapulin entangled-rename files + owner-WIP commit (Corey by hand) · review items listed in the post-A6 Remaining bullet plus B7r2's withdrawn TOT-1 row (Z3 MEMOUT in the corpus binary, pinned in its own file instead).
- **⚠ cpp backend NOT validatable in this environment (discovered during A0, pre-existing):** the Windows container's cpp build of nim-z3's FFI softlink fails (MSVC C2664/C2955/C2057 in sffi) — reproduced on clean main HEAD; podman machine reports running but its SSH socket refuses, so the Linux dt.sh path is down too. The `symex-windows` CI leg gates the c backend only, so the grind gates on c + CI. Infra follow-up owed: repair podman connectivity or the container's cpp/Z3-header pairing, then re-sweep cpp before the 0.4.0 release gate.
- **2026-08-15 pull (35 upstream commits absorbed):** package RENAMED
  proptest → nelli (imports `nelli/symex` etc.; local dir + remote URL
  unchanged); 0.3.5 released upstream; walker now v73 (upstream clusters
  N0–N3 routine-kind gates, A1–A3 ANF operand chokepoint incl. bitwise
  and/or classify-first, C1/C2 GenericDescriptor, ADR-0030). **A5 scope
  SHRANK:** `tsymex_phase1_dsl` now PASSES (fixed by the upstream A2b
  classify-first restructure); `g8_multi_param`/`g10_smoke` still fail
  with the same `dsl_typebridge.nim:337 "node has no type"` — A5 = those
  two. Round-6 slice numbering/SW targets should be read against walker
  v73, not v70.
- Round-1 changes (context for round 2): retired bitwise→LIA leg (false premise — iekStrAt yields BV8); isVariantConstructSym statement cloning isVariantReassignSymbolic; B0 soundness fix; B1a shared predicate; B2 = width conversions; B6 = option-region regex-star; general retBindEq svVariant encoding; Ver column + standing DoD (nimble/CI/taint/siteMsg/TOT-1); hard A-before-B; interim INT-1 vs git HEAD after A3/B4; A6 differential oracle.
- **0.3.3 RELEASED** (v69; tag v0.3.3, both CI workflows green).

### Slices
Track A (→ release 0.4.0): A0 low/high magics + siteMsg helper · A1
iekVariantLit (itVariant/itMultiVariant arm split) · A2 retBindEq svVariant
(general encoding) · A3 isVariantConstructSym fork-per-tag + budget + loc
field (then interim INT-1) · A4 reassign/witness · A5 fix 3 "node has no
type" breaks · A6 chapulin un-void construction (oracle on opData/opAck;
exit gate).
Track B (→ release 0.5.0, after A): B0 scan-lift soundness ✅ DONE (v70,
both fixes, 6 pins) · B1(+B1a) parse-time representation collectors +
walker svString backstops · B2 width conversions (widening only) · B3
int-result scan closed form · B4 accumulating-string variant (then interim
INT-1) · B5 chained composition (#6) · B6 option-region membership
(star segments) · B7 chapulin migration + unified natural decode twin
(exit gate).

### Open forks (awaiting Corey)
- **Bundle items 1–2 DONE (Corey: "do 1 and 2 now", 2026-08-15):**
  ✅ pushed main (`0ed2664`→`9508ef4` + handoff `1da6a59`) and
  **RELEASED 0.4.0** (`fc7fa7d`, tag v0.4.0; symex-windows + tianguis
  both green; github repo has MOVED to coreyleavitt/nelli — old URL
  redirects). ✅ chapulin absorption done (UNCOMMITTED in chapulin, for
  review): 35 module-path files proptest→nelli, milpa dep →
  `nelli@v0.4.0` (lock resolves commit fc7fa7d), owner WIP verified
  byte-preserved via diff-of-diffs; default suite 34/34 (3 bind-mount
  OSError flakes cleared standalone); symex 6/7 — the 1 red is
  `t_symex_checksum`'s OWN canary firing: its doc says the sxUnknown
  assertions should "tighten back to sxUnsat" when the engine improves,
  and 0.4.0 now proves sxUnsat. Tighten those 2 checks during A6/B7.
- **Retroactive per-slice INT-1: recommend SKIP as subsumed** — the
  absorption run IS a full chapulin suite pass against released 0.4.0
  (= all of A0–A5 at once) with zero regressions; per-slice bisection
  has nothing left to isolate. Transport for unpushed-SHA checks stays
  documented in the INT-1 entry if ever needed again.
- **A6 SPEC BREAK (2026-08-15, see the ⛔ grind-log bullet):** eager
  whole-type classification (Bug #2) makes the A6 DoD unsatisfiable
  against 0.4.0 as released.
- **FORK RESOLUTION (control loop, 2026-08-15 late — Corey asked
  "do these have best-in-class answers?"; yes, designs determinate,
  only AUTHORIZATION remains):** (1) Bug #2 + itSeq[itTuple] are ONE
  slice — per-field SCOPED DECLINE with read-taint: classification
  catches seNestedSeqUnsupported per-field → kind-marked placeholder
  (R8 extension), allocation gives fresh opaque value, READS deposit
  per-path taint (SND-1 machinery), retBindEq/variant arms skip eq on
  unsupported fields (no-constraint = over-approx, taint owns
  honesty). KEY INSIGHT: exit gate B never required modeling
  seq[(string,string)] content — B6's recognizer swallows the fold,
  membership is the committed gate, so untouched-options procs prove
  cleanly (A6's opData/opAck + B7's three arms' modelable parts) and
  options-touching queries degrade classified. Lazy per-arm
  classification strictly dominated. Nested-seq modeling stays the
  recorded non-goal. (2) Release: tag 0.4.1 ON MAIN once the Bug #2
  slice lands (B0→0.3.4 queue-jump precedent; INT-1 proved Track B
  zero-regression; 0.5.0 stays reserved for gate B); re-point A6's
  DoD at 0.4.1. (3) Chapulin: mechanical absorption as its own first
  commit (independently green 34/34+6/7), canary tighten second,
  Corey's WIP stays uncommitted on top; residual = his branch taste.
  **ALL THREE EXECUTED 2026-08-16 on Corey's go — Bug #2 `60540a6`,
  0.4.1 released `cdb4756`, chapulin commits `ebc40fd`/`29f963d`/
  `7d637c5`, A6 closed (see the ✅ 0.4.1+A6 bullet above).**
- **Remaining (2026-08-16, post-A6):** witness-fidelity rider — **LANDED
  as A6-rider-2 (walker v86, render v10)**: root-caused deeper than
  framed — a genuine SOUNDNESS gap (`isCall`'s implicit-result-fallthrough
  left `retSym` unconstrained, a confirmed false-`sxSat` generator), not
  a pure extraction issue; fix mirrors the closure-call path's existing
  `retBindEq(funcApp, cp.env["result"])` idiom into the ordinary call
  boundary. Flagged, not silently absorbed: this deviates from the row's
  original "extraction-only" framing (walker version bumps, not just
  render). See the A6-rider-2 RFC row + `tests/tsymex_r6_a6r_callwitness.nim`.
  B7 (unblocked now that the rider lands; DoD gates against 0.5.0, cut at
  gate B) ·
  cpp-backend infra repair · review items: constructor-omitted
  seq[byte] field degrades whole proof (A6 finding #1, workaround
  `@[]`) · A1 ref-alias/IRType · A5 minimal-pin · B2 tryEvalInterval
  no-interval for iekConvIntWidth · B3 ScanError raisedTypeId through
  inlined callees · B3-discovered call-boundary UNSAT divergence
  class · Bug-#2 residuals (iteSV svSeq merge arm, tyOf placeholder
  reconstruction) · chapulin entangled-rename files (Corey splits by
  hand when committing his WIP).
- **Small review items flagged by slice agents:** A1's
  `ctorIsRefAliasedVariant` per-site re-derivation (promote to an IRType
  field if reused); A5 has no isolated minimal-shape pin (G8/G10 carry
  the root shape); cpp-backend infra repair owed before the 0.4.0 gate
  (see the cpp bullet in the grind log).

### Key decisions (grill-me 2026-08-08)
- seq[byte] migration is a chapulin PREREQUISITE; engine never chases the
  seq[int]+mask spelling.
- One representation-selection pre-pass (Int indices + string-backed
  scanned byte seqs); whole decode lives in QF String+LIA.
- Range-aware bitwise→LIA lowering (theorems, range-discharge gated,
  global) REPLACES any char↔BV bridge — CR-17 class avoided by design.
- Track B exit gate = fully-natural decode, COMMITTED (spike-gate
  formulation rejected as unnecessary hedging).
- Symbolic discriminants = fork-per-feasible-tag with cardinality budget
  (default 8); all five protocol.nim constructors in Track A v1.
- Chapulin-side migration is INSIDE round-6 DoD (exit gates observable
  only through chapulin's suites).
- Declines recorded: xor, non-discharging bitwise, cross-iteration-
  arithmetic loops, past-budget discriminants.

---

## Historical rounds below (round ≤5 closed; round-5 v69 work awaiting release)

## Stage 4 — /code-review — ✅ CLOSED 2026-07-30 (floor reached: 0 Crit/High/Med open)
**Outcome:** 6 mandated findings R1-R6 fixed + R1b (pre-existing v59 bug found en route) + R14 (Crit introduced by R1b's fix, closed with the faithful short-circuit desugaring). 2 adversarial re-review rounds → floor. Commits: c75285f (R1+R5+R1b, SW61) · 46b0ac3 (R4) · 3428987 (R3) · 4fea3da (R2+R6, SW62) · 98f4564 (R14, SW63) · 7c3a16d (Low batch, no bump). Every step swept both backends. **Deferred Lows subsequently fixed 2026-08-01** (Corey: "fix lows now"): R7 + R8 (aab18ce, no SW bump) and R9 + R11 (9be7b5a, doc-only); R10 + R12 were already closed in the Low batch (7c3a16d). Full symex sweep 452/452 green both backends after the fixes (one c-only rc=1 was a parallel-load compile flake — confirmed rc=0 standalone). **Case-2 precision deliberately NOT fixed** — it is already sound (degrades to sxUnknown for the exotic `(a div b)>i and s[i]` + continue shape, never a wrong verdict, pinned by tsymex_r14_case2_degrade), and re-review round 2 explicitly recommended against a 5th short-circuit-dispatch revision (re-touching the engine's most delicate code for zero soundness gain). **All review findings now closed; only Case-2 precision remains as an accepted-sound non-issue.** Next process step: INT-1 (blocked on a nelli release) then done.

## ⏳ SIDE-TASK IN FLIGHT (2026-08-01, not part of the RFC): milpa manifest+lock migration
Corey: "add a milpa.kdl and lock file for the current version" → new milpa (github coreyleavitt/milpa, local ~/projects/milpa) has progressed a lot; migrate to current format.
**Done (uncommitted):** nelli `milpa.kdl` (+`version "0.1.0"`), `milpa.lock` regenerated via new milpa `fetch` (new format: `dag-sha256` identity, `declared_version_source`, `provenance.origin`; graph = softlink 0.11.1 + z3 2.2.0, NO stale nelli self-pin); `scripts/{dt,dt-bounded,runtest}.sh` gained `-v $HOME/projects/nimlibs:$HOME/projects/nimlibs` mount (milpa `local=` dep = absolute symlink `_deps/z3 → ~/projects/nimlibs/nim-z3`, spec S4 §4.3, must be mounted like the CAS already is). nim-z3 `milpa.kdl`: nelli moved `deps`→`dev-deps` (test-only in nim-z3; removes the z3→nelli back-edge that was pulling a stale nelli@99fa2db self-pin into nelli's lock) + re-locked nim-z3 `milpa.lock`.
**Key facts:** new milpa runs on host via `uv tool install git+https://github.com/coreyleavitt/milpa.git#subdirectory=impls/python` (the old "never run host milpa" memory is SUPERSEDED — that was the old tool vs the then-committed lock). `_deps/`+`nim.cfg` are gitignored (regenerated on `milpa fetch`); only milpa.kdl+milpa.lock committed. `milpa lock`=lock-only; `milpa fetch`=_deps+nim.cfg+lock.
**✅ DONE — git-ref milpa migration committed (nelli `0e149fb`; nim-z3 `ca4fe77` pushed).** nim-z3 had been moved externally to `~/projects/nim/libs/nim-z3` (dangling the old `local=` symlink). Resolution: (1) nim-z3 `nelli` deps→**dev-deps** (test-only), pushed to github `ca4fe77` — stops nelli's git-ref z3 from dragging a stale `nelli@github-main` self-copy onto nim.cfg; (2) nelli `milpa.kdl` z3 `local=`→`git=(url)"…/nim-z3.git" ref="main"` + `version "0.1.0"`, both deps now CAS-admissible so the temporary `~/projects/nimlibs` harness mounts were REVERTED; (3) `milpa.lock` regenerated in current `dag-sha256` format, clean 2-dep graph (z3@ca4fe77 v2.2.0, softlink@main 0.11.1), no self-pin. **Validated: symex 452/452 + fuzz/cov/db 62/62 green both backends.** Committed nelli `milpa.kdl`+`milpa.lock` only (scripts reverted, NOT committed). NOT pushed. Memory `nimz3-ffi-vendoring` rewritten for the git-ref model + sanctioned new milpa host tool. **Nothing outstanding on the milpa side-task.**

## Stage 4 — /code-review ledger (opened 2026-07-29; scope = full RFC diff 99fa2db→HEAD src/, 4007+/382-)
5 review agents (symex-core, parser/IR, fuzz/cov/db, security, design) + 5 adversarial verifiers.
Presented to Corey 2026-07-29; **Corey approved the default mandate (fix through Medium, leave Lows).**

### Fix loop — IN FLIGHT (2026-07-29)
Ordering constrained by: R1/R3/R5 all touch runtime.nim (serialize); R1 & R2 both bump SW+CR2 pin (serialize SW).
- **Wave 1:** R4 ✅ DONE (fuzz.nim nil-guard — gates loadCorpus/saveCorpus on their OWN *CorpusImpl fields; new tests/tfuzzcorpus_nilguard.nim RED=SIGSEGV→GREEN both backends; uncommitted). R1+R5 SPLIT after subagent ab7cd7cf DIED on an API error mid-compile:
  - **R5 ✅ SALVAGED** (control loop): subagent had cleanly parameterized the 4 drain procs into `genRaiseForkDrain(procName,field,gate,defectType,msg)` template but it didn't compile (died on it). Control loop fixed 2 compile bugs: (1) bare `{}` gate inferred `set[empty]` → added `const noArithGate: set[ArithCheck] = {}` (∅⊆arithChecks ⇒ vacuously-true ⇒ unconditional drain, the intended semantics — NOT a `when` guard); (2) the sibling `genSyncRaiseCond` template broke forward-decls (sync procs fwd-declared ~909-943; template procs can't satisfy fwd decls) → REVERTED that half, kept 4 explicit sync procs (only 6 lines each; the 230-line DRAIN dedup is the real R5 value). Verified compile+behavior-preserving BOTH backends: snd4(uncond), R16_3_divzero + R16_4_overflow(gated). NO SW bump (behavior-identical). Uncommitted in runtime.nim.
  - **R1 IMPLEMENTED but BLOCKED on a newly-discovered Critical** (subagent a155ad01 added drains at all 5 arms + tsymex_r1_draingap.nim + SW 60→61 + CR2 pin→61; targeted tests green). **Control-loop sweep (442 pairs) found 4 fails, both backends (NOT flakes):**
    - `tsymex_phase3_summarization` — SOUND CORRECTION: `helper(x)=x+1` (direct return) now correctly forks an OverflowDefect (default acOverflow), so its return survivor carries a defect-survivor fact → summarization cache-eligibility (runtime.nim:6483, "only cache a clean defect-free single return") correctly declines → walked==2/cacheHits==0. Pre-R1 cache-hit relied on the DROPPED overflow. Needs test migration (run under overflow-off settings to still exercise the cache, or assert the new sound stats).
    - `tsymex_q1_scanlift` Q1-4a — **FALSE POSITIVE (R1 unsound at isWhile):** `while i<s.len and s[i]==' '` now returns false `sxRaised(IndexDefect)`. Root cause CONFIRMED by probes: short-circuit `and` in IF-conditions IS desugared into guarded path-splits (isIf var-index bounded → sxSat, correct), but a WHILE-guard keeps `and` FLAT, so `s[i]`'s OOB is deposited UNGUARDED and R1's isWhile drain forks it even though real Nim short-circuits (`i<len` false ⇒ s[i] never read). Ubiquitous idiom (`while i<len and s[i]…`); mostly Q1-lifted, but unlifted forms (==/skip-while) hit it. Other 4 arms are fine (isReturn's drain even surfaced the legit overflow above).
    - **Corey chose: fix while-guard short-circuit properly (option A).** ROOT CAUSE (fully confirmed by probes, deeper than R1): a PRE-EXISTING SND-4 (v59) soundness bug — `rhsHasInlineDefectFork` (dsl_parser.nim:581) handles `iekStrAt` only by recursing into strArgs, never SELF-REPORTING true like `iekBinop{bDiv,bMod}` (line 549). So SND-4's `s[i]` OOB fork doesn't trigger D1c's short-circuit guard (dsl_parser.nim:1187-1219) → `A and s[i]` lowers as flat `bAnd`, OOB deposited UNGUARDED → false `sxRaised(IndexDefect)` for bounded `i<len and s[i]` whenever no reachable target masks it (proven: `if`/`let` forms with a tRaisedExn(IndexDefect) search both false-raise TODAY, pre-R1). R1 extended it to while-guards (q1_scanlift Q1-4a).
    - **THE FIX (delegated, subagent):** (1) `rhsHasInlineDefectFork`: `iekStrAt` self-reports `true` → D1c guarded path fixes if/let/assign/return (once-eval arms) immediately. (2) while-guard: when D1c preamble `wp` non-empty, restructure `wp; while cond: body` → `while true: wp; if not cond: break; body` so the guard (incl. D1c short-circuit) re-runs each iteration (also fixes a latent stale-sc for div/etc. in while-guards). Q1-lifted scans bypass (recognizer runs first). MUST keep: unbounded `while s[i]` STILL raises (real OOB). (3) migrate phase3_summarization (isReturn overflow sound-correction) — run its cache test under acOverflow-disabled settings so `x+1` stays clean+cacheable. SW stays 61 (part of R1 landing). Then control-loop FULL sweep.
    - **R1b landed (uncommitted) + completeness follow-up IN FLIGHT (subagent ad9502e0):** Part1 (iekStrAt+iekStrToInt self-report) + Part2 while-restructure done in the parseStmtInner while-arm (~2979); new tests/tsymex_r1b_shortcircuit_oob.nim (17 checks) + phase3 migrated (noOverflowSettings) + Q1-4a restored, all targeted green both backends. GAP found by control loop: the SECOND nnkWhileStmt arm (~2665, parseIterBodyStmt / while-in-for-body) still had the stale-hoist pattern → resumed subagent to extract a shared `mkGuardedWhile(cond,body,guardPre)` helper and route BOTH arms through it + add a while-in-for-body regression test. Then control-loop FULL sweep (prior sweep bufxwww9u was killed mid-run since the tree was about to change; also cleaned up ~8 orphaned sweep-waiters from earlier sessions).
    - **R1b COMPLETE (both arms, uncommitted).** Shared `mkGuardedWhile(cond,body,guardPre)` helper (dsl_parser.nim ~2591) routes BOTH nnkWhileStmt arms. Subagent made an autonomous design change (sound, control-loop reviewed OK): NOT `while true: guardPre; if not cond: break; body` (that blows up the path frontier when nested in another k-unrolled loop → hang at unwind=5), but a DO-WHILE LOOP ROTATION `guardPre; while cond: (body; guardPre)` — keeps a real SAT-able guard, composes under nesting. Correctness: the duplicated `let sc` is an env overwrite (flat env), refreshed sc flows to next guard check via body output paths — verified nested case both backends. tsymex_r1b_shortcircuit_oob extended with for-nested-while cases (run at maxLoopUnwind=3 — nested defect-guarded scans are inherently costlier now: the s[i] fork re-fires inner×outer). All targeted tests green both backends; SW stays 61.
    - **FULL SWEEP IN FLIGHT: `scratchpad/r1b_final_sweep.log` (background butlyabrg, 220s timeout, 442 pairs).** WATCH: a 137 (hang) on any existing test with a defect-guarded while nested in another k-unrolled loop at default unwind=5 — the soundness fix makes that shape correct-but-expensive; if one hangs, investigate (bump that test's unwind / accept / reconsider), do NOT wave through.
    - **RESUME after sweep:** tally r1b_final_sweep.log (438+ rc0; any 137 → investigate per above; any rc=1 → sound-correction vs regression analysis). If clean → commit R4 (fuzz.nim + tfuzzcorpus_nilguard.nim) separately, then R5+R1+R1b (runtime.nim + dsl_parser.nim + canonicalize.nim + CR2 pin + phase3 migration + tsymex_r1_draingap + tsymex_r1b_shortcircuit_oob) as one "R1 Critical + R5 refactor" commit (SW 60→61). Then fcsweep for R4. Then Wave 2: R2+R6 (dsl_parser.nim Q1 recognizer: reject loop-counter-dependent bound + sameSymbol; SW→62, rebased on committed R1) ∥ R3 (runtime.nim forkPath/forkPathTainted split, no SW bump). Ledger R1..R6 in the table above.
- **Wave 2 (after R1 lands, rebased on its SW):** R2+R6 (dsl_parser.nim: reject loop-counter-dependent bound + sameSymbol identity, SW→62) ∥ R3 (runtime.nim forkPath→forkPath/forkPathTainted split, no SW bump; disjoint files from R2).
- **RESUME:** await wave-1 subagent completions → review diffs (esp. did the 5-site drain flip any existing test = soundness correction vs regression?) → `scripts/psweep.sh` ONE both-backend sweep (watch [[sweep-waiter-self-match]]) → commit R4 and R1+R5 separately → launch Wave 2 → re-review changed scope (standing Security+Design agents too) → loop to floor (0 Crit/High/Med). Ledger below tracks status.

| id | sev | finding | status | proof / verifier |
|----|-----|---------|--------|------------------|
| R1 | CRIT | scalar-raise sinks (strIndexOob + parseInt/div/overflow) NOT drained at 5 walk arms; lowerInExpr resets sink on entry → silent discard → false sxSat past a real IndexDefect. **+ exposed a pre-existing (v59) short-circuit false-sxRaised: `A and s[i]` forked OOB unguarded (rhsHasInlineDefectFork didn't self-report iekStrAt/iekStrToInt); fixed via self-report + mkGuardedWhile do-while rotation on both while-arms (R1b).** | **FIXED c75285f** (SW 61) | drain at 5 arms + R1b short-circuit guard; tsymex_r1_draingap + tsymex_r1b_shortcircuit_oob; phase3 migrated (overflow-off); FULL sweep 444/444 both backends |
| R2 | CRIT | Q1 `tryRecognizeScanIdiom` accepts non-loop-invariant bound; closed form evals bound once at entry → `while i<(n-i)` mis-rewritten → witness divergence | **FIXED 4fea3da** (SW 62) | CONFIRMED a6aa79cd; refersToSym gate; tsymex_r2_scanbound RED sxSat→GREEN sxUnknown both backends |
| R3 | HIGH | `Path.uncertain` taint convention-based: forkPath bool 4th arg, ~45 sites; `forkPath(…,false)` silently drops taint → latent false sxSat | **FIXED 3428987** | forkPath/forkPathTainted + internal forkPathWithTaint; 45 sites (37 propagate/7 tainted/1 computed-bool); behavior-identical, no SW bump, sweep green |
| R4 | MED | fuzz.nim `dbActive` (gates on saveImpl only) drives loadCorpus/saveCorpus through un-nil-guarded new fields → minimal custom ExampleDatabase nil-crashes under fuzz() | fixed (uncommitted, fcsweep verifying) | gate on loadCorpusImpl/saveCorpusImpl own nil-ness; tfuzzcorpus_nilguard RED=SIGSEGV→GREEN both backends |
| R5 | MED | raise-fork drain family = 4 near-identical templates (SND-4 added 4th verbatim) → parameterize `genRaiseForkDrain` | **FIXED c75285f** | genRaiseForkDrain template + noArithGate const; behavior-identical both backends (folded into R1 commit) |
| R6 | MED | Q1 recognizer identifies "same var" by `.strVal` not symbol identity → template/gensym mis-match | **FIXED 4fea3da** | sameSym (macros.== on nnkSym) at 3 recognizer sites |

**✅ ALL 6 mandated findings (R1-R6) FIXED + committed** (c75285f R1+R5+R1b · 46b0ac3 R4 · 3428987 R3 · 4fea3da R2+R6). Wave-2 combined sweep 445/446 (1 c-only z3_infra parallel-load flake cleared standalone). Working tree clean; stale stash@{0} dropped.
**RE-REVIEW ROUND 1 RESULTS:** security CLEAN, design CLEAN (2 Low polish), correctness found **R14 (CRIT, introduced by R1b)**: mkGuardedWhile do-while rotation is NOT continue-safe — trailing guardPre (guard refresh) is skipped when body hits `continue` (walkBlock stops on the zero-path return) → stale `sc` → false verdict. CONFIRMED by probe (fCont continue-loop gives sxUnknown, real Nim sxSat). 2nd subtle failure of the parse-time rotation approach.
**✅ R14 FIXED + COMMITTED 98f4564 (SW 62→63).** Full sweep 447/448 (1 c-only z1_canary parallel-load flake cleared standalone); new tsymex_r14_continue_guard 10/10 + all R1b/Q1/R2/snd4/draingap green both backends. Control loop reviewed mkShortCircuitWhile dispatch + hasContinueShallow as sound.
**✅ RE-REVIEW ROUND 2 DONE — FLOOR REACHED (0 Crit/High).** correctness (a99fdc4c): R14 mechanism verified sound (continue-safe by construction, break/nesting correct, `continue` is the only hazard); one MEDIUM = Case-2 sound over-degrade (`while (a div b)>i and s[i]` + continue → sxUnknown via degrade because LHS hoists a no-op artifact preamble so `preA.len==0` gate misses it; NEVER a wrong verdict — Inv-3 holds; recommended+accepted as sound Low, don't do a 5th while-guard revision). design/security (a11b78c8): CLEAN; one Low = hasContinueShallow dups hasBreakContinueShallow.
**Corey chose: FLOOR + BATCH THE SAFE LOWS.** Low-cleanup batch IN FLIGHT (subagent a16028e1, NO version bump — all behavior-identical/doc/test): (1) hasContinueShallow+hasBreakContinueShallow → shared hasKindShallow (dsl_parser); (2) genRaiseForkDrain gate set→Option[ArithCheck], drop noArithGate (runtime; fallback = fix stale `card` comment if static Option won't compile); (3) svIntToBV doc fix (parseInt negative ok via mod-2^W); (4) NEW tsymex_r14_case2_degrade pinning the sound degrade; (5) coverage.nim registerEdgeSource/edgeSources internal-doc note; (6) db multiplexedDatabase carry secondary meta when primary meta empty.
**BATCH DONE (subagent a16028e1, all 6 items, uncommitted, no SW bump). Symex sweep 450/450 CLEAN (bzrjx9yss); fcsweep running (bqi69ivoi).** Item 2 took the Option[ArithCheck] path (compiled clean, noArithGate deleted). **RESUME: tally fcsweep → if clean `git add` the 5 batch files (dsl_parser.nim, runtime.nim, coverage.nim, db.nim, tests/tsymex_r14_case2_degrade.nim) + commit `refactor+docs(symex,fuzz): batch low-severity code-review cleanups (hasKindShallow, Option gate, docs, meta-fill, case2 pin)` → update ledger to CLOSED → report final /code-review outcome to Corey. THEN the whole /code-review is COMPLETE (floor reached, all Crit/High/Med fixed, safe Lows batched, R7/R8/R11 tracked follow-ups).**
**TRACKED FOLLOW-UP LOWS (deliberately deferred, not in batch):** R7 Q1 AST-unwrap dup→unwrapHidden · R8 P2a omitted-non-scalar→weInternalWalkerFault telemetry hygiene · R11 C1 edgeSourceTable unguarded-global/redundant-reregister · Case-2 precision (inert-preamble detection, if ever wanted).
**[impl detail] R14 FIX IMPLEMENTED (subagent ad1221c0):** dispatch reviewed sound by control loop — mkShortCircuitWhile: Case1 clean and-split (continue-safe by construction) / Case1b flat / Case2-3 rotation-if-continue-free ELSE sound mkUnsupported degrade / Case4 plain mkWhile (incl. unbounded `while s[i]` still raises). hasContinueShallow verified correct (stops at nested loops + routine boundaries). mkGuardedWhile DELETED. SW 62→63, CR2 pin 63, r1b `==` pin converted to `>=` floor (only CR2 is live == pin now; the 4 other grep hits are `##` comment prose). Subagent self-caught + fixed an over-degrade (nnkStmtListExpr artifact preamble ≠ fault; gated on continue not preamble-shape; regression caught by tsymex_r1_draingap whileDivZero). New tsymex_r14_continue_guard 10/10 + all R1b/Q1/R2/snd4/draingap re-run green both backends (targeted). **RESUME: tally r14_sweep.log (446+ pairs; verdict flips = sound-correction analysis; commit R14 as its own fix commit SW 62→63) → then re-review ROUND 2 on the R14 diff (Step 7 loops till clean) → floor = report Lows.**
**[superseded detail] R14 FIX (Corey: "best-in-class design"):** replace mkGuardedWhile with FAITHFUL short-circuit desugaring at the loop level: `while (A and B): body` → `while A: (Bpre; if not B: break; body)`. B (with its s[i] fault) lowers INSIDE the body where guard A holds → the OOB fork is `pc∧oob=(i<len)∧(i≥len)`=UNSAT, guarded FOR FREE by loop semantics (no sc temp, no D1c-for-while). continue-safe by construction (continue re-checks A + re-runs Bpre at top); no blowup (A is a real SAT-able guard, not `while true`); handles hoisted stmts. or-with-fault/nested → sound mkUnsupported degrade (Inv-3). Deletes mkGuardedWhile. SW 62→63. New tsymex_r14_continue_guard; also converts the stray tsymex_r1b `==` pin to a `>=` floor. Then control-loop FULL sweep + ANOTHER re-review round (the fix changes code → re-review again per Step 7).
**RE-REVIEW ROUND 1 (Step 7 termination check) — done, reopened by R14:** 3 agents (correctness a7ec8401 / design a97d14f6 / security a1111c6e) on `git diff 485eced HEAD -- src/`, hunting bugs the FIXES introduced (hardest look: mkGuardedWhile do-while rotation's duplicated `let sc`; 5-arm drain survivor threading). If nothing above Low → floor hit → report Lows + stop. Any Crit/High → adversarial-verify, fix, re-review again.
**Remaining Lows (from initial review, deferred per mandate):** R7 Q1 AST-unwrap dup→unwrapHidden · R8 P2a omitted non-scalar→weInternalWalkerFault telemetry hygiene · R9 coverage.nim public-surface leak · R10 svIntToBV doc comment wrong · R11 C1 edgeSourceTable unguarded/redundant-reregister · R12 multiplexedDatabase drops secondary meta on collision · R13 F2 seed-replay coverage-credit (wontfix). Plus tiny cleanup: tsymex_r1b uses a 2nd `==` SW pin (should be `>=` floor per convention).
| R7 | LOW | Q1 AST-unwrap idiom duplicated ~10× inline → unwrapHidden helper | **fixed** | aab18ce — unified 10 blind-peel sites into `unwrapHidden`; subtle ref/ptr-deref sites deliberately left; pure refactor, no SW bump; sweep 452/452 both backends |
| R8 | LOW | P2a omitted non-scalar field → mkIntLit(0) mismatch → caught ValueError → sxUnknown (NOT a crash) but pollutes weInternalWalkerFault telemetry; classify at construction | **fixed** | aab18ce — kind-correct `unsupportedFieldPlaceholder`; verdict stays sxUnknown (taint owns it), only error KIND cleaned; new tsymex_r8_omitted_field_degrade.nim; no verdict/witness flip → no SW bump |
| R9 | LOW | coverage.nim exports macro-plumbing procs (registerEdgeSource/edgeSources) beside the one real entry point uncoveredSources | **fixed** | 9be7b5a — kept exported (tcovsourcetable.nim uses them directly; macro uses bindSym so export not needed for it); corrected the inaccurate doc rationale |
| R10 | LOW | svIntToBV doc comment "always non-negative" false for parseInt("-5"); mechanism sound (int2bv mod 2^W) — doc fix | **fixed** | 7c3a16d (Low batch) — doc corrected |
| R11 | LOW | C1 edgeSourceTable unguarded global + redundant per-eval re-registration | **fixed** | 9be7b5a — documented single-threaded-by-contract + idempotent set-merge invariants on the global; no lock/guard added (dead weight); doc-only |
| R12 | LOW | multiplexedDatabase drops secondary backend metadata on choice-seq collision (db.nim:658-664) | **fixed** | 7c3a16d (Low batch) — secondary meta carried forward when primary meta empty |
| R13 | INFO | F2 up-front seed-replay credits a crashing seed's coverage without surfacing in irCrashes (documented intentional) | wontfix | fuzz reviewer |

**Verified CLEAN:** DB v2→v4 parse (bounds-checked, safeLen 64MiB cap → DbCorrupt); Z3 FFI lifetimes; heap-snapshot 2-axis recursion bound; cache-key distinctness (3-arg find/assume/tupleLit) + full version-pin discipline; SND-1/SND-3/Cluster-H taint+chokepoints; Q1 start-offset threading; corpus channel independence; metadata stickiness; collision honesty; no exploitable security findings.

**Synthesis:** R1 (live) + R3 (latent) + R5 (duplication) are three faces of ONE root flaw — soundness-critical per-path bookkeeping threaded by site-local convention with no structural enforcement. Fixing R1 via a central per-statement drain chokepoint + R5 parameterization + R3 forkPath split = one systemic close, not three patches.

---


- **Stage:** 3 (tdd grind) — **AUTONOMOUS GRIND COMPLETE 2026-07-26; then Q1 spike + SND-3 soundness fix (post-grind, with Corey)**   •   Architect rounds 1&2 done
  - **✅ SND-3 (finding-B fix) LANDED `5dd965a`** (SW 57→58, RC stays 7; full symex sweep 436/436 both
    backends — the two augmented_assign pairs that hung under accidental 3×-concurrent-sweep contention
    re-verified passing STANDALONE; new test 8/8 both backends). New soundness slice (NOT in original
    RFC — surfaced by the Q1 spike). Fixed the C-backend false `sxUnsat`. Subagent
    `a2b5d4dce3ec39bd9` implemented (detached on sweep, 14th stall); control loop verified independently
    + swept + committed: runtime.nim mechanism = in-band degrade at 3 loop-reachable lowering raise-sites
    (CR-17(a) char-ordering, cmpString string-ordering, iekContains non-int64-set membership) →
    threadvar sinks (`loweringDegradeErrors`/`loweringDidDegrade`) → per-path `uncertain` taint at
    `drainPendingLowerEffects` (fork-before-mutate) + `w.sawUnknown` + dedup-drain into errors. NOT a
    bare sawUnknown (would fabricate false sxSat — the crux). Left `allocateSym` param-alloc raises
    (safe, pre-walk). SW 57→58, RC stays 7, CR2 pin→58. ADR-0023 + RFC SND-3 row added.
    New `tests/tsymex_snd3_loopdegrade.nim` 8/8 BOTH backends (tracer + per-path-no-false-sxSat trap +
    no-over-degrade + non-loop regression + classified-kind + equality regression). probe5778
    (walk-arm Table raise) showed NO divergence → not an additional hole. **RESUME: tally
    `scratchpad/snd3_sweep.log` (via `scratchpad/psweep.sh`, expect 436=218×2 all rc0; re-verify any
    lone c rc=1 standalone), then `git add -A` (runtime.nim, canonicalize.nim, CR2 pin, RFC.md,
    SYMEX_PLAN.md, new test — NOT scratchpad/NOT handoff) + `git commit --no-verify` "feat(symex):
    SND-3 — loop-guard lowering degrades taint in-band (fix C-backend false sxUnsat)".**
  - **DECISION (Corey 2026-07-27): build Q1 + finding A together.** Sequenced A→Q1 (SW-bump
    serialization: both bump SW + touch CR2 pin; AND Q1's scan-then-OOB test depends on A's IndexError
    modeling).
  - **✅ SND-4 (finding A) LANDED `39953ff`** (SW 58→59, RC stays 7; full symex sweep 438/438 both
    backends = 219 files ×2; new test 6/6 both backends). Subagent `a14135c5fe7db76ef` implemented
    (detached on sweep, 15th stall); control loop verified + swept + committed. **Control loop caught +
    fixed an unmigrated flip the subagent's detached sweep missed:** `tsymex_snd2_assume` flipped
    sxUnsat→sxRaised because its SUT indexed `s[0]` on a FREE string (`s==""` → real reachable
    IndexDefect, which SND-4 correctly now forks → sxRaised dominates sxUnsat per D1a). This is a genuine
    soundness catch, NOT a regression — migrated the unreachable condition to `s.len==3 and s.len==4`
    (no OOB, preserves the symexAssume-masking test intent). S3 `s[5]` test also migrated (→sxRaised).
    ADR-0024. Mechanism below:
  - **[landed, detail]** SND-4 (finding A). String char index reads
    (`iekStrAt`, `runtime_strings.nim:114-127`) have ZERO IndexError modeling (OOB silently returns
    0xFF per its own comment) → `tIndexError()` over `s[i]` falsely returns sxUnsat. FIX: mirror the
    parseInt/divByZero lowering-sink→drain-fork-raise machinery (new `strIndexOobConds` sink +
    `syncStrIndexOobCond` + `drainStrIndexRaises` folded into `drainScalarRaiseForks`; deposit
    `oob=(i<0 or i>=len)` in iekStrAt lowering, fork IndexDefect at the drain — NOT a fork from
    lowering, the SND-3 anti-pattern). Continuing path gains `not oob` (may migrate string-index
    tests — bounds corrections). SW 58→59, RC stays 7, CR2 pin→59. New ADR (after 0023) + RFC SND-4
    row. **RESUME: verify subagent (tracer sxRaised, scan-then-OOB A4 repro sxRaised, bounds-safe
    sxUnsat, migrations sane), ONE clean psweep 436/436 both backends, commit if subagent didn't.**
  - **✅ Q1 LANDED `260267d`** (subagent self-committed after its stall cleared; SW 59→60, RC stays 7,
    CR2 pin 60, ADR-0025). **🎉 Q1+A PAIR COMPLETE.** Control loop verified: Q1 test 13/13 (subagent
    counts 15 incl. version pins) both backends; SW/pin/ADR confirmed in committed tree; E5_finally +
    the other 4 exception-test 137s from the sweep = -P6 contention flakes, re-verified rc0 standalone
    (subagent + control loop). Full sweep 440 pairs, 435 rc0 + those 5 flakes cleared; an earlier
    control-loop partial reached 291/438 clean independently. **No final full clean re-sweep run** —
    convergent evidence (partial-clean + Q1 green + flakes-cleared) deemed sufficient for a doc/verify
    close; if belt-and-suspenders wanted, run `scratchpad/psweep.sh <log> 200` ONCE (host is slow —
    ~30s/compile; watch [[sweep-waiter-self-match]]). Implementation detail below:
  - **[landed, detail]** Q1. SW 59→60, RC stays 7, CR2 pin→60,
    ADR-0025 + RFC Q1 row updated to LANDED. **New `tests/tsymex_q1_scanlift.nim` 13/13 BOTH backends**
    (control loop ran standalone): P1a/b (3-arg find honors start + UNSAT companion), Q1-1 tracer scan
    lift sxSat +Q1-1b UNSAT clamp companion, Q1-2 chained/dependent scan +Q1-2b UNSAT (j<i impossible),
    Q1-3 scan-then-OOB sxRaised (end-to-end w/ SND-4), Q1-4a/b/c scope trip-wires (==-guard, char-class,
    non-inc body all stay sxUnknown → recognizer proven NARROW), Q1-5 plain loop still k-unrolls.
    Mechanism (control loop reviewed): `tryRecognizeScanIdiom` (dsl_parser.nim) called in BOTH
    nnkWhileStmt arms before mkWhile; matches `while i<bound and s[i]!=lit: inc i` (validates every
    node's type/sym identity; handles `!=`→not(==) desugar; body exactly inc/+=1), emits closed form
    `i = (let p=s.find($lit,i); if p==-1 or p>=bound: bound else: p)`; ANY mismatch → mkWhile untouched.
    3-arg `iekStrFind` (optional strArgs[2] start → nim-z3 `indexOf(a,sub,start)`), cache-key distinct
    (canonicalize renders all strArgs), ALSO fixed a latent start-drop unsoundness. **Sweep status
    2026-07-28:** a partial sweep reached 291/438 CLEAN (only augmented_assign cpp 137 = known
    load/contention flake, passes standalone) before a self-perpetuating waiter orphan
    [[sweep-waiter-self-match]] confounded process-counting; killed all, RE-RUNNING one clean sweep →
    `scratchpad/q1_sweep2.log` (200s/test). **RESUME: tally that log** (438=219×2 all rc0; re-verify any lone non-zero standalone), then
    `git add -A` the 7 changed/new files (dsl_parser.nim, runtime_strings.nim, types.nim,
    canonicalize.nim, CR2 pin, RFC.md, SYMEX_PLAN.md, tests/tsymex_q1_scanlift.nim — NOT scratchpad,
    NOT handoff, NOT the stray tests/probe_q1 binary [already rm'd]) + `git commit --no-verify`
    "feat(symex): Q1 — lift bounded scan-to-delimiter loops to closed-form indexOf".** That COMPLETES
    the Q1+A pair. Then only INT-1 (blocked on release) → Stage 4 /code-review.
  - **[SUPERSEDED — Q1 done above]** Q1 QUEUED after SND-4 (rebase on SW 59→60). GREEN/buildable per spike. Core: (1) proper
    3-arg-find IR path (distinct kind + parser arity dispatch, NOT the spike's `iekStrFind` strArgs
    overload trick — nim-z3 `indexOf(a,sub,start:Z3Int)` @ sequence.nim:180 already exists) + (2) the
    idiom-RECOGNIZER at the `nnkWhileStmt` parse site (`dsl_parser.nim:2498`/`2805`): match
    `while i<bound and s[i]!=lit: inc i` (single + dependent chains) → rewrite to `indexOf(s,lit,start)`
    closed-form instead of mkWhile k-unroll. Scope LOCKED to literal/substring delimiter scans; predicate/
    char-class scans stay degraded (Phase-C boundary). Headline test = scan-then-OOB found (needs SND-4).
  - **After Q1:** INT-1 (blocked on release) is the last item; then Stage 4 `/code-review`.
  - **✅ CLUSTER H COMPLETE (2026-07-25):** named ref-object HEAP IDENTITY fully delivered.
    A `d85f0f7` (nominalId) → B `ddc9196` (refPointeeTypeId) → C `40ed16f` (heap identity: aliasing/
    identity real verdicts) → H_containers `5f4639c` (seq/array/tuple of Node) → H_witness `2244d1b`
    (recursive cycle-safe heap-snapshot) → closeout `46c7b80` (verification + ADR-0022 marked LANDED).
    Final versions **SW 57, RC 7**; 432/432 both backends. No bugs found in verification. Supersedes
    P2b value-model `42eafde`. **NEXT: base clusters Q → TOT → INT → F → C** (see slice order below).
  - **✅ TOT-1 LANDED `85603f5`** (test+cfg only, no version bump): 9-item table-driven §0-totality
    corpus across the 3 open surfaces (CR-2a parser catch-all; CR-2b/2c type+witness classifier;
    SND-1/1b/CR-1c taint+fault), each asserted to degrade to a CLASSIFIED sxUnknown. Backstop VALIDATED
    (reverting SND-1's taint made the corpus fail → restored; control-loop confirmed the revert did NOT
    leak into the commit — runtime.nim byte-identical vs 46c7b80). Documents historical repros excluded
    as now-modeled (&=→M4, if-expr→M5, bitwise→CR-1a). 434/434 (lone phase13_macro c flake re-verified
    standalone). No §0 blocker found.
  - **🎯 MILESTONE: the entire soundness/crash-totality/model-gap/parser/heap-identity/totality CORE of
    the mega-RFC is COMPLETE and green.** Landed: Clusters 1-4 (SND-1/1b, SND-2, CR-1a/b/c, CR-2a/b/c,
    M1-M6, P1, P2a) + Cluster H (P2b heap-identity, 6 slices) + TOT-1. **REMAINING work splits:**
    (a) **needs-Corey / non-autonomous:** INT-1 (chapulin pin-bump + workaround removal — cross-repo
    exit gate, external `/mnt/c/.../chapulin` repo + its harness; the recurring per-SW-slice smoke runs
    were NOT done during the grind, so this is a batch exit-gate now); Q1 (dependent-bounded-loops
    research SPIKE, may have no viable encoding); SH1 (does-not-repro-at-HEAD, deferred pending
    chapulin's exact repro). (b) **decoupled autonomous-able Fuzz/Coverage track (lower-stakes polish,
    do NOT gate on soundness):** F1 (L, non-pruned coverage-corpus channel), F2 (M), F3-F5/F8 (S),
    F6/F7 (M), C1 (L, slot→file:line:col side-table), C2 (S doc-only).
  - **DECISION (Corey, 2026-07-26): grind the F/C decoupled track.** INT-1/Q1 deferred for deliberate
    handling with Corey. **F1 IN FLIGHT** (subagent `afd4941f8af443a66`): non-pruned coverage-corpus
    channel separate from dbReusePhase's prune-on-pass (engine/phases.nim + db.nim + fuzz.nim). Fuzz/DB
    subsystem — NO symex version-pin obligation. Regression gate = the tdb*/tfuzz*/tcov*/tengine suite
    both backends. Order after F1: F2 → F3 → F4 → F5 → F6 → F7 → F8 → C1 → C2.
  - **✅ F1 LANDED `422869a`:** dedicated never-pruned `corpus` DB section (3rd alongside primary/
    secondary), saveCorpus/loadCorpus, on-disk format v2→v3 w/ backward compat; fuzz.nim persists there
    (not primary). tfuzzpersist migrated (loadPrimary→loadCorpus); new tfuzzcovcorpus proves pass/reject
    retained-in-corpus-but-pruned-from-primary + channel independence + round-trip. 46/46 real
    fuzz/db/cov/engine tests both backends. **F/C-track regression gate helper: `scratchpad/fcsweep.sh
    <log> [timeout]`** (globs the 23 real tdb/tfuzz/tcov/tengine .nim sources, both backends — do NOT
    hand-list DB test names: `tests/tdbgc*`/`tdbg_e4a*` are STRAY GITIGNORED BINARIES, not sources).
  - **✅ F2 LANDED `dd0f14d`:** up-front coverage-replay of preloaded seeds (fuzz.nim, `preloadedCount`
    guard excludes the fallback seed → no-seed path unchanged; reuses newReplaySource→generate→run;
    frontier.admit up front). New tfuzzseedcov (7). 48/48 both backends.
  - **✅ F3 LANDED `eb09f70`:** exported `minimalCovering*` (pure export, no behavior change). New
    tfuzzmincover (3, greedy set-cover pins). Both backends.
  - **✅ F4 LANDED `3562ff8`:** `FuzzSettings.stopOnFirstCrash` (default false); break in the generic
    `fuzz()` crash-recording block gated on `isNewCrash = not seenCrashKeys.containsOrIncl(key)` (first
    NEW de-duped crash; byte-mode `fuzzWithBytes` has no de-dup → out of scope); post-loop bookkeeping
    (minimizeCorpus/iterations/coverageHits) preserved. New tfuzzstopcrash (6). 52/52 both backends.
  - **✅ F5 LANDED `01fb3a8`:** doc-only — documented `dedupPrepend`/`applySave` newest-first
    insertion-order contract (prepend@0, truncate TAIL→evict-oldest, re-save moves to front, reload =
    reverse-insertion order which dbReusePhase relies on). Order already pinned by tdb.nim (20/20).
  - **✅ F6 LANDED `0307b3d`:** per-primary-entry metadata slot — `PrimaryEntry` tuple +
    `Table[string,string]` meta (mirrors secondary's label table, string-valued for witnesses; FUZZ_PLAN
    defines no payload → bare extensibility slot, goal-determined by precedent). Format v3→v4 backward
    compat (v1-3 read empty, upgrade on write; parseContents now lists v3 explicitly). API additive:
    `save(...,meta)` overload + `loadPrimaryWithMeta`; `save`/`loadPrimary` unchanged. Sticky dedup
    (plain re-save preserves meta; explicit non-empty meta overwrites). New tfuzzprimarymeta (15). 54/54
    both backends. **6 F-slices done (F1-F6).**
  - **✅ F7 LANDED `d1a431d`:** documented the 2N+1 choice-IR draw protocol on the lists combinator
    (N+1 continue-gates interleaved with N element draws; nested strategies recurse) + added
    `FuzzReport.droppedSeeds` (counts PRELOADED seeds — initialIRCorpus + DB loadCorpus — that fail
    captureIR; fallback random seed doesn't count). New tfuzzdroppedseed. Green both backends (lone
    tfuzzloop c sweep failure = parallel-load flake, re-verified passing standalone). **7 F-slices done.**
  - **✅ F8 LANDED `ee70ce0`:** `sectionSizes(db, testId): tuple[primary, secondary, corpus: int]` —
    thin wrapper over load* counts (all backends, no new plumbing). New tfuzzsectionsize (7). Green both
    backends (tfuzzdroppedseed/tfuzzstopcrash c sweep failures = parallel-load flakes, re-verified
    standalone). **✅ ALL 8 FUZZ SLICES DONE (F1-F8).**
  - **✅ C1 LANDED `5da1e20`:** slot→`file:line:col` side-table at `{.cover.}` expansion. The pragma
    hashed each branch's `(file,line,col)` into the 8192-slot bitmap and discarded the location; C1
    emits the other half — a module-global (NOT threadvar — static program structure) `Table[int,
    OrderedSet[string]]` slot→locations populated at expansion, plus `registerEdgeSource`/`edgeSources`/
    `uncoveredSources` (the source-mapped gap report: registered slots whose bitmap byte is 0, ascending
    slot order). Collisions handled honestly (OrderedSet; slot reads covered iff any colliding loc hit),
    NOT disambiguated. proc/func emit regs as sibling stmts; lambdas wrap regs+lambda in nnkStmtListExpr
    (no `newStmtListExpr` convenience in std/macros → built via `nnkStmtListExpr.newTree`). NO version
    pins (leaf module, not symex, not DB-persisted). New tests/tcovsourcetable.nim (7). Both backends
    GREEN; control loop independently ran tcovsourcetable+tcoverage+tcoveragemode+tcovguided ×2 (all rc0);
    subagent fcsweep 60/60.
  - **✅ C2 LANDED `a2ab210`:** doc-only — module-header note "Why 8192, and how it converges (#C2)":
    power-of-2 for `and`-mask + contiguous 8KiB + hash-pure slot (no global counter); occupancy
    `M·(1−e^(−E/M))`, collision-free only while `E ≪ √M≈90`, converges toward M as E→M; `currentCoverage`
    is a monotone LOWER bound + colliding edges indistinguishable; non-issue at nelli per-SUT scale;
    **C1's side-table (not a bigger map) is the lever** that makes collisions visible/nameable. Pure
    additive comments (34 insertions), compiles clean both backends. Documentation DoD.
  - **🏁 ENTIRE AUTONOMOUS-ABLE RFC COMPLETE.** All of Clusters 1–4 + H + TOT-1 + F1–F8 + C1–C2 landed
    and green. **ONLY INT-1 + Q1 REMAIN — BOTH STRUCTURALLY NEED COREY, NOT the /loop:**
    (a) **INT-1** = chapulin cross-repo exit gate: build + smoke-run the EXTERNAL `/mnt/c/Users/corey/
    projects/chapulin` repo + its harness against hardened nelli, then remove chapulin's workarounds +
    bump its nelli pin. The per-SW-slice smoke runs were deferred during the grind, so this is a BATCH
    exit-gate now. Cannot run in an unattended /loop (drives another repo). (b) **Q1** = dependent
    bounded-loops research SPIKE (Solver, L); RFC says it "may have no viable sound-and-fast encoding" and
    "names no candidate encoding" → may dead-end with nothing committable; poor unattended fit — handle
    deliberately with Corey. **SH1 deferred** (does-not-repro-at-HEAD; needs chapulin's exact repro/bisect).
    **LOOP STOPPED here (2026-07-26) — the grind is done; INT-1/Q1 are a deliberate, with-Corey decision,
    not a next /loop iteration.** Original F7-cluster line: (choice-IR
    2N+1 draw-order protocol doc + surface `captureIR` dropped-seed count in FuzzReport,
    `strategy.nim:451-480`+`fuzz.nim:262-277`, M) → F8 (corpus section-size introspection helper,
    `db.nim:83-101`, S) → C1 (slot→file:line:col side-table at {.cover.} expansion,
    `coverage.nim:85-88`, L) → C2 (doc-only: 8192-bitmap convergence, `coverage.nim:23`, S).
    **F/C regression gate: `scratchpad/fcsweep.sh <log> [timeout]`** (globs real tdb/tfuzz/tcov/tengine
    sources both backends; NEVER hand-list — `tests/tdbgc*`/`tdbg_e4a*` are stray gitignored BINARIES).
    Trivial doc/export S-slices done inline; M/L slices delegated to sonnet subagents.
    **After F/C: INT-1 + Q1 remain (both NEED Corey — external chapulin repo / research spike).**
  - **⚠ Q1 REORDERED AFTER TOT (control-loop judgment, note for Corey):** Q1 (dependent bounded
    loops, Solver, size L) is a **timeboxed RESEARCH SPIKE** the RFC says "may have no viable
    sound-and-fast encoding" and "names no candidate encoding" — a poor autonomous-grind fit (may
    dead-end with no committable slice). RECOMMEND handling Q1 deliberately (likely with Corey) rather
    than in an unattended /loop iteration. Doing TOT-1 (high-value, buildable, all deps met) first.
  - **H_containers LANDED `5f4639c`** (SW 57, RC 6; 428/428 both backends; storeSeqElem itRef arm +
    iteSV svRef un-stub for array indexing; Table/HashSet stay degraded).
  - **H_witness LANDED `2244d1b`** (RC 6→7, SW stays 57; recursive cycle-safe heap-snapshot;
    430/430 — the lone `phase12_sink` cpp sweep failure was a parallel-load compile flake,
    control-loop re-verified GREEN standalone [trivial import-smoke test, touches zero heap/witness
    code]). Two support fixes surfaced: emitIRType `isPlaceholder` round-trip (dsl_parser),
    emitTyAndReader itSeq `nameIsRefAlias` special-case (symex). **H_verification+H_final closeout CODE-COMPLETE but
    UNCOMMITTED** (subagent `afc32c9dd9dfd7c9a`): TEST+DOC ONLY — new `tests/tsymex_h_verification.nim`
    (identity transitivity +UNSAT companion, mixed param↔constructed-node aliasing, nil edges,
    variant field-access exclusion [precisely: single-axis itVariant read SUPPORTED, MULTI-axis
    itMultiVariant read EXCLUDED via heRefVariantUnsupported], deep-chain verdict) + `docs/SYMEX_PLAN.md`
    ADR-0022 "Cluster H LANDED" status note. **NO production code, NO version bump** (SW 57, RC 7
    unchanged); **no bug found** (verification passed — Cluster H sound). Full sweep running →
    `scratchpad/sweep_hverification.log` (waiter `blc3hh3pe`); as of last check ~349/432, lone failure
    `phase14_b2_forcephases` cpp — a parallel-load compile flake (this slice touches ZERO production
    code so cannot regress phase14). **RESUME: confirm sweep 432/432 (re-run any lone cpp rc=1
    standalone to confirm flake — `scripts/dt-bounded.sh cpp tests/<file> 240`), then `git add -A`
    the new test + SYMEX_PLAN.md (NOT scratchpad/, NOT handoff) and `git commit --no-verify`
    "test(symex): Cluster H verification + closeout — aliasing/identity/nil/variant edge cases".**
    That CLOSES Cluster H. Then base clusters Q/TOT/INT/F/C.
  - **[SUPERSEDED — H_witness landed]** H_witness IN FLIGHT (subagent `a62841603bee24ed1`): recursive pointsTo, ADR-0010 inv#4 —
    buildHeapSnapshot/pointeeRendering descend into ref-typed fields + container elements bounded by
    maxHeapDepth, cycle-safe via visited-set. Code WRITTEN + UNCOMMITTED (runtime.nim, types.nim
    [HeapSnapshotEntry format], new `tests/tsymex_h_witness.nim`); **RC 6→7, SW STAYS 57** (witness-only,
    verdicts unchanged); CR2 pin set (SW==57, RC==7). Full sweep NOT yet run at last check. **RESUME:
    if subagent didn't finish — confirm the new witness test GREEN both backends (esp. the CYCLE
    termination test: a ring/self-cycle must not hang), run full sweep both backends (expect ~430=215×2),
    then commit "feat(symex): Cluster H H_witness — recursive heap-snapshot witness (ADR-0010 invariant #4)".**
    Then verification → H_final, then Q/TOT/INT/F/C.
  - **[SUPERSEDED — H_containers landed]** In flight: Cluster H **H_containers CODE-COMPLETE but UNCOMMITTED** (subagent
    `a5f795775ca45f18c`). Diff: `runtime.nim` two arms — (1) `storeSeqElem` itRef/itPtr (raw
    `Z3_mk_store` into the `Z3Array[Z3Int,Ref_T]` for seq[Node] literals) + (2) `iteSV` svRef/svPtr
    un-stub (value-level `Z3_mk_ite` over two same-sort Ref_T addresses — needed for array[N,Node]
    INDEXING, discovered via RED test) — plus `canonicalize.nim` SW 56→57 (RC STAYS 6, seq elem
    witness is the pre-existing length-only R3 stub) + CR2 pin (SW==57) + new `tests/tsymex_h_containers.nim`
    (seq/array/tuple-of-Node real sxSat each w/ load-bearing UNSAT companion; Table/HashSet stay
    sxUnknown guards). array[N,Node]/tuple[a:Node] were already structurally reachable (only indexing
    + seq-literal-store needed code). Diff reviewed clean. Full both-backends sweep running →
    `scratchpad/sweep_hcontainers.log` (waiter `beiz81w24`); as of last check ~301/428, 0 failures,
    **all ref/heap regression tests green (r5/r6/r7/r9/r10/r11/r11b/rectify) — iteSV un-stub did NOT
    regress**. **RESUME: confirm sweep 428/428 both backends (all rc=0), then `git add -A` the 3 changed
    files (runtime.nim, canonicalize.nim, CR2 pin) + new tsymex_h_containers.nim (NOT scratchpad/) and
    `git commit --no-verify` "feat(symex): Cluster H H_containers — seq/array/tuple of named ref-objects".**
    After it lands: **H_witness** (recursive pointsTo, ADR-0010 inv#4) → verification → H_final, then
    Q/TOT/INT/F/C.
  - **Pause state:** autonomous /loop STOPPED (no wakeups armed). **Step C (atomic H1) LANDED
    `40ed16f`** — named ref-object HEAP IDENTITY: classifyType flip (both nnkObjectTy + nnkSym
    branches → itRef) + real mkNewT+mkFieldDerefWrite construction + universal isNew zero-write
    + witness provenance flag + isHeapRef/isRecursionPlaceholder + 3 carve-outs deleted +
    buildHeapSnapshot for named-alias svRef params; **SW 55→56, RC 5→6**; CR2 pin both. Full
    sweep **426/426 both backends**. Migrated (re-derived): p2b (P2b-9/10 flip), r9/r10/r11b,
    and **r6 test 3 — an UNTRACKED regression the ADR's "5-test" impact list missed** (Corey
    approved the migration): local `new(T)` fields now zero-init (Nim-faithful), so a write
    through p is NOT observable through a distinct fresh q (`q.x==42` unsat) — the pre-Step-C
    free-field model asserted `q.x==7` reachable, an UNSOUND false-SAT the zero-write corrects.
    Verified by probe: `q.x==0` sat + `q.x==42` unsat = genuine zero-init, no aliasing bug.
    New flagship `tests/tsymex_h_stepC_heapidentity.nim` (aliasing sxSat + load-bearing UNSAT
    companion, identity, sym-indirection, zero-field Token). **REMAIN STOPPED.** To resume the
    grind (**H_containers** [storeSeqElem itRef arm for seq[Node] literals + seq/array/tuple
    tests; own SW bump] → **H_witness** [recursive pointsTo, ADR-0010 inv#4] → verification →
    H_final, then Q/TOT/INT/F/C): re-run the `/loop` command below. Do NOT auto-start new
    slices until re-invoked.
- **Resume:** The autonomous grind is COMPLETE — do NOT re-fire /loop expecting more slices (it
  would find nothing implementable unattended). Only **INT-1** and **Q1** remain, both with-Corey:
  - **INT-1** (BLOCKED — needs a nelli RELEASE first, Corey 2026-07-26): batch chapulin cross-repo
    exit gate — build + smoke-run `/mnt/c/Users/corey/projects/chapulin` + its harness against hardened
    nelli (HEAD `a2ab210`), then remove chapulin's nelli-bug workarounds + bump its nelli pin.
    Cannot start until nelli cuts a release chapulin can pin to. Deferred behind that release.
  - **Q1** (timeboxed research spike, with Corey — DESIGN ANALYSIS DONE 2026-07-26, see below).

## Q1 design analysis (2026-07-26, control-loop + Corey discussing; NO code yet)
Q1 = chapulin finding #6 "chained bounded scans (2nd scan's bound = 1st's symbolic result) → sxUnknown"
(repro `/mnt/c/.../chapulin/.../t_symex_decode.nim:210-253`). Sibling #9 (Q2) = "ANY loop + string
param → sxUnknown" (`t_symex_uri.nim:65-100`).
- **ROOT-CAUSE MECHANISM (diagnosed from code, not RFC framing):** the isWhile walker
  (`runtime.nim:5616-5651`) is EAGER FINITE PATH-UNROLLING: fork the guard `maxLoopUnwind`(=5)×; any
  path whose guard is STILL SAT after k iters → `w.sawUnknown=true` (line 5646) → whole-run sxUnknown.
  When the trip-count is bounded by a SYMBOLIC quantity (`while i < s.len …`, s.len free), the
  continuation guard `i < s.len` is SAT at EVERY k (always a longer string) → `active` never empties →
  degrade fires regardless of k. THIS is why the finding is "independent of maxLoopUnwind (2 & 5)."
  Finite unrolling STRUCTURALLY cannot decide a loop with symbolic-unbounded-above trip count. Full
  "dependent bounded loops" is undecidable; `∀k.inv(k)` over Z3 Sequence theory = the dt-bounded-reject
  trap the RFC feared ("names no candidate encoding").
- **RECOMMENDED DIRECTION — loop summarization by idiom recognition.** Don't target arbitrary loops;
  target the DECIDABLE ubiquitous fragment the findings are actually about: the bounded-sequence-scan
  idiom `while i<n and pred(s[i]): inc i` IS a `find`/`indexOf`, closed-form
  `i' = (if ∃k∈[start,n):pred(s[k]) then min-such-k else n)`. The codebase ALREADY lowers find/rfind to
  Z3 `seq.indexof`/`seq.last_indexof` (M2/M3) and those are PROVEN fast + explicitly NOT hang-class.
  So: recognize the scan/span pattern at IR level, LIFT it to those Sequence primitives, ELIMINATE the
  loop. Kills #6 (chained scans → nested indexof w/ dependent start offset — no unroll, no 2^k paths)
  AND the scan-slice of #9 (loop gone → no Sequence×unwind interaction). Sound+complete FOR THE
  RECOGNIZED CLASS; everything else keeps today's k-unroll+degrade (Invariant 3 preserved). Principled
  decidability-boundary, not a hack.
- **Alternatives rejected:** universal invariant `∀k.inv(k)` (quantifier over Sequence → dt-reject,
  not fast); loop acceleration/widening (sound over-approx → sxUnknown for defects → finds no real
  defects); concolic length-concretization (only proves chosen s.len → UNSOUND universal claim →
  violates Inv-3). 
- **GATING EMPIRICAL QUESTION (the spike):** does CHAINED `indexof` w/ dependent offsets stay
  dt-bounded-GREEN both backends? (single indexof known-fast; composed-with-dependent-offset is the
  unknown.) ~1-session spike: (1) minimal tsymex repros of #6 + #9-scan, CONFIRM they degrade today +
  confirm symbolic-bound mechanism (vary maxLoopUnwind 2/5/8, stays sxUnknown); (2) prototype narrowest
  recognizer (`while i<len and s[i]!=lit: inc i` → indexof), re-run dt-bounded tight-timeout c AND cpp;
  (3) green → real feature slice (SW bump, versioned); any 137 → close Q1 as documented negative result
  ("confirmed no viable sound-and-fast encoding for the chained case") — legit per RFC line 616-617.
  **OPEN with Corey:** run the spike now, or first pin idiom-recognition SCOPE (bare find-shape only vs.
  also `while i<n: if s[i]==c: break; inc i` vs. span/skipWhile).
- **SPIKE RAN 2026-07-26 → VERDICT GREEN (control-loop independently re-verified both backends).**
  Prototype = throwaway 3-arg-find lift (`iekStrFind` honoring `strArgs[2]` via nim-z3's
  `indexOf(a,sub,start)` overload); REVERTED after measuring (tree clean, no code kept). Evidence:
  - Phase B (mechanism): B1 single-scan + B2 chained-scan → sxUnknown at maxLoopUnwind ∈ {2,5,8} on
    BOTH c+cpp (12/12 cells). Confirms symbolic-bound degrade is unwind-independent exactly as diagnosed.
  - Phase A (crux): A1 offset-0 find = sxSat; **A2 chained-dependent find = sxSat BOTH backends**;
    **A3 chained-UNSAT-soundness = sxUnsat BOTH backends** (load-bearing — proves real modeling, not a
    masked degrade); full suite ~8–13s wall, 60s bound never approached, zero hangs. Chained/dependent
    indexof composes cleanly through Z3 Sequence theory.
  - Phase C (boundary): char-class/predicate scans (digit run) do NOT lift (no Sequence primitive for
    "first predicate-violating position") → stay OUT of scope, keep degrading.
  - **CONCLUSION: Q1 is a real, buildable small-to-MEDIUM slice.** Core = (1) a proper 3-arg-find IR
    path (distinct IR kind + parser arity dispatch, NOT the spike's overload trick) + (2) the
    idiom-RECOGNIZER (the bulk): pattern-match the canonical loop `while i<bound and s[i]!=lit: inc i`
    (single + chains where each scan's start derives from the prior result) in the walker/parser and
    rewrite to `indexOf(s, lit, start)` instead of unrolling. SW bump (verdict surface changes). Scope
    LOCKED to literal/substring delimiter scans; predicate/char-class = explicit follow-up, not Q1.
- **TWO COLLATERAL FINDINGS surfaced by the spike (NEW, not in RFC — need Corey routing):**
  1. **String index reads have ZERO IndexDefect wiring.** `iekStrAt` (`runtime_strings.nim:114-127`)
     never calls `forkDefect`, unlike seq/array/table indexing. So a `tIndexError()` search over `s[i]`
     returns sxUnsat ("no OOB reachable") even when an OOB IS reachable (spike A4 + A4b control both
     sxUnsat, both backends). This is a FALSE-NEGATIVE on defect finding for string code — a soundness
     under-approximation for defect targets (claims safe when unsafe). Independent of Q1, but it means
     Q1's find-lift makes string-scan REACHABILITY (tLabel) provable while the OOB-DEFECT half stays
     blind until this is fixed. Candidate its own §0/CR-class slice. RECOMMEND treating as a real finding.
  2. **Backend-divergent verdict on a multi-condition while-guard → C-BACKEND FALSE sxUnsat.
     ROOT-CAUSED + FIX VALIDATED 2026-07-26 (Corey chose "triage B first").**
     - **Localization:** NOT 3-way-ness, NOT loops-in-general. Trigger = a RELATIONAL (`<`/`<=`/`>`/
       `>=`) comparison on a string-indexed char (`iekStrAt`) INSIDE A LOOP GUARD. Equality (`==`) is
       fine (v2 sxSat); pure-int relational is fine (v4 sxSat); the SAME relational char-index OUTSIDE a
       loop degrades to sxUnknown on BOTH backends (p1, sound); a LET-bound char relational works
       (p3 sxSat). Only the inline-relational-char-index-in-loop-guard diverges: **c=sxUnsat (false
       "unreachable"), cpp=sxUnknown.**
     - **ROOT CAUSE:** the CR-17(a) defensive guard (`runtime.nim:3069-3074`) RAISES
       `SymexUnsupportedStringOpError` from deep inside expression lowering to avoid a String+Int+BV
       ordering hang. Outside a loop that raise propagates cleanly to the runSymex boundary catch
       (`7437`) → sound sxUnknown. INSIDE a loop guard the raise unwinds through the walk's live
       `seq[Path]` machinery and is SILENTLY LOST on the C backend's goto-exception model (the exact
       b7258f7/CR-1c divergence class the file's own comments at 7595-7624/7844 warn about) → the walk
       continues with a mis-lowered guard → false sxUnsat. cpp native exceptions propagate → sxUnknown.
     - **FIX VALIDATED (experiment, reverted):** replacing the `raise` at 3071 with an IN-BAND degrade
       (return a fresh unconstrained `allocateSym(tBool(),...)` bool instead of raising) ELIMINATED the
       divergence — v1/v3/v5 went sxSat on BOTH backends, c=sxUnsat gone. Confirms the raise was the
       sole cause. The PROPER fix also sets `sawUnknown=true` for an HONEST sxUnknown (not sxSat with a
       possibly-bogus char witness) — needs a lowering-callable sawUnknown helper (WalkCtx is defined at
       4444, BELOW the lowering code at 3071, so the 7406-style `cast[ptr WalkCtx](currentWalkCtxPtr)`
       isn't in scope there → forward-declare a tiny `noteSawUnknownFromLowering()` helper).
     - **⚠ SYSTEMIC (high-confidence, by construction — the mechanism is exception-propagation-through-
       loops, not CR-17-specific):** EVERY expression-lowering degrade-`raise` reachable inside a loop
       is the same latent C-backend false-verdict hole. Candidates below WalkCtx in the lower/lowerBool
       path: `2539` (unsupported string op), `1703` (table val-type), `1716`/`2985` (set-char interop),
       `3071` (CR-17, manifested). PARAM-ALLOCATION raises (1438/1452/1479 in allocateSym, run before
       the walk) are safe. **The sound systemic fix = SND-1 philosophy applied to LOWERING-time
       degrades: taint sawUnknown in-band, never `raise` from lowering that can run inside a loop.**
     - **SEVERITY: HIGH.** Live unsoundness (false "unreachable"/"no-defect" — the dangerous direction)
       on the C backend, triggered by ORDINARY code (a char range-check in a loop — ubiquitous in
       parsers/decoders, exactly chapulin's domain). RECOMMEND this JUMPS AHEAD of Q1 (soundness hole in
       shipped behavior > capability add). A proper slice: (1) lowering-callable sawUnknown helper, (2)
       convert CR-17's raise → in-band taint, (3) audit + convert the other in-loop lowering raises,
       (4) both-backend regression pinning c==cpp on the repro set. Bumps SW (verdict surface changes).
  - Minor: `{'0'..'9'}` set-membership `contains` doesn't compile through the parser (`getImpl for
     callee contains` gap) — a distinct small parser gap, noted only.

## Stage-3 grind ledger
Slices land serially (each SW bump serialized against a live base). Sweep = all
`tsymex_*.nim` × {c,cpp} via `scripts/dt-bounded.sh`.
**Clusters 2 & 3 COMPLETE (M1–M6); Cluster 4 P1 (`ad6c46c`, v52) + P2a (`4292f4c`, v53) landed.**
**GRIND PAUSED — P2b REOPENED by Corey (2026-07-23) for a heap-identity redesign.**
The value-modeled P2b (`42eafde`, v54, ADR-0021, 422/422) is committed+green but being
SUPERSEDED. Corey scrutinized it; control loop empirically confirmed it is SOUND (aliasing
`q=p;q.val=99;p.val==99` AND identity `p==q` both degrade to `sxUnknown`, never a false
verdict) but does NOT model ref identity/aliasing. Corey chose to invest in true heap
identity. Exploration (see below) reframed it: the value-model of named ref-object aliases
is NOT a deliberate lock — it's Phase-14 `classifyType` legacy (`dsl_typebridge.nim:195-213`,
"#136 unwrap ref T", comment "Aliasing tracking is a follow-up") predating the heap model;
heap-identity was always the intended follow-up. The full heap machinery (svRef identity,
field-split heap, refEq, freshness, heapSnapshot alias-group rendering) already exists +
is tested for INLINE `ref T` (Cluster R: R6/R7/R9); named aliases just don't route to it.
**Design written: ADR-0022 in SYMEX_PLAN.md** (lines 765-873; Cluster H — named-ref heap
identity; classifyType policy flip → itRef + ~10 dsl_parser routing sites + real heap
construction + variant stays excluded + RC bump 5→6; sliced H1–H7).
**ARCHITECT REVIEW ROUND 1 COMPLETE** (Corey invoked `/architect docs/SYMEX_PLAN.md`
2026-07-23; 4-lens team). Finding: ADR-0022's original H1 was **self-contradictory** — the
empty `namedRefPlaceholder` is load-bearing for Z3-sort identity ($-structural keying) yet
3 consumers (construction, witness reader, new-zero-init) need the FULL field list off the
same pointee. **Fixes APPLIED to ADR-0022** (see "ADR-0022 Round-1 architect review"
subsection, SYMEX_PLAN.md after line 873): resolution = key sort identity on a canonical
NOMINAL type-id (name+generic-args, symbol-unique) not `$fields` → bare symbol gets FULL
pointee, recursive field keeps placeholder, both share the sort. Plus: universal isNew
zero-write (closes a false-SAT hole), preserve variant detection, H2/H3/H5 reframed as
verification (routing auto-activates), RC+SW bump AT H1, container types (seq[Node]) added,
isHeapRef predicate + delete 3 bare-symbol carve-outs, +3 regressing tests (r10/r11b/
rectify_refs). Consolidated notes: `scratchpad/adr0022_review_consolidated.md`.
**3 GENUINE SCOPE FORKS AWAITING COREY** (below in Open forks). **RESUME: get Corey's answer
on the 3 forks + H1 green-light, then implement the revised H1 (atomic: classifyType→itRef
full-pointee + nominal sort-id + H1a/b/c + isNew zero-write + variant-detect + SW/RC bump).
Do NOT implement until he signs off.** Remaining after Cluster H: Q/TOT/INT/F/C.
P2b detail: (`ref object` expression-position allocation — genuinely NEW
capability, needs a preamble `isNew` + field-writes, NOT a P1/P2a clone; `isNewCall`
(dsl_parser.nim:805-821) documents `new T` is handled ONLY at let-statement level today
("no expression-context model for allocation"). **Variant construction EXCLUDED** per round-2
(field-split heap declines variant *reads* via `heRefVariantUnsupported` dsl_parser.nim:1299-1305;
adding variant *writes* needs its own ADR revisiting that read gap). Depends on SND-1; CR-2a
`Soft(t)` backstop. **Likely needs its own ADR** (object construction as expressions vs the
field-split heap model). Bumps SW; RC per the same construction-only test as P1/P2a. Size L.
Watch: P2b's ref-object return may surface the same `retBindEq` composite-return gap below.)
Then Q/TOT/INT/F/C.
Slice order: SND-1✓ SND-1b✓ SND-2✓ CR-1a✓ CR-1b✓ CR-1c✓ CR-2a✓ CR-2b✓ CR-2c✓ M1✓ M2✓ M3✓ M4✓ M5✓ M6✓ P1✓ P2a✓
→ **P2b** → Q/TOT/INT/F/C. 17/27 done, 10 remaining.
Versions now: symexWalkerVersion **"56"**, renderAsChoicesVersion **"6"**;
only `tsymex_phase15_CR2_cachekey.nim` pins either with `==`.
**Cluster H sub-track (supersedes P2b `42eafde`): Step A LANDED `d85f0f7`** — added
`IRType.nominalId` field + recursive `nominalId(NimNode)` helper (signatureHash;
bracket-arg recursion for generics), populated at the 3 named-object `tTuple`
sites; PURE no-op (field unread → no version bump), 424/424 both backends. **NEXT:
Step B** (flip `refPointeeTypeId` to prefer nominalId, fallback `$`; SW 54→55;
verify inline-ref R6/R7/R9/R12 + watch mixed-naming sort-mismatch) → Step C (atomic H1).
**Discovered gap (P1, not yet sliced):** a value-returning HELPER proc that *returns* a
tuple (`makePair(x): (int,int) = (x,x+1)`) still degrades to sxUnknown via `retBindEq`
(runtime.nim: "composite-typed proc return not yet wired — got svTuple"), safely caught by
CR-1c (no crash). This is the call/RETURN-binding path, distinct from P1/P2a's CONSTRUCTION
arm — documented in-code in tsymex_p1_tupleconstr_expr.nim. A future return-binding slice
(not currently an RFC row) would close it; P2b's ref-object return may surface the same.
NOTE for P2a/P2b (and any witness-shape slice): bump SW always (verdict surface changes);
bump RC **only if a genuinely NEW witness SHAPE is introduced** — P1 showed the tuple/object
witness *reader* already existed (built for variant/object values), so a construction-only
slice reuses it and RC stays "5" (a spurious RC bump needlessly invalidates every cached
witness). Update the CR2 pin's `== "<sw>"` (and `renderAsChoicesVersion == "<rc>"` iff bumped);
re-grep `== "` set each time. Witness-shape slices that make previously-degraded types
renderable ALSO tend to require widening CR-2c's `isRenderableWitnessTy` (types.nim) in
lockstep — for P1 no change was needed (itTuple already recursed). Check P2a's object shape.

| # | Slice | Commit | walker ver | Sweep | Notes |
|---|-------|--------|-----------|-------|-------|
| 17 | P2a | `4292f4c` | 52→53 (RC stays 5) | 420/420 ✓ | Value-object (non-ref) `nnkObjConstr` in EXPRESSION position (`let p=Point(x:a,y:b)`, object return). Before P2a, nnkObjConstr recognized only inside `newException(...)`; any other value-object construction fell to CR-2a → whole-run sxUnknown. **REUSES P1's `iekTupleLit` wholesale — NO new IR kind** (types/runtime/abstraction untouched): a value object's IRType is itTuple-shaped (`classifyType` yields tTuple w/ objectName populated), lowers to the same svTuple, and every existing iekTupleLit dispatch site + the itTuple/svTuple witness reader transfer for free (objects already render as SUT params). Only `dsl_parser.nim` gets the `of nnkObjConstr:` arm + `canonicalize.nim` SW bump + CR2 pin. **Field ordering/omission** (the one real diff from tuples): builds a name→value map from present fields (skip n[0]=type symbol; each field is nnkExprColonExpr), walks the TYPE's declared `fieldNames` order. **Omitted field** → `zeroValueForType(fields[i])` (genuinely SOUND — Nim zero-inits omitted value-object fields, NOT a degrade); omitted field whose type has no clean zero (nested seq/tuple/variant/ref) → SND-1 degrade via feUnsupportedExprKind+mkUnsupported (never a guessed-zero false sxSat). Present field parses via ordinary parseExpr recursion → unsupported field degrades same way (Invariant 3). lowerTupleLit's per-field proto (from ttupleTy.fields[i]) gives heterogeneous fields (`Point(a:x, b:5'u8)`) their declared width. Bumps SW 52→53 only; CR2 pin `== "53"`, RC stays `== "5"` (construction-only, no new witness shape — per P1). No CR-2c change (itTuple already renderable). 14-test strong-form (basic/out-of-order/omitted/heterogeneous + UNSAT soundness each, newException regression, SND-1 soundness×2, `>=` floor pins), verified GREEN both backends in isolation. Subagent `aa4d3f9f` implemented (elegantly, reuse-not-new-kind per brief) then stopped post-sweep without committing (13th detach); control loop verified diff independently + ran clean 420/420 sweep + committed. |
| 16 | P1 | `ad6c46c` | 51→52 (RC stays 5) | 418/418 ✓ | General N-ary `nnkTupleConstr` in EXPRESSION position (`let t=(a,b)`, `return (a,b,c)`, named `(x:a,y:b)`). Before P1 only the narrow `yield (e1,e2)` special-case recognized nnkTupleConstr; any other tuple-constructor expr fell to the CR-2a catch-all → whole-run sxUnknown. New `iekTupleLit` IR kind (types.nim: telems + whole `ttupleTy`, heterogeneous — carries full itTuple type not one elemTy; `mkTupleLit` ctor with arity/kind doAsserts + render arm) lowering to `svTuple` via new `lowerTupleLit` (runtime.nim). **Construction-only** — the itTuple/svTuple witness *reader* (emitTyAndReader itTuple arm, isRenderableWitnessTy itTuple recursion) already existed for variant/object values; field *reading* (`t[0]`/`t.x`) already supported. parseExpr `of nnkTupleConstr:` unwraps `nnkExprColonExpr` for named tuples, parses each element via ORDINARY parseExpr recursion → an unsupported field independently hits CR-2a → SND-1 taint → whole-run sxUnknown (Invariant 3: never false sxSat from a field dummy). **lowerTupleLit derives per-field proto from `ttupleTy.fields[i]`** so a heterogeneous field (`(x,5'u8)`) lowers at declared width, not default signed BV64. abstraction.nim (tryEvalInterval non-int + collectVarRefs), probeProto (none — own kind always svTuple), collectSetLitMembers/collectTableLitKeys, emitExpr, rhsHasInlineDefectFork all got iekTupleLit arms (all `else`-terminated sites). **RC-stays-5 is a resolved decision** (brief said bump both; subagent correctly identified no new witness shape — control loop verified & concurs). Bumps SW 51→52 only; CR2 pin `== "52"`, RC pin stays `== "5"`. P1 test uses `>=` floors. No CR-2c change (itTuple already renderable). Subagent detached-sweep + re-armed poll (12th); control loop verified diff independently (all 4 core files + abstraction), ran own confirming sweep. |
| 1 | SND-1 | `84668ab` | 37→38 | 388/388 ✓ | isUnsupported taints Path.uncertain. Fallout: added no-op parse arm for assert-scaffolding `nnkConstSection`/`nnkBindStmt`/`nnkMixinStmt` in dsl_parser (else every assert degraded). Legit soundness corrections: a3_closure_iterators T5/T6 sxSat→sxUnknown (accidentally-correct false-sxSat SND-1 closes). New test uses `>=`-floor pin idiom; 7 legacy `==` pins bumped to "38". |
| 2 | SND-1b | `bc2c8d9` | 38→39 | 390/390 ✓ | `applyClosureGround` skips `assertArm` for uncertain closure-body sub-paths (both return channels), pushes new `ceClosureBodyUncertain` (sevError) → existing `closureForcedUnknown` whole-run degrade fires. ADR-0018 in SYMEX_PLAN. Fixed fork-registry comment (descentBase = 2nd raw `Path(`). **One-time SW pin-idiom migration executed**: canonical CR2_cachekey stays `== "39"`; 6 incidental pins → `>=`-floor. Strong-form test checks error-kind, not just verdict. Subagent died on API error post-sweep; control loop verified + committed. |
| 3 | SND-2 | `0a156c7` | 39→40 | 392/392 ✓ | `isAssume` promoted from bool flag to distinct `IRStmtKind` (`mkAssume` ctor, own render arm). 12 switch sites: 10 uniform `of isAssert, isAssume:`, 2 non-uniform — canonicalize renders distinct `St<Am:…>` vs `St<At:>` (avoids `symexCacheKey` collision → silent wrong answer), walker dispatch shares steps 1/2/4 but omits step 3 `forkDefect`. Round-2 fix: `collectAssertRanges` had `else: discard` silently dropping isAssume range facts → now included. `symexAssume(cond)` lowers to `mkAssume` not `mkAssert`. ADR-0019 in SYMEX_PLAN. 7-test strong-form suite (flagship verified genuinely RED). CR2 pin → `== "40"`. Subagent committed itself; control loop verified (both backends green, ADR landed, only CR2 as `==`). |
| 4 | CR-1a | `ee9763c` | 40→41 | 394/394 ✓ | bitwise `and`/`or`/`xor` on a Z3-Int-sorted operand (`.len`/`.find`/`.indexOf`/`parseInt` — unconditionally svInt, no BV-promotion choice) hard-raised `ValueError: bitwise op on promoted Z3Int` (native crash). New `svIntToBV` (runtime.nim ~1980) bridges the Int operand to BV via `Z3_mk_int2bv` (unsigned; values always non-negative); former crash arm (~2977) dispatches through existing `binBV` → SOUND sxSat/sxUnsat (NOT degrade). Width follows the BV operand, BV64 default (native int) when both Int-sorted. No new ADR (bug fix at existing locus). Strong-form test (SAT witness-parity + load-bearing UNSAT `and 1==2`/`xor self==1`; and/or/xor). CR2 pin → `== "41"`. Subagent stalled on detached-sweep pattern; control loop verified (diff-review + 394/394 both backends) + committed. |
| 5 | CR-1b | `5fc018e` | 41→42 | 396/396 ✓ | tail-return-of-local KeyError (`runtime.nim` `of iekVar: env[e.vname]`) was a SYMPTOM — **true fault at PARSE time**: `dsl_parser.nim`'s `nnkPar`/`nnkStmtListExpr` arm did `parseExpr(n[^1])`, silently dropping every LEADING statement (`let hi = ...`) of an implicit-tail-return body (`result = (let hi=…; hi+1)`). Fix: parse each leading child as a statement into the existing `preamble` accumulator (mirrors `nnkLetSection` A-normalization) before the tail child. **runtime.nim untouched** — fixed at the true lowering site per DoD, NOT by soft-failing the read. Strong-form test: SAT w/ load-bearing witness replay (`data[o] mod 256==4` ∧ `f==5`) + UNSAT `==300` (rules out dropped-local-as-free-symbol false positive). No new ADR. CR2 pin → `== "42"`. Subagent blocked in-turn correctly + self-committed; control loop verified (runtime untouched, both backends green, only CR2 `==`). |
| 7 | CR-2a | `3f01b1f` | 43→44 | 400/400 ✓ | `parseExpr` catch-all `else:` (dsl_parser.nim ~1866) `error()`ed at MACRO-EXPANSION on any unhandled NimNode kind → aborted compilation (worse than sxUnknown). Converted to the A7-S3 `mkUnsupported`-degrade idiom: new `feUnsupportedExprKind` (types.nim, enum tail) sevError + `preamble.add mkUnsupported` + type-correct dummy via new `zeroValueForType(classifyType(n).ty)` (returns Nim's guaranteed ZERO default for int/bool/float/string — sound for read-before-write per Inv-3; nil→`mkIntLit(0)` fallback for aggregates under SND-1 taint). Depends on SND-1✓ (dummy never yields a witness; also Class-A → capForcedUnknown backstop). RED = compile-abort on `(if c:1 else:2)+1` (nnkIfExpr nested as `+` operand). Strong-form 6-test incl load-bearing CR-2a-2 (dummy 0 would trivially-SAT `y==1` ∀x but SND-1 → sxUnknown≠sxSat). No new ADR (idiom reuse). CR2 pin → `== "44"`. Subagent stalled on detached-sweep (4th); control loop verified diff+test, then subagent's Monitor resumed it + self-committed (5 files, no race); control loop re-verified 6/6 both backends + 400/400. |
| 8 | CR-2b | `6c9210d` | 44→45 | 402/402 ✓ | Parameter-type macro-`error()` → whole-run forced-`sxUnknown`. `classifyType`'s resolved-type-name text-match catch-all (`dsl_typebridge.nim:452`) `error()`ed at macro-expansion on an unmodeled SUT PARAM type (RED: `cstring`), aborting compilation before any body was walkable. Option 2 (control-loop-resolved, mirrors `__ownership:`): catch-all now returns `unranged(tUninterp("__unsupported:" & s))`; allocateSym gains a `"__unsupported:"` prefix branch raising CR-1c's `SymexClassifiedDegradeError` carrier with new `feUnsupportedParamType` kind (types.nim, enum tail) → caught at runSymex boundary → whole-run sxUnknown. **Second crash-trap found + fixed (in-scope, not a fork):** `emitTyAndReader` (symex.nim) builds a witness-reader for every param at macro-expansion; its itUninterp catch-all knew only `"__closure"` — an `"__unsupported:"` name fell through to an uncaught ValueError in the NimVM (relocated the abort). Added a placeholder-int/`{.warning.}` branch mirroring `__closure`; placeholder never evaluated (sxSat/sxRaised codegen arms unreachable once allocateSym forces sxUnknown). Carrier REUSED (no new exception type). Strong-form test asserts KIND + `status != sxSat && != sxUnsat` (no silent wrong verdict, no walk-time crash) both backends. CR2 pin → `== "45"`. Subagent blocked in-turn + self-committed; control loop verified independently (commit/tree/pin/version + CR-2b test 4/4 green BOTH backends + diff-reviewed carrier reuse & second-trap fix). |
| 15 | M6 | `b369168` | 51 (no bump) | 416/416 ✓ | probeProto svString-proto for `iekStrToLower`/`iekStrToUpper`/`iekRadixFmt`/`iekRuneToStr`. Catch-all `StrOpKinds - {…}` (runtime.nim:1827) returned `none` for these 4 string-producing ops; moved into the svString-proto arm (:1820). **Defensive/inert — NO version bump** (v51 stays). Inertness is STRUCTURAL not just empirical: `lowerStrArm` (runtime_strings.nim:88) never threads `proto`; `lower()` dispatch (runtime.nim:2775) doesn't pass proto to it → probeProto's StrOpKind return can't affect any lowering today (bEq/bNe probe-miss fallback recovers). Subagent verified pre/post byte-identical `[OK]` diff both backends. Test (10) is HONEST regression-protection (explicitly NOT RED→GREEN — documented): all 4 ops via real surfaces (toLowerAscii/toUpperAscii=A9, toHex=A8, `$Rune`=A7-S2), `==`/`!=`, + compound cross-op comparisons (the site where proto would first matter if path went live). Subagent ran ~6h (thorough inertness investigation + detached-sweep stall, 11th) but self-committed b369168 correctly; control loop verified independently (416/416, walker 51, binary gitignored, test honesty). |
| 14 | M5 | `153f7dc` | 50→51 | 414/414 ✓ | `nnkIfExpr` in `parseExpr` + min/max. parseExpr had no nnkIfExpr arm → if-EXPRESSION as a sub-expr (operand, let-rhs, or inlined if-expr-bodied proc body like `max(a,b)=if a<b:b else:a`) hit the CR-2a catch-all → sxUnknown. Added an nnkIfExpr arm to parseExpr (dsl_parser.nim:1880) that A-normalizes via synthetic let+read (hoist into preamble reusing statement-if lowering, each branch tail → temp; return read of temp). min/max work via proc inlining, no dedicated modeling. Bumps SW 50→51; RC stays "5". Strong-form 10-test: if-expr-subexpr BOTH arms modeled (exact witnesses) + UNSAT soundness + let-rhs if-expr + max/min SAT+UNSAT both directions + symbolic max(a,b). **Migrated `tsymex_CR2a_expr_catchall.nim`** (its RED repro WAS an if-expr subexpr): SUT1 `(if x>0:1 else:2)+1==1` flips sxUnknown→genuine **sxUnsat** (y∈{2,3}); SUT2 let-rhs→genuine **sxSat** x>0; catch-all MECHANISM + SND-1 soundness retargeted to still-unsupported `cast[int32](x)+1` (nnkCast verified still hits catch-all) keeping dummy-would-sat framing — control loop reviewed as principled/strengthened, NOT gutted. Subagent detached sweep + stopped (10th); control loop verified M5 10/10 + CR-2a 7/7 BOTH backends, **removed leftover `zzrepro_cast` ELF scratch (not gitignored)**, blocked to 414/414, committed. |
| 13 | M4 | `3688a84` | 49→50 | 412/412 ✓ | String `.add`/`&=` modeled via existing `iekStrConcat`. `s &= x` (augmented-assign) was SND-1's Class-B silent-no-op→false-sxSat: `binopForInfix` has no `"&"` case → `else: error()`. Fix (RFC round-2 mechanism, NOT a set edit): augmented-assign lowering gains a TYPE-CLASSIFY branch — string LHS stores `lhs := mkStrOp(iekStrConcat,"&",@[lhsRead,rhs])` (mirrors binary `s & x` at dsl_parser:1081); every other LHS keeps existing mkBinop path. Did NOT add `"&"` to binopForInfix (concat ≠ numeric IRBinop). `s.add(x)` string receiver → same in-place concat-assign; char-arg `s.add('c')` deliberately kept sxUnknown (char=itInt/uint8, no char→string; documented follow-up, S11 addChar asserts it still degrades); `seq[T].add` untouched (receiver type-classified). Bumps SW 49→50; RC stays "5" (string witness). Strong-form new test (12): &= happy/UNSAT, .add happy/UNSAT, symbolic-param LHS + symbolic RHS operand (round-tripped vs real Nim `&`), seq.add regression. **Migrated 3 existing tests** (SND-1/SND-1b/S11 used `&=` as unsupported-drop vehicle): the `&=` repro flips to correct sxSat; taint-mechanism SUTs retargeted to still-unsupported `/=` — control loop reviewed the diffs (principled, NOT gutted). Subagent detached sweep + stopped (9th); control loop verified 12/12 + 3 migrated tests BOTH backends, distinguished a concurrent-binary-write race (spurious "Permission denied") from real failure, blocked to 412/412, committed. |
| 12 | M3 | `5be3310` | 48→49 | 410/410 ✓ | `s.rfind(sub)` (strutils) modeled as a near-clone of `s.find(sub)`: new `iekStrRfind` IR kind lowering to nim-z3's native `lastIndexOf` (Z3 `seq.last_indexof`, `_deps/z3/src/z3/sequence.nim:199`) instead of find's `indexOf` — NOT bounded-scan, NOT Q1/Q2 hang class. New `smkStrRfind` recognizes `"rfind"`; smk→iek map + runtime_strings lowering (`svInt zi: lastIndexOf(recv,sub)`) + every exhaustive iek case set (abstraction/canonicalize/runtime/probeProto) gained the kind alongside iekStrFind. canonicalize renders a DISTINCT cache key from find (test-asserted via `symexCacheKeyForFn` — closes SND-2-class silent-collision risk). Bumps SW 48→49; RC stays "5" (int witness). Strong-form 6-test: **load-bearing rfind≠find** (doubled substring: rfind=last=5, find=first=1, both round-tripped vs real Nim) + -1 not-found parity + UNSAT soundness + cache-key distinctness. Subagent detached sweep + stopped (8th); control loop verified 6/6 BOTH backends independently, blocked to 410/410, committed. |
| 11 | M2 | `f9b64c1` | 47→48 | 408/408 ✓ | `parseBiggestInt(s)` modeled by routing to the SAME `iekStrToInt` IR as `parseInt`. Two callee-name guards in dsl_parser (expression match ~1537 + discarded-call raise fork ~2955) extended `"parseInt"` → `{"parseInt","parseBiggestInt"}`. **runtime.nim UNTOUCHED** — `iekStrToInt`'s ValueError raise-path (runtime.nim:1930/1966) already models malformed-input failure → failure-mode parity automatic. RED empirically = `sxUnknown` (seUnsupportedStringOp via string-call fallthrough), NOT compile-abort (CR-2a removed that). strOp label keeps actual callee name → distinct cache key from parseInt. Bumps SW 47→48 (verdict sxUnknown→real); RC stays "5". CR2 pin walker→"48". Strong-form 8-test: happy (incl −42) ground-truthed vs real Nim `parseBiggestInt`; **load-bearing M2-3 failure-parity** (`"abc"`→sxRaised+ValueError, identical to parseInt); UNSAT (s="100"≠42); discard-raise fork parity + soundness; valid-discard regression. Unblocks chapulin v2 B4. Subagent detached sweep + stopped (7th); control loop verified 8/8 BOTH backends independently, blocked to 408/408, committed. |
| 10 | M1 | `f2b41ee` | 46→47 (+RC 4→5) | 406/406 ✓ | `seq[byte]`/fixed-width-int witness readers. `emitTyAndReader`'s `itSeq` arm (symex.nim) rendered only `seq[int64]`/`f32`/`f64`/`ref T` → every other elem type degraded to sxUnknown via CR-2c's gate. Widened reader + read-helpers (runtime.nim `readSeq*`) to the full fixed-width-int family (int8/16/32/64, uint8/16/32/64; byte=uint8 alias). **Walker already allocated/constrained these seqs** (extractSeqElements/allocateSeqDataRaw/seqElemAt dispatch on every (signed,width) since Phase 15 C4) — ONLY the post-solve reader was missing → M1 is reader-only (scope confirmed). **`isRenderableSeqElemTy` (types.nim) widened in EXACT lockstep** so CR-2c stops demoting these; composes through recursive `isRenderableWitnessTy` → nested `tuple[a:seq[byte]]` renderable too. Bumps BOTH renderAsChoicesVersion 4→5 (new witness shape) AND symexWalkerVersion 46→47 (verdict sxUnknown→sxSat; precedent canonicalize.nim:496). CR2 pin → walker `== "47"` + RC `== "5"`. Strong-form 12-test: exact byte/uint/int witnesses incl boundary 255 (no trunc) + NEGATIVE sign-extended int8/16/32 + load-bearing UNSAT (byte≤255) + nested tuple[seq[byte]] sxSat + regression seq[string]/seq[Widget] still sxUnknown. Subagent detached its sweep + stopped (6th); control loop took over: verified M1 test 12/12 BOTH backends independently, blocked on sweep to 406/406, committed. |
| 9 | CR-2c | `25ebf9c` +`0007b03` | 45→46 | 404/404 ✓ | Third macro-`error()` class: post-solve witness-reader codegen `emitTyAndReader` (symex.nim:716/727/735) `error()`ed at macro-expansion on unmodeled witness shapes (seq non-scalar/ref elem, Table≠string-int, HashSet≠int) → whole-file compile abort. Degrade at `parseProc` TOP-LEVEL SUT-param loop via new `demoteUnrenderableWitnessTy` (dsl_parser) → `tUninterp("__unsupported_witness:" & s)`; allocateSym `__unsupported_witness:` branch raises CR-1c's `SymexClassifiedDegradeError` carrier w/ new `feUnsupportedWitnessType` (types.nim, enum tail) → whole-run sxUnknown. **Scoped to params, NOT classifyType** (naive classifyType-gating over-triggered on internal non-witness types e.g. in-body `seq[byte]` helper — broke S5_strops/S7a_bytes; caught by sweep, rescoped; dsl_typebridge reverted clean). **Control-loop caught a completeness gap in core `25ebf9c`:** top-level-only check missed NESTED unrenderable shapes (`tuple[a:seq[Widget]]` etc.) because emitTyAndReader recurses into aggregate fields → still compile-aborted (confirmed repro). Handed back → follow-up `0007b03`: new RECURSIVE `isRenderableWitnessTy*` (types.nim) mirroring emitTyAndReader's whole type-tree (distinct/tuple/array/seq-ref/variant arms), reusing the 3 leaf predicates as single source of truth; demotes whole param iff any leaf unrenderable. Carrier reused (no new type). Strong-form test: 5 top-level + 4 nested (tuple/array/variant-arm/double-nested `seq[tuple[seq[Widget]]]`) RED→sxUnknown+kind, +4 flat +3 renderable-NESTED regression guards stay sxSat (no over-demotion). NO SW re-bump for follow-up (nested sigs had zero cache entries). CR2 pin → `== "46"`; RC stays "4". Both subagent runs blocked in-turn + self-committed; control loop verified independently BOTH backends (17/17 + clean tree at 0007b03) and diff-reviewed the recursion + param-only scoping. |
| 6 | CR-1c | `505354c` | 42→43 | 398/398 ✓ | §0 last-resort walker-fault safety net. New `SymexClassifiedDegradeError{kind: SymexErrorKind}` generic carrier + distinct `weInternalWalkerFault` kind → `sxUnknown`. **⚠ DESIGN DEVIATION FROM RFC (sound, resolved not a fork):** RFC specified a catch "around per-statement dispatch" — that PER-FRAME placement caused a **C-backend SIGSEGV** (per-frame try/except interacting with ORC destructor-unwind of `walkBlock`'s live `seq[Path]` whose elements hold refcounted Z3 ASTs w/ destructors; heisenbug, C-only, b7258f7 divergence class). 1st subagent impl shipped this regression + stalled; control loop caught it (E8_getcurrentexn + S11_mutation SIGSEGV), handed back. Fix: moved catch to the SINGLE already-existing `runSymex` boundary `try/except` (no new try, per-frame reverted 0 refs) — unanticipated native unwinds naturally (sound, as pre-CR-1c) → caught ONCE → classified. Coarser: whole-run sxUnknown on unforeseen fault vs per-path (conservative/safe). Anticipated carriers consumed by their arms first; Defects still crash loudly. Fault-injection test via companion `.nim.cfg` (`-d:symexTestInjectWalkerFault`) — **found `.cfg` was gitignored** (`tests/t*[!.]`, no `.nim.cfg` negation); added `.gitignore` `!tests/*.nim.cfg` negation. ADR-0020 (rewritten for boundary design). CR2 pin → `== "43"`. Control loop verified: E8/S11/CR1c-fault green BOTH backends, 398/398, .cfg in commit, walk case un-try-wrapped. |

## Round-2 architecture review — applied (2026-07-12)
Second 4-agent team (depth/breadth/design/feasibility), all grounded in the code.
Round 2 CONFIRMED round-1's core claims (SND-1 chokepoints, ADR-0012 D2 precedence,
call-cache uncertain-gate, 12-switch count) and surfaced new material work:
- **SND-1b (NEW, CRIT):** closure ground-axiom path (`applyClosureGround`,
  runtime.nim:6244-6531) bypasses `Path.uncertain` — folds closure-body returns into
  GLOBAL `currentClosureCallAxioms` (drained into every solve) with no uncertain-gate.
  SND-1's fix doesn't reach it. New slice; reuse `closureForcedUnknown` whole-run
  degrade. Fork-registry comment (4409-4454) stale re raw `Path(` at 6381.
- **CR-2c (NEW):** third macro-`error()` class — `emitTyAndReader` witness-reader
  codegen (symex.nim:697/708/716). §0 widened to THREE site classes.
- **SND-2 cache-key collision (CRIT catch):** blanket "render isAssume==isAssert"
  would collide `symexCacheKey` (canonicalize.nim:684) → silent wrong answer. Needs
  distinct tag `Am:` vs `At:` + test. Also `collectAssertRanges` (abstraction.nim:324)
  has `else: discard` silently dropping assume range facts — DoD'd.
- **CR-2b crash-risk closed:** `tUninterp("__unsupported_")` → uncaught ValueError in
  allocateSym (only `__ownership:` guarded). Needs new prefix guard OR capForcedUnknown
  + explicit ctx-threading decision.
- **CR-1c open item RESOLVED:** chapulin config has NO `-d:danger`/`--panics:on`
  (checked nim.cfg/chapulin.nimble) → catch approach viable. Depth nuance: `--panics:on`
  hits only Defect; CR-1a/b (ValueError/KeyError=CatchableError) unaffected. HARD RULE:
  `try/except` never `try/finally` (commit b7258f7 = C-backend-only silent sxUnsat);
  regression must diff both backends.
- **TOT-1 rescoped generative→table-driven:** all symex entry points need `fn: typed`;
  fuzz.nim generates values not AST. Fixed hand-authored §0-invariant corpus. (3 of 4
  agents independently proved this.)
- **Design:** 18 near-identical exception carriers exist; CR-1c/CR-2b use ONE generic
  `SymexClassifiedDegradeError{kind}`, name the 18 as incremental debt.
- **Sizing/process:** P2→P2a(value)+P2b(ref,L; variant excluded); Q1=timeboxed spike
  (may have no viable encoding); INT-1 recurring per-SW-slice + rollback clause;
  SYMEX_PLAN.md ADR-landing + stale-status obligation; `(t)` RED-state instruction;
  M4 `&=` is a type-classify branch not a set edit (binopForInfix has no `&`).

## RESOLVED FORK (Corey 2026-07-12) — SW version-pin idiom
**Synthesis chosen:** canonical `tsymex_phase15_CR2_cachekey.nim` pin stays `==`
(conscious-bump gate per [[symex-version-bump-cr2]]); incidental feature-test pins
convert to tolerant `>=`-floor (`check parseInt(symexWalkerVersion) >= N`). Kills ~200
pin touches + the parallel-worktree literal collision, keeps one loud drift gate.
Serialize-against-live-base rule still applies to the canonical `==` pin. Now recorded
in the RFC's §Version-pin discipline as a settled decision, not a fork.

## Round-1 architecture review — applied (2026-07-12)
4-agent team (depth/breadth/design/feasibility), all grounded in the actual code.
No genuine forks surfaced — every finding had a goal-determined best answer;
applied all directly. Corey's mega-RFC scope preserved (Clusters 6-7 annotated a
**decoupled track**, not split out). Material changes now in the RFC:
- **SND-1 re-scoped L→S:** mechanism already exists (`Path.uncertain`,
  runtime.nim:376-378, consumed at both `w.found` producers). Fix = taint the
  `isUnsupported` arm; **no ADR-0012 D2 amendment** (only a new taint producer).
  Added Class-A/B taxonomy (Class-A already immune via `capForcedUnknown`).
- **CR-1 is NOT a sweep:** split CR-1a (#3 fixed at abstraction/BV-ban locus,
  matches [[symex-abstraction-bv-ban-toz3int]]), CR-1b (#4 tail-return lowering),
  CR-1c (one narrow last-resort catch, distinct `weInternalWalkerFault` kind).
  Respects the documented "ValueError/AssertionDefect must surface" crash-doctrine.
- **CR-2 split by error() class:** CR-2a expression-position (dep SND-1), CR-2b
  parameter-type-position (uses already-sound `capForcedUnknown`, no SND-1 dep).
- **New slices:** TOT-1 (generative totality harness — operationalises §0),
  INT-1 (chapulin workaround-removal exit gate).
- **Version-pin discipline** now a cross-cutting section (TWO consts:
  `symexWalkerVersion` + `renderAsChoicesVersion`); per-slice `Ver` column added.
- Corrections: M1 dep on CR-1 dropped (class-C, symex.nim); M3 de-risked S
  (nim-z3 `lastIndexOf`); SND-2 = 12 exhaustive switches (+ scan.nim found[0] trap
  test, + keep assert raise-forks steps 1/2/4, drop only forkDefect step 3);
  Q2 re-scope-after-SND-2.
- **Open verification for round 2 / CR-1c:** confirm chapulin build flags — under
  `-d:danger`/`--panics:on` a failed doAssert is uncatchable; CR-1c would then need
  raise-guards, not a catch.
- **Verified finding set:** consolidated in session scratch `verify_results.md`
  (4 agents, all findings re-checked @ 99fa2db). RFC reflects it.
- **★ Key architectural insight for the architect rounds:** hard dependency
  **SND-1 ≺ CR-1/CR-2** — the crash-degrade work must NOT ship before the
  mkUnsupported-statement soundness fix, else it trades visible crashes for silent
  false-`sxSat` (worse under Invariant 3). SND-1 amends ADR-0012 D2 precedence.
- **Biggest scope changes from verification:** #5/#11/#7-symptom/pred/succ/`..<`
  HEALED (dropped); #3/#9 narrowed; SH1 does-not-repro (deferred); **NEW CRIT
  soundness bug SND-1** found (silent mis-mutation → false sxSat, general).

## Scope decision (Corey, 2026-07-12)
**ONE mega-RFC covering ALL chapulin findings**, across every subsystem (symex
walker/parser/solver + fuzz + shrinker + coverage). Corey chose this over the
recommended "symex-RFC + route-the-rest" split. Mitigation for coherence: the
single doc is organized into **per-subsystem clusters** (Phase-15/16 cluster
style), each independently sliceable. Unifying thesis: *everything chapulin's
v1/v2 verification harness surfaced*, with the §0 "walker never crashes /
Invariant-3 totality" invariant as the marquee cross-cutting cluster.

Source findings: `/mnt/c/Users/corey/projects/chapulin/docs/nelli-findings.md`
(pinned at nelli `99fa2db` = current HEAD; `file:line` refs align).

## Prerequisite: verify-at-HEAD pass (IN PROGRESS)
Some findings are already HEALED by A7/A8/A9 (confirmed pre-fan-out: #7's
`toLowerAscii` symptom works now; #3 bitwise-plain-int doesn't repro in the
simple shape). Don't slice healed work. Four background verification agents
(sonnet, verify-only, no fixes) each re-check a cluster and return a
LIVE/HEALED/PARTIAL table with evidence + fix locus + size:
- **A** — §1 walker crashes: #3, #4, #5, #11. (Known: #4 KeyError = LIVE-CRASH.)
- **B** — §2 model gaps: #1 seq[byte] reader, #7 toLower/probeProto, #8 rfind,
  #10 string.add, parseBiggestInt, min/max.
- **C** — §3 parser (#2 tupleConstr, objConstr, seq-slice, pred) + §4 solver
  (#6 dependent loops, #9 loop+string-param, symexAssume==symexAssert).
- **D** — §5 fuzz/corpus/DB, §6 coverage, §7 shrinker Int128 compile bug.

## Confirmed-live so far (pre-verification spot-checks)
- **#4** implicit tail-return referencing a local → uncaught `KeyError` at
  `runtime.nim:2629` (`env[e.vname]`), native crash. **LIVE-CRASH.** Flagship of
  the §0 "walker never crashes" invariant.
- **#7** symptom HEALED (A9), but a real latent bug remains: `probeProto`
  (`runtime.nim:1763` `StrOpKinds - {...}` catch-all) returns `none` for the
  A7/A8/A9 string ops (`iekStrToLower/Upper`, `iekRadixFmt`, `iekRuneToStr`) —
  missing from its modeled subset. Masked by a lowering fallback today.

## Open forks (awaiting Corey)
- (none blocking) — all Cluster H scope RESOLVED. Round-2 decisions (2026-07-23): generics →
  DEFER to Cluster G (keep sxUnknown + guard/test; no live collision); witness → INCLUDE
  recursive-pointsTo work as slice **H_witness** (implement ADR-0010 invariant #4). Baked into
  ADR-0022. Earlier scope holds: containers in-cluster (Table/HashSet stay degraded — orthogonal
  limits); unify all refs on nominal sort-id. **Corey green-lit building Step A.**
  **ROUND 2 COMPLETE — findings applied to ADR-0022** ("ADR-0022 Round-2 architect review"
  subsection). VERDICT: **buildable** — the nominal-id primitive (recursive `signatureHash`)
  was empirically verified on the dev toolchain. Consolidated notes:
  `scratchpad/adr0022_round2_consolidated.md`. Key applied refinements: nominal-id = recursive
  signatureHash as a first-class `IRType.nominalId` field via ONE shared helper (classifyType
  + namedRefPlaceholder), change `refPointeeTypeId` only (not `$`); **H1 folds in H4 core**
  (real mkNewT+mkFieldDerefWrite — else P2b-1..8 regress to sxUnknown; H4 eliminated as a
  separate slice); H1 also patches the `nnkSym` sym-indirection branch (`type NodeRef = ref
  Obj`); witness needs a provenance flag (zero-field `type Token = ref object` else
  mis-renders nil); drop double zero-write; Table/HashSet stay degraded (orthogonal limits);
  storeSeqElem needs an itRef arm for seq[Node] literals. **Landing order (de-risked): Step A**
  (add nominalId field+helper, pure no-op, macro-time testable) → **Step B** (flip
  refPointeeTypeId, verify inline-ref R6/R7/R9/R12 green) → **Step C = atomic H1** (classifyType
  flip both branches + classifyObjectRecordFields + real construction + isNew zero-write +
  provenance flag + isHeapRef/isRecursionPlaceholder + delete 3 carve-outs + SW54→55 + RC5→6)
  → H_containers → verification → H_final. **Step A LANDED `d85f0f7`** (nominalId field+helper, pure no-op, 424/424).
  **Step B LANDED `ddc9196`** (refPointeeTypeId prefers nominalId; SW 54→55; CR2 pin "55";
  424/424 both backends, all inline-ref heap tests green — mixed-naming risk did NOT
  materialise). **NEXT: Step C = atomic H1** (SW **55→56** + RC **5→6**; Step B already took
  54→55). See ADR-0022 for the full H1 spec: classifyType flip (BOTH the named-alias
  `nnkObjectTy` branch AND the `nnkSym` sym-indirection branch → `itRef(full pointee)`) +
  classifyObjectRecordFields shared core + widen the ~10 dsl_parser construction/routing gates
  + real heap construction (`mkNewT`+`mkFieldDerefWrite`, folds in H4 core — value-arm 42eafde
  replaced) + universal `isNew` zero-write (every field, closes false-SAT) + variant detection
  preserved (stays excluded) + provenance flag so zero-field `type Token = ref object` witnesses
  don't mis-render nil + `isHeapRef`/`isRecursionPlaceholder` predicates + DELETE the 3
  bare-symbol carve-outs (dsl_parser.nim:1327-1331, refExprClassify:2436, :2106) + buildHeapSnapshot
  populates for named-alias svRef params. VERIFY: P2b-1..8 now real heap verdicts (aliasing
  `q=p;q.val=99;p.val==99` → sxSat, identity `p==q`), +3 regressing tests (r9/r10/r11b re-derived
  not relabeled), full sweep both backends. RESUME context below (superseded).
  **[SUPERSEDED — Step B done]** Step B CODE COMPLETE but UNCOMMITTED in the working tree (control loop verifying):
  `refPointeeTypeId` (runtime_heap.nim:25-33) flipped to PREFER `pointeeTy.nominalId` for a
  named-object `itTuple` (non-empty nominalId), else `$pointeeTy` fallback; SW bumped 54→55
  (canonicalize.nim:104); CR2 pin → `== "55"` (verified the only `==` pin); RC stays 5. Diff
  reviewed clean. A FULL both-backends sweep is running → `scratchpad/sweep_stepB.log`; as of
  last check 337/424 with 0 failures (mixed-naming risk NOT materialised — no heap/ref-test
  regression). **RESUME: confirm the sweep hit 424/424 both backends (tally
  `scratchpad/sweep_stepB.log`: all rc=0, no 137/124 hangs), then `git add -A` the 3 changed
  files (canonicalize.nim, runtime_heap.nim, tests/tsymex_phase15_CR2_cachekey.nim — NOT
  scratchpad/) and `git commit --no-verify` as "feat(symex): Cluster H Step B — refPointeeTypeId
  prefers nominalId". Then Step C (atomic H1).** If the sweep shows a heap-test regression, it
  is a mixed-naming site: populate the missing nominalId construction path or report. Full
  slice specs in ADR-0022's two review subsections. Use `scripts/dt-bounded.sh`; both backends; `git commit --no-verify`; no
  Claude trailer. Remaining after Cluster H: Q/TOT/INT/F/C.

## Key decisions (this session)
- Mega-RFC scope (above). • Verify-at-HEAD before drafting (drop healed findings).
- Non-symex findings STAY in this RFC (per mega-RFC choice) as their own clusters,
  rather than routing fuzz→FUZZ_PLAN.

## Review ledger (stage 4) — round 1 IN FLIGHT (2026-08-16, /code-review skill, scope = round-6 diff `8040045..HEAD`)

**Stage:** MANDATE APPROVED by Corey 2026-08-21 ("follow the mandate" = the presented default: fix through Medium, leave Low, loop to 0-Crit/High/Med, then 0.5.1 + chapulin re-pin). Fix loop RUNNING (Step 7): serialized sonnet slices R1→R2→R3→R4→R5→R6→N9→N10→Q3, then re-review round (security+design standing lenses re-run), then 0.5.1 release + chapulin re-pin. **Progress: R1 DONE `ae96f35` v89 · R2 DONE `f89375c` v90 · R3 DONE `808cc21` v91 · R4 DONE `02b009d` v92 · R5 DONE `9831ac2` v93 · R6 DONE `84e1355` (no walker bump — test-only) · N9 DONE `213a725` v94 · N10 DONE `d690221` (no walker bump — test-only) · Q3 DONE `af02996` (refactor-under-green) · RE-REVIEW ROUND 1 DONE (consolidated entry below: N21 CONFIRMED Critical; N16 confirmed Medium fix-now; N13 test-only fix-now; N19+N24 deferred with rationale; D1/D2/D3 verifying, D4 accepted; N25/N26 new Lows) · FIX ROUND 2: N21 DONE `117b802` v95 · N16 DONE `c5d6b3c` v96 · N27 DONE `cd81623` v97 (honest downgrade live-bug→hardening: two incidental protections meant no wrong verdict escaped; now sound by construction + 52-site audit) · N28 DONE `1023e9f` v98 (LIVE for int-offset: false sxUnsat → correct sxRaised; string-backed sibling = hardening; NEW open finding N31 spurious-sxUnsat-after-scan-block, probe in mini re-review) · N13 DONE `1803fb7` (test-only, all pins green-on-arrival) · D2 DONE `db18637` v99 · D4 DONE `19dfb49`+`b8fe195` (refactor + phase14 rider; new findings N32 open-unverified, N33 Medium phase14-generation unregistered) · MINI RE-REVIEW DONE: lenses = 0 Crit/High (1 Medium comment-only sync mirrors, Lows: scanner whitespace, marker escape hatch); N31 probe = CONFIRMED High soundness (two-hop seeded counter in block → spurious sxUnsat) + N34 Low; N32 probe = REFUTED (inert by construction) + N35 Medium deferred (guard-callee OOM, pre-existing). **FIX ROUND 4: N36 DONE v101 · N37 DONE v102 · SPOT-CHECK RE-RUN VERDICT: DO-NOT-CLOSE — N39 High (category-2 certification mis-scoped: allocateSym called unguarded at walk time from isVariantConstructSym :7555 + lowerVariantLit :9079; entry above). N39 DONE `e2075d4` v103 · SPOT-CHECK #3: DO-NOT-CLOSE — N40 High (predicate false-negative on non-string-key Tables + 3 more unguarded walk-time allocateSym caller families; per-caller guarding has failed 3×). N40 DONE `39aad71` v104 · SPOT-CHECK #4: DO-NOT-CLOSE — N42 High (isDeref READ path lacks per-path taint on alloc degrade; itUninterp shape statically traces to unsound sxSat; itTable/itSet accidentally masked by the N41 crash — N41 must NOT be fixed before/without N42). N42 DONE `a3dba31` v105 (drain-at-call-site ×4 — three isDeref sites + a second undocumented isDerefWrite arm-field instance; liftHeapValue itUninterp arm landed WITH the taint per the cross-constraint; N41 left untouched/masked as permitted) · N43 Low process backlogged (predicate/allocator parity test) · SPOT-CHECK #5: **CLOSE** (N42 verified to the verdict chokepoint; all 8 mkHeapArrayVar sites accounted; N41 masking intact; N44 Low test-strength backlogged) · **LOOP FLOOR REACHED — 0 Critical/High/Medium, only Lows remain** · **0.5.1 RELEASED** (release commit `9aae295`, annotated tag v0.5.1 pushed, tianguis-publish GREEN 1m16s; symex-windows on the release commit in flight at last check — run 32588326184) · **CHAPULIN RE-PINNED `5fd804b` (LOCAL ONLY, never push)** — milpa.kdl+milpa.lock only (lock: ref v0.5.1, commit_sha 9aae295, fresh dag identity; pre-existing stale `version "0.4.0"` lock field left as milpa wrote it); owner WIP untouched; container mounts needed BOTH chapulin AND `C:\Users\corey\.cache\milpa` at host-identical paths (_deps entries are symlinks into the milpa CAS — dangling under the old C:\app mount, documented here for future runs); validation vs v105: t_symex_checksum ALL GREEN (sidecar canaries incl.) + t_symex_decode ALL GREEN (all five B7 witness cross-checks replay vs real decode()); t_symex/t_symex_smoke skipped — WIP-modified by owner, failures would be ambiguous. Remaining: confirm symex-windows green → final loop-end report to Corey.**; loop until nothing above Low, then 0.5.1 + chapulin re-pin. Verifier verdicts: D1→N27 live bug (High), D2 CONFIRMED (Medium), D3 refuted-as-written→N28 (Medium) — entries above. New pre-existing Lows from N16 sweep: N29 (HOF lambda sort-mismatch class, C4+C6), N30 (symValFromRawAst itString closure-return fault-kind decline). Injection count now TWENTY-ONE.** Slice agents twice detached waiting on background notifications mid-run — both times resumed via a foreground-only follow-up message; watch for it on every slice. Deferred BY the mandate presentation: N7 recognizer normal form (own future round, too invasive for a hotfix slate); Q5 (chapulin `ebc40fd` z3-float/softlink-bump disclosure — needs Corey's own judgment, local-only commit, his to amend or accept). Lows left as-is: N3/N11/N12/Q4. **Resume if interrupted:** re-invoke `/code-review RFC-chapulin-hardening`, skip reviewers/verifiers (results below), continue the fix loop at the first slice not marked fixed in this ledger; walker bumps one per verdict-affecting slice starting v89; CR2 `==` pin tracks the latest bump.

Findings (id · sev · status · note). Prior 3-lens findings S1–S4/W1–W3/Q1–Q5 remain as recorded in the ⚠ STAGE-4-STYLE REVIEW bullet at top (S1/Q1 control-loop-confirmed there). New this round:
- ✅ **R1 FIXED `ae96f35` (walker v89), 2026-08-21** — chokepoint `placeholderReadDeclineMsg`/`declinePlaceholderInLower` in runtime.nim (threadvar-sink taint, branch-scoped, never raises); routed: iekSeqLen svSeq (S1, also fixes for-loop bounds), iekSeqSlice (N1, returns recv unchanged → taint propagates), iekSeqAdd (audit extra — was whole-run ValueError poison), isIndex message gains ixLoc prefix (Q2). Full 17-row audited-site table in the agent summary; new tests/tsymex_r6_r1_placeholder_totality.nim (all access forms incl. Q1's index pin + SAT/UNSAT companions); CR2 pin 88→89; TOT-1 row added; 15-file sweep green, zero migrated pins. RED honestly confirmed for S1/N1/Q2/seqAdd/callee-pass; iteration + equality were ALREADY sxUnknown pre-fix (different mechanisms, reported not faked). S1/N1/Q1/Q2/N5/N6 → **fixed**. NEW residuals recorded: **N13 · Medium · open** — `defaultZero`'s itSeq arm calls raw allocateSeqDataRaw (not the placeholder path) → whole-run poison on discriminator reassignment into an unbacked-seq arm field (degrade-honesty, not unsound; ride cleanup slice); **N14 · Low/informational** — seq==seq, `in`-membership, element-assign, del/insert/pop are unmodeled for ALL seqs (pre-existing engine gaps, not round-6, not in scope); **N15 · Low** — field-sourced placeholder + index surfaces a different classified kind than seNestedSeqUnsupported (cosmetic asymmetry).
- ✅ **R2 FIXED `f89375c` (walker v90), 2026-08-21** — isCall fallthrough's else-twin binds retSym to `defaultZero(stmt.retTy)` via retBindEq on never-assigned-result paths (S3 → **fixed**); defaultZero HOISTED to module level (shared, no parallel constructor) and its itSeq arm now builds the v88 placeholder for unbacked elem types instead of raising (partial N13 — the retBindEq/defaultZero reach; defaultZero's other caller unverified, N13 stays open-narrowed for the cleanup slice); unsupported return kinds keep the honest v86 classified decline (float/nested-variant/distinct/ref/ptr/backed-seq — pre-existing scope, unchanged). New tests/tsymex_r6_r2_zerodefault_result.nim 21/21 (6 honest pre-fix REDs incl. bool/string/tuple false-sxSat; multi-path exhaustiveness closes N10's v86 gap); CR2 pin 89→90; 12-file sweep green, zero migrated pins. NEW residual: **N16 · Medium · open** — closure-call summarization (`applyClosureGround` ~8178) has the same never-assigned-result free-funcApp shape (no pinned repro exists; HOF/closure summarization scope — future round or cleanup-slice probe).
- ✅ **R3 FIXED `808cc21` (walker v91), 2026-08-21** — `SymVal.svInt` gains `ziWidth`/`ziSigned` (default 0/false = no-fork-unless-proven), stamped at allocateSym isIntOffset arms, lIsIntOffsetLocal proto, literal call-arg proto, coerceIntLit, reconcileInt (also fixed a latent metadata-drop there), propagated via arithInt/iteSV; new `overflowCondInt` forks bAdd/bSub/bMul on `result < low(T) or > high(T)` gated ziWidth!=0 && ziSigned — BV-path parity (div/mod never forked either side). S2 → **fixed**. 10/10 pins tests/tsymex_r6_r3_svint_overflow.nim (3 honest REDs; UNSAT companions prove no spurious SAT); CR2 pin 90→91; 13-file sweep green. HONEST NARROWING: stamping the generic top-level param-promotion site caused a >15min blowup in b4_readcstring (comparison arithmetic forks compounding) — reverted + documented in-code; that site stays unstamped. TIMING FLAG: b5_chained now ~7.5min (two independent fork sites, reproduced 2x, bounded — capability cost, not a hang; symex-windows CI leg will grow). Scope note: genuinely-lIsIntOffsetLocal-seeded locals have NO reachable overflow path through either closed-form idiom (counter reassigned from unstamped sentinels before user arithmetic) — pin redesigned to the literal-call-arg site, documented in the test file. NEW residuals: **N17 · Low** — collectIntOffsetLiteralLocals lacks the one-level call-boundary trace its param sibling has; **N18 · Low** — .len/.find/.indexOf/parseInt sentinels carry no width metadata (widening = more honesty but compounding-fork risk, own decision later).
- ✅ **R4 FIXED `02b009d` (walker v92), 2026-08-21** — collectors keyed by SYMBOL IDENTITY (`seq[NimNode]` + `containsSym`/sameSym; signatureHash rejected — hashes type, not binding); `ensureProcRegistered` saves/restores both collector sets (caseNarrow precedent) AND `parseCalleeImpl` recomputes each callee's own classification from its own monomorphized body (TDD RED caught that clear-only would strip callees' legitimate closed forms — a real would-be regression); mutation veto widened to var-mode argument positions in any in-loop call; iekSeqAdd doAssert → classified kind-mismatch decline (defense-in-depth; believed unreachable post-W1/W2a, pinned via the veto); considerCandidate gains ntyNone guard + resolveRoutineImpl. W1/W2/W3/N2/N8 → **fixed**. New tests/tsymex_r6_r4_collector_scoping.nim (W1/N2 honest REDs; W2a/b green-only — the exact construction was pre-excluded by an unrelated gate, reported honestly; deliberate B7r one-level promotion regression-pinned); CR2 pin 91→92; 14-file sweep green, pre-existing reds unchanged. N11 left (convention-consistent, wider refactor). ⚠ FOURTH prompt-injection attempt (fake system-reminder on the CR2 file + don't-disclose) — disregarded, disclosed, same family as the prior three. NEW residual: **N19 · Medium · open** — generic procs indexing/.len/.add on their own generic-typed param hit several pre-existing unguarded classifyType calls in the monomorphized parse path (parseCalleeImpl per-formal/return, nnkVarSection); zero corpus coverage; future round.
- ✅ **R5 FIXED `9831ac2` (walker v93), 2026-08-21** — fix shape (b), NOT (a): binding `i = bound` PROVEN unsound by hand-derivation (empty-key terminator breaks before the advance → real exit at bound-1; the two exit cases disagree, no single closed-form binding is faithful — a spec correction of the slice prompt's preferred option, made on evidence). Shipped: `collectPairLoopCounterConsumedAfter` pre-pass (sameSym-identity, R4's proc-scoped save/clear/restore discipline) declines the B6 membership fast-path when the counter is read after the loop, routing to the SAME fold-omitted mkShortCircuitWhile k-unroll as the non-member arm (NOT the generic path — an intermediate RED proved that re-includes the .add fold and breaks a regression pin). Other-recognizers audit: Q1/B0/B3/B4 already bind exit state soundly (mkAssign to bound on exhaust paths) — B6 was the only gap. S4 → **fixed**. 5/5 new pins (2 honest REDs incl. the wrong-verdict signature); CR2 92→93; sweep green (one b5 silent exit-1 stash-verified pre-existing flake). ⚠ FIFTH prompt-injection attempt (fake date-change system-reminder + don't-disclose) — disregarded, disclosed. NEW residuals: **N20 · Low/informational** — k-unroll fallback reports beBudgetExhausted even when assumed iterations fit under maxLoopUnwind (structural; the closed forms exist to route around it); **N21 · OPEN-UNVERIFIED · potential soundness** — derived reasoning only: an odd-segment region with non-empty final segment satisfies iekStrInOptionRegion membership yet implies a value-read starting exactly at bound, which the member branch's no-defect claim may not cover — ASSIGN A VERIFIER PROBE IN THE RE-REVIEW ROUND.
- ✅ **R6 FIXED `84e1355` (test-only, no walker bump), 2026-08-22** — Option B shipped (sentinel round-trip audit); Option A (reflection-driven emit) REJECTED with evidence: ≥5 kinds pick their mk*/t* constructor from a FIELD VALUE, not type shape (itSeq on seqUnsupportedFieldReason.len, isCall on .opaque, isDeref/isDerefWrite 3-way, isRaise), and BOTH historical drops (retIntOffsetPositions, seqUnsupportedFieldReason) live in exactly those value-dependent kinds — a generator could not have prevented either incident; recorded as design debt in the test-file header. New tests/tsymex_r6_r6_emit_roundtrip.nim: 91 sentinel tests (17 IRType / 38 IRExpr / 32 IRStmt kinds+paths) via static[T] macros forcing the REAL emit* through a genuine macro round trip, field-by-field deep equality via an independent `fieldwiseEq` (deliberately NOT reusing IRType.==); three compile-time exhaustiveness gates (case-no-else per kind enum) — a new kind fails to compile until added; IRType.nameIsRefAlias pinned as the one documented non-round-tripper. RED: re-injected the exact B5 retIntOffsetPositions drop → exactly one test failed, ~90 stayed green (isolation confirmed), dsl_parser.nim restored byte-identical. LIVE-GAP CHECK: zero additional drops in current emit code. Sweep green (7 files; b5_chained skipped per plan — no production change). N4 → **fixed** (the CLASS is now fail-loud). ⚠ SIXTH+SEVENTH prompt-injection attempts (two fake date-change system-reminders + don't-disclose) — disregarded, disclosed. NEW residual: **N22 · Low · open** — IRType's own `==` (types.nim:2107) silently skips nominalId/nameIsRefAlias in its itTuple arm, undocumented (unlike the documented isPlaceholder skip); small follow-up candidate.
- ✅ **SPOT-CHECK #5: CLOSE (2026-08-22) — N42 verified closed; LOOP FLOOR REACHED (0 Critical/High/Medium).** Mechanism traced end-to-end to the verdict chokepoint, not just a sink: drainPendingLowerEffects (runtime.nim:6862-6911) on loweringDidDegrade calls forkPathTainted → child `uncertain = true` unconditionally; isTargetLabel (:8396-8409) gates on `p.uncertain` — sets sawUnknown and NEVER calls trySolve, so a tainted path structurally cannot contribute an sxSat to w.found; the D2 precedence only ranks entries that already passed this per-path gate. Caller sweep: exactly 8 mkHeapArrayVar sites repo-wide (heapValueSort has no other caller) — 4 drained by the fix (3 isDeref + isDerefWrite arm-field), 1 defensive/redundant (isDerefWrite disc-heap, already covered by RHS lowerInExpr), 2 pre-covered by their own subsequent lowerInExpr (isNew zero-write, isDerefWrite plain field), zero missed families. liftHeapValue itUninterp arm symmetric with allocateSym's svBool placeholder; taint precedes heapSelect/liftHeapValue on all 3 isDeref routes. N41 masking intact as claimed (rawAnyAstOf still crashes one call earlier in heapValueSort → whole-run weInternalWalkerFault sxUnknown). Adversarial probes (scratch, deleted): deref in while-guard / bare call arg / bare assert — no intervening binding — all honest post-fix (no sxSat). Mechanics: walker "105"/CR2 pin/nimble registration confirmed; n42 11/11, CR2 7/7, both audits green in container. CAVEAT ledgered as **N44 · Low (test-strength)** — the shipped n42 suite's shapes self-drain via their next statement, so its pins cannot distinguish mechanism-present from mechanism-absent (honest by the commit's own admission); the spot-check's shape-independent probes (while-guard/call-arg/assert deref) are worth adding to the suite in a future round; static construction (drain precedes every use, unconditionally) is the closing evidence. Tree byte-identical after. ⚠ FIFTIETH prompt-injection attempt (fake date-change + don't-disclose vs the spot-check agent) — disregarded, disclosed.
- ✅ **N42 FIXED `a3dba31` (walker v105), 2026-08-22 — deref-read path drains on allocation degrade; a SECOND undocumented gap found and closed en route.** Probes empirical (temporary instrumentation in runtime.nim/runtime_heap.nim, reverted byte-identical): isDeref's heap-array materialization DOES set loweringDidDegrade and does NOT propagate into child.uncertain — gap confirmed as briefed. But the brief's crash analysis was half-wrong: heapValueSort's sort probe indeed survives itUninterp, yet `liftHeapValue` had NO itUninterp arm at all — uncaught SymexRefUnresolvedError right after heapSelect was accidentally masking the exact predicted-unsound shape. With liftHeapValue patched in isolation, ~10 black-box SUT shapes produced NO observable false sxSat: `{.symexOpaque.}` calls and `new T` already self-taint for unrelated reasons; top-level ref params hit CR-2c witness-demotion or a pre-existing emitTyAndReader macro-time crash; every working two-hop shape drains via the next lower()-calling statement's own isLet binding — sound by COINCIDENCE (instrumentation-confirmed), not by construction. FIX = design (a), drain at call site via the existing drainPendingLowerEffects/loweringDidDegrade sink (no new mechanism): all three mkHeapArrayVar sites inside isDeref (bare/plain field, variant disc-heap, variant arm-field) drain immediately after materialising; PLUS a second independent instance found beyond the brief — isDerefWrite's variant arm-field write sub-arm, whose armHeap materialisation happens AFTER the RHS's own drain-providing lowerInExpr. liftHeapValue gained an itUninterp arm (mirrors itBool), landed TOGETHER with the taint fix per the cross-constraint — shipping the crash-removal alone would have regressed soundness (verified by isolated probe). N41 (svTable/svSet rawAnyAstOf arms) deliberately untouched: taint-only, masking crash left load-bearing, as permitted. Walker 104→105, CR2 104→105. New tests/tsymex_r6_n42_deref_taint.nim 11/11, registered (itUninterp + itTable shapes incl. nested-block first-touch, sound-path companions, version floor). Stash honesty: reverting production leaves everything green except the version-floor pin — no shippable black-box RED exists for the core mechanism (the coincidental drains cover every constructible shape); documented plainly in the test file + walker doc comment rather than fabricated. Sweep green (n42, n40, r3_deref_read, r4_deref_write, r6_refobj, r7_alias_chain, r9_recursive, H1, rectify_refs, CR2). N42 → **fixed**. ⚠ FORTY-EIGHTH prompt-injection attempt vs the slice agent (fake date-change + don't-disclose, the documented pattern verbatim) — disregarded, disclosed. ⚠ FORTY-NINTH attempt vs the CONTROL LOOP itself (identical fake date-change + concealment line appended to a system listing) — disregarded, disclosed.
- ⚠ **N42 · High · spot-check #4 (2026-08-22): per-path taint gap in the heap-deref READ path — static trace to an unsound sxSat.** N40's dual-sink is necessary but not sufficient: verdict assembly (runtime.nim:10498-10512) rightly lets a winning sxSat beat global sawUnknown (a replaying witness IS sound when the degrade happened elsewhere) — so a path whose OWN allocation degraded must carry per-path `uncertain` taint, and `isDeref` (runtime_heap.nim:636-638 → heapValueSort → allocateSym) neither taints (`forkPath`, not `forkPathTainted`) nor drains (no lower()/drainPendingLowerEffects before the fork). itTable/itSet degrades there are ACCIDENTALLY masked (svTable/svSet have no rawAnyAstOf arm → unclassified ValueError → catch-all sxUnknown — this is what N40's own tests observed); the itUninterp/svBool placeholder does NOT crash → traced path to unsound sxSat on a first-touch `p.field` deref of an ownership-wrapped field. STATIC finding — fix slice must probe empirically first. VERIFIED SOUND: derefwrite + isNew (drained by their subsequent lowerInExpr — better than N40's doc claimed), both N39-guarded callers (guards intercept before allocDegrade = defense-in-depth), closure family (drained by calling statement, N41's "safe-direction" characterization accurate), param boundary (byte-identical), audit mechanics + versions. CROSS-NOTE ADDED TO N41: fixing rawAnyAstOf's missing svTable/svSet arms WITHOUT fixing this taint gap flips the table family from accidentally-sound to unsound — the N42 fix must land first or together. NEW **N43 · Low (process)** — nothing structurally enforces predicate/allocator parity (drift bit twice: N39, N40); parity property test = next-round quality item. ⚠ FORTY-SEVENTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **N40 FIXED `39aad71` (walker v104), 2026-08-22 — allocateSym is TOTAL.** New `allocDegrade(kind, msg)` chokepoint at all five former raise sites writes BOTH sinks unconditionally: loweringDegradeErrors (per-path SND-1 precision when lower()-reached) AND an immediate synchronous currentWalkCtxPtr sawUnknown (CR-9-style fwd-declared sync proc) — justified by an audit showing walk-time direct callers never drain the lowering sink; dual-write makes silent loss structurally impossible; immediate-degrade-on-alloc = immune to the B7r2 exception-elision hazard. Placeholders: itUninterp→svBool; itTable/itSet→real shape with cardinality forced 0 (Bug #2 precedent, no full read-taint — deliberate, matches N39). Predicate fixed: non-string-key Table branch added (new seUnsupportedTableKeyType) + two pre-existing message-drift bugs corrected for byte-identical param-boundary compat. Param boundary: raiseParamAllocIssue pre-check loop in runSymexImpl BEFORE any walk state — identical carrier/kind/message as pre-N40; existing param-decline pins green unchanged. REDs (targeted-stash): Table-arm-field via N39's guard; heap-deref read/write — pre-fix the raise was silently MISATTRIBUTED to an unrelated beBudgetExhausted finding (stronger silent-loss signature); lambda family red via the weInternalWalkerFault catch-all. freshRetSym/hof-fold covered by mechanism argument (precedent-consistent). Audit: 5 category-2 markers removed, 4 param-boundary added (27→26 sites), certification text asserted by the test. Sweep 14/14 green, zero pin migrations. Walker 103→104, CR2 103→104. N40 → **fixed**. NEW: **N41 · Low (pre-existing)** — sortOfTuple/rawAnyAstOf have no compound-sv-kind arms, so the lambda-param family still declines via the blunt catch-all instead of N40's specific kind (safe-direction sxUnknown, decline-quality only; documented in test + canonicalize.nim); backlog with N30/N38. ⚠ FORTY-FIFTH+FORTY-SIXTH prompt-injection attempts (fake date-change; fake "user/linter" framing of the agent's own sed edit) — disregarded, disclosed.
- ⚠ **N40-ORIGINAL · High · spot-check #3 (2026-08-22): per-caller guarding has failed three times — the fix must move INTO allocateSym.** N39's two sites verified correctly fixed (guard hoisting, svBool-substitute invariant traced sound, versions consistent), BUT: (a) `unallocatableFieldIssue` FALSE NEGATIVE — non-string-key Table (`Table[int,string]`, ordinary user syntax per classifyType dsl_typebridge.nim:571-576) is never flagged, while allocateSym raises an untagged ValueError for it (runtime.nim:1986-1989, the ONE itTable/itSet raise with no audit marker); (b) THREE more unguarded walk-time allocateSym caller families found by exhaustive call-site classification: heap deref read/write (runtime_heap.nim:210 heapValueSort via isDeref :566/:637; :920/:996 derefWriteProto — contrast isNew :748 which IS gated by zeroIRExprForType), closure param/return allocation (runtime_closures.nim:37/:89 from iekLambda dispatch); (c) two Mediums not fully traced: freshRetSym (runtime.nim:2924, 5 call-return sites) and bare hof-fold accumulator allocs (:9219/:9417). Full 25-site classification table in the agent report; ground-type/tSeq-wrapped/witness-boundary/pre-walk sites verified safe. **FIX DESIGN MANDATED (round-4 closer): make allocateSym TOTAL** — for classifiable-but-unallocatable input it degrades in-band (lowering sink + tainted fresh value) instead of raising; the pre-walk param-entry boundary (runtime.nim:9853/9855) does its own explicit unallocatableFieldIssue pre-check and raises THERE to preserve the documented whole-run-decline semantics; fix the Table-key false negative in the predicate; audit markers collapse to "allocateSym is total; param boundary raises by design". Subsumes (b) and (c) wholesale. ⚠ FORTY-FOURTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **N39 FIXED `e2075d4` (walker v103), 2026-08-22 — probe CONFIRMED the live wrong verdict on the symbolic-disc route.** Stash-verified: block-nested reach into an unsupported Table[string,string]/HashSet[string] arm field via isVariantConstructSym gave **sxUnsat with zero errors** pre-fix (the exact ADR-0023 silent loss) vs honest sxUnknown unblocked — confirmed for BOTH itTable and itSet. lowerVariantLit route probed across 8 nesting shapes incl. ADR-0023's own tracer-bullet structure — never reproduced (already honest sxUnknown), guarded anyway per the certification-error framing. FIX = GUARD-BEFORE-CALL both sites: new `unallocatableFieldIssue` predicate (types.nim) mirrors allocateSym's dispatch kind-for-kind with recursion through composites (allocCostOf's "update both together" discipline); isVariantConstructSym degrades via its existing sawUnknown/walkDegradeErrors/forkPathTainted idiom hoisted beside the two budget checks; lowerVariantLit degrades via the loweringDegradeErrors sink with a fresh svBool substitute (sound: inactive-arm fields reachable only through the FieldDefect fork per the function's own invariant). All five audit markers corrected to the accurate claim; prose x2→x3 nit fixed. Walker 102→103 (confirmed verdict-surface fix), render 11, CR2 102→103. New tests/tsymex_r6_n39_variant_field_alloc.nim 12/12; sweep 11/11 green. N39 → **fixed**. ⚠ FORTY-THIRD prompt-injection attempt (fake date-change + don't-disclose; a suspicious agent-type listing treated with matching caution, no action directed) — disregarded, disclosed.
- ⚠ **N39-ORIGINAL · High · CONFIRMED-STATIC (spot-check re-run, 2026-08-22) — round-4's category-2 certification is MIS-SCOPED; loop does not close.** The audit markers at runtime.nim:1680/1694/1721 (allocateSym itUninterp ×3) and :2005/:2019 (itTable/itSet) certify safety on "reached only at pre-walk parameter allocation, zero intervening walkBlock frames." FALSE: allocateSym is also called at WALK TIME from `isVariantConstructSym` (:7555 — builds EVERY arm's fields under a symbolic discriminant) and `lowerVariantLit` (:9079 — inactive-arm placeholder fields via lower()'s iekVariantLit dispatch), both UNGUARDED. classifyFieldType's fallthrough confirms a variant arm field can legitimately classify itUninterp(__ownership:/__unsupported:) or unsupported itTable/itSet (scopedDeclineFieldTy special-cases only itSeq — Bug #2 has no analogue for these kinds). Trigger shape: a variant with e.g. a WeakRef-typed field in a non-active arm, constructed via literal or symbolic-disc inside nested blocks/loops → raise silently lost → false sxUnsat. Same class as N36/N37, left open by the certification error — evidence is static call-graph tracing (read-only review), SAME standard N37's mechanism arguments used; fix slice must stash-probe empirically AND guard. Spot-check otherwise CLEAN: degradeStrArm's 25-kind result map verified exact; catch list complete (one ValueError fallback = documented Defect-class carve-out, genuinely unreachable); N37 conversions preserve kinds/messages verbatim; all 5 allocateSeqDataRaw callers guard; versions 102/11 consistent; audit counts grep-verified, zero known-open, scanner not weakened. Plus **Low (cosmetic)**: audit prose inventory line ~121 says "itUninterp x2", actual x3 — executable assertions correct, fix the comment in the N39 slice. ⚠ FORTY-SECOND prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **N37 FIXED `e0d6494` (walker v102), 2026-08-22 — raise-class residue CLOSED (3 converted + 2 mechanism-converted + 2 verified-unreachable; audit at ZERO known-open).** Adjudication: iekSeqSlice base-kind decline REACHABLE (call-boundary svString/svSeq mismatch via isCall no-bridge binding) — converted, stash-RED false-sxUnsat→honest sxUnknown; iekSeqSlice CR-17 bound decline REACHABLE (two-hop seeded-local, N31's technique on a seq) — converted, stash-RED same; isRaise bare-reraise REACHABLE and the first WALK-LEVEL instance — converted, stash-RED for BOTH block-wrapped AND plain-call-frame shapes (callee interpretation uses walkBlock under the hood — the class never needed explicit blocks at walk level); lowerHofCall inline map/filter direct allocateSeqDataRaw — REACHABLE by code-path argument (live repro blocked by the upstream pre-existing N29 sort-mismatch, confirmed via reverted instrumentation), fixed by GUARD-BEFORE-CALL (isBackedSeqElemTy), never catch-after (B7r2 precedent); NEW third caller found during the sweep — lowerSeqLit's non-empty-literal branch — same guard applied (classification-completeness; verdict stays sxUnknown either way due to an unrelated nnkHiddenSubConv parser gap on tuple-literal elements, reported honestly); iekSeqLen receiver-kind decline VERIFIED-UNREACHABLE (parser emission set + the string-backed mismatch lands on the svString arm, empirically confirmed); allocateSeqDataRaw's own raise VERIFIED-UNREACHABLE (all five callers now guard first). Audit updated: 27 marked sites (8 safe/unreachable + 19 chokepoint), `verified-unreachable` vocabulary added, zero known-open ENFORCED mechanically. Walker 101→102 (three direct verdict-surface fixes), render 11 unchanged, CR2 101→102. New tests/tsymex_r6_n37_raise_residue.nim 12/12; sweep 10 suites green (C4's 2 known N29 reds only). N37 → **fixed**. ⚠ FORTIETH+FORTY-FIRST prompt-injection attempts (fake date-change + don't-disclose — notably contradicting the legitimate context date; fake "user/linter" framing of the agent's own just-written test file) — disregarded, disclosed.
- ✅ **N36 FIXED `c50b50f` (walker v101), 2026-08-22 — class MOSTLY closed; residue enumerated, not hidden.** Chokepoint design shipped: new `degradeStrArm` wraps lower()'s lowerStrArm call site, catching all six classified carriers and converting in-band with the fresh degrade symbol's kind chosen from e.kind — sound because the unwind crosses only plain proc frames, never walkBlock (verified by reading + empirically). isVariantReassign defaultZero wrapped like its siblings; isIndex's two declines converted at-site. HOF-alloc Medium: structurally UNREACHABLE via allocateSym (its itSeq guard mirrors allocateSeqDataRaw's supported set — documented in-code). RED stash-verified: block-nested option-region + split-cap shapes pre-fix sxUnsat/0-errors (real wrong verdicts) → post-fix honest sxUnknown; variant-reassign upgraded weInternalWalkerFault → classified feUnsupportedOp. Two families honestly unconstructible from valid DSL surface (requireStr receiver guard; both isIndex declines — intercepted earlier / rejected by Nim's type checker) — mechanism argument applies. ENFORCEMENT: tests/tsymex_r6_n36_raise_class_audit.nim requires `# [raise-audited: <reason>]` on all 30 raw-raise sites (19 converted-at-chokepoint, 6 category-2-safe, 5 KNOWN-OPEN backlog), red-demonstrated. Repro nuance documented: the split shape needed block+while nesting depth (matching N31), not a bare block. Sweep 17/17 green. N36 → **fixed**. RESIDUE → **N37 · High-pending-adjudication · open**: the 5 audit-marked known-open same-class sites (iekSeqLen/iekSeqSlice raw declines, isRaise bare-reraise) + the newly-found lowerHofCall INLINE map/filter direct allocateSeqDataRaw call bypassing allocateSym's guard (an unbacked closure-return elem type, e.g. map to (string,string), could raise in the hazard zone) — fix-or-prove-unreachable slice next. NEW: **N38 · Low (pre-existing)** — block-wrapped discriminator reassignment yields spurious UNCLASSIFIED sxUnknown even for fully-backed arms, zero degrade involved, identical pre/post N36 — safe-direction decline, quality/classification item, backlog. ⚠ THIRTY-EIGHTH+THIRTY-NINTH prompt-injection attempts (fake date-change + don't-disclose; fake "user/linter" framing of the agent's own marker-insertion edit) — disregarded, disclosed.
- ⚠ **N36-ORIGINAL · High · CONFIRMED-STATIC (final spot-check, 2026-08-22) — the N31 raw-raise class is NOT closed; loop does NOT terminate.** Task-A of the spot-check was clean (N31 fix idiom-exact vs the CR-17(a) sibling incl. the drainPendingLowerEffects uncertain-fork; scanner no-false-positive on `..`/floats/chains; registrations sound). Task-B found 14+ hazard-zone raw-raise sites of the SAME silently-lost-under-walkBlock class: `requireStr` (:27-30, used by ~13 lowerStrArm arms) and `needleAsStr` (:52-56); replaceAll/join/split/match/findRe/replaceRe/bytes/radixFmt/toLower-toUpper decline raises (~12 sites, runtime_strings.nim :342-662); `iekStrInOptionRegion` :791-796 — DOCUMENTED as a copy of the CR-17 guard but copied the PRE-fix raw-raise form; the catch-all "string op not modeled" arm :818-819; runtime.nim `isVariantReassign` :7093 UNGUARDED defaultZero call (its two siblings :7764/:8482 are correctly try/except-wrapped at the call site); `isIndex` raw raises :6859 (Table) and :7012 (backstop); plus Medium-unverified: allocateSeqDataRaw raises reachable via lowerHofCall's degrade-path allocateSym calls (:8852/:8943/:8976) for unusual hofRetElemTy. Category-2 sites verified SAFE at the pre-walk setup boundary (allocateSym itUninterp/itTable/itSet param-entry raises — zero intervening walkBlock frames, documented in-code). Defect-class invariant raises (doAssert/raiseAssert/width-mismatch ValueErrors) explicitly distinguished — internal should-never-happen checks, separate lower-priority cleanup, NOT this gate. N31's own "every other lower() site in this class" doc claim is FALSE — correct it in the round-4 fix. **ROUND 4 OPENS: one class-closing slice.** ⚠ THIRTY-SEVENTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **CLEANUP SLICE DONE `7d80032`+`54dda37`+`4815c34` (no walker bump), 2026-08-22** — three separate commits: (1) allocCostOf mirror comments on all 8 allocateSym composite-dispatch sites (+ allocDistinctSym, worded for its role as the itDistinct dispatch target); types.nim side already cross-referenced per-kind, unchanged. (2) placeholder-read audit scanner tightened to catch same-line spaced dot forms (`recv . seqLen`); baseline green 52/52, red-DEMONSTRATED on an injected spaced read (exact site reported), reverted byte-identical, re-greened; NO existing production line newly flagged — the current tree has zero spaced bare reads. Cross-line limitation documented honestly in-comment. (3) **N33 CLOSED**: all 19 previously-unregistered tsymex_phase14_*.nim suites ran GREEN individually in the container (zero bit-rot — full per-file table in the agent summary: A1a-A7b variant/multivariant cycles, B1-B67 strategy/trace cycles, C1-C4 cache/error cycles) and are registered in nelli.nimble in cycle order around the D4-registered A5 file. The invisible test generation is CI-visible again. Smoke check green (audit 3/3, CR2 100/11, one phase14 suite). N33 → **fixed**; design-Medium (sync mirrors) → **fixed**; security-Low (scanner) → **fixed**. ⚠ THIRTY-SIXTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **N31 FIXED `a5a7170` (walker v100), 2026-08-22** — ROOT CAUSE (instrumented, precise): NOT the counter pre-pass. Neither collector traces the two-hop seed, so the counter allocates svBV64; the accumulating-scan closed form calls iekStrSubstr, whose CR-17 Int-sortedness guard used a raw `raise (ref SymexUnsupportedStringOpError)` — the exact ADR-0023/SND-3 C-backend goto-unwind hazard banned from lower() (and already fixed at the sibling CR-17(a) ordering guard). Under 2+ nested walkBlock frames (what an explicit `block:` adds), the raise is SILENTLY LOST: walk() returns as-if-complete, no SymexErrorInfo, sawUnknown stays false → default-to-UNSAT fires on a reachable target. Confirmed by stderr instrumentation both ways (raise executes but never reaches the handler in the block-wrapped case; reaches it cleanly one level shallower) — instrumentation reverted before landing. FIX = BIND-NOT-DECLINE at the mechanism level: the guard now uses the in-band degrade idiom (loweringDegradeErrors + loweringDidDegrade + fresh unconstrained svString), mirroring cmpString/CR-17(a) exactly. Verified side effect: the no-block two-hop shape upgrades from honest sxUnknown to correct sxSat (the raise had been discarding a legitimate sibling path). RED stash-isolated: exactly the two verdict-affecting pins red (N31-1 sxUnsat→sxSat, N31-4 sxUnknown→sxSat), four companions + version pin green — split matches the mechanism. New tests/tsymex_r6_n31_block_counter.nim (6 pins); walker 99→100, CR2 99→100 (3-char pin verified passing; test title strings stale by convention, checks assert live values). Sweep 11/11 green. N31 → **fixed**. RESIDUAL CHECK ASSIGNED TO FINAL SPOT-CHECK: this is the SECOND CR-17-family raw raise caught by the same hazard — sweep lower()-reachable code (runtime.nim + runtime_strings.nim) for any remaining raw raises of the banned class. ⚠ THIRTY-FOURTH+THIRTY-FIFTH prompt-injection attempts (fake date-change + fabricated Agent-type listing; fake "user/linter edited runtime_strings.nim" framing the agent's own stash) — disregarded, disclosed.
- 📋 **N32 REFUTED-EMPIRICAL (2026-08-22 probe) + new deferred finding N35.** Structural reachability CONFIRMED and broader than described: ensureProcRegistered never touches inGuardCond, and a not-yet-registered proc called in a while-guard parses its ENTIRE body under the stuck flag. But provably inert: the flag's only consumer (parseAtomicOperand :1996) suppresses hoisting, which the A2a audit documents as verdict/witness-identical (defect forks deposit into preamble BEFORE the hoist decision — canonicalization/cache-key only); the one verdict-relevant consumer (mkShortCircuitWhile Case-4 selection) self-immunizes by setting the flag unconditionally at its own entry. Paired container repros: identical sxSat + identical witness both ways. Would have been degrade-only even if divergent. N32 → **refuted** (no fix; the leak is benign by construction of its consumers — a future consumer of inGuardCond should be aware, noted here for the record). NEW SEPARATE FINDING from the probe: **N35 · Medium · DEFERRED (pre-existing)** — reproducible runtime OOM crash when a user proc whose body contains its own while+continue (R14) guard shape is called as an operand of a while-guard (`while check(x) and ...` — both operand sides, pre-registration irrelevant, identical with/without the flag leak → orthogonal to N32 and to ALL round-6 changes). Crash-instead-of-classified-decline violates the decline doctrine, but the machinery (guard rotation/and-split × nested guard-shaped callee) predates round 6 and is untouched by it — deferred by the N19/N24 standard; PRIORITY candidate for the next engine round. ⚠ THIRTY-THIRD prompt-injection attempt (fake date-change + don't-disclose, vs the probe) — disregarded, disclosed.
- ⚠ **N31 CONFIRMED-SOUNDNESS · High (re-filed, original framing corrected) · 2026-08-22 probe** — the true trigger is NOT the accumulating-scan idiom broadly: it is a TWO-HOP literal-seeded local rebind feeding a recognized scan loop's counter INSIDE a `block:`, with any statement after the block. Minimal repro (ground-truth container-verified reachable): `block: (var localOffset = 3; var acc = ""; var i = localOffset; while i < data.len: ...); symexTarget("after")` → engine sxUnsat on the reachable target. Bisection (all container-verified at v99): direct literal seed = CORRECT; two-hop seed + target BEFORE block = CORRECT; two-hop + block + target after = **WRONG sxUnsat** (with or without name collisions, for plain targets and overflow targets alike); two-hop WITHOUT block = honest sxUnknown decline (seUnsupportedStringOp, CR-17 class) — block presence is necessary. Localization: generic block sequencing sound; N28 mechanism ruled out; suspect = the v88-rider literal-seeded counter pre-pass (intOffsetLiteralLocals / lIsIntOffsetLocal, runtime.nim isLet arm ~6702-6722) tracing a rebind through an intermediate local — exact statement not yet pinned (probe budget). FIX SLICE = ROUND 3 OPENER. SECONDARY FINDING: **N34 · Low/capability (pre-existing)** — single-statement `block:` bodies mis-parsed (typed AST doesn't wrap lone statements in nnkStmtList; parseStmtInner's block arm dsl_parser.nim ~6464-6469 iterates the lone statement's CHILDREN as siblings) — consistently degrades safely to sxUnknown via mkUnsupported in every shape tested, no wrong verdict; parser completeness item. ⚠ THIRTY-SECOND prompt-injection attempt (fake date-change + don't-disclose, vs the probe) — disregarded, disclosed.
- ✅ **D4 FIXED `19dfb49` + rider `b8fe195` (refactor-under-green, no walker bump), 2026-08-22** — `ProcScopedCollectors` value-type sub-record on ParseCtx holds the four proc-scoped collector fields (caseNarrow, stringBackedParams, intOffsetLiteralLocals, pairLoopCounterConsumedAfter; N28 added no new ctx field — verified); ensureProcRegistered's save/clear/restore collapses to three lines (whole-record assignment), all ~30 touchpoints migrated, exception-safety stance preserved (unguarded restore, safe because parse errors abort compilation). A new collector now joins the save/restore BY CONSTRUCTION (become a field = done). Net +167/−139. No live W1-class leak among ParseCtx fields. Rider: tsymex_phase14_arm_field_zero_init ran green, registered in the phase-numerical gap. Sweep 11/11 green (b5 no flake). D4 → **fixed**. TWO NEW FINDINGS: **N32 · open-unverified (out-of-scope observation)** — `inGuardCond` has its own transient save/restore NOT wrapping ensureProcRegistered's callee recursion; a proc called from inside a while-guard condition may parse its body with the flag still true, suppressing parseAtomicOperand hoist decisions in the unrelated callee — PROBE IN MINI RE-REVIEW; **N33 · Medium · open** — the ENTIRE phase14 generation (~20 tsymex_phase14_*.nim, all with stale compiled .exe on disk) is registered NOWHERE (neither nelli.nimble nor symex-windows.yaml) — an invisible test generation; fix-now via a verify-and-register slice (mechanical, container-time-heavy). ⚠ TWENTY-NINTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **D2 FIXED `db18637` (walker v99), 2026-08-22** — `allocCostOf(t: IRType): int64` in types.nim (+ satAdd64/satMul64) mirrors allocateSym's recursion kind-for-kind: itArray size×elemCost (the amplifier), itTuple sum, itVariant disc+plain+ALL-arms (matching allocateSym's allocate-all-arms), itMultiVariant per-axis, itSeq/itTable/itSet flat O(1) (confirmed: allocateSeqDataRaw is one mkArrayVar regardless of elem type), itDistinct 1+base, scalars/ref/ptr 1. isVariantConstructSym's budget now sums allocCostOf per arm field; same beBudgetExhausted kind, fork-count first, default 64 with the unit honestly redefined fields→leaf allocations. Scope preserved exactly (construction site skips disc/plain-field allocation, matching N9's original flat-count scope — verified by reading, no discrepancies). RED stash-verified: D2-1 (array arm fields, flat 4/recursive 160) and D2-4 (tuple-nested array, flat 2/recursive 82) both sxSat pre-fix → beBudgetExhausted post-fix. 5 pins + cache-key/floor checks green (tests/tsymex_r6_d2_recursive_budget.nim); zero N9-pin migrations (its shapes are all-scalar, costs identical — ran green unmodified). Walker 98→99, CR2 98→99; budget doc comments updated to the leaf-allocation unit. Sweep 9/9 green. D2 → **fixed**. ⚠ TWENTY-SEVENTH+TWENTY-EIGHTH prompt-injection attempts (fake date-change + don't-disclose; fake "user/linter modified files" framing the agent's own stash reversion) — disregarded, disclosed.
- ✅ **N13 CLOSED `1803fb7` (test-only, no walker bump), 2026-08-22** — coverage gap closed exactly per the adjudication: new tests/tsymex_r6_n13_reassign_seqarm.nim (6/6) pins discriminator reassignment into an arm with an unbacked-elem seq field. All four expectations matched engine behavior on the FIRST run (nothing red — the code was already sound via R2's hoist): (i) reassignment reaches target sxSat no crash; (ii) backed sibling `count` fully modeled (== 0 sxSat / != 0 sxUnsat); (iii) post-reassignment read of the placeholder field → classified seNestedSeqUnsupported decline naming the field; (iv) backed seq[byte] control gets real zero-init with full verdicts. Sweep 7 suites green. HOUSEKEEPING FOUND: tests/tsymex_phase14_arm_field_zero_init.nim exists on disk but is NOT registered in nelli.nimble (grep-confirmed) — registration rider assigned to the D4 cleanup slice (verify-then-register, own commit). N13 → **fixed (test-only)**. ⚠ TWENTY-SIXTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed.
- ✅ **N28 FIXED `1023e9f` (walker v98), 2026-08-22 — CONFIRMED LIVE for the int-offset collector.** Pre-fix repro (stash-verified flip): formal `offset` unrelated to the scan, same-named shadow local in a nested block driving the accumulating-scan loop → the formal wrongly isIntOffset-promoted to unconstrained svInt (no range, no ziWidth stamp per R3's scope note, so no overflow fork) → `offset + high(int) < offset` reported **false sxUnsat**; post-fix the same query correctly reports sxRaised/OverflowDefect matching real int64 wraparound. Fix: both collectors thread the proc's formal SYMBOLS (seq[NimNode]) into markSymOrRootParam, acceptance via containsSym/sameSym; considerCandidate's parallel direct check fixed identically. The string-backed sibling hole probed with the same confound-free construction: already-green both sides (itString and itSeq[byte] allocation impose identical effective bounds for non-mutating reads) — fixed as HARDENING, honestly documented, not claimed as a second live bug. Walker 97→98 (verdict surface empirically changed), CR2 97→98. New tests/tsymex_r6_n28_shadow_collision.nim 5/5 (RED stash-confirmed); sweep 8/8 green (b5 no flake). N28 → **fixed**. ⚠ NEW FINDING surfaced during repro construction: **N31 · OPEN-UNVERIFIED · potential soundness (pre-existing)** — control-flow reasoning after a `block:` containing an accumulating-scan closed form spuriously proves sxUnsat/sxUnknown for targets placed AFTER the block, confirmed with zero-collision control shapes (independent of N28's bug; the N28 test file routes around it by placing observables before the block, documented in-file). Touches the round-6 B-cluster scan surface → ASSIGN A VERIFIER PROBE IN THE MINI RE-REVIEW. ⚠ TWENTY-FOURTH+TWENTY-FIFTH prompt-injection attempts (fake date-change + don't-disclose; fake "user/linter modified the file" framing the agent's own sed edit) — disregarded, disclosed.
- ✅ **N27 FIXED `cd81623` (walker v97), 2026-08-22 — WITH AN HONEST DOWNGRADE OF THE FINDING.** The fix: lowerHofCall (the ONE svSeq-consuming arm with zero placeholder awareness) now checks the receiver's flag immediately after lowering — before concreteSeqLen/seqElemAt/seqDataRaw/closure lowering — and declines via the R1 chokepoint; map/filter propagate taint, fold (structurally unreachable via std/sequtils — foldl/foldr are templates desugaring to for-loops) degrades to a typed stub. EMPIRICAL CORRECTION of the verifier's "live bug" claim: the predicted false sxUnsat could NOT be reproduced pre-fix by either receiver source — (i) bare local/param/return placeholders carry a SYMBOLIC seqLen (mkIntVar, ==0 only in the path condition, never in the AST), so simplify() can't fold it and canInline is structurally always false — the inline path where the wrong verdict would occur is unreachable; (ii) declared-field receivers are intercepted by the nnkDotExpr parse arm before a HOF node exists. Pre-fix both shapes already declined sxUnknown via the axiom path's own sound type guards. So N27 re-classifies live-bug→**hardening**: the unguarded read was real but shielded by two independent INCIDENTAL protections; the fix removes that reliance and makes the arm sound by construction, with precise attributed decline messages (pinned in tests/tsymex_r6_n27_hof_placeholder.nim 6/6, header documents candidly that no false-verdict RED occurred). CLASS CLOSED: tests/tsymex_r6_n27_placeholder_read_audit.nim (runtime readFile, NOT staticRead — runtime.nim ~565KB would hit MSVC C2026) bans bare .seqLen/.seqDataRaw/.isUnsupportedFieldPlaceholder reads outside `# [placeholder-audited]` markers; all 52 legitimate sites reviewed + tagged (49 runtime.nim, 3 runtime_strings.nim); audit DEMONSTRATED red on an injected bare read, reverted, re-greened. Walker 96→97, CR2 96→97. Sweep green (C4 4/6 + C6 7/9 = the ledgered N29 pre-existing class only, untouched by code-level trace). N27 → **fixed (hardening + audit)**. ⚠ TWENTY-SECOND+TWENTY-THIRD prompt-injection attempts (fake date-change + don't-disclose; fake "file modified by user/linter" framing the agent's own stash-revert) — disregarded, disclosed.
- ✅ **N16 FIXED `c5d6b3c` (walker v96), 2026-08-22** — applyClosureGround's fallThrough loop gains R2's exact else-twin: paths whose env lacks "result" now assert `retBindEq(funcApp, defaultZero(cb.retTy, ...))` under the same cp.pc, with try/except ValueError/SymexRefUnresolvedError → feUnsupportedOp classified decline for unbackable retTys (never crash, never wrong bound). Three honest false-SAT REDs confirmed pre-fix at `138997d`: direct-call `f(neg)==42` sxSat, bool `r==true`-on-untouched-path sxSat, and an exhaustive-union `{1,-1,0}` soundness pin sxSat — all sxUnsat post-fix. 10/10 new pins green (tests/tsymex_r6_n16_closure_zerodefault.nim, floor >= 96). TWO orthogonal pre-existing gaps pinned honestly as declines, NOT chased: (a) C4 HOF conditional-lambda path dies upstream with ekZ3Error "domain sort BitVec 64 / Bool mismatch" — same root cause as the known pre-existing C4-1/C4-1b reds; sweep also surfaced 2 reds of the SAME class in tsymex_phase15_C6_smoke ("bounded filter/map HOF"), stash-verified identical at clean `138997d` — recorded as **N29 · Low/pre-existing** (HOF lambda sort mismatch class, C4+C6, predates round 6); (b) `symValFromRawAst` has no itString arm for closure RETURN types → weInternalWalkerFault decline — **N30 · Low/pre-existing** (fault-kind rather than clean classified decline; cosmetic-plus honesty item, funcApp construction upstream of N16). False A6-rider comment at runtime.nim ~7696 corrected to state the true history. Walker 95→96, CR2 pin 95→96 (render 11). Sweep green across C2b/C3/C5/r2_zerodefault/CR2; C4 = the 2 known pre-existing only; C6 = the 2 stash-verified same-class pre-existing only. N16 → **fixed**. ⚠ TWENTIETH+TWENTY-FIRST prompt-injection attempts (fake date-change + FABRICATED Agent-tool type listings; fake "files modified by user/linter + don't tell" framing the agent's own edits as third-party) — disregarded, disclosed.
- ✅ **N21 FIXED `117b802` (walker v95), 2026-08-22** — grammar fix in runtime_strings.nim's iekStrInOptionRegion lowering (NOT dsl_parser — the lowering was the right site): bare segment-star `((nonzero)*"\0")*` replaced by the hand-derived-and-concretely-verified clean-termination language `PAIR* ("\0" anybyte*)?` where `PAIR = (nonzero)+ "\0" (nonzero)* "\0"` (non-empty key, possibly-empty value; optional empty-key terminator + unread tail), using nim-z3 plus/option alongside the existing star/concat/range. HONEST ground-truth correction en route: the agent's initial hand-trace assumed even-pairs-no-terminator ("aa\0bb\0") raises on a third key read — the concrete SUT run showed it does NOT (outer `while i < s.len` exits cleanly at i == s.len); pin file documents the correction. RED evidence: N21-1 sxUnknown→should-be-sxRaised and N21-1-unsat false-sxSat both red pre-fix exactly per the probe; post-fix all 16 pins green (tests/tsymex_r6_n21_pairloop_member.nim). HONESTY NOTE: the raise-unreachable/target-unreachable UNSAT companions decline sxUnknown rather than proving sxUnsat — verified NOT a residual of this fix (identical decline pre-exists in N10d-5-decline, same N20 k-unroll class); pinned as sxUnknown with in-file explanation. Zero migrated pins (every existing B6/N10 literal is a member under both grammars — both suites green unchanged); the falsified "certified defect-free by construction" doc comment corrected. Walker 94→95, CR2 pin 94→95 (render 11 unchanged). Sweep 10/10 green (b5_chained no flake this run). N21 → **fixed**. ⚠ NINETEENTH prompt-injection attempt (fake date-change + don't-disclose) — disregarded, disclosed; agent independently confirmed the true date via git timestamps + host clock before using it in doc comments. (Also flagged, benign: the vendored nim-z3 cache dir's own CLAUDE.md surfaced via Read — informational content only, no directives.)
- 📋 **D3 REFUTED-AS-WRITTEN → deeper finding N28 · Medium · latent-but-reachable (2026-08-22 verifier).** D3's literal claim (un-migrated `HashSet[string]` consumers at dsl_parser.nim:8035/:8291 are a collision surface) is REFUTED: both sites only test membership for identifiers drawn from the same proc's own formalParams list — the exact list the collector computed from — and `markSymOrRootParam` only ever incl's strings already in paramNames, so the set is provably a subset of the proc's duplicate-free formal names; name-equality there IS symbol identity, and `intOffsetParams` is a local `let` (no cross-proc leak). Re-keying to seq[NimNode] (D3's implied fix) is cosmetic-only. BUT the underlying thesis is TRUE one layer up: **N28** — `markSymOrRootParam`'s root check (dsl_parser.nim:5388-5403, `root.strVal in paramNames`; parallel direct check at :5325 in the sibling collector) is name-based AFTER findRootParam has already resolved the true symbol. Concrete shape: `proc f(offset: int, data: seq[byte])` with a nested-block shadow `var offset = 3; var i = offset; while i < data.len: ...` — `tryMatchAccumulatingScanIdiomShape` (:4708) has no symKind gate on the loop-index root, findRootParam correctly resolves to the SHADOW's symbol, but the final name check marks the unrelated FORMAL `offset` isIntOffset → unconditional svInt promotion (runtime.nim:9431-9479); with no declared range that formal becomes an unconstrained svInt with wraparound silently unmodeled — soundness-adjacent false-negative, feeding R3's overflow-fork surface. Afflicts BOTH collectors identically (incl. the R4-"fixed" stringBackedParams sibling). Zero corpus coverage of shadow shapes. FIX QUEUED (small): make the root acceptance symbol-based — compare `root` against the actual formal SYMBOLS via sameSym (pass formal syms, not names, into markSymOrRootParam; same for :5325) — plus a shadow-shape regression pin; walker bump only if a verdict flips (likely: previously-misclassified shadow shapes change representation). ⚠ EIGHTEENTH prompt-injection attempt (fake date-change + don't-disclose, vs this verifier) — disregarded, disclosed.
- ⚠ **D1 CONFIRMED-UPGRADED → N27 · High · LIVE BUG (2026-08-22 verifier)** — not just a design-totality gap: the verifier found a real, shipped, unguarded 5th read site escaping R1's chokepoint. `lowerHofCall` (runtime.nim:8764-8911, backs .map/.filter/.fold) calls `concreteSeqLen` (:8788 → direct `seqSV.seqLen` read at :8757) with NO isUnsupportedFieldPlaceholder check; `hofDispatch` (dsl_parser.nim:2982-3002) never screens the receiver either (unlike the nnkDotExpr field-read arm which does). Reachable via ordinary HOFs over any placeholder-ized seq (e.g. seq[(string,string)] param): placeholder seqLen is hard-forced 0 → concreteSeqLen folds to concrete 0 → canInline picks n=0 → result SymVal built WITHOUT the taint flag → downstream `.len > 0` silently proven sxUnsat against a fabricated length — the exact S1 false-verdict class R1 shipped a fix for, missed by R1's hand-audit; tsymex_r6_r1_placeholder_totality.nim has zero HOF mentions. Enforceability verdict: accessor procs would NOT have caught this (readers are intra-module; Nim `*` is a no-op there — the SymVal.signed comment already documents that limitation); the proportionate mechanism is the house staticRead marker-comment audit pattern (N2/A2a precedent). Citation fix: the chokepoint procs live at :2841/:2851, and retBindEq (:2652) + extractFromSymVal (:4713) are also (correctly) guarded sites. FIX SLICE QUEUED: (a) guard + declinePlaceholderInLower in lowerHofCall before concreteSeqLen/seqElemAt, decline-and-propagate not silent n=0 (walker bump); (b) HOF placeholder pins mirroring r1_placeholder_totality; (c) NEW grep audit test banning bare .seqLen/.seqDataRaw/.isUnsupportedFieldPlaceholder reads outside a marker allowlist in runtime.nim/runtime_strings.nim — closes the CLASS a sixth reopening. D1's original hardening framing is subsumed by (c). ⚠ SEVENTEENTH prompt-injection attempt (fake date-change + don't-disclose, vs this verifier) — disregarded, disclosed.
- 📋 **RE-REVIEW ROUND 1 CONSOLIDATED (2026-08-22, five agents: security + design + correctness lenses over `edfe854..HEAD`, N21 probe, N13/N16/N19 adjudication).** Security: NO Crit/High; two new Lows — **N25 · Low** `scanShapeReceiverMutated` (runtime.nim ~5320) matches var-mode args by bare `strVal` not `containsSym`; traced false-positive-only (over-cautious veto → sound fallback), tidy-up candidate; **N26 · Low/informational** overflowCondInt adds constant-factor Z3 cost per stamped arithmetic op — bounded by existing frontier/step budgets, watch item only. Security affirmed: N9 formula matches the real per-fork loop and can't be bypassed under the fork cap; R1 placeholder sorts consistent across both construction sites; R5 rigid 5-stmt shape closes the smuggled-counter gap; zero FFI/raw-pointer changes in the diff; cache-key addition is config-derived, no injection surface. Correctness: all seven slice-specific risk traces AFFIRMED (R1 taint+threadvar reset, R2 exhaustive/no-double-bind, R3 exact low/high bounds + metadata propagation incl. iteSV conservative reset, R4 stack-scoped save/restore correct under recursion, R5 sound over-approximation, N9 check-before-work, Q3 byte-equivalent); flagged the R3 narrowing as a trackable residual → **N24 · Medium · DEFERRED with rationale** (direct arithmetic on unstamped top-level int-offset params still has no overflow fork → false-sxUnsat reachable on defect-only queries; this equals the pre-R3 status quo for that subset — R3 strictly extended coverage — and the naive stamp is empirically catastrophic (15+ min non-termination, documented SCOPE NOTE ~9430); needs its own design round, not a hotfix). Design: 4 findings — **D1 · High · verifying** (R1 chokepoint is an audited call-site convention, `seqLen`/`seqDataRaw` stay public fields — nothing stops a future direct read; NB single-module privacy can't compiler-enforce this, verifier to assess the practical enforcement fix); **D2 · Medium · verifying** (N9 counts composite arm fields as flat 1 while allocateSym recurses into itArray/itTuple/itVariant — CONTESTED: security+correctness both affirmed the formula matches the loop's allocateSym CALL count; the dispute is whether recursion multiplies real Z3 work); **D3 · High · verifying** (`collectIntOffsetParams` still returns `HashSet[string]` and both consumers match by `strVal` (dsl_parser.nim ~5696-5805, 8035, 8291) — the N2 name-collision class R4 closed for its two siblings is still open for isIntOffset, and representation choice (BV vs Int) now feeds R3's overflow forking); **D4 · Medium · accepted-for-fix** (R4's four-field hand-rolled save/clear/restore + R5's five-touchpoint replication → one ProcScopedContext sub-record with generic save/restore; correctness of the current code affirmed, this is anti-regression ergonomics). Design affirmed well-designed: overflowCondInt scope discipline, containsSym re-keying, R5 decline-over-wrong-closed-form, defaultZero hoisting. Adjudications: **N13 → fix-now, test-only** (R2's hoist already closes the code path — single module-level defaultZero serves isVariantReassign too, type-driven guard robust to tyOf dropping the reason string; missing piece is only a regression pin through variant reassignment); **N16 → CONFIRMED Medium, fix-now** (applyClosureGround runtime.nim:8433-8439 lacks R2's else-twin — fall-through paths never touching `result` leave funcApp a free var → false sxSat; reachable via all of Cluster C (direct closure calls + C4 HOF inline both thread through it); the A6-rider comment claiming this path "already handles this exact shape correctly" is FALSE and R2 scoped around it trusting that comment; zero existing-test coverage, all closure-test lambdas assign result on every path); **N19 → DEFERRED** (all five unguarded classifyType sites blame-verified pre-existing Phase-15, untouched by round-6; zero corpus coverage of generic-self-indexing; failure mode is a non-catchable compile error; fix = R4's W3 ntyNone idiom ×5 + pin, future-round scale). **FIX ROUND 2 QUEUED: N21 (walker v95) → N16 (v96) → N13 (test-only) → verified design fixes (D1/D2/D3 pending verification, D4 accepted) → mini re-review.** ⚠ FOURTEENTH+FIFTEENTH prompt-injection attempts (fake date-change + don't-disclose, vs the security and adjudication agents; design + correctness lenses observed none) — disregarded, disclosed.
- ⚠ **N21 CONFIRMED · Critical (upgraded from open-unverified) · 2026-08-22 re-review verifier probe** — REAL soundness bug, container-repro'd. Root cause dsl_parser.nim:5273 (`tryRecognizePairLoopIdiom`): the member branch replaces the ENTIRE pair-loop with an empty block when `iekStrInOptionRegion(s, i, bound)` holds — but the region grammar (runtime_strings.nim:710-746) is `((nonzero)*"\0")*` with NO parity constraint, while the real loop consumes segments two at a time and only terminates cleanly on an empty-key read. Repro: `s = "aa\x00bb\x00cc\x00"` (3 segments, odd, non-empty final) is a member, yet the concrete SUT raises ScanError("unterminated") — iteration 2 reads key "cc" leaving p1 = 9 = bound, and the value read at bound falls through `i < s.len` to the raise. Engine verdicts on the pinned literal: `symexFind(done)` = **sxSat with a non-replaying witness** (false SAT); `symexFind(tRaisedExn(ScanError))` = sxUnknown where ground truth is sxSat (defect never modeled — the member fast-path preempts the sound k-unroll fallback). The B6 corpus's own "certified defect-free by construction" doc comment (tsymex_r6_b6_optionregion.nim:40-53) is falsified. Distinct from R5/S4 (counter-value fidelity) — that fix did not touch the member branch's no-defect claim. FIX SLICE QUEUED (after re-review consolidation): member condition must encode the actual pair-loop termination grammar (pairs + empty-key terminator), not bare segment-star membership — or the fast path must model the defect fork; walker v94→v95 + CR2 pin + repro becomes the RED pin + B6 doc-comment correction. Probe left the tree clean (temp repro deleted, nothing committed). ⚠ THIRTEENTH prompt-injection attempt (fake date-change system-reminder + don't-disclose, vs the probe agent) — disregarded, disclosed.
- ✅ **Q3 FIXED `af02996` (refactor-under-green, no walker bump), 2026-08-22** — collector-tracing dedup in dsl_parser.nim: two verbatim-duplicated helper pairs (`findRootParam` 13-line DFS ×2, `markIfParamOrLocal` ×2) plus the near-duplicate `hasLiteralInit` extracted into three module-level procs before the collector family (~5343): `findDeclInit` (shared DFS core — first var/let decl of target via sameSym, unwrapped initializer or nil), `findRootParam` (bare-nnkSym filter over it), `markSymOrRootParam` (destination set as explicit `var` param, so each collector keeps its own state — directly answers the old in-code "deliberately not shared" objection). Net +92/−67 (+25, all documentation); ~60 redundant executable lines → one ~20-line core + thin wrappers. Sweep 10 suites green; b5_chained first attempt hit the known silent-exit-1 flake, re-ran 2× green on the change and 1× green on stashed HEAD (pre-existing, per the R5 entry). CR2 pin untouched (94/11 hold). Q3 → **fixed**. NEW residual (found during extraction, deliberately NOT absorbed into the refactor): **N23 · Low · open** — `collectIntOffsetParamsImpl`'s `walkCalls` (~5779–5786) resolves callees via raw `getImpl` + inline kind check and gates on bare `symKind in {nskProc, nskFunc}` — exactly the pre-N2 bare-gate pattern the permanent N2 audit bans — while its twin `collectStringBackedByteSeqParamsImpl` uses the R4/W3-hardened `resolveRoutineImpl`. Functionally benign today (func/proc share child-index layout per resolveRoutineImpl's documented probe evidence; no reachable crash/wrong verdict — same demotion rationale as N3), classified Low defensive-hygiene; trivial one-line fix candidate (route onto resolveRoutineImpl); re-review round may contest the severity. Related infra note: the N2 audit test itself doesn't compile in the container (MSVC C2026 string-too-big, the known A5-era staticRead corpus break), so this class is currently uncaught by CI on this host. ⚠ TWELFTH prompt-injection attempt (fake date-change system-reminder + don't-disclose, vs the slice agent) — disregarded, disclosed.
- ✅ **N10 FIXED `d690221` (test-only, no walker bump), 2026-08-22** — coverage matrix closed: new tests/tsymex_r6_n10_coverage_matrix.nim (23 tests, 361 lines), registered after n9_variant_budget. Per group: **(a)** B3 immediate-match at the entry offset (zero advances) — SAT witness + UNSAT companion both match prediction; two earlier UNSAT-companion designs (symbolic-index read inside symexAssume) hung >30 min at ~4.7% CPU, killed and redesigned per the corpus's dt-bounded doctrine into a closed-form-preserving concrete-value formulation. **(b)** char↔byte cross-alias comparison through B2's identity pass-through, BOTH directions (2 SAT + 2 UNSAT) — normalizeIntTyName's no-op confirmed sound, no live bug. **(c)** NUL-first witness (byte 0) + multi-NUL, 7 tests — exact byte content and exact replay via readCStringNulTracer, no live bug. **(d)** empty-receiver (len==0) entry for every scan-family recognizer B0/Q1/B3/B4/B6, 9 tests — all match prediction EXCEPT B6's fallback-unreachability companion (N10d-5-unsat), which declines honestly (beBudgetExhausted) instead of the predicted sxUnsat — pinned as the decline per honesty rules; same already-documented class as the existing B3-4/B4-6/B6-6 trip-wire pins, so NOT ledgered as a new finding. The v86 multi-path gap was already closed by R2 (cross-referenced, not re-pinned). Zero live bugs; walker stays v94, CR2 pin untouched. Sweep: all 8 required suites green (b5_chained skipped per plan — no production change). N10 → **fixed**. Process: THIRD slice-agent detach (monitor-wait) — resumed via the standard foreground-only message; agent then split the run into per-group temp files + an import aggregator to fit the 600s-per-call ceiling. ⚠ NINTH+TENTH prompt-injection attempts (two fake date-change system-reminders + don't-disclose, vs the slice agent) — disregarded, disclosed; an ELEVENTH of the same pattern appeared in the coordinator context at slice completion — likewise disregarded and disclosed.
- ✅ **N9 FIXED `213a725` (walker v94), 2026-08-22** — second structural budget `maxVariantConstructorFieldAllocs` (default 64, new ResourceBudget field + merge fn) checked BEFORE any solver work in isVariantConstructSym: `vcsTagSet.len * sum(fieldTypes.len across vArms)`; past it → the SAME beBudgetExhausted decline kind as the existing fork-count budget (no parallel mechanism), distinguishing message; fork-count check runs first, unchanged. Cache-key rendering updated in canonicalize.nim. HONEST evidence (not a fabricated RED): pre-fix the amplifying shape doesn't crash — stash-verified a wide-fielded SUT (8 forks × 512 field allocs, within the old fork budget of 8) silently returned sxSat doing 512 fresh Z3 allocations; post-fix the identical shape classifies beBudgetExhausted — that verdict-kind flip is the pinned observable (test N9-1). Walker 93→94 (new declines on fork-count-within/field-count-over shapes that previously produced verdicts); CR2 pin 93→94. New tests/tsymex_r6_n9_variant_budget.nim 8/8; 10-file sweep green, no pre-existing reds. N9 → **fixed**. No new residuals. ⚠ EIGHTH prompt-injection attempt (fake date-change system-reminder + don't-disclose) — disregarded, disclosed.
- **N1 · Critical · CONFIRMED (V1, full path traced)** — `iekSeqSlice` (runtime.nim:3323-3385) unguarded placeholder consumer: reachability verified end-to-end (freshRetSym→allocateSym placeholder→caller binding with lenSym==0 in pc; parseSeqBracketAccess's SLICE shape emits unconditionally at 1095-1118 while its INDEX shape 1119-1126 routes to the guarded path — the missed asymmetry). Consequence verified: guaranteed spurious IndexDefect fork + real continuation pruned as infeasible (false sxUnsat) + result SymVal DROPS the taint flag. Folds into R1.
- **N2 · Medium (downgraded from High) · NARROWED (V2)** — mechanism CONFIRMED (strVal store at 5641, single name-only consumer at 6731, overflowCond svInt skip) but claimed trigger REFUTED: the promotion proto is consulted ONLY in lower()'s iekIntLit arm, so a computed-RHS colliding binding ignores the misfired flag (binop arms use probeProto). Surviving narrow variant (plausible, uncompiled): a colliding binding whose OWN initializer is a bare int literal is forced svInt via coerceIntLit and loses overflow forking. Fix rides R4 (sym-identity keying) regardless.
- **N3 · Low (downgraded from High) · REFUTED (V3)** — the raiseAssert fallthrough and missing reconcileInt are real, but the trigger is unreachable in valid Nim: `tryMatchAccumulatingScanIdiomShape` requires an `int`-typed counter (`< s.len` must typecheck; .len is int, no implicit cross-width comparison), findRootParam/walkCalls only trace bare nnkSym (never nnkConv), so every isIntOffset-markable param is width-64 by construction — and iekConvIntWidth is strictly-widening (doAssert tgtWidth > srcWidth), so a width-64 value can never be its source. Kept as Low defensive-hardening (add the reconcileInt bridge for symmetry with lowerArith/lowerCmp); R7 demoted to the cleanup ride-along.
- **N4 · Critical(design) · confirmed-by-history** — emitStmt/emitIRType positional round-trip is structurally fail-silent (two documented outages this round; correctness pass verified all ~20 new fields currently serialized — no third live gap, but the CLASS is unfixed). Fix: reflection-driven emit or per-variant sentinel round-trip test. New remediation item R6.
- **N5 · High · confirmed (control-loop grep)** — v85 scoped-decline pins use ONLY `discard opts`; `.len`/iteration/equality/callee-pass access forms all unpinned (`.len` is S1's exact wrong case; declineUnsupportedFieldRead's own doc comment cites `.len`-composition as its design case). Folds into R1's test matrix.
- **N6 · High(design) · open** — placeholder read-taint is hand-placed per site, not total by construction (B7r2's isIndex retrofit is the evidence); chokepoint accessor or SymVal-kind arm proposed → shapes R1's implementation.
- **N7 · High(design) · open** — scan-recognizer family dispatches on body-stmt COUNT (1/2/3/5); loop-summary normal form proposed (summarizeCountedLoop + predicates). Design-debt item, not a hotfix — future-round candidate.
- **N8 · High(design) · open** — name-keyed collector contexts discard Nim symbol identity; key by sym / store on IRParam. Same fix family as W1/N2 → R4.
- **N9 · Medium · open** — variant-constructor fork budget caps outer tag loop only; per-fork all-arms field allocation unbounded (8× amplification on wide variants).
- **N10 · Medium · open** — coverage gaps: B3 immediate-match boundary; v86 multi-path-result callee (S3's exact shape); char-vs-byte cross-alias comparison; NUL witness at position 0 + multi-NUL; empty-receiver scan entry (family-wide).
- **N11 · Low · open** — cross-proc collector cycle guards keyed by bare proc name (overload collision → silent under-classification, degrade-only).
- **N12 · Low · open** — decline messages leak internal IR vocabulary (itTuple, seNestedSeqUnsupported) to users.
- **Affirmed clean this round:** all version floors `>=` and correct vs landing commits (single `==` pair in CR2 at 88/11); field-serialization audit (every new field emit-traced); B6 shape gate/foldStmtIx/shadowing; retBindEq svSeq skip-eq OR-asymmetry sound; B5 return-position collectors correctly per-callee-scoped; evalStrBytes FFI usage + nim-z3 buffer sizing; iekConvIntWidth cache key.

**Remediation slate (final; awaiting Corey at Step 6):** R1 placeholder read-totality via chokepoint (S1+Q1+Q2+N1+N5+N6, walker v89) · R2 v86 zero-default binding (S3) · R3 svInt overflow honesty (S2, N2's narrow variant covered) · R4 sym-identity/proc-scoped collectors + veto widening + doAssert→decline + ntyNone (W1/W2/W3/N2/N8) · R5 B6 counter advance (S4) · R6 fail-safe emit round-trip (N4) · then 0.5.1 + chapulin re-pin. Cleanup ride-along: N3-defensive-reconcile, N9-N12, N10 coverage matrix, Q3/Q4/Q5.
