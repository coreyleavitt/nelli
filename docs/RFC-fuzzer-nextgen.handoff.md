# RFC — next-generation structure-aware hybrid fuzzer — handoff

## FOLLOW-ON — MSVC parity: **DONE** (post-RFC, Corey-requested 2026-08-26)

**PROVEN:** `fuzzer-msvc` run 33030096952 (commit `843e04f`) SUCCESS — `cl.exe 19.44.35225 x64` compiled and ran **all 63 discovered `tfuzz*`/`tdb*` suites** ("All 63 Windows MSVC fuzzer suites passed"), inside `ghcr.io/coreyleavitt/nim:latest`'s Windows entry on a `windows-2025` runner. `fuzzer-windows` (mingw) and `symex-windows` green on the same commit. nelli now has three independent Windows legs: mingw-Nim-stock, MSVC-in-container (also the only Nim-patch-parity leg), and symex.

Detail of how it got there below — keep it: three of the four defects it found were invisible to every GCC-based check, and the local-proof method is reusable.

### Follow-up: mingw leg moved into the patched image too — **DONE** (`1ba0830`)

**PROVEN:** `fuzzer-windows` run 33037049533 SUCCESS — `ghcr.io/coreyleavitt/nim:2.2.10-mingw` (windows/amd64, OsVersion 10.0.26100.32522) with `gcc.exe` at `C:\mingw64\bin\gcc.exe`, **all 63 suites passed** ("All 63 Windows mingw fuzzer suites passed"). Critically, `[OK] at least one coverage backend is available` ASSERTED (did not skip) and `[OK] build & run an instrumented C target — cbGcc` really built — i.e. leaving `NELLI_NO_COV_BACKEND` unset here did preserve external-C scaffold verification, which is the whole point of the flag being a per-environment declaration. `fuzzer-msvc` and `symex-windows` green on the same commit.


Corey pointed out a second patched image: **`ghcr.io/coreyleavitt/nim:2.2.10-mingw`** — same ltsc2025 base and same Nim backport series, but pinned **mingw-w64 gcc (winlibs)** instead of MSVC. A SEPARATE tag; it is NOT in `:latest`'s manifest list (`:latest` resolves to the MSVC entry on a Windows host). `fuzzer-windows.yaml` was rewritten from native `setup-nim-action` on `windows-latest` to `docker run` that image on `windows-2025`, mirroring `fuzzer-msvc.yaml`'s proven shape (Z3 bind-mount, provenance-SHA deps clone, docker-daemon readiness wait, `.ps1` runner, glob discovery).

Why it matters beyond tidiness: that leg had never been Nim-patch-parity with the Linux dev image, and its mingw was whatever setup-nim-action bundled. Now every Windows toolchain nelli claims to support ships as a pinned, patched image.

Two deliberate decisions, both load-bearing — do NOT "clean them up":
- **`NELLI_NO_COV_BACKEND` is absent here on purpose.** This image HAS gcc, so `tfuzzcbuild.nim`'s coverage-backend canary must keep ASSERTING and the external-C scaffold must keep really building. Setting it would silently delete coverage verification on the only Windows leg that has a C backend to prove it with.
- **`symex-windows.yaml` deliberately stays on stock setup-nim-action Nim**, so the unpatched-Nim / stock-mingw configuration (what a downstream consumer actually gets) keeps being exercised somewhere. If all three legs converge on the patched images, that coverage vanishes. Recorded in `fuzzer-windows.yaml`'s header too.

**NEXT on resume:** read `fuzzer-windows` run 33037049533 (commit `1ba0830`) — plus `fuzzer-msvc`/`symex-windows` on the same commit, which must stay green. Likely first-failure candidates, in order: gcc not on `PATH` inside that image the way assumed (the diagnostic block surfaces it immediately), or a `windows-2025` docker-daemon flake (the readiness wait exists to absorb it). Suite-logic failures are unlikely — the MSVC leg already proved this exact suite passes in-container.



Not an RFC slice; a parity task Corey asked for after stage 3 closed ("it's important to me everything works with msvc"). **Premise correction:** Eci concluded no Windows container was reachable — that was tested only as "can this Linux host run one" (it cannot) and never as "does one exist." `ghcr.io/coreyleavitt/nim:latest` (this repo's own Containerfile base) has a `windows/amd64` manifest entry: Server Core ltsc2025 **with MSVC**, Nim 2.2.10 carrying the same backport series as the Linux entry. See the CORRECTION block in `docs/RFC-fuzzer-nextgen.windows-capability.md`.

- **New leg:** `.github/workflows/fuzzer-msvc.yaml` — `windows-2025` runner, explicit `docker run` of that image (GitHub does not support `jobs.<id>.container` on Windows runners), `--cc:vcc`, same glob-discovered `tfuzz*`/`tdb*` suite, Z3 4.13.4 bind-mounted in. Also closes a gap nobody had noticed: the existing `fuzzer-windows` leg uses setup-nim-action's stock Nim, so it was never Nim-patch-parity with the Linux dev image; this leg is.
- **Real MSVC defects found and fixed** (`cf8fde8`, `dadacb1`): (1) `nelli_shm.c` used `__atomic_load_n`/`__atomic_store_n` at four generation-counter sites — GCC/Clang builtins with no MSVC equivalent, and that file is `{.compile.}`d INTO the Nim binary, so `--cc:vcc` could not build nelli at all; now routed through `pt_gen_load`/`pt_gen_store` (identical builtins on GCC/Clang, `_Interlocked*` on MSVC). (2) `nelli_cov.c`'s `__attribute__((constructor))` gained a `.CRT$XCU` MSVC arm. (3) **MSVC caps a string literal at 16380 bytes**; nine test sites `staticRead` `nelli_cov.c` (26100 B) and `nelli_shm.c` (29451 B), which Nim emits as ONE literal → `error C2026`. Fixed with `embedCSource` in tests/fuzzsupport.nim (macro-time slice into 8000-byte chunks, `&`-chain of small literals, build-time self-assert) + `tests/tfuzzembedguard.nim` proving byte-for-byte reproduction. None of these were reachable by any GCC/mingw check — mingw IS GCC.
- **CI progress:** round 1 died on the leg's own PowerShell diagnostic (`cl.exe` writes its banner to stderr; under `$ErrorActionPreference='Stop'` a native command's `2>&1` becomes a TERMINATING error) — fixed `51e3e7b`. Round 2: **52 of 57 suites compiled and passed under cl.exe 19.44**, 5 failed, all C2026. Round 3: environmental — the `windows-2025` Docker daemon was not running (`npipe` missing) though rounds 1-2 connected fine; leg now starts the service and waits for the pipe (`d32de5e`). Round 4: the C2026 fix DID NOT HOLD and the failure list GREW to 9 (incl. the new guard test) — **Nim constant-folds a `&`-chain of string literals back into one literal during semantic analysis**; `let` does not prevent it, foldability is a property of the all-literal `&` expression. Round 5 (`3191b04`, IN FLIGHT at session close): chunks are now a `const` ARRAY joined by a runtime loop (array construction is not a fold candidate), chunk size 8000→4000.
- **Locally provable now (do not go back to guessing):** GCC's `-Woverlength-strings` enforces ISO C99's 4095 minimum — STRICTER than MSVC's 16380 — so a green direct-gcc compile proves MSVC-safety without cl.exe. **Trap:** passing `--passC:-Werror=overlength-strings` to `nim c` is a FALSE NEGATIVE, because this project's gcc profile appends `-w` and it wins. The working method is `nim c --compileOnly` then compile the emitted `.c` files directly with `gcc -c -std=c99 -Woverlength-strings -Werror=overlength-strings`; exact commands are in `tests/tfuzzembedguard.nim`'s header. Verified with a negative control: the check correctly FAILS against the old `&`-chain macro (`dadacb1`) and passes 9/9 against the fix. Longest emitted literal is now 4366 chars.
- **Round 6 (`420908d`) — the fold fix WORKED: 56 of 57 suites compiled AND passed under cl.exe 19.44.** No MSVC-vs-mingw runtime divergence appeared (the 1 MB vs 2 MB stack difference `smt/runtime.nim` notes did not bite). Single remaining failure was an ENVIRONMENT premise, not a defect: `tfuzzcbuild.nim`'s "at least one coverage backend is available" canary — the MSVC image ships cl.exe and NO gcc/clang, so the external-C scaffold has no backend there (its other tests already skip individually on `available(...)`).
- **Round 7 (`843e04f`, IN FLIGHT at session close):** the canary now skips only when the environment DECLARES it has no backend, via `NELLI_NO_COV_BACKEND=1`, which `fuzzer-msvc.yaml`'s `docker run` sets. Linux and the mingw leg keep the loud failure, so a Linux image that lost gcc still trips it — verified locally (`dt-bounded.sh c tests/tfuzzcbuild.nim` still `[OK]`s the canary and both backend builds). Deliberately NOT weakened to an unconditional skip.
- **NEXT on resume:** read `fuzzer-msvc` run 33030096952 (commit `843e04f`). Expected: green (57/57). If green, MSVC parity is DONE — record it here and in the `fuzzer-nextgen-impl-status` memory. `fuzzer-windows` + `symex-windows` are already green on `843e04f`.
- **Not attempted (MSVC scope, stated not silent):** nothing in `src/` embeds these C sources, so the C2026 fix is test-layer only; the MSVC leg does not run the external-C scaffold at all (no gcc/clang in that image), so external-tier behavior under MSVC is proven only by cross-compile + the mingw leg, never executed under cl.exe.

## STATUS (2026-08-26, end of session) — STAGE 3 COMPLETE, 30/30 SLICES DONE

- **Stage:** 3 (implementation) **FINISHED**. Stage 4 (review ledger) **NOT STARTED** — that is the next stage.
- **Slices:** 30 done / 0 remaining. Track E (incl. all four Windows slices Eci/E4a/E4b/E4c), Track G, Track S, Track U all complete. Per-slice SHAs + narrative below.
- **Branch:** `rfc-fuzzer-nextgen`, pushed to origin, HEAD `02427a9`. Not merged to `main` — merge is Corey's call.
- **Verification at close:** final full Linux sweep 65/65 green (`tfuzz*` + `tdb*` + CR2 pin + tengine_crashisolation + tlearnedstate, `c` backend via `scripts/dt-bounded.sh`, cpp spot-checks throughout); both Windows CI legs green on `2fe8ce1` (fuzzer-windows run 33021836310, symex-windows run 33021836295). A docs-only re-run (33024560787/33024560783) was still in flight at session close — **if it is anything but green, that is a CI-infra flake to investigate, not a code regression: the identical test set passed on the immediately-preceding code commit.**
- **Open forks:** none.
- **Resume command:** `/rfc-flow` (it will route to stage 4 — the review ledger); or to re-verify from scratch: `scripts/dt-bounded.sh c tests/<file>.nim 300` locally and `git push origin rfc-fuzzer-nextgen` + `gh run watch <id> -R coreyleavitt/nelli --exit-status` for the Windows legs.
- **Windows grind shape (still current for any future Windows work):** read `docs/RFC-fuzzer-nextgen.windows-capability.md` first — no local Windows RUN exists; `scripts/dt-crosswin.sh <c|cpp> <file>` is the local build check, CI push-and-wait is the run channel (authorized on this branch).
- **Deliberate future work (recorded, not deferred-and-forgotten):** chain upgrade to softlink 0.12.x (carries a Windows `LoadLibraryEx` security fix worth taking before Windows fuzzing GA); external one-shot tier's Windows pipe-buffer caveat and `newExternalWorker` persistent-mode (the RFC's own stated E5 asymmetry); `KILL_ON_JOB_CLOSE` cross-process-death proof is structural (needs a second CreateProcess'd test binary, no fork on Windows).

**E4c — DONE, Windows-run-PROVEN (slice 30/30, the FINAL slice; C1 `06c8d29` Job Objects, C2 `1022640` external-tier un-gate, C3 `0e77fb8` limit parity + external-test Windows arms, C3r2 `b17b365` drain loop + debug channel, C3r3 `2fe8ce1` precedence flip + shm pre-attach). ALL 30 RFC SLICES IMPLEMENTED.** DoD proof: fuzzer-windows run 33021836310 SUCCESS — `[OK] a worker that over-allocates past its Job Object memory limit is vResourceExceeded, not a hang or an ordinary crash` + G4C3 `[OK] an instrumented external binary logs its comparison operands, read back by the orchestrator over shm` + `[OK] a second fresh-exec'd run ... publishes its OWN operand`; symex leg green. Three real Windows-behavior findings landed via the push-and-wait grind: (1) `checkJobLimitCode` single-dequeue always saw `JOB_OBJECT_MSG_NEW_PROCESS` first — bounded drain loop; (2) `JOB_OBJECT_LIMIT_PROCESS_MEMORY` fails commits, never kills — the worker survives with a caught-OutOfMemDefect frame, so a drained limit message must WIN over any frame (`hadJobLimit` checked first); (3) Windows named mappings die with their last handle — a read-after-child-exit shm consumer must pre-attach and hold (`shmHoldCoverage`/`shmHoldCmpLog`, coverage.nim), which also fixed a REAL production race in `newProcessWorker`'s Windows closure. `NELLI_COV_DEBUG` opt-in tracing kept in nelli_cov.c/nelli_shm.c permanently. Push-and-wait grind via subagent a5dbc5dee2bb46b65 (resume it by SendMessage with CI evidence; it must NOT push — control loop pushes). CI history: round 1 (run 33017017592) newly-PASSED tfuzzcbuild Phase-1a/tfuzzcovdump/tfuzzprobe natively on Windows, FAILED tfuzzwinjoblimits (worker exit 0x1 Nim OutOfMemDefect, generic-death precedence beat the limit message) + tfuzzcbuild G4C3 (clean child, 0 cmp entries; shm mechanism independently ruled out by tfuzzwinshm). Round 2 root causes: `checkJobLimitCode` dequeued ONE port message — `JOB_OBJECT_MSG_NEW_PROCESS` always arrived first, the limit message behind it was never read; fixed with a bounded drain loop, test target simplified to the production-faithful Defect-death shape. G4C3 still ambiguous (Windows-clang sancov vs publish path — no local Windows clang): `NELLI_COV_DEBUG` opt-in channel added to nelli_cov.c + nelli_shm.c (init/env/callback/publish/GetLastError tracing), tfuzzcbuild G4C3 tests set it and echo child output. **CURRENT: waiting on CI run 33020523296 (fuzzer-windows) for b17b365 — on wake read the tfuzzwinjoblimits verdict + G4C3 NELLI_COV_DEBUG output, feed the agent the next evidence or record E4c done if green. E4c DoD = fuzzer-windows leg fully green incl. tfuzzwinjoblimits + the G4C3 suite on Windows.** Remaining scope cuts already accepted (RFC-worded, in the C3 report): external one-shot pipe-buffer caveat, newExternalWorker persistent-mode (E5's own stated asymmetry), KILL_ON_JOB_CLOSE structural proof.

**E4b — DONE, Windows-run-PROVEN (slice 29/30; `a83bf53` + fix `26cb33b`).** fuzzer-windows run 33013350915 SUCCESS with `tfuzzwinshm.nim`'s Windows suite executing natively — [OK]: N>1 shm coverage (second input independently valid = the nelli_cov reset path live), `$NELLI_COV_SHM`-unset opt-in default, `$NELLI_CMP_SHM` covercmp operand-pair publish, `newProcessWorker` real coverage through `Orchestrator.admit`, naming-transform pins; symex leg green. Substance: `nelli_shm.c` `#ifdef _WIN32` arm (CreateFileMapping/MapViewOfFile inside the channel-parameterized core, create-or-open in one call — no OpenFileMapping TOCTOU; ONE naming function `pt_shm_win_name` = Nim's `shmWinName`, strip `/` + `Local\` prefix; plus an `extern "C"` guard fixing a latent cpp-backend symbol-mangling gap), `coverage.nim` shm procs widened to windows, Windows `runWorkerLoopAndExit` per-input reset/publish wiring + `newProcessWorker` per-submit segment via `shmProbe`; db.nim sweep stays POSIX with a comment (Windows named mappings die with their last handle — no sweep needed). The one CI failure round: `entries.len == 1` → 3 was the TEST's error (sentinelProp has 3 instrumented comparisons — `covercmp` rewrites every infix cmp; expectation was copied from `tfuzzcmplogshm.nim`'s single-comparison property), diagnosed against the dispatch chain first (reset/publish boundaries identical cross-platform), fixed by pinning the load-bearing assertion (exactly one magic-pair entry) + a new platform-neutral in-process test proving the 3-count on POSIX; a `case`-reshape dodge was tried and REVERTED (breaks the concolic DSL parser — `classifyType` "node has no type" on `nnkCaseStmt`). **NEXT: E4c (FINAL slice)** — Job Object limits consuming `workerproto.JobLimitPolicy` (`SetInformationJobObject`/`TerminateJobObject`), `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (E-cleanup's carried Windows analog of PDEATHSIG), unhandled-exception-filter → `ckWinException` mapping, `vResourceExceeded` tagging, and un-gating the external tier to Windows (`tests/fuzzsupport.nim` `available()`/`traceCmpSupported()`, tfuzzcbuild G4C3 gate, rt.c's POSIX-only SIGBUS/unistd surface needs its Windows arm).

**E4a — DONE, Windows-run-PROVEN (slice 28/30; C1 `24285ba`, C2 `0d1acb2`).** Push-proof landed 2026-08-26: fuzzer-windows run 33009204939 SUCCESS with `tfuzzwinworker.nim`'s Windows suite executing NATIVELY on windows-latest — `[OK] a real CreateProcess'd worker round-trips one framed input and proves genuine reconstruction` + `[OK] a worker that dies before answering is reported vCrashed via GetExitCodeProcess, not a hang`; symex-windows run 33009204927 also green. **NEXT: E4b** (Windows shm coverage: CreateFileMapping/MapViewOfFile + nelli_cov.c reset path; widens `tests/fuzzsupport.nim`'s `available()`/`traceCmpSupported()` gates + tfuzzcbuild's G4C3 suite + lifts the Windows worker's zero-coverage default and makes `NELLI_WORKER_MAX_INPUTS > 1` production-valid there), then E4c (Job Objects consuming C1's `JobLimitPolicy`). C1: `src/nelli/workerproto.nim` — platform-independent leaf seam (frame codec split pure/fd-wrappers; workerArgv/workerEnv/parseWorkerModeId; `BootstrapBreaker` pure fold — consecutive dead-before-first-read, `"construction-not-reentrant"` diagnostic distinct from storm-breaker; `JobLimitPolicy`/`verdictForJobLimit` → vResourceExceeded/ckWinException, syscalls deferred to E4c by RFC design); fuzzworker.nim consumes+re-exports it, zero behavior change; `tests/tfuzzworkerproto.nim` 22 tests; worker-family suite green c, cpp spot, cross-compiles clean.
**C2 `0d1acb2`:** fuzzworker.nim's sibling `when defined(windows)` block (readFrame/writeFrame over a `Handle`, `spawnWorkerProcess` via raw `CreateProcessW`/`CreatePipe` FFI (`std/winlean`, anonymous inherited-handle pipes — NOT named pipes, NOT stdin/stdout; equivalence decision + handle-value-via-env-var rationale in the commit body), `reapWorker`/`observationForDeath` (`GetExitCodeProcess` decode → `ckWinException`), `runWorkerLoopAndExit`, `newProcessWorker`); `fuzzmacro.nim`'s worker-mode dispatch widened `when defined(posix)` → `when defined(posix) or defined(windows)` (the check itself was never POSIX-specific — only the `runWorkerLoopAndExit` it called had no Windows arm before now). Bootstrap circuit-breaker WIRED LIVE at `Orchestrator.run`'s spawn/first-read site (`fuzz.nim`): new `bootstrapWindow` knob (0 = off, `stormWindow` convention), watches only a freshly-(re)spawned worker's first submit, raises `BootstrapBreakerError` on N consecutive genuine process-death (not `ckException`) first-submit failures — `tests/tfuzzbootstrapbreaker.nim` RED-GREENs it over a scripted `Worker[T]` (`tfuzzrespawnstorm.nim` precedent). `tests/tfuzzworkerprocess.nim` stays POSIX-gated (genuinely posix-only: raw `posix.pipe`/`kill`/`SIGSEGV`/`Pid` throughout) with an else-branch SKIP echo naming its new counterpart; `tests/tfuzzwinworker.nim` (new) proves the SAME reconstruction-sentinel round-trip through the real `fuzz(...)` macro call site on both a `when defined(windows)` suite and a `when defined(posix)` parity suite, one call site, both `tfuzz*`-glob-discovered. Local verification: `dt-bounded.sh` c/cpp green on the full worker-family matrix (tfuzzworkerproto/tfuzzworkerprocess/tfuzzworker/tfuzzorchestrator/tfuzzloop/tfuzzrespawnstorm/tfuzzbootstrapbreaker/tfuzzwinworker-POSIX-suite); `dt-crosswin.sh` c/cpp clean on `src/nelli.nim`, `tests/tfuzzworkerprocess.nim`, `tests/tfuzzwinworker.nim`, `tests/tfuzzbootstrapbreaker.nim` — the full CreateProcess/pipe/winlean FFI surface compiles+links for `--os:windows --cpu:amd64`. **Not yet done: the actual Windows CI push-and-wait RUN-proof** (push `rfc-fuzzer-nextgen`, read `fuzzer-windows` workflow result) — E4a is code-complete and cross-compile-clean but not yet run-verified on real Windows per the RFC's own push-and-wait contract; do that before marking E4a fully DONE. Then E4b (Windows shm coverage; widens `available()`/`traceCmpSupported()` gates in tests/fuzzsupport.nim + tfuzzcbuild's G4C3 suite, and lifts the Windows worker's coverage off its zero-value default), E4c (Job Object limits consuming C1's JobLimitPolicy; widens external tier).

**Eci — DONE (2026-08-26; slice 27/30). Windows verification channel LIVE: both Windows CI legs GREEN on the pushed branch.** Commits `e9444fc` C1+C2, `464ae8e` C3+C4, `53252a2` C5-fix. **DoD proof:** fuzzer-windows run 33004844917 SUCCESS (all 57 glob-discovered `tfuzz*`/`tdb*` on windows-latest) + symex-windows run 33004845001 SUCCESS, triggered by pushing `rfc-fuzzer-nextgen` — the real push-and-wait channel E4a-c will use.
- **C1 local channel:** dev image (`scripts/Containerfile`) carries mingw64-cross-gcc/-c++ + `/opt/z3-headers` (isolated z3 header stage — NEVER put host `/usr/include` on the cross include path); `scripts/dt-crosswin.sh <c|cpp> <file>` cross-compiles+LINKS for `--os:windows --cpu:amd64` (explicit `--amd64.windows.gcc.exe:` pins — the image's patched Nim config lacks the `-d:mingw` mapping). The `when defined(posix)` gating in fuzz/fuzzworker/db already existed; the umbrella + all 57 tests cross-compile.
- **Upstream chain (the C1 blocker):** softlink's `compatManifest` resolved its manifest path with TARGET-separator compile-time ops → "manifest file not found" under cross-compile. Fixed by separator-flavor probing (`applyCompatManifest`); pushed to softlink main (`e4856ae` on v0.12.3) AND backported as **softlink v0.11.2** (tag + branch `release/v0.11.x`; full suite green on BOTH trees) so nelli's 0.11.x line takes the fix without the 0.12.x upgrade (that upgrade is deliberate future work, incl. a Windows LoadLibraryEx security fix in 0.12.3 worth having before shipping Windows fuzzing — noted, not smuggled into Eci). nim-z3 `d3ae858` bumps its softlink ref to v0.11.2 (pushed; its full CI green, rc=0). proptest re-locked via `milpa fetch --upgrade z3` (frozen-mode `milpa fetch` does NOT re-resolve).
- **C2 portable test surface:** platform truth lives in ONE place — `tests/fuzzsupport.nim`'s `available()`/`traceCmpSupported()` return false off-POSIX until E4b/E4c widen them; tests with existing skip-guards self-skip. In-file `when` gates only where a test asserts availability (tfuzzcbuild's backend-present assertion + its G4 C3 shm suite). `tfuzzexec`'s Windows failure was a REAL bug (hardcoded `/`-joined expected paths vs `os./`'s native separator) — fixed, stays fully live on Windows. Linux sweep 60/60 green post-lock-bump; cpp + cross spot-checks green.
- **C3 CI legs:** `.github/workflows/fuzzer-windows.yaml` — GLOB discovery (`t(fuzz|db)*`), Z3 4.13.4, deps at milpa.lock provenance SHAs, shared `--nimcache:C:\nimcache`, 90-min budget (actual: 8m). Both legs trigger on push to `main` + `rfc-fuzzer-nextgen`.
- **C4 capability flag:** `docs/RFC-fuzzer-nextgen.windows-capability.md` (greppable keys: `WINDOWS_LOCAL_TOOLCHAIN: cross-compile-only`, `WINDOWS_RUN_CHANNEL: ci-push-and-wait`, authorized). **E4a-c preflight MUST read it** — local RED cycles cannot exercise CreateProcess/named pipes/Job Objects; the cycle shape there is: platform-independent policy via dt-bounded locally, Windows glue via dt-crosswin + CI push-and-wait.
- **C1 local channel — mostly done:** `scripts/Containerfile` + rebuilt `localhost/nelli-dev:latest` now carry `mingw64-cross-gcc(-c++)` + `/opt/z3-headers` (isolated z3 header stage; NEVER add host `/usr/include` to the cross build). New `scripts/dt-crosswin.sh <c|cpp> <file.nim>` cross-compiles+links for `--os:windows --cpu:amd64` (explicit `--amd64.windows.gcc.exe:x86_64-w64-mingw32-gcc` etc. — the image's patched Nim config lacks the `-d:mingw` mapping). `src/nelli.nim` umbrella cross-compiles+links to a PE exe — the `when defined(posix)` gating in fuzz.nim/fuzzworker.nim/db.nim already existed from the Linux slices.
- **BLOCKER FIXED UPSTREAM (softlink):** softlink's `compatManifest` built its manifest path with TARGET-separator compile-time path ops → cross-compile for windows on a POSIX host failed "manifest file not found". Fixed by separator-flavor probing in `applyCompatManifest` (directives.nim); worktree `/tmp/softlink-xwin-fix`, branch `fix/xwin-manifest-path`, commit `e4856ae` REBASED onto softlink v0.12.3 (`a467a8c` — note: nelli's lock still pins softlink 0.11.1, so the coming lock bump jumps 0.11.1→0.12.3+fix; full Linux re-verify required). Full softlink suite green pre-rebase; REBASED-tree suite running in bg (`softlink-suite-rebased.log` in job tmp). NEXT: on green → push `e4856ae` to softlink main → `milpa` re-lock in proptest → full Linux `tfuzz*`+`tdb` sweep → commit Eci C1.
- **C2 (pending batch RED):** batch cross-compile of all 57 `tfuzz*`/`tdb*` tests running in bg — failures get IN-FILE `when defined(posix)` self-gating (explicit SKIP echo on Windows), so the CI array stays a pure glob. 10 files already have platform guards; 47 don't.
- **C3 drafted (uncommitted):** `.github/workflows/fuzzer-windows.yaml` — glob-discovered `tfuzz*`/`tdb*` on windows-latest, Z3 4.13.4, deps at lock SHAs, triggers on push to `main` + `rfc-fuzzer-nextgen`; `symex-windows.yaml` trigger extended to the branch.
- **C4 drafted (uncommitted):** `docs/RFC-fuzzer-nextgen.windows-capability.md` — greppable flags: local = cross-compile-only, run channel = ci-push-and-wait (authorized). E4a–c preflight must read it.
- **C5 not started:** push branch, drive `fuzzer-windows` + `symex-windows` legs green via `gh run` (this is Eci's end-to-end DoD).

- **Stage:** 3 (implementation) — architecture rounds 1, 2 & 3 all DONE; **no open forks/escalations.** Grinding slices via `/loop`.   •   **Done:** E0 (corpus-sync spike — verdict recorded), E1 (Orchestrator/Worker seams + macro entry), E2a (POSIX persistent worker), E2b (shm + `nelli_cov.c` reset). **E3a DONE** (`561c86d` C1+C2 finding record + reproRate/divergentReproduction + re-verify-gated `admit` — `spawnFreshWorker`/`reVerify` knobs, default OFF so `fuzz()` and every existing caller are byte-for-byte unchanged; `ea84cee` C3 order-independent-fold pure-algebra permutation tests over fakes; C4 worker recycling policy (`recycleAfterInputs`, crash-forces-recycle) + `fuzzworker.nim`'s new `newForkWorker` (fork-per-input, forks only ever from the live orchestrator process itself, never from a prior child — captured-once by construction) + `tests/tfuzzforkworker.nim`'s characterization test (N forks from one snapshot are state-identical; the ONE process-spawning test in this slice, deterministic/sequential, not raced). The prior "IN FLIGHT: subagent ac53e0991da4c4fbc" note was a stalled agent from an earlier session with zero commits landed — this session took over fresh per the subagent-stall-takeover convention and redid E3a in full. `reportFinding` is the un-gated first-observation report hook (crash reporting is never gated by re-verify per §0); `admit`'s re-verify path is what's gated. Dual-backend (`c`+`cpp`) full `tfuzz*`+`tdb` suite green after every commit, no existing assertion touched.
**E3a verified** 10/10 dual-backend. **E3b — DONE** (6/6 dual-backend verified; C5 `0018a69` committed by agent). Note: E3b never-unlinks superseded corpus generations (GC scope-cut) — FOLDED into E-cleanup's startup sweep (reclaim `.corpus.<gen>.log` not referenced by head; lease-bounded live GC stays U2's). **E-cleanup — DONE** (4/4 dual-backend verified; see the `### E-cleanup — DONE` section below for full detail). C1 `f667bd0` campaign-startup sweep (stale nelli shm segments by prefix + superseded corpus generations vs the head pointer), C2 `f5f81bd` `PR_SET_PDEATHSIG` (workers die with a hard-killed orchestrator), C3 `6a8a174` `isolateOwnProcessGroup`/`killWorkerGroup` (clean-shutdown process-group-kill backstop, reaches a worker's whole subtree), C4 `44db313` steady-state respawn-storm breaker (`Orchestrator.stormWindow`/`stormBackoff`, sliding-window non-diversifying-`CrashInfo.kind` trip, distinct from the not-yet-built E4a bootstrap breaker). **⚠ WINDOWS SLICES BLOCKED — verification-channel fork (surfaced to Corey via push 2026-08-26).** This host is Linux (`os: linux, amd64`); no runnable Windows container exists here (CLAUDE.md's "Windows container" lives on CI/a Windows machine, not this host), and podman/docker on Linux cannot run Windows images. **Eci, E4a, E4b, E4c are Windows-specific and CANNOT be TDD-verified here** — blind-writing untested Windows systems code would violate the quality bar. They are HELD (not done, not skipped-and-forgotten): resume them once Corey provides a channel — authorize a CI push (branch `rfc-fuzzer-nextgen` isn't pushed yet) OR Windows-machine/container access. NOT a deferral of effort; a hard capability gap.
**E5 — DONE** (`755fb39` C1 `newExternalWorker` + shared `choiceSeqTargetWorker` bridge, `290372e` C2 external-tier-through-Orchestrator characterization — zero new prod code, the seam design paid off; 43/43 c + 5/5 cpp verified). **This completes the Linux-verifiable Track E.**
**Lesson (applied to all future briefs): agents MUST run every test synchronously via dt-bounded (blocks + returns exit code), NEVER background a suite run and yield — that orphans uncommitted work (recurred on E1-macro/E3b/E5; resumable via SendMessage to finish).**
**Track G STARTED.** **G1a — DONE** (`3a14b59`): `WalkMode = wmExplore|wmFollowConcrete` on `WalkCtx`, all 21 walker arms get a no-op `case w.mode` seam (wmExplore byte-identical), SW NOT bumped (structure-only, CR2 pin "73" unchanged — correct), `tests/tsymex_g1a_mode.nim` char test, verified synchronously dual-backend. **G1b — DONE** (`3cc275a`): draw-symbolication + concrete-trace constraint collection. `runConcolicCollectImpl(..., maxDraws=256)` in `smt/runtime.nim`: each `ChoiceNode`→symbolic var w/ its declared constraint (reuses `mkIntVar`+`min≤v≤max`); `walkIfFollowConcrete` picks the concretely-taken arm via scratch Z3 solves (draws stay FREE for G2's flip); draw→param linkage is a **caller-supplied `ConcolicParamBinding` (cbDrawLinked|cbConcretized)** — classification deferred to G3/G6 (transparency descriptor), by design. `ConcolicCollectResult.pcSatByConcreteInputs` soundness pin; `ConcolicYieldCounters` (tracesTruncated/paramsConcretized/unsupportedDrawKinds). SW not bumped (wmExplore byte-identical, CR2 "73" unchanged). Verified synchronous dual-backend, no hangs.
**G2 — DONE** (`8bca6e1`): branch-flip solve + materialize + bounded optimistic fallback + yield taxonomy. **Concolic bridge works end-to-end** — magic-byte test: 1 Z3 query materializes `0xCAFEBABE`, replay confirms `ccoIntendedCovered`. `ConcolicBranchRecord`/`runConcolicFlipImpl` (prefixPc ∧ ¬observedTruth), `materializeConcolicModel` (solved draws→ChoiceNode, clamped to declared bounds), optimistic = drop last-k prefixPc conjuncts (deterministic, bounded `min(maxRelaxationAttempts=8, len)`), `z3CheckBounded` (random_seed=0 + timeout on EVERY attempt). Sharp: re-collects on the materialized seed to verify the flip landed (clamp can undo an optimistic flip). SW not bumped (CR2 "73"). Verified synchronous dual-backend, no hangs.

**REORDER (rationale): doing S1 BEFORE G3.** RFC order is G3 next, but G3 consumes S1's `FrontierStats` + energy; doing G3 first needs a throwaway fallback energy + the later G3b rewire (do-it-twice). S1 is independent and the RFC designed it as "owns shared FrontierStats (G3 consumes)"; S/U-interleave is sanctioned. So: **S1 → G3 (real energy, no G3b) → G4 → G5 → G6 → S2/S3/S4 → S5a/S5b/S6 → U0-U3.** G3b is thereby avoided.
**S1 — DONE** (`96b0d84` C1, `eb7f40c` C2, `1b6d7d2` C3). Entropic (Böhme information-gain) power schedule, replacing the coarse recency/lineage `2.0`/`+1.0` scheme. **C1** adds `CoverageFrontier.stats: FrontierStats` (per-slot `hitCounts` + `totalAdmitted` + per-slot `lastImprovedSeq`), folded in at the ONE fold site — `coverage.nim`'s `admit` — never rescanned; structured so G3 adds its orchestrator-wide staleness/stall fields to the SAME object later (round-3 breadth fix), not a sibling one. **C2** adds the pure formula: `rarityWeight(stats, slot) = -log2(hits/totalAdmitted)` (Shannon self-information — chosen over raw `1/hits` for smooth degradation and because it's the literal information-theoretic quantity Entropic is named for), `coveredSlots(c)` (sparse nonzero-slot list, so energy recomputation never rescans the full up-to-8192-slot map), and `entropicEnergy(coveredSlots, stats, sizeChoices, execNanos) = (entropicBaseEnergy + Σ rarityWeight) * executionCostFactor(size, nanos)` — the exec-cost term is a plain `(0,1]` discount favoring fast/small inputs, degrading to size-only when `execNanos <= 0` (no timing signal). `entropicBaseEnergy` floors energy strictly positive so no corpus entry is ever permanently starved. **C3** wires it into the loop: `energy[i]` is recomputed fresh from `frontier.stats` every parent-selection tick (the rarity denominator moves on every admit, so a cached value would go stale) via each corpus entry's frozen `coveredSlots`/size/`corpusNanos`; the loop asks coverage.nim for energy, never recomputes rarity inline. `FuzzSettings.powerSchedule` is GONE — `uniformSchedule: bool` replaces it with inverted default semantics (Entropic is now the unconditional default; `uniformSchedule: true` reproduces the pre-S1 default trajectory byte-for-byte, kept as Track S's future ablation-harness uniform baseline). No test in the suite pinned an exact schedule-dependent trajectory against the old default (checked all `tfuzz*`/`tdb` call sites), so the flip needed no separate determinism gate. `observeInProcess` now measures real wall-clock duration into `Observation.runResult.durationNs` (previously only external targets set it), so the exec-cost term is live for the common in-process path, not just external targets. `tfuzzschedule.nim`'s two `powerSchedule`-dependent tests were rewritten (Entropic-as-default + `uniformSchedule` as the opt-out); `tfuzzfrontier.nim` gained two new suites (`FrontierStats`, `Entropic energy`) with the RFC's four RED-able properties (rare-edge energy > common-edge energy; smaller/faster > larger/slower covering the same edges; hitCounts increments exactly once per admit; lastImprovedSeq advances only on a bucket-raising admit) — no existing assertion in either file was weakened, only the two flag-shaped ones were re-pointed at the new default. Full `tfuzz*`+`tdb` (41 files) verified GREEN dual-backend (`c`+`cpp`) after the final cycle. **Next: Track G** (G1a/G1b/G2 already done — see above; **G3 — DONE** (C1 `641ada0` staleness on FrontierStats @ one fold site; C2 `ecba035` `Orchestrator.concolicBridge`/`tryConcolicBridge` + `admit` provenance param + `ConcolicBridgeEntry`/`ConcolicBridgeResult` type-erased Z3-free mirror keeping fuzz.nim Z3-free; C3 `66b9cd9` wired into `fuzz[T]` loop, concolic seeds through same S1 energy bookkeeping, no G3b placeholder — worked end-to-end with a FAKE bridge). Wiring the REAL bridge against a `{.cover.}`'d property surfaced a walker gap first: **G3fix** `3918eaa` — descending into `recordEdge` (every `{.cover.}`'d proc's own instrumentation) crashed on its free-standing `coverageMode` threadvar (`env[e.vname]` KeyError, no binding for a non-local/non-param name). Two-layer fix: (1) `recordEdge` tagged with a LOCAL `{.symexOpaque.}` pragma in `coverage.nim` (matched by name only, no import needed — keeps `coverage.nim` a leaf module and `fuzz.nim` Z3-free) so the parser routes it through the existing #137 `mkOpaqueCall` graceful path, confirmed already-graceful; (2) `smt/runtime.nim`'s `lower(iekVar)` degrades ANY unresolved free reference to a fresh symbolic havoc, but ONLY in `wmFollowConcrete` (gated via a new forward-declared `isFollowConcreteWalk()`, the `currentWalkCtxPtr` idiom) — `wmExplore` stays byte-identical, CR2 pin unchanged at "73". **C4** `cf3f887` — the real bridge: `fuzzmacro.nim`'s `fuzz(...)` macro now builds a real `ConcolicBridgeEntry` closing over the captured `propSym`, running `concolicFlip` with a minimal positional `cbDrawLinked` classifier (one binding per property param; G6 does full classification later) and translating the real outcome/coverage taxonomy into fuzz.nim's erased mirror types — unconditionally wired but inert unless `settings.stallRounds > 0`. `fuzzmacro.nim` now imports AND re-exports `nelli/symex` (anticipated in G3 C2's own doc comment) — the re-export was load-bearing: without it, macro-generated code for any property touching a construct outside the narrow tsymex_*-exercised set (a free global var read, a raw FFI call) failed to compile for any `import nelli`-only caller. Headline (`tests/tfuzzconcolicbridge_real.nim`, real Z3, real `{.cover.}`, plain `import nelli`): a `stallRounds: 1` campaign over a `{.cover.}`'d `0xCAFEBABE` gate reaches `coverageHits == 2`; the identical campaign at the `stallRounds` default (0) stays at `1`. Passed first try. Full `tfuzz*`/`tdb` + a broad `tsymex_*` sweep verified green (c; cpp spot-checks on both new tests) after both commits — two PRE-EXISTING unrelated failures (`tsymex_phase15_g8_multi_param`/`g10_smoke`, a `classifyType`/`node has no type` Nim-AST issue) confirmed failing identically on the pre-G3fix commit, not a regression. **Track G core (G1-G3) is now DONE. Next: G4 cmp instrumentation, G5 I2S+auto-dict, G6 transparency descriptor.**
**⚠ ESCALATION (design gate fired, RESOLVED BY ME — decided, not a fork): real-bridge wiring blocked by a WALKER GAP.** Wiring real `concolicFlip` against a `{.cover.}`'d property crashes — `lower`'s `iekVar` (`runtime.nim:2992`) has NO degrade path for a free/global/threadvar ref; `{.cover.}`'s `recordEdge` (coverage.nim:90) reads the `coverageMode` threadvar → crash when the walker descends into it. Blocks G4/G5 too (cmp hooks same shape). **DECISION (implementing now):** (1) nelli instrumentation hooks (`recordEdge`, future cmp hooks) are OPAQUE to the walker (exclude from proc registration / special-case isCall — no descent); (2) `iekVar` free-ref degrades to havoc in `wmFollowConcrete` ONLY, mode-gated so wmExplore byte-identical → NO SW bump, CR2 stays "73". **G3-fix + C4 — DONE** (`3918eaa` + `cf3f887`; full detail in the G3 entry above). Concolic bridge PROVEN end-to-end in a real `{.cover.}`'d fuzz campaign (0xCAFEBABE breakthrough). Walker free-ref gap (which would also have blocked G4/G5) is retired: instrumentation-opacity via `{.symexOpaque.}` + `wmFollowConcrete`-gated free-ref havoc (wmExplore byte-identical, CR2 "73"). **G4 — DONE** (`0bd9d8a` C1, `a42960a` C2, `da68f85` C3; a prior subagent's C1 attempt orphaned uncommitted work AGAIN — resumed synchronous-only, redone/finished in one pass, matching the standing "subagent stall takeover" convention). **C1** — the Nim-tier comparison hook: `coverage.nim`'s `{.covercmp.}` pragma macro (a `{.cover.}`-SIBLING, not an extension — composes on the same proc via two orthogonal AST rewrites) walks a proc body for `==`/`!=`/`<`/`<=`/`>`/`>=` and rewrites each into a block that evaluates both operands once, calls `logCmp(lhs, rhs, op)`, then compares the same temporaries. `logCmp` is an OVERLOADED proc (`SomeInteger`/`string`/`seq[byte]` ~ `ckInt`/`ckString`/`ckBytes`, plus a generic `[T]` no-op fallback so any other comparison type still compiles) — the macro emits an untyped `bindSym"logCmp"` closed symbol choice, resolved by ordinary overload resolution at the (fully-typed) call site, so no type info is needed at macro-expansion time. Every overload carries the SAME local `{.symexOpaque.}` pragma `recordEdge` uses (G3fix) — `tests/tsymex_g4_cmpwalk.nim` confirms a `{.cover, covercmp.}`'d property still runs through `concolicFlip` clean. `CmpOp`/`CmpLogEntryKind`/`CmpLogEntry` types + `resetCmpLog`/`currentCmpLog`/`parseCmpLog` (gracefully truncates a cut-off trailing record, never raises). SW not bumped (CR2 "73" unchanged — instrumentation, not walker semantics). **C2** — the per-run shm log: `nelli_shm.c`'s `pt_shm_*` primitives were a SINGLETON per process (one `pt_shdr`), which would have silently collided the cmp log onto the coverage channel's segment — parameterized the six core operations over an explicit `pt_shm_channel` (original zero-arg functions kept as thin wrappers over a default channel, byte-identical ABI for every existing caller) and added a second static channel (`pt_cmplog_*`) for the cmp log. `coverage.nim` gained `shmResetCmpLog`/`shmPublishCmpLog`/`shmReadCmpLog`; `fuzz.nim`'s `observeInProcess` toggles the cmp-log recording gate at the same per-run boundary as `coverageMode`; `fuzzworker.nim` wires per-input reset/publish into the persistent worker loop behind `$NELLI_CMP_SHM` (opt-in, default off, orthogonal to `$NELLI_COV_SHM`). **C3** — the external tier: `nelli_cov.c`'s `__sanitizer_cov_trace_cmp{1,2,4,8}`/`_const_cmp{1,2,4,8}` hooks, built via `-fsanitize-coverage=inline-8bit-counters,trace-cmp` (clang-only, no gcc analog — `tests/fuzzsupport.nim`'s `buildInstrumentedTraceCmp`/`traceCmpSupported`). Every entry is tagged `coUnknown` (a new `CmpOp` variant, appended last so existing ordinals are unchanged) since the sanitizer-coverage ABI never conveys which operator fired. **Two real bugs found+fixed while writing the C3 interop test** (not hypothetical — both reproduced RED first): (1) the channel-parameterized `pt_shm_ch_init`'s "idempotent: already attached" guard silently kept the FIRST shm name a process ever attached to even when asked for a DIFFERENT name later — a process reading more than one distinctly-named segment over its lifetime (a test suite moving between cases; a future orchestrator reading several workers' own segments) got the first segment's stale contents back for every later name; fixed by tracking the attached name and re-attaching (with a clean `munmap` of the stale mapping) on change, staying a no-op for the same-name case every existing caller relies on. (2) the cmp-log channel was reusing coverage's `pt_dumped` publish-once gate, so whichever channel published first in a run silently starved the other's publish for the rest of that run; each channel now carries its own gate variable — proven by a regression test with BOTH `$NELLI_COV_SHM` and `$NELLI_CMP_SHM` set on one worker run. Full `tfuzz*`+`tdb` (48 files) + a `tsymex_*` subset incl. CR2 "73" verified green dual-backend after each of the three cycles, no existing assertion touched. **G5 — DONE** (`e950b03` C1, `198e149` C2, `019f9c7` C3; a prior subagent's attempt (`a04f8afbcc1a263bf`) left only this handoff note's stale "IN FLIGHT" marker and zero commits — this session took over fresh per the standing subagent-stall-takeover convention). I2S (input-to-state) replacement + auto-dictionary, consuming G4's `{.covercmp.}` operand log. **C1** — the operand->choice-node mapping (int) + the mutator: `fuzzir.nim`'s `collectI2SMatches` walks a parent's cmp log against its OWN choice nodes — a node whose concrete value equals one side of a logged comparison earns a candidate replacing it with the OTHER side (both directions tried, since `logCmp` doesn't distinguish "the input" from "the constant"). Exactness for int despite the log not recording source signedness: `decodeIntCandidates` tries both `cast[int64]` (recovers the value if `T` was signed — sign extension preserves it up through int64) and the raw `uint64` (recovers it if `T` was unsigned), matching against the node's exact `Int128`. Out-of-range replacements are CLAMPED into the node's declared bounds (never skipped — an integer constraint is a closed interval, clamp is always well-defined). Wired as `fuzz.nim`'s 6th mutation operator behind `FuzzSettings.enableI2S` (opt-in, default off — pick distribution stays `mod 5` and the cmp log is never parsed when unset, so every pre-G5 caller's trajectory is byte-for-byte unchanged, the same convention `stallRounds`/`uniformSchedule`/`reVerify` use). `corpusCmpLog` (parallel to `corpusCov`/`corpusSlots`) captures each corpus entry's OWN log at the exact moment its run produced it (`captureCmpLog`, the one call site `currentCmpLog()` is read from — guarded to only fire on a non-rejected run, since a rejected run may never have reached `observeInProcess`'s reset and the thread-local buffer would still hold a PRIOR run's stale log). Headline (`tests/tfuzzi2s.nim`, plain `fuzzWith`, no macro/Z3): a `stallRounds:0` campaign over a `{.covercmp.}`'d `0xDEADBEEF` int gate reaches both edges with `enableI2S:true` and only one at the default — passed first try. **C2** — bytes/string (already generic from C1's design; this cycle is the headline proof): `bestBytesReplacement` clamps by truncate/zero-pad (bytes have no interval-set legality concern); `tryStringReplacement` SKIPS (never coerces) a replacement that violates the node's `StringConstraints` (codepoint-length + interval-set membership — no safe generic "clamp" exists for codepoints). Real finding: the public `bytes()` strategy is built on `lists(integers(0,255))` — one `ckInteger` node PER BYTE plus per-element continuation booleans, never a single `ckBytes` node — so it has no node an exact I2S match can land on; the `"MAGIC"` `seq[byte]` headline uses a small strategy over `datasource.drawBytes` directly (one real `ckBytes` node, matching what §G-cmp's scope actually describes), while `strings()` already draws a single `ckString` node via `drawString` and needed no such workaround. Both the `ckBytes` and `ckString` `"MAGIC"` gates are broken by I2S alone, unbroken at the `enableI2S` default. **C3** — the auto-dictionary: `harvestDictionary` folds BOTH operands of every logged comparison (int/bytes/string, deduped, capped at `maxDictEntries=512`) into a per-campaign `Dictionary`, called from the SAME `captureCmpLog` site as the log capture (harvests from every non-rejected run, not just admitted/corpus-growing ones — broader than "the surviving corpus", matching the RFC's "seen across the campaign"). `mutateIRI2SReplace` falls back to `dictReplacementCandidates` (a same-kind dictionary entry legally replacing some node, same clamp/skip discipline as direct I2S) when the parent's own log has no direct match — deliverable 3's "basic insertion"; S3 deepens havoc-style insertion later. `FuzzReport.dictionary` exposes the harvested set; stays empty at the `enableI2S` default (proven, not just asserted-inert). Full `tfuzz*`+`tdb` (49 files) + CR2 "73" pin + a `tsymex_*` subset verified green dual-backend (`c`+`cpp` spot-checks) after all three cycles, no existing assertion touched. SW not bumped throughout (mutation-loop wiring, no walker semantics touched). **Track G (G1-G5) is now DONE.** **G6 — DONE** (`5582ca8` C1, `b66a11d` C2, `6f17d31` C3, `e4d7a77` C4; the prior "IN FLIGHT: subagent a32b947c53ae4df4e" note was a stalled agent with zero commits landed — this session took over fresh per the subagent-stall-takeover convention and did G6 in full). Combinator transparency descriptor + composition algebra, replacing G1b/G3's minimal positional `cbDrawLinked`-for-everything classifier. **C1** — `smt/transparency.nim` (pure, Z3-free): the 6-category descriptor set (`dkIdentity`/`dkAffine`/`dkPredicated`/`dkSpanComposite`/`dkOpaque`/`dkBranching`) + a total, DIRECTED `compose(first, second)` — pinned to LEFT-TO-RIGHT PIPELINE order (`first` applied then `second`, matching how a chain is WRITTEN, e.g. `s.map(f).map(g)` = `compose(descOf(f), descOf(g))` — the OPPOSITE of standard math `g∘f`). Full rule table incl. every mixed pair the RFC names (`affine∘affine` composes coefficients; `predicated∘t`/`t∘predicated` carry+re-anchor the conjunct via pure algebraic substitution, never a division, so comparison direction never flips; `predicated∘predicated` conjoins BOTH; `span-composite∘affine`/`∘predicated` distribute per the span rule; `branching∘t` distributes across cases, `affine∘branching`/`predicated∘branching` re-anchor guards; anything`∘opaque`/`opaque∘anything = opaque`); undefined/rare pairs (e.g. `span-composite∘span-composite`, `span-composite∘branching`) fall back to the opaque absorbing default — CLOSED by construction, proven by an exhaustive 36-pair totality test plus a 3-combinator-chain direction pin (folding the wrong way gives a materially different, wrong answer) in `tests/tsymex_g6_algebra.nim`. **C2** — `smt/runtime.nim` gains a THIRD `ConcolicParamBinding` kind, `cbTransformLinked` (`tDrawIndex`/`tA`/`tB`/`tConjuncts: seq[ConcolicConjunct]`) — the Z3-facing flattened primitive twin of the descriptor tree (runtime.nim never imports `transparency.nim`, mirrors `fuzz.nim`'s erased-mirror convention); `runConcolicCollectImpl`'s binding loop builds `env[p.name] = tA*drawVar+tB` and asserts each conjunct into `initialPC`. `cbDrawLinked`/`cbConcretized` untouched. Proven directly (hand-built bindings, no macro) in `tests/tsymex_g6_transform_binding.nim`: the headline affine inversion at the runtime-API level, `tA=1,tB=0` reproducing plain `cbDrawLinked` exactly, a conjunct genuinely constraining an exact-only flip attempt (UNSAT when contradicted, SAT when consistent), graceful out-of-range degrade. **C3** — the AST classifier: `fuzzmacro.nim` walks the CAPTURED (typed, macro-`typed`-param) strategy-construction AST — confirmed walkable by direct compile-time inspection (`s.map(f)` types as `nnkCall(sym"map", s, f)`; an inline `proc(x:T):U = expr` lambda arrives as `nnkHiddenStdConv` wrapping `nnkLambda`, single-expr body compiles to `nnkAsgn(sym"result", expr)`) — no escalation needed, the design gate cleared. `affineOf`/`predicateOf` (handles Nim's own `x>5` → `5<x` typed-AST rewrite via operand-order detection) classify `map`/`filter`'s fn bodies; `classifyBranching` recognizes a bounded 2-way `if`/`else`-expression `flatMap` (produces a real `dkBranching` descriptor for algebra purposes; WIRING it into a runtime binding needs the CONCRETE trace to pick the taken case, only known at bridge-invocation time — scoped out and documented, not silently dropped, falls back to `cbDrawLinked` same as opaque). `bindingExprFor` flattens the finished descriptor into a `ConcolicParamBinding` NimNode at macro-expansion time. Scope cut (documented): only the single-strategy/single-property-param shape is AST-classified this way; a multi-param property's N-ary applicative strategy expression keeps the pre-G6 positional classifier unchanged — the RFC's headline is single-param. Two incidental fixes surfaced (not silently patched around): a Nim macro-hygiene footgun (splicing a multi-statement `NimNode` into the middle of a `quote do` nests it as a scope-opening child `StmtList` — fixed by branching at the outer quote-do level, two self-contained blocks); and a pre-existing, previously-unexercised `collectBoundNames` gap (never bound the auto-generated `result` var, so ANY `fuzz(...)` caller passing an inline-lambda combinator was already rejected by capture-validation — G6's own headline was the first such caller to trip it). **Headline** (`tests/tfuzzconcolicbridge_g6_affine.nim`, real Z3, real `{.cover.}`, plain `import nelli`): a `stallRounds:1` campaign over `integers(0,1000).map(proc(x:int):int = x*2+1)` gated on `mapped==501` reaches `coverageHits==2`; the `stallRounds` default (0) stays at `1` — concolic inverts the affine map (`draw=(501-1)/2=250`) and breaks the gate, exactly the case the pre-G6 positional classifier could never reach (it solved for the wrong equation, `draw==501`, never satisfiable). **C4** — confirmatory, no prod changes: `tests/tfuzzconcolicbridge_g6_predicated.nim` proves a 2-combinator `filter(x>500).map(x+1)` chain (composes to `predicated(affine(1,1),[x>500])`) also breaks its gate (`mapped==601`) through the real macro, exercising `predicateOf`'s `>`-rewrite handling and conjunct-carrying-through-a-later-map live. SW NOT bumped throughout G6 (pure macro/AST-classification + Z3-constraint-building addition, no walker semantics touched — CR2 "73" pin verified green after every cycle). Full `tfuzz*`+`tdb` (49 files, `c`) green after C3 (no existing assertion touched), incl. `tfuzzconcolicbridge_real.nim` (the G3 C4 direct-draw headline, unaffected) and `tfuzzmacroreject.nim` (capture-rejection negative tests, unaffected) cpp-spot-checked; a representative `tsymex_*` sweep (phase15 C4/C6 HOF suites, phase14 strategy-digest, G1b/G2/G3fix/G4) green, plus confirmed the two pre-existing `tsymex_phase15_g8_multi_param`/`g10_smoke` `classifyType`/"node has no type" failures reproduce IDENTICALLY (not a new regression). **Track G is now fully DONE** (G6 verified 4/4). **S2 — DONE** (`6ef1e44` C1, `589498d` C2; the prior "IN FLIGHT: subagent a59ddacfb49c48292" note was a stalled agent — zero commits landed, only an uncommitted edit to THIS handoff file marking itself in-flight — this session took over fresh per the standing subagent-stall-takeover convention and did S2 in full). Operator bandit: replaces the uniform `mod pickMax` mutator pick with a discounted-UCB1 multi-armed bandit (`nelli/bandit.nim`, a new leaf module — `OperatorBandit`/`chooseOperator`/`credit`, depends only on `rng`). **C1** — the bandit core, proven standalone (`tests/tfuzzbandit.nim`) against a synthetic reward signal: discounted UCB1 (Garivier & Moulines), not plain UCB1 or MOpt's PSO swarm — chosen because plain UCB1's lifetime running mean is sticky long after the corpus outgrows an operator's productive region (the RFC's named non-stationarity failure mode), while discounting EVERY arm's (pulls, rewardSum) by `banditDecay=0.97` on every selection tick gives an effective ~33-pull sliding window without literally storing one. Exploration is UCB1's own bonus term (`c*sqrt(ln(totalPulls)/pulls[arm])`, `c=sqrt(2)`) PLUS an explicit cold-arm priority path: `pulls[i]` starts at exactly `0.0` and decay only ever multiplies existing mass, so a never-yet-pulled arm reads `pulls[i] == 0.0` on the nose (no epsilon needed) and gets absolute priority over any UCB score. All four of the RFC's RED-able properties proven directly on the bandit: higher-yield arm selected strictly more often; a persistently-zero-yield arm still gets nonzero selection in a trailing window (never starved); an arm that WAS productive but stops loses share to a newly-productive one within a bounded trailing window (non-stationarity); deterministic in the RNG seed. **C2** — loop wiring (`fuzz.nim`'s main loop): one `OperatorBandit` sized `pickMax` (5, or 6 with `enableI2S`'s I2S arm) constructed once per campaign; `chooseOperator` replaces `rng.next mod pickMax`; the chosen arm is `credit`ed `1.0` on the SAME `admit(...).admitted`/`vInteresting`/`vTimedOut` outcome the loop already computed (a rejected/no-yield pick is simply never credited — additive `credit`, so omitting the call is exactly a `0.0` reward). `FuzzSettings.uniformOperators` is the determinism-gating opt-out, polarity mirroring S1's `uniformSchedule` (bandit is the unconditional DEFAULT; `uniformOperators: true` reproduces the pre-S2 uniform trajectory byte-for-byte). Default choice was made EMPIRICALLY, not assumed: the full `tfuzz*`+`tdb` suite (49 pre-existing files, dual-backend `c`+`cpp` spot-checks) was run bandit-as-default BEFORE picking it as the default, including `tfuzzi2s.nim`'s exact `coverageHits == 1`/`== 2` seed-pinned headline gates (`0xDEADBEEF`, `"MAGIC"` bytes/string) — all green, no existing assertion touched or weakened, so no separate determinism gate was needed on any pre-existing test. `tests/tfuzzoperatorbandit.nim` adds S2's own loop-level coverage: bandit-default determinism, `uniformOperators` determinism, bandit-default coverage `>=` uniform-fallback coverage on an S1-style monotone-coverage fixture, and an explicit proof that the I2S arm (index 5) is genuinely reachable through `chooseOperator`'s arm space under `enableI2S` (not just present via the old forced `mod 6`). SW not bumped (fuzzer-scheduling only, no walker semantics touched — CR2 "73" pin verified green). **S3 — DONE** (`0e14ab5` C1, `ed5694c` C2). Havoc stacking + interesting-value tables, absorbing the FUZZ_PLAN D4 byte-havoc gap at the IR level. **C1** — three pure `fuzzir.nim` helpers, none yet wired into the loop: `interestingIntValues`/`mutateIRInterestingValue` (deliverable 2 — a boundary-value table for integer choice nodes: min/max/0/±1/min±1/max±1/every power of two up to the 62-bit bound `logScaledDeltasForWidth` already uses and its ±1 neighbors, clamped/filtered to the node's declared `[min, max]` and deduped); `mutateIRDictInsert` (deliverable 3 — the G5 dictionary as a STANDALONE insertion source, independent of the parent's own cmp log, unlike `mutateIRI2SReplace`'s fallback-only reach); `drawHavocStackCount` (deliverable 1 — geometric count in `[1, maxHavocStackOps=8]`, continuing with probability `havocStackContinueP=0.5` per step). **C2** — loop wiring (`fuzz.nim`): `FuzzSettings.uniformHavoc` is the determinism-gating opt-out (mirrors `uniformSchedule`/`uniformOperators`'s polarity) — `true` pins the stack count at exactly 1 and skips the geometric draw entirely (zero extra `rng.next` calls), reproducing the pre-S3 trajectory byte-for-byte, PROVEN via a golden-fingerprint comparison against the pre-wiring commit (three scenarios: plain settings, `uniformOperators`, `enableI2S` — exact match on iterations/coverageHits/corpus length in both directions, not just reasoned about). The default (unconditional) draws a geometric stack count each iteration and applies that many operators in SEQUENCE to the evolving mutant via a `MutationArm` enum + `arms: seq[MutationArm]` built once per campaign from `settings` (base 5 IR mutators; `+maI2SReplace` under `enableI2S`; `+maInterestingValue` unconditionally when not `uniformHavoc`; `+maDictInsert` under `enableI2S` when not `uniformHavoc`, since the dictionary is only ever populated when `enableI2S` is set). Every operator in a stack is credited on the stacked result's SAME admission/interesting outcome (documented choice — the RFC left this to the implementer: a stacked candidate's yield is a joint product of every op that touched it). `FuzzReport.totalMutationOps` is the new campaign-level observable (`== iterations` under `uniformHavoc: true`; `> iterations` whenever stacking actually stacked). **Default choice made EMPIRICALLY** (S1/S2's own precedent): the full `tfuzz*`+`tdb` suite (53 files) was run stacking-as-default BEFORE picking it — 52/53 green unmodified; the one failure (`tfuzzbias.nim`'s `maxIterations: 1` boundary-bias test) read the LAST `prop` call as a proxy for "the seed input," a technique that implicitly assumed "one iteration == one mutation op" — an assumption orthogonal to what that test actually exercises (seed-generation bias threading, unrelated to S3). Fixed by pinning `uniformHavoc: true` on THAT test (scoping away an unrelated new axis), not by loosening its boundary-set assertion — the check is exactly as strict. `tests/tfuzzhavoc.nim` (26 tests) covers all three deliverables bottom-up: pure-function tests for the table/dict-insert/stack-count-draw, then loop-level headlines — a `totalMutationOps` stacking-happened proof, a power-of-two boundary gate reached by the interesting-value table alone (unreachable under `uniformHavoc: true` at the same seed/budget), and a two-int single-campaign headline (`dictComboGate`) where node `b`'s gate is deliberately never `{.covercmp.}`-logged (a `case`, not `==`) so the ONLY path to it is dictionary-sourced insertion — reached under the default, NOT reached under `uniformHavoc: true` (arm5's own fallback is structurally blocked there by node `a`'s ever-present trivial self-match). SW not bumped (mutation-loop wiring only, no walker semantics touched — CR2 "73" pin verified green). Full `tfuzz*`+`tdb` (53 files, `c`) + cpp spot-checks on new/touched files verified green after both cycles. **S4 — DONE** (`f4cb921` C1, `b02af6d` C2). Continuous corpus culling: promotes `minimalCovering*` (fuzz.nim's one-shot end-of-run greedy set cover, kept as-is for `minimizeCorpus`) to a periodic IN-CAMPAIGN cull, AFL-style, using S1's own rarity/size signal. **C1** — `favoredIndices*` (fuzz.nim, pure, sits right before `minimalCovering`): for every covered slot, the covering entry with the highest S1 `entropicEnergy` (rarer edge / smaller / faster wins, ties to lowest index) becomes that slot's CHAMPION; the favored set is the union of every slot's champion; an entry that is champion for no slot is DOMINATED (its edges are all covered, at least as well, by a favored entry) — the RFC's domination test verbatim. A unique-edge entry is trivially its own slot's sole candidate and can never be dominated (proven directly, `tests/tfuzzcull.nim`). `FuzzSettings` gains `cullCadence` (0 → `defaultCullCadence = 50`, the "how often" knob) and `uniformCorpus` (the determinism-gating opt-out, polarity mirroring `uniformSchedule`/`uniformOperators`/`uniformHavoc`). **C2** — loop wiring: every `cullCadence` iterations the loop runs `favoredIndices` over the live corpus and compacts `corpus`/`corpusCov`/`corpusNanos`/`corpusSlots`/`corpusCmpLog`/`energy` down to the favored set in lockstep (skipped, not emptied, when nothing has any recorded coverage yet — `while corpus.len > 0` never sees an empty corpus). The reported/persisted corpus is rebuilt from the FINAL live corpus at loop end (not the raw incremental-append history), so a mid-run cull is reflected in `FuzzReport.corpus.irEntries` — a no-op whenever culling never fires. **Disk-eviction decision (deliverable 2, RFC's own escape hatch taken):** db.nim's `saveCorpus`/`dedupPrepend` stays UNTOUCHED and purely recency-based — it has no coverage signal, and `dedupPrepend` is SHARED with the `primary` regression-replay section, which has no coverage concept at all, so teaching it coverage-value would leak fuzz-domain concepts into a generic persistence layer non-fuzz callers also use. Instead the FUZZ LOOP decides what to re-persist coverage-awarely BEFORE calling `saveCorpus`: at each cull tick it re-saves the still-favored set, ORDERED least-to-most-valuable by `entropicEnergy`, so `dedupPrepend`'s own documented move-to-front-on-resave contract (db.nim ~352-368, "a re-save of an existing entry MOVES it to the front, refreshing its recency") lands the MOST valuable entries frontmost — safest under `corpusLimit`'s recency-only cap, even when the favored set itself exceeds `corpusLimit`. Gated by the same `uniformCorpus` flag, so the disk trajectory is also byte-for-byte pre-S4 when opted out. Proven in `tests/tfuzzcullpersist.nim`: a preloaded rare-edge SEED — admitted into the in-memory frontier by F2's up-front pass but, because its edge is already covered by the time the loop starts, never individually `saveCorpus`-able via ordinary fresh-discovery admission — reaches disk and SURVIVES a flood of dozens of redundant admits past a small `corpusLimit` under the S4 default, and never reaches disk at all under `uniformCorpus: true` (the pre-S4 gap this deliverable closes); a direct characterization test pins that `db.nim`'s `saveCorpus`/`dedupPrepend` itself is still pure-recency and untouched. **Default choice made EMPIRICALLY** (S1/S2/S3's own precedent): ran the full `tfuzz*`+`tdb` suite (51 pre-existing files, `c`) with culling default-on BEFORE picking it — one pre-existing test (`tfuzzschedule.nim`'s "minimization shrinks the corpus" test, over a strictly-nested-coverage fixture where periodic culling now ALSO collapses the "full"/non-minimized campaign down to one entry, correctly per S4's own contract, making its two campaigns converge) needed `uniformCorpus: true` pinned on its "full corpus" baseline — an unrelated new axis scoped away, same precedent S3 used on `tfuzzbias.nim`, no assertion loosened. `tfuzzmincover.nim` (the existing `minimalCovering` pin) stayed green throughout, unmodified. SW not bumped (mutation-loop/corpus-bookkeeping wiring only, no walker semantics touched — CR2 "73" pin spot-checked green). Full `tfuzz*`+`tdb` (51 pre-existing files + 2 new, `c`) verified green after both cycles; cpp spot-checks on all new/touched files (`tfuzzcull.nim`, `tfuzzcullpersist.nim`, `tfuzzschedule.nim`, `tfuzzmincover.nim`) green. **S5 (a+b) — DONE** (`bfd7a7b`, one commit: G2 was already done, so S5b's gate cleared and both parts landed together rather than splitting per the round-3 slice re-cut). AFL-`fuzzer_stats`-equivalent, READ-ONLY `CampaignStats` on `FuzzReport`, populated once at `fuzz[T]` loop exit from state the loop/`Orchestrator` already track: `execs`/`elapsed`/`execsPerSec`, `corpusSize`, `coverageEdges`, `respawnCount` (new `Orchestrator.respawnCount`, incremented at the one `run`-recycle site — always 0 through `fuzz()`/`fuzzWith()`, which never wires `spawnFreshWorker`), `stormTripped`/`stormBackoffLevel` (mirrors E-cleanup's existing accessors), `sinceLastCoverageAdmits` (`coverage.nim`'s existing `staleness(frontier.stats)`, no new tracking needed), `sinceLastCrashIters` (NEW: `lastCrashIter` tracked in `recordCrashIfInteresting`, the one site every crash observation already passes through), `crashCount` (mirrors `irCrashes.len`), `totalMutationOps` (mirrors S3's field), `cullCount` (NEW: incremented where S4's cull block's `anyKept` fires), `operatorPulls` (S2 bandit's own `pullsOf`, read-only). **S5b** — `provenanceCounts: array[Provenance, int]` (NEW: incremented in `recordCorpusGrowth`, the one site both the mutation-admit and concolic-bridge-admit paths funnel through) and `concolicYield: ConcolicYieldTotals` (NEW Z3-free erased-mirror type over G2's `ConcolicFlipCounters`/`ConcolicYieldCounters`, with a field-wise `+=`; `tryConcolicBridge` accumulates every bridge attempt's tally — solved or not — into `Orchestrator.concolicYieldTotals`; `fuzzmacro.nim`'s real bridge translates `concolicFlip`'s actual taxonomy into it, proven end-to-end by a new assertion added to the EXISTING real-Z3 headline `tests/tfuzzconcolicbridge_real.nim`). **Provenance-tagging fix (was silently missing):** the ordinary mutation-admit path never distinguished I2S-produced mutants from plain ones — `admit`/`recordCorpusGrowth` always got the implicit `pvMutation` default even when G5's I2S-replace operator won the havoc stack. Fixed minimally: the loop now checks whether `maI2SReplace` appears in the iteration's `picks` and tags `pvI2S` accordingly (pure lookup, no `rng` consumed, no trajectory change). Purely observational throughout — no scheduling/mutation/admission decision reads `CampaignStats`, confirmed by the full suite staying byte-identical. `tests/tfuzzcampaignstats.nim` (13 tests, two suites) proves every RED-able property against deterministic fakes (a "plateau" target for staleness-advances, an "ever-novel" target for staleness-resets-to-0, crash-every-call vs. crash-once-early for the crash timer, direct `Orchestrator.run` recycling for `respawnCount`, a fake concolic bridge for the S5b breakdowns) — no real Z3 needed for the mechanism proof, since G3's existing real-bridge test independently confirms the production translation. SW not bumped (CR2 "73" unchanged — additive bookkeeping only, no walker semantics touched). Full `tfuzz*`+`tdb` (56 files, `c`) green; cpp spot-checked on every touched/new file (`tfuzzcampaignstats.nim`, `tfuzzconcolicbridge_real.nim`, `tfuzzmacro.nim`); CR2 "73" pin spot-checked (`tsymex_r14_continue_guard.nim`). **S6 — DONE** (`1027615` C1, `690d030` C2). Learned-state checkpoint/resume: the corpus already survives a restart via `ExampleDatabase`, but S1 rarity/energy, S2 bandit weights, and G5's dictionary were pure in-memory `fuzz[T]`-loop state, cold-starting on every Ctrl-C/OOM/CI-timeout restart — the exact long-running workload the `fuzz` contract exists for. **C1** — `nelli/learnedstate.nim` (new leaf module): `LearnedState` (frontier `hitCounts`/`lastImprovedSeq`/`totalAdmitted`/`lastGlobalImprovedSeq` — G3's staleness counter included since it lives on the SAME `FrontierStats` object S1 owns; bandit per-arm `pulls`/`rewardSum`/`totalPulls`; the harvested `Dictionary`) with a versioned (`"NLS0"` magic + u16) header — a version mismatch or any corrupt/truncated input decodes to `ok: false` (cold start), NEVER raises, mirroring E0/E3b's corpus-format discipline. `coverage.nim`/`bandit.nim` gained `frontierStatsSnapshot`/`restoreFrontierStats` and `banditSnapshot`/`restoreOperatorBandit` accessor pairs since both types keep their state private. **Stated limitation (S4's own pattern, not silent):** the favored-set is NOT persisted — it's a pure function of `FrontierStats` + the corpus (both already restored/resumed independently), so a resumed campaign just re-derives it at its own next cull tick; persisting favored INDICES would be meaningless anyway since a resumed corpus reloads in `loadCorpus`'s order, not the prior run's live `corpus` seq. **C2** — `FuzzSettings.checkpointCadence` (`0`, the default, disables checkpointing ENTIRELY — no load, no save, gating on the SAME `!= nil` convention `loadCorpusActive`/`saveCorpusActive` use — so every pre-S6 trajectory is byte-for-byte unchanged, this flag touches no `rng` state either way). `db.nim`'s `ExampleDatabase` gains `saveSchedImpl`/`loadSchedImpl` (`saveSched`/`loadSched` wrappers) across all four backends — the directory backend as a sibling `<safeKey>.sched` file (atomic tmp+rename, covered by the existing `.tmp.` sweep), in-memory as a `Table`, multiplexed prefers primary then falls back to secondary, read-only raises on save — a single overwritten blob, not the corpus's append-log/generation machinery, since a campaign has exactly one current checkpoint. The loop restores frontier stats + dictionary BEFORE the F2 preload-replay pass folds its own admits on top (so restored state isn't clobbered by that pass), and restores the bandit separately once `arms`/`opBandit` exist, ONLY when the checkpoint's arm count matches the current run's `arms.len` (arm index is positional — a settings change like `enableI2S` toggling since the checkpoint was written is treated as "no checkpoint" for the bandit specifically, while frontier stats/dictionary still resume). Checkpoints save every `checkpointCadence` iterations plus once more at campaign end. `tests/tlearnedstate.nim` proves the round-trip + every ignore-on-mismatch path (bad magic, future version, truncated, corrupt dict-entry kind) at the codec level; `tests/tfuzzcheckpoint.nim` proves loop-level resume-warm (bandit pull-mass/frontier `totalAdmitted`/dictionary size each provably above what one cold iteration could produce — e.g. pull-mass > `maxHavocStackOps`=8, the hard ceiling one iteration's own stacking could ever contribute), a fresh `persistKey` cold-starting, a corrupted checkpoint cold-starting without crashing, `checkpointCadence: 0` never touching the checkpoint at all, and the arm-count-mismatch safety path. SW not bumped (no walker semantics touched — CR2 "73" pin verified green). Full `tfuzz*`+`tdb` (57 files, `c`) green; cpp spot-checked on `tlearnedstate.nim`/`tfuzzcheckpoint.nim`.
**Track U STARTED.** **U0 — DONE** (`30d2e86`). Investigation first: contrary to the RFC's ground-truth-1 framing ("forAll... a property Defect is fatal to the whole run"), `engine/eval.nim`'s `evalReplay` (the shrinker/DB-replay/symex-seed/targeted-phase's ONE shared "try this candidate" call) has caught `Defect` at its `prop(x)` call site since `05187da` (2026-05-28, three months pre-RFC) — as have `phases.nim`'s `randomPhase`/`explicitExamplesPhase` (their own inline generate+prop try/except) and `shrinker.nim`'s `tryFalsifies`. The RFC's claim is accurate ONLY for `--panics:on` builds (`engine.nim`'s own standing warning says exactly this — under panics:on a Defect is unconditionally fatal/uncatchable by Nim's own design, no in-process boundary can change that; dt-bounded.sh's default build is panics:off, where the catch already fires). So the real, verifiable U0 gap wasn't "Defects abort the run" (already false) but architectural: forAll's crash handling was scattered ad hoc string-formatting across 4 sites, never unified with the typed `CrashInfo`/`CrashKind` identity Track E's `fuzz.nim` Worker/Observation machinery already uses ("one engine," crash identity included, not just execution). **Fix:** moved `CrashKind`/`CrashInfo` out of `fuzz.nim` into a new leaf module `nelli/crashinfo.nim` (pure type move, `fuzz.nim` re-exports — zero behavior change for existing `fuzz` callers) so `engine/*` can share the identical type without a fuzz↔engine import cycle (`fuzz.nim` itself imports `engine`). `Report[T]`/`RawFalsification[T]`/`Eval[T]` gain `crash: Option[CrashInfo]`, populated ONLY in each site's `except Defect` branch (FalsifiedError/CatchableError falsifications are untouched — "not a behavior change to normally-falsifying runs" per the slice's own contract) — `evalReplay`, `randomPhase`, `explicitExamplesPhase`, and `targetedPhase` (via `runTargetedPhase`'s `handleFalsification` callback, extended with a `crash` param) all populate it identically; `shrinkPhase` RE-DERIVES `shrunkCrash` from the shrunk candidate's own re-`evalReplay` (mirrors how `shrunkNotes` already works) so the minimal REPORTED example's own crash classification is authoritative, not the originally-found (possibly larger) example's. Did NOT literally route through `fuzz.nim`'s `Worker[T]`/`newInProcessWorker` (`submit`'s own doc comment anticipated this) — `choiceSeqTargetWorker`'s internal `observeInProcess` unconditionally resets coverage/cmp-log on every call, which would corrupt `#107` coverageGuided forAll's own before/after `currentCoverage()` delta wrapping (a live, real conflict, not hypothetical — traced through `runForAllPipelineWithPhases`'s coverage wrap in `engine.nim`); U1 (unifying the coverage model itself) is the right slice to revisit literal Worker routing once that conflict is structurally resolved. Documented as a confident-recommendation resolution, not an escalation. **RED→GREEN** (`tests/tengine_crashisolation.nim`, 5 tests, first-try compile-RED on the missing `Report.crash` field confirmed before implementing): a failed `doAssert` and an `IndexDefect` both shrink to their minimal falsifying example with `crash.get.kind == ckException`/`defect` naming the raising type; a same-seed determinism pin (identical outcome/counterexample/choices/crash across two runs); an `ensure` falsification and a passing run both confirmed `crash.isNone` (pre-U0 behavior pin). Full `tfuzz*`+`tdb` (60 files) + `tengine.nim`/`tengine_crashisolation.nim` verified green on `c`; cpp spot-checked (`tengine.nim`, `tengine_crashisolation.nim`, `tfuzzcrashinfo.nim`, `tfuzzworker.nim`, `tfuzzmacro.nim`); CR2 "73" pin green (`tsymex_phase15_CR2_cachekey.nim`, `tsymex_r14_continue_guard.nim`) — no walker touch, SW not bumped. No existing assertion touched or weakened.
**U1 — DONE** (`9526460` C1, `1b80431` C2, `f0ae95c` C3). Unify the coverage model + score/admit split (RFC lines ~1235-1250). **C1** — `CoverageFrontier.score` (added in E3a for the Orchestrator's re-verify pre-filter, Appendix C's non-mutating peek query) already had the exact non-mutating/idempotent contract U1 needed for forAll's targeted-phase scoring, but nothing exercised it directly; `tfuzzfrontier.nim` gained a dedicated suite proving it's stable across repeated calls, never mutates `f`, and correctly reads 0 once `admit` has actually folded the same coverage in — contrasted directly against `admit`'s own first-time-only collapse. No production change (score's E3a implementation already satisfied the property) — this is the "safe, self-contained half" the brief called out to do first. **C2** — routed `forAll`'s `coverageGuided` prop-wrap (`engine.nim`) off the old `currentCoverage() - before` raw scalar delta onto the SAME bucketed `CoverageFrontier` model `fuzz.nim` uses (a per-run-local frontier instance, not a shared one — "same model," not same instance): each call snapshots its coverage, peeks the Pareto-visible value via `score` (repeatable across the targeted phase's hill-climb/SA re-scores without collapsing to 0 on a re-ask — the hill-climb's own `iter` sweep can re-propose an unchanged Pareto entry's identical perturbation on a later outer iteration), then folds it via `admit`. `Report.coverageHits` now comes from the frontier's `coveredEdges()` (set once, post-pipeline, in `engine.nim`) rather than a second raw-bitmap read in `phases.nim`'s `finalizePhase` — one fold site, not two (`phases.nim`'s `import ../coverage` dropped, now unused there). The in-process `{.cover.}` bitmap is inherently binary (first-hit-only, never a true multi-bucket count), so this is numerically EQUIVALENT to the retired scalar computation for every existing scenario — proven by the full pre-existing `tcovguided.nim` suite staying green unmodified; new coverage pins the admit-fold discipline through the real `forAll` path (a singleton-strategy repeat-heavy run must land `coverageHits == 1`, not drift upward across redundant re-admits). Also closes the U0 handoff note's speculative concern about a future literal `fuzz.nim` `Worker`/`observeInProcess` routing: `observeInProcess`'s unconditional per-call `resetCoverage()` no longer conflicts with anything, since `score`/`admit` work identically whether fed a cumulative-since-run-start snapshot (what forAll does today) or a genuinely per-call-isolated one (what `Worker` would produce) — structurally unblocked, though literal Worker-routing itself stays out of U1's scope (not named in the RFC's U1 bullet; a future slice's call if wanted). **C3** — deduped the integer-perturbation kernel: `fuzzir.nim`'s `mutateIRPerturbInteger` and `engine/targeting.nim`'s hill-climb each carried an identical `±2^k` log-scaled-delta copy, split only because `fuzzir.nim` needed to stay a leaf (no engine deps) with no shared home before E1's `Worker`/`Pool` seams + U0's `crashinfo.nim` pattern resolved that class of fuzz↔engine sharing problem. Extracted to a new dependency-free leaf module `nelli/intdeltas.nim`; `engine/targeting.nim` re-exports it (`logScaledIntDeltas` was already part of its `import nelli` public surface, consumed directly by `tests/twiderange.nim`) so no external caller needed to change; `fuzzir.nim`'s private duplicate is deleted outright. `tests/tfuzzir.nim` gained a cross-check that `mutateIRPerturbInteger`'s chosen delta is always a member of the public `logScaledIntDeltas(width)` set (or lands on a clamped range boundary) — proving both call sites draw from the one kernel; behavior unchanged (all pre-existing assertions in `twiderange.nim`/`tfuzzir.nim`/`ttarget.nim` stayed green, unmodified). SW not bumped throughout U1 (fuzzer/engine-scoring-layer + a pure module relocation, no walker semantics touched) — CR2 "73" pin verified green after every cycle. Full `tfuzz*`+`tdb`+`tengine*` suite (59 files) green on `c` after each cycle; cpp spot-checked on every new/touched file (`tfuzzfrontier.nim`, `tcovguided.nim`, `tengine.nim`, `tengine_crashisolation.nim`, `treservedlabel.nim`, `tfuzzir.nim`, `twiderange.nim`, `ttarget.nim`). No existing assertion touched or weakened.
**U2 — DONE** (`088ea26`, one commit). `forAll`/`given` replay-only corpus reads (RFC lines ~1251-1254). New `corpusReplayPhase[T]` (`engine/phases.nim`), placed **dbReuse → explicit → corpusReplay → random** — after user-pinned explicit examples, before blind random generation — mirroring `dbReusePhase`'s per-entry `evalReplay` loop but STRICTLY READ-ONLY (no `removeMany`/pruning/re-save on a pass/reject; corpus admission stays exclusively `fuzz`'s job, per the RFC's "two front doors" split). `db.loadCorpus(testId)` is called exactly ONCE per run (the RFC's "snapshot the corpus once at run start" policy) — `loadCorpus` itself already reads through E3b's generation-pointer cut point, so no extra snapshotting machinery was needed at this call site. Self-gates on `dbEnabled` and prior falsification, same convention as every other source phase — empty corpus / no DB is a pure no-op, proven directly (not just reasoned about). `fromPhase: "corpusReplay"` feeds `finalizePhase`'s existing message-prefix switch (`"from fuzz corpus"`) and is counted into `Report.dbReplays` alongside primary replays (a confident-recommendation call: both are literally "found via a DB replay," and no existing test's `dbReplays` assertion is scoped to primary-only under a corpus-bearing testId). **Read-only proof:** `sectionSizes(...).corpus` identical before/after a run that finds-and-shrinks a falsifying corpus seed. **Determinism pin:** two full runs (3 corpus entries + 5 random examples, same seed/db) produce byte-identical per-call value traces, with the corpus prefix replayed most-recent-first (matching `loadCorpus`'s own documented order) both times. **No-op equivalence:** confirmed both for no-DB and DB-with-empty-corpus. Real bug found+fixed while running the full sweep: `tests/tdbbackends.nim`'s hand-rolled `failingDatabase()` fixture left `loadCorpusImpl` nil (implements only primary/secondary) — harmless before U2 since nothing called `loadCorpus` through the engine, but a nil-closure call is a SIGSEGV, not a `DbError`, once `corpusReplayPhase` reaches it unconditionally on every `dbEnabled` run; fixed by adding the missing closure (raises `DbError`, matching the fixture's existing style) — the only such incomplete fixture in the tree (grepped for other hand-built `ExampleDatabase` literals, none found). No settings flag/gate was needed: grepped every `forAll(`/`forAllUsing(` call site against every `saveCorpus` call site across the suite — no existing test exercises `forAll` against a testId with corpus entries except `tfuzzcovcorpus.nim`'s own F1 suite, and none of those assert an exact `dbReplays`/example-count/order that corpus-replay would perturb (confirmed by the full sweep staying green). Full `tfuzz*`+`tdb`+`tengine*` (61 files, `c`) + `tpipeline.nim` green; cpp spot-checked (`tengine_corpusreplay.nim`, `tdbbackends.nim`, `tdb.nim`, `tfuzzcovcorpus.nim`, `tengine.nim`). SW not bumped — CR2 "73" pin verified green (`tsymex_phase15_CR2_cachekey.nim`); engine/DB wiring only, no walker semantics touched.
**U3 — DONE** (`16c2b0a` C1, `e74a222` C2). Byte-mode demotion (RFC lines ~1255-1259). Investigation first: `tests/tfuzzbytes.nim` — the file the RFC's own U3 scoping assumed tested "the byte-mutation kernel" — actually tests something else entirely (`newReplaySourceFromBytes`/`fuzzOnce(s, prop, bytes)`, the single-shot bytes-as-DataSource decode that libFuzzer/AFL harnesses call per-input); it never referenced `fuzzWithBytes`/`fmBytes`/`mutationMode`, so it needed **zero** edits and stays exactly as-is — the sanctioned "remove the byte-mode-fuzzing assertions" clause had nothing to act on in that file. The actual coverage-guided byte-mutation kernel (`fuzzWithBytes`, dispatched via `FuzzSettings.mutationMode`/`FuzzMutationMode.fmBytes`) had no dedicated test at all. **C1** — added the byte-corpus-in interop half first (RED→GREEN, `tests/tfuzzinterop.nim`): `importCorpusDirAsIR[T](s, dir)` reads a directory exactly like `importCorpusDir` then decodes each entry through `s`'s existing byte-mode `DataSource` (the same fixed-width decode `fuzzOnce` already used) into an IR choice sequence — `DataSource.recorded` is populated identically whether `bytesMode` or not, so this needed no new decode machinery, just wiring — suitable for `FuzzSettings.initialIRCorpus`; a too-short entry is dropped (mirrors `fuzzOnce`'s `foRejected`). Proven end-to-end: decoded seeds actually replay to the right value AND work as real `fuzz()` seeds (`droppedSeeds == 0`, corpus grows from them). **C2** — removed `fuzzWithBytes` (its own ad hoc corpus/admission loop entirely OUTSIDE `fuzz`'s `Orchestrator`/`CoverageFrontier` — no crash de-dup, no Entropic/Pareto scheduling, no havoc stacking, none of Tracks S/G; confirmed via grep that nothing else in the tree called it), its private `mutateByteFlip`/`mutateByteReplace`/`randomBytes` helpers, and the `settings.mutationMode` dispatch in `fuzzWith` — `fuzzWith` now runs the (former `fuzzWithIR`) IR kernel unconditionally, merged into one proc. `FuzzMutationMode` is REMOVED OUTRIGHT (not stubbed) along with its now-dead `FuzzSettings` fields (`initialCorpus`, `initialInputSize`) and `FuzzReport.crashes` (byte-mode's crash-repro field, unread anywhere in the tree). `FuzzCorpusKind`/`FuzzCorpus` keep `fckIR` as a now-single-value enum rather than collapsing to a bare `seq` — several existing suites (`tcoverage.nim`, `tfuzzloop.nim`, `tfuzzstopcrash.nim`, `tfuzzir.nim`) already assert `report.corpus.kind == fckIR`, and this way those assertions keep compiling and meaning the same thing, at zero cost, rather than forcing an unrelated edit to 4 test files for a cosmetic reshape. The spec'd-but-absent byte-level `dictionary` extension point (D4, `docs/fuzz/INTERFACE.md:145`) is confirmed dropped, not built — nothing in the tree ever implemented it, and it's superseded by S3/G5's IR-level auto-dictionary (`FuzzReport.dictionary: Dictionary`, gated by `enableI2S`); `INTERFACE.md:135`/`:145` doc/code drift reconciled to match (stale `fuzzWithIR`/`fuzzWithBytes` pairing language + the phantom byte-mode `dictionary` field, both corrected with an explanatory note rather than silently deleted). Two pre-existing tests needed edits as a **direct, mechanical consequence** of the field/type removal, not a weakening of what they check: `tfuzzir.nim`'s `check settings.mutationMode == fmIR` line (the field no longer exists — deleted) and two `tcoverage.nim` comments referencing `fmIR`/the byte corpus by name (reworded; the assertions themselves, `r.irCrashes.len >= 1` and `r.corpus.kind == fckIR`, are untouched). No dead code left: grepped clean for `fmBytes`/`fuzzWithBytes`/`FuzzMutationMode`/`.mutationMode`/`initialInputSize`/`fckBytes`/`byteEntries` as live symbols — every remaining hit is inside an explanatory "this used to exist, here's why it's gone" doc comment. SW not bumped — CR2 "73" pin verified green (`tsymex_phase15_CR2_cachekey.nim`); fuzzer-only, no walker touch. Full `tfuzz*`+`tdb`+`tengine*`+`tcoverage.nim` (61 files) green on `c` after each cycle; cpp spot-checked on every touched file (`tfuzzir.nim`, `tfuzzinterop.nim`, `tcoverage.nim`). No existing assertion weakened (the two mechanical rewrites above check the identical behavior as before; no coverage was removed anywhere).

**Track U complete (U0-U3, all DONE).** This was the last Linux-verifiable slice — the rest of the open work (Eci, E4a-c) is Windows-specific and held for the Windows verification channel per the E-cleanup note above. **26/30 slices done.**
<!-- superseded detail below retained for history -->
_E3b build detail:_ **C1-C4 COMMITTED** (`f5590d1` corpus delta-log transport, `0c5f2ef` tombstone+size-triggered compaction, `77fa3ff` generation-files+head-pointer+snapshot cut point, `3580e32` `.bin` single-writer funnel + F-1 invariant). **C5 (versioned-header rule) UNCOMMITTED** — the agent orphaned its final suite-waiter; db.nim has ONLY a comment reflow (version-check logic already shipped in C1's reader) + `tests/tdbcorpuslog.nim` +63 lines of version-rule tests (newer→refuse naming both versions; unknown magic→refuse; older→read+migrate-on-compaction). **ON RESUME: if bg verify `byuciwuym` (tdbcorpuslog/tdb/tdbbackends/tfuzzcovcorpus/tfuzzpersist/tfuzzdbfunnel/tfuzzloop on c) is GREEN, `git add src/nelli/db.nim tests/tdbcorpuslog.nim && git commit` C5 (`feat(fuzzer): E3b C5 - versioned corpus-log header rule`), then run dual-backend confirm and mark E3b DONE.** **Next after E3b: E-cleanup** (resource lifecycle + steady-state respawn-storm breaker), then Eci/E4a-c/E5, then Track G (G1a first).
- **Resume (stage 3):** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; after each slice report one progress line; stop when every slice is implemented`
- **E0 decision record:** `docs/RFC-fuzzer-nextgen.E0-findings.md` (throwaway spike lives in `scratchpad/e0_corpus_sync/`).
- **Safe to `/compact`** at any slice boundary. After compact, re-read this doc + MEMORY.md before continuing.

### Round 3 — 4-lens team in; all clear-best fixes APPLIED, no forks
User asked for an extra hardening round beyond the mandatory two. Two anchors:
**(1) orchestrator execution model made explicit** — one single-threaded
completion loop; completion-oriented `Worker` seam (no thread-per-worker); the
Z3 bridge, fresh-spawn re-verify, and `reproRate` sampling all run OFF the hot
dispatch path on bounded slot budgets like shrink; **read-before-redispatch** is
a named+pinned per-worker invariant (the shm generation word is a single check,
not a seqlock — safety rests on this). **(2) Appendix C admission surface
corrected** — three code defects: `AdmitResult` baked async-updating fields
(`reproRate`/`divergentReproduction`) into a one-shot return (→ read by
`FindingId` handle); `Observation` lacked the `ChoiceSeq` re-verify must replay
(→ `admit(input, candidate)`); `CrashInfo` was illegal Nim (common field after
`case`). Also: **`Pool`→`Orchestrator`** (type name; prose uses both); **`wouldAdmit`→`score`**.
Applied too: dedup indexes kind→findings-observing-it-as-primary-OR-variant
(closes a second `divergentReproduction` fragmentation path); macro adds a
best-effort **impurity denylist** on captured initializers (env-impurity residual
= stated limitation — the one Critical: scope-check ≠ purity-check); **E0 gains**
Windows-safe compaction rename + shrinker-vs-shrinker race + corpus-format
version tag; **G6 algebra closed + `∘` directed + `branching` descriptor** for
enumerable-flatMap; steady-state **respawn-storm breaker** (E-cleanup); config
surface section; S1 owns shared `FrontierStats` (G3 consumes); shmProbe reset is
worker-internal (capability flag). **Slice re-cuts:** E3→**E3a/E3b** (E3b sized
after E0), G1→**G1a/G1b**, S5→**S5a/S5b** (S5b G2-gated), new **S6** (learned-state
checkpoint), E1 spike strengthened (real dispatch + generic case + E's 2nd
capability), E2a N=1-until-E2b coverage pin, Eci greppable capability-flag +
E4a Linux-testable seam, SW pre-flight adds commit-time re-check + claim marker.
Full per-lens detail in the four round-3 task transcripts.

### Round 2 — 4-lens team in; all clear-best fixes APPLIED, no forks
**Anchor decision:** topology resolved to **centralized orchestrator** (Pool =
one process owning the single in-memory frontier/corpus/dedup/scheduler + the
one Z3 bridge; workers are dumb execution seams; admission single-writer).
Supersedes round-1's contradictory "AFL `-M`/`-S`" gloss (federated `-M`/`-S` =
out-of-scope cross-node #112). Restores fidelity to fork-2's own "one shared
corpus" words → clear-best, not an escalation.
Also applied: bounded-async **reproRate** N-of-M sampler (was undefined); first
**report immutable** + re-verify kind-mismatch → `divergentReproduction` (dedup
fragmentation fix); **E0 retargeted** — under single-writer orchestrator the
real race is orchestrator-vs-shrinker + orchestrator-vs-forAll-reader on the
**shared per-testId db file**, plus mandatory **delta-log compaction** design;
**macro split into two contracts** (syntactic AST for G / reconstructible
constructor for E) with **compile-time free-identifier check**, **argv
call-site-ID dispatch** (not env var), isolated-proc reconstruction, bootstrap
circuit-breaker; **shm = push/copy double-buffered** w/ atomic generation word
(not zero-copy directly-mapped — infeasible for clang counters); **`fork()`
gated to single-threaded builds**, CRIU named; **yield metric typed by walker
construct-kind** (+ superseded / intended-vs-unrelated buckets); **G6 adds
`predicated`** (filter) + chain-composition algebra; **optimistic solving
bounded** (maxAttempts + Z3 timeout); **forAll gets crash isolation** via
in-process Worker (U0); **wouldAdmit** name; ablation-harness cadence/tolerance/
per-platform policy; SW-bump **pre-flight mechanical check**; campaign
observability surface (S5); resource-leak cleanup (E-cleanup); seed provenance.
**New/re-cut slices:** E2→**E2a/E2b**, **E-cleanup**, **U0**, **S5**, **G3b**.
Full per-lens detail in the four round-2 task transcripts.
- **Escalation RESOLVED (opaque-closure boundary):** Corey — pre-v1, breaking
  changes fine if improvements; best-in-class whatever it ends up being. Design
  call taken: **the concolic-capable entry is a `macro` that captures the
  property (and strategy-construction) expression at the call site.** One
  decision solves both tracks — Track G gets the property AST for named procs
  AND inline literals (only opaque closure *values* threaded through
  indirection are sfNotApplicable); Track E gets worker reconstruction with NO
  new user-facing API (same macro emits an env-var-gated worker-mode entry
  that re-runs captured construction). Folded into §Open items (RESOLVED),
  Track G/E inline notes, E1 (now macro entry, size S→M), E4a, ADR D1/D3,
  Risks. Supersedes the grill's "symexFind API untouched" framing.

### Round 1 — all four lenses in; clear-best fixes APPLIED to the RFC
Applied: Worker/Pool seam split + AFL -M/-S topology; coverage via existing
CoverageProbe (shmProbe); typed CrashInfo + vResourceExceeded; §0 precondition
(re-verify gates admission not reporting; fresh obs authoritative); parked-
snapshot-captured-once; db.nim RMW race named + E0 spikes real backend w/ 3
candidate serializations + shared crash-dedup; live-mapped-shm vs signal-dump
+ nelli_cov.c per-input reset as E2 deliverable; clang-cl/coverage-blind
Windows fallback; slices re-cut (Eci Windows-CI-toolchain; E4→E4a/b/c; E3 test
split structural-RED vs numeric-ablation; E5 Windows persistent-mode caveat);
Track G edge-selection (frontier selects entry, walker identifies branch),
additive-mode corrected to threaded-through-dispatch, transparency descriptor
= G6, G-cmp broadened to bytes/string + identity-flow boundary; G3→S1 dep;
S4 governs persisted eviction; Track U snapshot policy + peek/admit split;
non-goals (no trajectory-reproducible parallel campaigns; processes-not-
threads); G/U1 SW mutual serialization; ADR D1–D4 updated; hybrid draft marked
SUPERSEDED. Full per-lens detail in the four task transcripts.
- **RFC:** `docs/RFC-fuzzer-nextgen.md`. **ADR:** claims **ADR-0031**
  (0030 taken by parser-normalization). **Umbrella issue #158 filed.**
  #151 commented with the fold-in disposition (worker-pool + byte-havoc
  → #158; hygiene items stay in #151).

## What this RFC is
Next-gen fuzzer from the 2026-08-14 `/grill-me ideal:` session. Four tracks:
- **E (Executor)** — portable persistent worker pool (Windows first-class),
  crash isolation, shm coverage, freshness by recycling + pristine
  re-verification; POSIX fork = recycling policy. PREREQUISITE for all.
- **G (Guidance)** — choice-space concolic bridge (new walker mode,
  draws-as-symbols, aggressive concretization, models ARE choice
  sequences) + IR-level cmp-correspondence (typed RedQueen) + auto-dict.
- **S (Scheduling)** — Entropic energy, operator bandit, havoc stacking,
  continuous culling.
- **U (Unification)** — one coverage frontier for fuzz + forAll; forAll
  gains replay-only fuzz-corpus reads; byte-mode → interop-only.
Hard order E→G; S/U interleave after E. Evaluation = committed ablation
harness (per-track exit gates) + informal scoreboard + one-time byte
comparison at E exit.

### E0 outcome (2026-08-25) — decision in `docs/RFC-fuzzer-nextgen.E0-findings.md`
Spike empirically confirmed the **append-only delta log**: 87% baseline lost-update
loss vs 0% for the log; log out-scales an advisory `flock` RMW ~70–115× and does not
degrade under contention. Corpus **split** to `<key>.corpus.log`; `.bin` writes funnel
**single-writer through the orchestrator** (kills shrinker-vs-shrinker); compaction via
**generation-file + head-pointer + reader-lease** swap (POSIX-safe by test, Windows-safe
by construction); versioned log header (refuse-on-newer / migrate-on-older; legacy
single-file corpus externalized at U3). Two findings fold forward: **F-1** orchestrator
holds ONE long-lived DB handle (concurrent constructors race on tmp-sweep); **F-2** log
also sidesteps the backend's whole-file-rewrite O(n²) cost. **E3b now sized M.** Spike
code (throwaway, untracked) in `scratchpad/e0_corpus_sync/`.

### E1 stage-2 finding (walker-ingestion, verified in code 2026-08-25)
The AST proof-spike's RFC premise is partly wrong — corrected (clear-best, not a fork):
the symex walker ingests **lowered `IRStmt`** derived from a **typed proc symbol**
(`getImpl`→`parseProc`→`walk`, `smt/runtime.nim:5939`/`:8499`); its ONLY door is
`fn: typed` (`symexForAll` symex.nim:461, `symexFindAllWitnesses` :1627). There is **no
entry that ingests a raw captured AST**, and **no `wmFollowConcrete` mode exists yet**
(single symbolic mode; Track G adds concrete-follow later). Consequences for E1 stage 2:
(a) the `fuzz(...)` macro must capture the property as an **emittable typed proc symbol**,
not an untyped AST blob — that is the shape Track G's walker consumes; opaque closure
*values* that can't be so presented stay `sfNotApplicable` (already the RFC's rule).
(b) the spike targets the **real single-mode `walk`** with a bounded step budget, not a
nonexistent stub mode. (c) generics are fine — `instKeyFor` (dsl_parser.nim:5262) already
carries the bodyHash+typeSubst collision fix; a generic `Strategy[T]` flows via
`s.getTypeInst()`. Fold this correction into the RFC E1 text when stage 2 is briefed.

### E1 — DONE (commits 768e554, d0d58d9, 26b3a5f, fbe8322, b8e3aef, 50577a6)
- typed `CrashInfo` (matched on `kind`, `message` derived) + `Observation.crash`.
- `Worker[T]`/`Orchestrator[T]` seams; Worker is the load-bearing in-process seam,
  `ChoiceSeq` currency (generate happens inside `Worker.submit`); Orchestrator drives
  a single Worker + owns admission (opaque `admit(input, candidate): AdmitResult`).
- `fuzz(...)` call-site macro (`src/nelli/fuzzmacro.nim`): captures strategy+property
  construction; lifts inline props to named proc symbols (walker-ingestible);
  in-process worker-mode re-entry + call-site-id registry (`runWorkerReentry`);
  compile-time capture checks (free-identifier + impurity denylist).
- C7 freeze-guard (`tests/tfuzzmacro_astspike.nim`): PROVEN that a macro-lifted proc
  symbol AND a generic `Strategy[T]` (+generic callee at 2 inst types) both reach and
  run the real walker (`symexFindAllWitnesses`/`symexForAll`→`getImpl`→`parseProc`→
  `walk`), bounded. Capture point validated — safe for the 7 downstream E-slices.
- **Verified GREEN**: agent 60/60 (30 files × c/cpp, twice) + independent 18/18
  dual-backend sweep, 0 fail/hung, no existing assertion edited.

### E2a — POSIX persistent worker — DONE (commits `6f684ea`, `ea0a3df`, `06ab9e2`, `910df1d`)
All four cycles landed, each its own GREEN commit, full `tfuzz*` + `tdb` on `c` green
after every cycle (32/32 files, no existing assertion edited). Files added:
`src/nelli/fuzzworker.nim`, `tests/tfuzzworkerprocess.nim`; touched: `fuzzmacro.nim`
(worker-mode branch in the macro expansion), `fuzz.nim` (`newWorker`/`newOrchestrator(worker,
frontier)` general constructors), `nelli.nim` (exports `fuzzworker`).
- **C1** (`6f684ea`): argv `--nelli-worker=<id>` dispatch + genuine self-re-exec
  (`getAppFilename()` + `fork`+`execvpe`, mirroring `runChild`'s no-GC-between-fork-
  and-exec discipline) + versioned framed pipe protocol (`magic|version|len|payload|
  checksum`, 16 MiB max-frame bound checked BEFORE the read, mirrors PCOV). Reconstruction
  sentinel (round-3 DoD): a strategy constructor bumps a process-local counter; parent's
  own front-door call leaves it at 1 in the PARENT before the worker spawns, so a COW
  fork-without-exec would inherit 1 and report 2 after one more construction, while a
  genuinely fresh process reports exactly 1 — the child's crash-message payload carries
  the marker back. Found+fixed a real obstacle: `std/unittest` treats every argv token as
  a test-name glob filter, so a `--nelli-worker=<id>` child had every `test:` silently
  filtered out; fixed via `unittest.disableParamFiltering()`.
- **C2** (`ea0a3df`): worker publishes its `{.cover.}` bitmap to `$NELLI_COV_FILE` in the
  SAME PCOV wire format `nelli_cov.c` uses (one reader, `parseCoverageMap`, serves both),
  gated to fire at most once per process (`dumpCoverageOnce`, mirrors `pt_dumped`) — this
  IS the N=1 recycle-policy mechanism, pinned by a characterization test (a worker forced
  via the `NELLI_WORKER_MAX_INPUTS` knob, off by default — the E2b seam — to serve 2 inputs
  shows the dump reflects only the first). Found+fixed a real bug: `spawnWorkerProcess` was
  forwarding `envPairs()` unfiltered, so a worker that itself spawns a nested worker (an
  artifact of an earlier test-file design with >1 `fuzz(...)` call site — since fixed by
  collapsing to ONE call site, reused across tests via `nelliLastFuzzCallSiteId`, which also
  eliminates a cascading-recursion trap any future multi-call-site worker test would hit)
  leaked its OWN inherited `NELLI_COV_FILE` into the child.
- **C3** (`06ab9e2`): `observationForDeath` maps a worker that died without answering
  (segfault/uncatchable signal) to `vCrashed`/`CrashInfo` via `reapWorker`'s exit-status
  decode. Two tests: a `kill(getpid(), SIGSEGV)` trigger (a Nim nil-deref is NOT reliably
  uncatchable under checked builds — `NilAccessDefect` is a `Defect` `observeInProcess`
  already catches, which would've defeated the test) shows the pipe read comes back
  cleanly empty, `reapWorker` reports signal 11, no hang; and the persistent-loop geometry
  (one worker forced to answer input 1 normally then die on input 2) shows the failure is
  detected cleanly and a freshly spawned worker still answers further input.
- **C4** (`910df1d`): `newProcessWorker[T](id): Worker[T]` — every `submit` spawns fresh
  (the shipped N=1 policy), round-trips one frame, maps a pipe failure to `vCrashed` via
  C3's `observationForDeath`, merges coverage from the file dump. `Orchestrator` gained a
  general `newOrchestrator(worker, frontier)` constructor over an arbitrary `Worker[T]`
  (the `(s, target, frontier)` overload is now sugar over it); `fuzz.nim` also gained
  `newWorker` (the private `submitImpl` field forced a cross-module public constructor).
  Test drives an `Orchestrator` over the process worker via `run`/`admit` and shows the
  same verdict/crash-kind/coverage an equivalent in-process `Orchestrator` produces.
Coverage-frame design note: the result frame carries ONLY `Verdict` + `Option[CrashInfo]`
+ `message` — coverage rides the file-dump path (stated, not silent), `RunResult` is
omitted (not meaningful for a Nim in-process property inside a worker).

### E-cleanup — campaign-level resource lifecycle — DONE (commits `f667bd0`, `f5f81bd`, `6a8a174`, `44db313`)
All four cycles landed, each its own GREEN commit, full `tfuzz*` + `tdb*` on both `c` and
`cpp` green after every cycle (42/42 files each backend by C4, no existing assertion
edited). Files added: `tests/tdbcleanupsweep.nim`, `tests/tfuzzworkerlifecycle.nim`,
`tests/tfuzzrespawnstorm.nim`; touched: `db.nim` (startup sweep), `fuzzworker.nim`
(PDEATHSIG + process-group helpers), `fuzz.nim` (storm breaker).
- **C1** (`f667bd0`): `directoryBasedDatabase`'s constructor — already sweeping orphaned
  `.tmp.<pid>.<tid>` files — now also sweeps (a) superseded `<safeKey>.corpus.<gen>.log`
  generation files not referenced by `<safeKey>.corpus.head` (E3b's compactor deliberately
  never unlinks these; safe here because no reader from a PRIOR campaign can still be alive
  at a NEW campaign's construction moment — no lease machinery needed, that stays U2's job
  for the live-campaign case) and (b) stale `nelli_`-prefixed POSIX `shm_open` segments in
  `/dev/shm` (E2b's coverage transport) — prefix-only scoping, mirroring the `.tmp.` sweep,
  never touching a foreign process's differently-prefixed segment. No FIFO/named-pipe sweep
  needed — nothing in the codebase creates one (`mkfifo` unused; only anonymous
  `posix.pipe()`).
- **C2** (`f5f81bd`): `PR_SET_PDEATHSIG`, armed in the child immediately after `fork()`
  (before `execvpe`, survives exec) at both worker-spawn sites (`spawnWorkerProcess`,
  `newForkWorker`) — the kernel now `SIGKILL`s a worker the instant its parent dies,
  regardless of what the worker itself is doing (the real hazard: a worker stuck INSIDE
  `dispatch` on a hung target never comes back around to notice even a closed input pipe).
  Guards the fork/arm race via a `getppid()` re-check. Test (`tfuzzworkerlifecycle.nim`)
  is a genuine three-process-level deterministic spawn-kill-assert: a subreaper test
  process forks a surrogate "orchestrator," which spawns a REAL worker via
  `spawnWorkerProcess` and dispatches it into a self-referential-pipe hang (deliberately
  decoupled from any fd the orchestrator holds, so pipe-EOF can't be the thing saving the
  day); SIGKILLing the surrogate and blocking-`waitpid`ing the reparented worker proves
  PDEATHSIG alone ends it — RED-confirmed by temporarily disabling the `prctl` call and
  observing the test hang (`dt-bounded.sh` kill), not just inferred.
- **C3** (`6a8a174`): `isolateOwnProcessGroup(pid)` (called by the orchestrator right after
  spawning, wired into both spawn sites) puts a worker into its own process group led by
  itself, so any further descendant IT forks inherits the same pgid; `killWorkerGroup(pid)`
  then reaches the worker's WHOLE subtree via one `killpg`, without touching the caller's
  own group (unlike a shared `setpgid(0,0)` campaign-wide group would). Each worker gets its
  own single-worker group rather than one shared campaign-wide group specifically so E2a's
  N=1-recycle-per-input policy never depends on a shared group whose sole (already-reaped)
  member must stay valid for the NEXT worker to join. This is the clean-shutdown (caught
  SIGINT) counterpart to C2's hard-kill/OOM defense. RED-confirmed the same way as C2.
- **C4** (`44db313`): `Orchestrator.stormWindow`/`stormBackoff` — the STEADY-STATE breaker,
  distinct from the (E4a, not yet built) BOOTSTRAP circuit-breaker that only catches
  dead-before-first-read. Tracks the `CrashInfo.kind` of the most recent `stormWindow`
  crash-triggered recycles in a sliding window (crash-event-count-based, not wall-clock, so
  it stays deterministic/testable); once the window is full and every kind in it is
  IDENTICAL (not diversifying — an environment fault, vs. a productive crash-finding
  campaign's varied/new kinds, which must never trip it), the breaker trips:
  `stormBackoff == false` (default) raises `RespawnStormError` with a distinct diagnostic;
  `true` degrades instead (campaign continues, `stormTripped`/`stormBackoffLevel` set for a
  caller/driver to pace its own respawn loop — this layer never sleeps itself). The window
  keeps sliding after a trip, so a later diversifying crash self-corrects it back to
  untripped. `stormWindow == 0` (default) is fully inert — byte-for-byte pre-E-cleanup
  behavior. Pure algebra over a scripted fake `Worker[T]` (E3a's "order-independent fold"
  precedent) — no real process spawns, all 5 tests green on the first implementation pass.

## Slices (first-pass; round-3 re-cuts folded in)
- [x] **E0 corpus-sync SPIKE — DONE** (delta log selected; 5 mandate items resolved) ·
  **E1 Orchestrator/Worker seams + macro entry — DONE** (typed CrashInfo; Worker=
  load-bearing seam; fuzz macro + worker re-entry + capture checks; C7 freeze-guard
  green) ·
  **E2a POSIX worker+framed pipe+crash-isolation — DONE** (N=1 coverage until E2b) ·
  **E2b shm+nelli_cov.c reset — DONE** (double-buffered + generation word; N=1→N>1) ·
  **E3a freshness machinery — DONE** (finding record/reproRate/divergentReproduction;
  re-verify-gated `admit`, default off; pure-algebra order-independent-fold tests;
  worker recycling + fork-per-input captured-once snapshot invariant) ·
  **E3b persistence discipline per E0 — DONE** (delta log + compaction + generation/head +
  `.bin` funnel + versioned header) ·
  **E-cleanup resource lifecycle + steady-state respawn-storm breaker — DONE** (startup
  sweep; PDEATHSIG; process-group-kill backstop; storm breaker) ·
  **Eci Windows toolchain — DONE** (`e9444fc`/`464ae8e`/`53252a2`; both Windows CI
  legs green on the pushed branch; capability flag emitted at
  `docs/RFC-fuzzer-nextgen.windows-capability.md`) ·
  **E4a Windows worker — DONE** (`24285ba`/`0d1acb2`; CreateProcess round-trip
  run-proven, CI run 33009204939) ·
  **E4b Windows shm coverage — DONE** (`a83bf53`/`26cb33b`; run-proven, CI run
  33013350915) ·
  **E4c Job Objects + external-tier un-gate — DONE** (`06c8d29`/`1022640`/`0e77fb8`/
  `b17b365`/`2fe8ce1`; run-proven, CI run 33021836310) ·
  **E5 external tier onto seams — DONE** (Linux `755fb39`/`290372e`; Windows
  un-gate landed with E4c) — **Track E COMPLETE on both platforms; ALL 30 SLICES DONE**
- [x] **G1a** thread mode through dispatch (mechanical) — **DONE** (`3a14b59`) ·
  [x] **G1b** draw-symbolication+bounded trace — **DONE** (`3cc275a`) ·
  [x] **G2** branch-flip solve+materialize — **DONE** (`8bca6e1`) ·
  [x] **G3** orchestration (Z3 bridge off hot path) — **DONE** (`641ada0`/`ecba035`/`66b9cd9`
  + `3918eaa`/`cf3f887` G3fix; S1's real energy/`FrontierStats` used from the start, no
  G3b throwaway, see the REORDER note above) ·
  [x] **G4** cmp instrumentation + operand log — **DONE** (`0bd9d8a`/`a42960a`/`da68f85`) ·
  [x] **G5** I2S+auto-dict — **DONE** (`e950b03`/`198e149`/`019f9c7`) ·
  [x] **G6** transparency descriptor + closed/directed algebra +
  `branching` — **DONE** (`5582ca8`/`b66a11d`/`6f17d31`/`e4d7a77` — see above)
- [x] ~~G3b wire real Entropic energy post-S1 (conditional)~~ — AVOIDED by the
  S1-before-G3 reorder (see above): G3 lands with real energy from the start.
- [x] **S1** Entropic energy (owns shared FrontierStats) — **DONE** (see above) ·
  [x] **S2** operator bandit — **DONE** (`6ef1e44` C1, `589498d` C2; see above) ·
  [x] **S3** havoc stacking + interesting-value tables — **DONE** (`0e14ab5` C1,
  `ed5694c` C2; see above) ·
  [x] **S4** continuous corpus culling — **DONE** (`f4cb921` C1, `b02af6d` C2;
  see above) · **S5a** observability
  (E-tier) · **S5b** concolic/provenance breakdown (G2-gated) ·
  **S6** learned-state checkpoint/resume
- [x] U0 forAll through in-process Worker (crash isolation) — **DONE** (`30d2e86`) ·
  U1 unify coverage model, score/admit split — **DONE** (`9526460`/`1b80431`/`f0ae95c`; SW not bumped) ·
  U2 forAll corpus reads — **DONE** (`088ea26`) ·
  U3 byte-mode demotion — **DONE** (`16c2b0a` C1, `e74a222` C2; the corpus-format-migration
  half of the original U3 scope was already delivered in E3b C5, not redone here)

## Open forks — ALL THREE RESOLVED 2026-08-14
1. **Umbrella #158 filed**; #151 execution-model items fold in, hygiene
   items stay (commented on #151).
2. **Parallel campaign IS v1** (Corey: "do it right from the start but
   prove it with a spike"). Single-node many-workers/one-shared-corpus;
   E0 spike de-risks the sync model before E2/E3 commit. Cross-node stays
   out (#112).
3. **Concolic yield: capability + measured-metric + optimistic-solving
   fallback, NOT a percentage bar.** Optimistic solving (QSYM) is
   soundness-free here because Track E re-verifies concretely; yield metric
   emits a failure taxonomy that becomes the walker-widening work-list;
   fragment widens on investment shared with symexFind. See RFC §Track G
   "Yield" subsection + ADR-0031 D3.

## Key decisions (this session — all grill-resolved, in RFC §Resolved forks)
Scope=(a1) no DBI; Windows first-class; persistent worker pool w/
re-verification; choice-space concolic (value-space+inversion rejected);
two front doors one engine; committed ablation harness. Supersedes the
earlier "#124 Shape A next" recommendation.

## Ground-truth anchors verified in code (RFC §Ground truth)
in-process no isolation (fuzz.nim:310-335, engine.nim:54-60); external
POSIX-only (fuzz.nim:858); spawn-per-input (fuzz.nim:871-935); admission-
only coverage (coverage.nim:357-370, D4); no cmp guidance; good mutation
core (fuzzir.nim); two coverage worlds one data world (engine.nim:118-153
scalar vs bucketed frontier, D10); no symex bridge; corpus channel exists
(db.nim, F1).

## Review ledger (stage 4) — round 1 complete, awaiting Corey's fix mandate

Method: 8 parallel review lenses (security, design/ergonomics, liveness, 3
correctness clusters, cross-platform parity, test coverage) over
`git diff main..HEAD`, then 7 adversarial refuters over every Critical and
load-bearing High. Severities below are POST-verification — several source
ratings were corrected down (or refuted outright) by the refuters, and those
corrections are recorded rather than silently dropped.

Status key: `open` (verified, unfixed) / `refuted` / `fixed` / `deferred`.

### Critical

| id | status | file | finding | verification |
|----|--------|------|---------|--------------|
| R1 | FIXED | smt/runtime.nim:9209-9211,9234,9467-9470 | `toInt64()` on `ChoiceNode.intC.min/max` with no `fitsInt64` guard; `toInt64(high(uint64))` wraps to `-1` → unsatisfiable `[0,-1]` domain → uncaught `ValueError` from `materializeConcolicModel` aborts the whole campaign. No try/except anywhere in fuzzmacro's bridge closure → `tryConcolicBridge` (fuzz.nim:1434) → loop (fuzz.nim:1990). | HOLDS. Trigger is the STOCK auto-derived `uint64`/`uint` strategy (derive.nim:399-405), not an exotic one. Gated: only fires when user sets `stallRounds > 0` (default 0). FIXED via `concolicIntRepresentable` (`fitsInt64` on min/max/shrinkTowards/intVal) at all four sites: an unrepresentable draw is left unsymbolicated and counted in a new `nonInt64Draws` counter rather than corrupting the domain — matching the RFC's "a wrong model wastes one candidate" contract. RED-first proof: new unit test in tsymex_g1b_concolic.nim:113 (confirmed failing before the fix) + end-to-end `fuzz(arbitrary(uint64), ...)` with `stallRounds: 1` in tfuzzconcolicbridge_real.nim:44. 11 Track G suites green, c + cpp. |
| R2 | FIXED | nelli.nimble | 44 of 64 branch test files are absent from the `test` task, incl. every worker/orchestrator/checkpoint/bandit/concolic-bridge suite. `nimble test` runs almost none of this RFC. Violates the RFC's own binding rule (RFC:1346-1348, "an unregistered pin never runs"). | CONFIRMED by direct enumeration (present=20, missing=44). FIXED: all 44 registered, grouped by track; task now lists 277 suites, no dupes, every name resolves to a real file. Pre-registration sweep ran all 44 via `dt-bounded.sh c` — **44/44 green**, so registration exposed no hidden breakage. |
| R3 | FIXED | fuzz.nim:1020,1068,1085; fuzzworker.nim:444,487,988 | Track E's persistent-worker tier is dark behind the primary surface: `newProcessWorker`/`newForkWorker`/`spawnWorkerProcess` have ZERO callers in `src/`. `fuzz*[T]` hardcodes `spawnFreshWorker = nil`, `bootstrapWindow = 0`; `fuzzWith*[T]` has no such parameter. Track E's exit gate ("structure-aware fuzzing is crash-isolated on both platforms") is unmet for `fuzz`/`fuzzWith`. Dark by consequence: Job Object limits, `shmHold*`, `BootstrapBreaker`, `CampaignStats.respawnCount/stormTripped/stormBackoffLevel`. | PARTIALLY-HOLDS, NARROWED: `fuzzBinary`/`externalTarget` IS genuinely process-isolated today (real fork+exec / CreateProcess per input, own `setrlimit` / Job Object). Finding is scoped to the in-process property-fuzzing tier. The RFC's N-worker `Pool`/`submitAsync`/`poll` type does not exist at all (`grep "type Pool"` → nothing). FIXED: new `FuzzSettings.processIsolation` (default **false**), `fuzz*[T]` gained `spawnFreshWorker` (mirroring the `concolicBridge` convention), and the macro now always emits the spawn closure via a shared `processIsolationSpawnWorker(id, propWitness)` — the runtime decision lives in `fuzz*[T]` since the macro cannot see settings at compile time. When isolated, the INITIAL worker is spawned too, not just recycles. `bootstrapWindow=3`/`stormWindow=5`/`recycleAfterInputs=256` chosen with stated rationale so the breakers are genuinely live, not merely reachable. `fuzzWith`/direct `fuzz*[T]` raise a new `ProcessIsolationError` naming the constraint rather than silently downgrading. Proof (`tests/tfuzzprocessisolation.nim`, 6/6 green, c+cpp, cross-compiles for Windows): the isolation test asserts each crash carries the **worker's own PID**, distinct from the parent's and from every other crash — the reconstruction sentinel alone reads `1` either way, so the PID is what actually discriminates real cross-process execution from a silent in-process downgrade. Exit-gate test proves an `AssertionDefect` no longer derails the campaign. Registered in nelli.nimble; matches the `tfuzz` glob so both Windows legs pick it up automatically.

**DESIGN CALL made in-flight, flagged for Corey:** isolation is **opt-in**, not the default. Rationale: it requires worker reconstruction, which only the `fuzz(...)` macro can supply (`fuzzWith` takes an arbitrary closure a fresh process cannot rebuild), and defaulting it on would change spawn cost and semantics for every existing caller. Making it default-on for the macro path is a one-line change if you want it — but then the RFC's Track E exit-gate wording should be revisited, since "crash-isolated" would still not hold for `fuzzWith`. |

### High

| id | status | file | finding | verification |
|----|--------|------|---------|--------------|
| R4 | FIXED | learnedstate.nim:118-154; coverage.nim:868-879; fuzz.nim:1721-1725 | `decodeLearnedState` bounds each length-prefixed array independently, never cross-checks PAIRED arrays. `hitCounts.len != lastImprovedSeq.len` decodes `ok:true`; `admit()` grows only `hitCounts`, then writes `lastImprovedSeq[i]` → IndexDefect, uncaught, aborts resumed campaign. | HOLDS, reproduced twice independently. Severity corrected Critical→High: tmp+rename gives atomic visibility, so Ctrl-C/OOM-kill can NOT produce this; real trigger is storage corruption / bit rot / tampering (no checksum anywhere in db.nim). FIXED at the DECODER (restores learnedstate.nim's own "never misreads" contract at the source, making the call-site indexing bugs unreachable without touching fuzz.nim/coverage.nim/bandit.nim). Full audit of `encodeLearnedState` found exactly two pairing invariants (this and R5); `dictionary.entries` audited and confirmed self-describing with no sibling count. Misleading module doc about when corruption arises also corrected. |
| R5 | FIXED | fuzz.nim:1829-1831; bandit.nim:83-87,127-136 | Restore gate checks `banditPulls.len == arms.len` but never `banditRewardSum.len`; `chooseOperator`'s decay loop indexes `rewardSum[i]` on `pulls.len`'s bound → IndexDefect on first operator pick after resume. | HOLDS, reproduced. Same severity correction as R4. FIXED by the same decoder-level pairing check. RED-first proof for both: source file stashed while KEEPING the new tests — both new tests failed (`decoded.ok was true`), the 6 pre-existing tests passed pre-fix, confirming the new tests are the only ones exercising the bug. Post-fix: tlearnedstate 9/9, tfuzzcheckpoint 5/5, tfuzzcampaignstats 13/13, tfuzzcullpersist 3/3. |
| R6 | open | fuzz.nim:2733-2744 vs 2428-2437 | POSIX `runChild` sends SIGTERM + 200ms grace so a sancov child dumps its map (nelli_cov.c:521 handles SIGTERM); Windows arm calls `p.kill()` → `TerminateProcess`, which runs no atexit and triggers no exception filter. Every Windows external-target TIMEOUT yields zero coverage, deterministically. | HOLDS at High. Hits the LIVE external tier (per R3). `tfuzzexternal.nim` asserts only `r.timedOut`, never coverage — no test would catch this. |
| R7 | FIXED | smt/runtime.nim:6024-6049 | `concreteBranchOutcome` makes two `newSolver(ctx)` calls per if-decision with NO rlimit and NO timeout, unlike every other solve site (`trySolve` sets rlimit; G2's `z3CheckBounded` sets timeout "because Z3 non-termination is the central hazard"). No ambient global param either. Runs on the main loop thread with no watchdog. | HOLDS. Fires on EVERY if-decision of every collection once the bridge is active. FIXED, after a rejected first attempt — v1 forwarded `settings.budget.queryRLimit`, which `defaultResourceBudget()` sets to **0 = unlimited**, so the "bound" was inert by default (the exact dark-mechanism pattern this review exists to find; sent back). v2 adds `concreteBranchRLimit()` → explicit caller budget wins, else `defaultConcreteBranchRLimit = 20_000_000`. **rlimit deliberately chosen over G2's wall-clock timeout**: this site decides which arm the walk follows, so it shapes the search trajectory — a wall-clock bound would make the same seed resolve differently on a fast vs slow machine, breaking replay determinism. Value reused from `tsymex_r4_strip.nim:21`, where it was bisected against a real 3+ hour divergent query, not invented. Tests: a pin that `concreteBranchRLimit(defaultSymexSettings()) > 0` (**verified load-bearing** — reverting to v1 makes it fail) plus a live degrade test with `queryRLimit: 1` proving exhaustion surfaces as `ambiguousBranches`, never a wrong definite arm. No `symexWalkerVersion` bump: the whole `wmFollowConcrete` path never computes a cache key or touches the witness DB, so it is categorically outside what that version guards. |
| R8 | FIXED | fuzzmacro.nim:158-172 | Impurity denylist walks the captured syntax tree only — never `.getImpl`s a referenced named proc. RFC:1543-1547 resolves this as IN SCOPE ("checked through called procs, not just the top-level call") and separately accepts only the closure/indirect-call residual; the code conflates the two. | HOLDS, reproduced: `fuzz(integers(0, len(getEnv("HOME"))), ...)` is correctly rejected, but `fuzz(integers(0, seedFromEnv()), ...)` compiles clean. Same hole passes `now`/`rand` via a named helper, which genuinely drift per worker spawn. FIXED: `checkCapture` now resolves named `nskProc`/`nskFunc`/`nskMethod`/`nskConverter` callees via `.getImpl` and recurses into their bodies. Bounded by depth 5 (justified: nelli's own strategy combinators are reachable, so an unbounded walk would tie compile time to that graph's depth) and a visited-set keyed on the callee's DEFINITION SITE `file:line:col`, so mutually-recursive helpers converge. Opaque bodies (importc/FFI, compiler magics, precompiled modules) **silently allow** — a false positive breaks valid compiling user code and is strictly worse than the false negative being closed. RED-first: 1-hop and 2-hop tests both compiled clean pre-fix, rejected post-fix. False-positive guards added (module-scope pure helper, a const, an importc proc) and tfuzzmacro/tfuzzmacro_astspike/tfuzzprocessisolation/tfuzzi2s/tfuzzconcolicbridge_real all stayed green; Windows cross-compile intact. **Residual, unchanged and RFC-accepted:** impurity reached only via a runtime closure value or proc pointer, and any impure proc outside the 7-name denylist. |
| R9 | FIXED (local; CI-unproven) | .github/workflows/*.yaml | 7 new `tsymex_g*` suites (the concolic bridge — the RFC's "differentiated capability") run on NO Windows leg: they miss the fuzzer legs' `^(tfuzz\|tdb)` glob and were never added to symex-windows.yaml's hand-curated list. `tengine_crashisolation`, `tengine_corpusreplay`, `tcovguided`, `tlearnedstate` likewise. | CONFIRMED by direct inspection of all three workflows. FIXED: both fuzzer legs widened to `^(tfuzz\|tdb\|tengine_)` plus exact `tlearnedstate\|tcoverage\|tcovguided` (63→68 suites — simulated against `tests/` to confirm exactly the 5 intended files; bare `tengine`, `tcoveragemode`, `tcovsourcetable` deliberately excluded and documented in-file). All 7 `tsymex_g*` added to symex-windows.yaml. R21's zero-suite guard added to all three legs. All three YAMLs parse. **NOT yet CI-proven — these 12 suites have never executed on Windows; a push is required and some may legitimately fail.** |
| R10 | FIXED | db.nim:785-833,935-951 | No mid-campaign corpus-generation GC. Compaction (`logBytes > 4×liveBytes`) writes a new generation file each time and never unlinks the old; the sweep runs ONLY at `directoryBasedDatabase` construction. N compactions at live-set S leak ~N×S bytes within one campaign. `openCorpusSnapshot`/`CorpusCutPoint` (the lease seam) has zero `src/` consumers. | HOLDS with scope correction: this is a DOCUMENTED, tracked scope-cut (db.nim:781-783 + handoff), deferred to U2 — not an unnoticed defect. Real risk for long unattended campaigns. FIXED by building the RFC's actual reader-lease scheme (shape (a)), NOT a retention-count heuristic: `openCorpusSnapshot` now refcounts a lease on the generation it returns, new `closeCorpusSnapshot` releases and re-attempts reclaim, and `reclaimUnpinnedGenerations` runs mid-campaign from `maybeCompactCorpusLog` — removing every generation except the head and any leased one. **Crash-safety ordering:** new generation fully written → head published via the existing atomic tmp+rename → reclaim reads the just-published `headGen`, so the live generation can never be the one removed; a crash mid-reclaim leaves unreferenced files unswept, never an unreadable corpus. No Windows gating needed: `openCorpusSnapshot` reads whole files and holds no OS handle, so the lease is purely logical. Tests: 300 compactions never exceed 1 generation on disk; contents+cap intact across many reclaims with cold-handle replay matching; a held lease survives multiple compactions byte-identical while an unleased sibling is reclaimed around it. **Note:** an existing tdbcleanupsweep assertion encoded the pre-fix bug as expected behavior ("superseded gen NOT unlinked yet") and was necessarily rewritten to exercise the startup sweep's real case (a lease never released, standing in for a hard-killed campaign). **Follow-up:** `openCorpusSnapshot` still has no `src/` consumer — U2's `forAll` replay reads the whole corpus atomically via `loadCorpus` and never needed a lease — so reclaim is effectively immediate today; the lease exists for correctness once a streaming reader appears. |
| R11 | open | fuzz.nim (FuzzSettings ~20 fields, Orchestrator ~30, `newOrchestrator` 13 params) | The RFC's binding config-surface rule (`ExecutorConfig`/`GuidanceConfig`/`SchedulingConfig`/`OrchestratorPolicy`, RFC:1301-1318) is entirely unimplemented — none of those four type names exist anywhere. Flat structs instead, incl. four near-identical `uniformXxx` opt-out booleans. | CONFIRMED by grep (two lenses independently). The RFC wrote this rule specifically to foreclose the `dictionary` doc/code-drift class it cites as precedent. |
| R12 | FIXED | coverage.nim:1100-1118; fuzz.nim:2763-2889; fuzzworker.nim:1060 | cmp-log / RedQueen cross-process correspondence has no consumer in `src/`. The live external tier wires `$NELLI_COV_FILE` but never `$NELLI_CMP_SHM`; `Observation[T]` has no `cmpLog` field to carry it; `newProcessWorker` hardcodes `cmpShm = ""`. C-side publish (nelli_cov.c:239-242) is real and correct but read back only by tests. In-process I2S IS live and proven (tfuzzi2s.nim). | Reported by the liveness lens; consistent with R3. Scoped to the cross-process tier only. FIXED end-to-end: `Observation[T].cmpLog: Option[seq[CmpLogEntry]]` (`.isSome` is the signal, NOT `.len` — collapsing them would let an isolated run with a genuinely empty log fall through to the parent's `currentCmpLog()` threadvar, which can hold stale data from an earlier in-process `fuzz()` on the same OS thread); `captureCmpLog` prefers the Observation and falls back to the threadvar; both `newProcessWorker` arms and `externalTarget` now request a real per-run-unique segment and read it back. Proof (`tests/tfuzzcmplogprocess.nim`): a `processIsolation: true` + `enableI2S: true` campaign solves the 1-in-4-billion `0xDEADBEEF` gate and `report.dictionary.entries` carries the constant — only reachable if the child's shm-published operand crossed the process boundary. **Two self-caught defects during the fix**, both worth recording: (1) a first attempt reused ONE segment name per Worker lifetime, so a later run publishing nothing would read back an EARLIER run's entries — caught by the agent's own negative test ("a target NOT built with trace-cmp reads back Some(@[])"); (2) the per-run naming then leaked one 65536-byte `/dev/shm` segment PER ITERATION (~6.5 GB over a 100k-iteration campaign) — **rejected by the control loop and sent back**, since it was a worse regression than the finding. Closed with `shmUnlinkCmpLog` (POSIX-only; `db.nim:872-874` already establishes that `removeFile` on `/dev/shm/<name>` IS `shm_unlink`, so no `nelli_shm.c` primitive was needed). Unlink is safe after read: the mapping is held via its own VMA and POSIX unlink removes only the name. Windows needs nothing — named sections die with their last handle. `tests/tfuzzcmplogshmleak.nim` proves it and is **verified load-bearing** (disabling the unlink leaks exactly 60 over 60 iterations and fails). **Residual, out of scope:** `differentialTarget` drops per-child cmp logs when fanning across multiple Targets. **Pre-existing nuance flagged:** `db.nim`'s startup shm sweep only runs from `directoryBasedDatabase`'s constructor, so a campaign with no `database` configured has no startup backstop and relies solely on the per-iteration unlink. |

### Medium — round 1 status

**FIXED (11):**
- **R13** Windows spawn orphan — assign check now runs BEFORE `resumeThread`; the still-suspended child is terminated and all six handles closed before raising. Raise (not a worker-death observation) deliberately kept: every other pre-first-instruction spawn failure aborts, and reclassifying just this one would misrepresent an infrastructure fault as input-attributable data. CI-unproven.
- **R15** fork single-thread guard — RUNTIME thread census via `/proc/self/task` before every `fork()`, raising `ForkUnsafeError`. A compile-time `--threads:on` gate was rejected because this project's own runner passes `--threads:on` universally, which would have made the feature permanently unreachable rather than safer. Checked per-submit, since `parallelCheck` can spawn threads mid-campaign. False "never calls createThread" doc claim corrected.
- **R16** decode containment — `decodeObservationLiteSafe` catches exactly `DbCorrupt` and `RangeDefect` (deliberately narrow: a blanket catch would swallow `OutOfMemDefect`), folding both into `vCrashed` per the module's documented contract. Tested with real checksum-valid frames wrapping malformed payloads.
- **R17** fd/handle leaks — `closeFds` helper on every early-failure path (both POSIX pipe/fork sites, `newForkWorker`, and the Windows second-`CreatePipe` branch). Not locally testable; structural parity with the sibling branch that was already correct.
- **R20** predictable temp path — 8 random bytes from `std/sysrand` salt the name, and the staging write uses `O_CREAT|O_EXCL|O_NOFOLLOW` 0600 instead of `writeFile`. Test pre-places a symlink at the worker's `.tmp` path and asserts the victim file is untouched and the worker still answers. See **R50** for a secondary defect this fix introduced and closed.
- **R22** storm-breaker realism — new tests use real death kinds (`ckSignal`/`ckExitCode`/`ckWinException`). Proves the dedup key is `CrashInfo.kind` ALONE, so three distinct SIGSEGV sites still read as non-diversifying. Verified red.
- **R23** spawn/shm failure paths — genuine `EMFILE` provoked by lowering `RLIMIT_NOFILE` one short of what `pipe()` needs; exec-failure classification tested through `reapWorker`/`observationForDeath`. Verified red. Residual → **R49**.
- **R24** read-before-redispatch — the RFC's round-3 pin now exists: with input B withheld, two `probe.read()` calls both return generation 1, and only redispatch advances it. Verified red.
- **R25** corpus corruption — torn-tail and garbage-mid-record tests added, plus a two-lease overlapping-reader-vs-compaction test (3 live generations shrinking 3→2→1 as leases release). Verified red. Surfaced **R46**.
- **R32** on-disk checkpoint — real `directoryBasedDatabase` round trip plus a truncated on-disk blob degrading to cold start. Verified red.
- **R33** S6 behavioral proof — the naive cold-vs-warm design was found unsound (G5's I2S harvests operands live *within* one run, so a cold campaign already hits the needle ~2/8). Redesigned at a 1-iteration budget where live I2S has no second iteration to exploit: cold 0/8 seeds, warm-resumed 8/8. Categorical, not statistical. Verified red, rerun 3× for determinism.

**PARTIAL (1):**
- **R6/R18** Windows publish-on-kill — `nelli_cov.c` now registers a `SetConsoleCtrlHandler` routine that publishes coverage, with an `InterlockedCompareExchange` publish-once latch serializing all three Windows publish sites (the console handler runs on an INJECTED thread, so `pt_dumped`'s same-thread check-then-set was not sufficient). R18's `PT_DBG` hazard deliberately not extended into the new handler. **But the mechanism cannot fire yet** — see **R48**. The `tfuzzexternal.nim` assertion was deliberately NOT flipped, since doing so would have created a false-red CI pin.

**STILL OPEN (9):** R14 (`wmFollowConcrete` implemented for `isIf` only), R19 (`pt_shm_ch_init` per-process capacity — see R47), R21 (FIXED, see High table), R26 (Z3 timeout/unknown untested), R27 (`fuzz[T]` 530-line monolith — overlaps R11's refactor), R28 (flat yield taxonomy, missing `yoSupersededByRace`), R29 (logic duplicated across the import-cycle boundary ×3), R30 (Defect→CrashInfo classification copy-pasted 6-7×), R31 (`Report.crash` populated but no renderer surfaces it).

### Low (abbreviated)

R34 `crashinfo.nim:8-10`'s module doc claims U0 "routes forAll's property
invocation through the same Defect-catching boundary fuzz's in-process Worker
uses" — FALSE; forAll uses 4 independent `except Defect` sites. The deviation
was deliberate and correctly recorded in the handoff (routing through
`observeInProcess` would stomp coverageGuided's own delta bookkeeping); only
the comment was never walked back. R35 no test runs `coverageGuided: true`
with a crashing property (behavior PROVEN correct today by a throwaway run —
gap is in the suite, not the code). R36 transparency algebra's two
`dkSpanComposite` cells are unconditional stubs behind a "closed" claim.
R37 `Dictionary` lacks the snapshot/restore accessor pair its two siblings
have. R38 exported bandit/havoc constants have no config path. R39
`runTargetedPhase`'s 12-param signature breaks the uniform phase interface.
R40 concolic bridge runs inline rather than on a dedicated slot — RFC:313-322
mandates a slot, but for the N-worker Pool that does not exist yet; in today's
Pool-of-1 there is no second worker to idle (scope-tracking, not a defect).
R41 breaker accounting bypassed by `admit`'s re-verify and `sampleReproduction`
(latent — both unreachable while `spawnFreshWorker` is nil). R42 shm-name
squatting (`shm_open` ignores mode when the object exists). R43 frame-size
tests omit the exact-limit accept case. R44 `operatorPulls.len == 6` pins an
implementation detail. R45 `ExampleDatabase`'s closure-record interface grows
by copy-paste per backend.


### Round-1 additions (findings surfaced BY the fix work itself)

| id | status | source | finding |
|----|--------|--------|---------|
| R46 | open | found closing R25 | `db.nim`'s corpus payload-decode path (`replayCorpusRecords` → `fromBytes`) lets a raw `DbCorrupt` escape, unlike the framing path which wraps as `DbError`. A caller catching only `DbError` misses this corruption shape. Pinned as-observed by the new test; fix needs `db.nim`. |
| R47 | open | found closing R23 | Attaching to a STALE shm segment of the same capacity silently reads the previous run's data as if fresh (`ok=1`, plausible values); attaching at a DIFFERENT capacity reads a wrong-but-plausible zero-filled snapshot. Never a loud failure. Related to R19. Both hazards now pinned by tests. |
| R48 | open | found closing R6 | Completing R6 is blocked in a file boundary: `std/osproc`'s `startProcess` never sets `CREATE_NEW_PROCESS_GROUP` (verified against this repo's vendored `/opt/nim/2.2.10-patched/lib/pure/osproc.nim`), so `GenerateConsoleCtrlEvent` cannot target the child without also signalling the orchestrator. Needs `runChild`'s Windows arm to spawn via raw `CreateProcess` — precedent exists in `fuzzworker.nim`'s `spawnWorkerProcess`. |
| R49 | open | found closing R23 | `spawnWorkerProcess` hardcodes `getAppFilename()` with no override seam, so genuine exec-failure classification cannot be provoked through its public API. Needs an optional executable-path parameter to test properly. |
| R51 | open (intermittent) | `tests/tfuzzworkerlifecycle.nim` | **Observed once: a genuine HANG (exit 137, killed at 400s) during the authoritative 79-suite sweep.** Not merely slow — the suite normally completes in well under 150s. NOT reproduced in ~8 subsequent attempts: 4 clean standalone runs, and 4 clean runs under deliberate parallel load alongside 5-7 other process-spawning suites. This suite SIGKILLs an orchestrator and exercises process-group kills, so a rare race where a waited-for process never dies is plausible. Recorded rather than dismissed: an intermittent hang here would surface as a flaky CI failure, and this project treats non-termination as a real failure mode (see `scripts/dt-bounded.sh`'s header). Watch the Windows legs and future sweeps for a second occurrence. |
| R50 | FIXED | found closing R20 | The R20 fix initially introduced a secondary defect: a refused (symlink-guarded) write raised `OSError` that escaped `runWorkerLoopAndExit`, breaking its documented "always exits via quit, never falls through" contract and causing runaway nested worker re-execution. Caught by the agent's own test; fixed by degrading a refused dump to "coverage not published this round". |

### Refuted / verified sound (recorded, not silently dropped)

- **REFUTED** `energyWeightedIndex` div-by-zero: unreachable. `while corpus.len
  > 0` (fuzz.nim:1884) guards the only call site, `energy.len` tracks
  `corpus.len` in lockstep, and culling explicitly refuses to empty the corpus
  (fuzz.nim:1917-1921 documents this as a deliberately considered hazard).
- **REFUTED as Critical** "U0's forAll-through-Worker claim is false" → the
  functional capability is intact and proven; reduced to doc-accuracy (R34).
- Verified sound: `binaryio`/`learnedstate` decoders' per-field bounds;
  `db.nim`'s `safeKey` path encoding; `workerproto`'s frame codec
  (truncated/oversized/checksum rejection); `parseCmpLog` truncation handling;
  the shm double-buffer generation-word protocol; `transparency.nim`'s
  composition algebra (hand-traced all four affine/predicated combinations —
  associative, correct, and its two approximations degrade in the safe
  direction); `fuzzmacro`'s macro hygiene and compile-error path (both
  directions); Z3 context hygiene across bridge attempts; frontier `score`
  non-mutation; AFL domination correctness in `favoredIndices` (a slot's sole
  coverer is always its champion, never culled); coverage bucketing with no
  modulo bias; `fuzzir` mutator totality; `symexWalkerVersion` NOT bumped is
  correct (every `wmExplore` arm is provably byte-identical — matches the
  documented exemption precedent).
