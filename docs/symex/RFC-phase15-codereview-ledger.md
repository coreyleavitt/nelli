# Phase 15 Code-Review Ledger

Multi-agent review of the Phase 15 "language fragments" delta (`242e6e6..26b0e82`, ~3,678 lines across `src/proptest/smt/`). 7 dimension reviewers → adversarial verification of every High claim → consolidation. Status legend: `open` / `fixed` / `deferred` / `wontfix` / `refuted`.

## Confirmed findings (post-verification)

| id | sev | status | file:line | finding | verify |
|----|-----|--------|-----------|---------|--------|
| F5-hang | High | fixed | runtime.nim:2741 | `iekConvFloatToInt` int64 case returned `svInt` via `bvToZ3Int(bv64)` (i.e. `bv2int(fp.to.sbv f)`). The NI-2 `isDerefWrite` coercion then wrapped it in `int2bv`, producing `int2bv(bv2int(fp.to.sbv f))` as the stored heap value. A subsequent ordering goal over that heap cell (`p[] > k`) gave Z3 a BV-ordering query over the Int+BV+FP round-trip term — the F5 pathology — which could cause non-termination. **Fixed**: int64 case now returns `SymVal(kind: svBV64, bv64: bv64, signed: true)` directly, symmetric with the int32 case (already returning `svBV32`). The `isDerefWrite` `svInt→BV64` coercion never fires for `int(f)` anymore; stored heap term is `fp.to.sbv 64 RTZ f` (pure QF_BVFP). Version bump: `symexWalkerVersion` "12"→"13". `renderAsChoicesVersion` unchanged at "4" (rendered int64 witness value is identical; only the Z3 internal term representation changes). No consumers required fixing: downstream code that previously reconciled `svInt` with `svBV64` now gets `svBV64` on both sides (direct BV comparison). Tested in `tests/tsymex_phase15_F5hang_derefwrite.nim` (2 tests: sxSat + round-trip + domain-bound hint). Regression green on both c + cpp backends: F5_float_conv, CR3_CR4_CR6_float, rereview_drains, CR1_CR5_closure_heap, CR2_cachekey (updated "12"→"13"), F8_smoke all pass. |
| F5-probeproto | High | fixed | runtime.nim:1894-1895 | `probeProto`'s `iekConvFloatToInt` arm was NOT updated when commit 6c983e4 fixed `lower()` to return `svBV64`/`svBV32` for int(f)/int32(f). The stale `svInt` proto caused: (1) `int(f) > 5` / `int(f) == 42` vs literal: reconciliation route through `bv2int(fp.to.sbv f)` reintroduced the F5 bv2int-over-FP pathology (hang on ordering goals, wrong encoding on equality); (2) `int(f) + 5 == K` / `int32(f) + 5 == K` arithmetic vs literal: `binBV`'s `doAssert l.kind == r.kind` fired → AssertionDefect crash (svBV64 vs svInt / svBV32 vs svInt). Variable-operand cases (e.g. `int(f) > n` with `n: int`) were fine because probeProto is not used when both sides are env vars. **Fixed**: `probeProto`'s `iekConvFloatToInt` arm now returns `some(SymVal(kind: svBV32, signed: true, bv32: mkBitVec[32](0)))` for `e.convWidth == 32`, and `some(SymVal(kind: svBV64, signed: true, bv64: mkBitVec[64](0'i64)))` otherwise — matching what `lower()` actually returns. Version bump: `symexWalkerVersion` "13"→"14". `renderAsChoicesVersion` unchanged at "4". Tested in `tests/tsymex_phase15_F5_probeproto.nim` (5 tests: ordering/equality/arithmetic vs literal for int64 and int32). RED: tests 3+4 crashed with `binBV: width mismatch [AssertionDefect]`; tests 1+2 passed due to Z3 simplifier. Regression green on both c + cpp backends: F5_float_conv, CR3_CR4_CR6_float, F5hang_derefwrite, F8_smoke, CR2_cachekey (updated "13"→"14") all pass. |
| field-container-assert | Med | fixed | runtime.nim:5026,5431,5984,6122 | Four `doAssert stmt.*.kind == iekVar` guards (isIndex/ixArr, isVariantField/vfRecv, isDeref/dPtr, isDerefWrite/dwPtr) added in commit cecc36a wrongly assumed the container/receiver/pointer expression is ALWAYS a bare env var (`iekVar`). The parser returns `iekField` for tuple/object field-access containers, which `lower()` has always handled correctly. So programs with a seq/table/variant/ref field accessed on an object parameter crashed: `bag.xs[i]` → ixArr=iekField → doAssert; `o.inner.x` → vfRecv=iekField → doAssert; `h.p[]` → dPtr=iekField → doAssert; `h.p[]=v` → dwPtr=iekField → doAssert. **Fixed**: relaxed each doAssert from `== iekVar` to `in {iekVar, iekField}` with updated diagnostic messages. Both `iekVar` and `iekField` are side-effect-free env/field projections that `lower()` handles without closure calls or float→int conversion; no seed/drain is needed. Note: the ref-field deref shapes (`h.p[]`, `h.p[]=v`) still produce sxUnknown (not sxSat) due to a pre-existing sort mismatch between `classifyFieldType`'s named-placeholder (`tTuple([], [], "int")`) and `isDeref`'s `dElemTy` (`tInt(64, true)`); that's a separate pre-existing bug beyond this fix's scope. No version bump (no verdict change; prevents crash). Tested in `tests/tsymex_phase15_fieldcontainer.nim` (5 tests: seq-field-index sxSat, deref-of-field sxUnknown, deref-write-of-field sxUnknown, variant-field-of-field sxSat, table-via-field sxSat). RED: all 5 crashed with AssertionDefect. Regression green on both c + cpp backends: r3_deref_read, r4_deref_write, r6_refobj, F9c_variant_float, phase11_fielddefect all pass. |
| CR-1 | High | fixed | runtime.nim:6144-6154 | Closure-body heap writes (`p[]=v`) never merged back to caller path → **false UNSAT** for refs mutated inside a closure | HOLDS (verifier: no return-merge after closure descent; comment "inert until R4" stale, R4 shipped). **Fixed**: added `currentClosureExitHeaps/AllocCounters/LiveRefs/DidMutateHeap` threadvars + `drainClosureExitHeap` helper; `applyClosureGround` merges exit paths' heaps after body descent; `isLet`/`isAssign`/`isIf` walk arms drain back to the survivor path. Also fixed spurious `sawUnknown = true` for void closures (no-value body with non-empty fallThrough no longer degrades to sxUnknown). Tested in `tests/tsymex_phase15_CR1_CR5_closure_heap.nim` (CR-1 tests 1–4). |
| CR-2 | High | fixed | canonicalize.nim:590-604 | 4 verdict-affecting settings absent from cache key → stale-cache unsoundness: `defectExclusions`, `maxClosureInlineCount`, `maxBytesEncodingLen` (flip sxSat↔sxUnknown), `maxFreshnessAssertions` (false-SAT dir) | HOLDS (verifier confirmed 4/6; `maxInstantiationsPerProc` refuted=self-protected via prog.procs; `maxSplitParts` harmless=unwired). **Fixed**: Added `;de=` (`defectExclusions` as `$set[DefectKind]` — deterministic ordinal-order bitmask), `;mcic=` (`maxClosureInlineCount`), `;mbel=` (`maxBytesEncodingLen`), `;mfa=` (`maxFreshnessAssertions`) to `canonicalize(SymexSettings)`. `maxInstantiationsPerProc` confirmed self-protected (prog.procs key already covers it; not added). `maxSplitParts` confirmed unwired (CR-18 tracks wiring; deliberately excluded with comment). Comment block replaced with an audited inclusion/exclusion list explaining EACH field's in/out reason. Consolidated version bump: `symexWalkerVersion` "10"→"11" (CR-1/CR-3/CR-4/CR-5/CR-6 model changes), `renderAsChoicesVersion` "3"→"4" (CR-3/CR-6 float witness + CR-1/CR-5 heap/closure witness shape changes; note CR-4 svBV32 is internal — `renderAsChoices` operates on the extracted Nim `int32` via `SomeSignedInt → integerChoice`, unchanged). Both version constants single-sourced in `canonicalize.nim` (grep confirmed no duplicate in runtime.nim). Tested in `tests/tsymex_phase15_CR2_cachekey.nim` (7 sub-tests: 4 inclusion tests, 1 exclusion guard for `maxSplitParts`, 2 version bumps). All pass on c + cpp backends (parity-check). Cross-reference: CR-18 (`maxSplitParts` wiring) remains deferred. |
| CR-3 | Med | fixed | runtime.nim:2637-2720 | float→int out-of-range: silent unsound witness, no sxUnknown/hint (won't round-trip; Nim RangeDefects) | HOLDS (documented "unsoundness window"; no guard). **Fixed**: domain-bounded + exclusion hint. Added `convFloatToIntBoundConds` threadvar; `iekConvFloatToInt` arm deposits `f >= lo and f < hi` constraint (which also excludes NaN/Inf via IEEE comparison semantics) drained into `p.pc` by walker arms (mirrors `parseIntRaiseConds`); `feConvDomainExcluded` (sevHint) emitted to `convFloatToIntDomainHints` and drained into `RawResult.errors`. RangeDefect modeling remains Phase-16. Tested in `tests/tsymex_phase15_CR3_CR4_CR6_float.nim` (CR-3 tests 1-4). |
| CR-4 | Med | fixed | runtime.nim:2637-2720 | `convWidth` set by parser but never read; `int32(f)` modeled as 64-bit truncation → wrong witness in (INT32_MAX,INT64_MAX] | HOLDS. **Fixed**: `iekConvFloatToInt` now reads `e.convWidth`; for width 32 uses `toSbv[..,32]` and returns `svBV32` (signed); domain bounded to `[-2^31, 2^31)`. Composes with CR-3 bounds. Tested in `tests/tsymex_phase15_CR3_CR4_CR6_float.nim` (CR-4 tests 1-2). |
| CR-5 | Med | fixed | runtime.nim:6146 | closure-descent `descentBase.liveRefs` empty → closure-body `new T` not asserted distinct from caller refs → spurious aliasing witness | HOLDS. **Fixed**: added `currentCallerLiveRefs` threadvar seeded by `seedCallerHeapThreadvars` from the caller path's `liveRefs`; `descentBase` now includes `liveRefs: currentCallerLiveRefs` so `assertFreshness` for closure-body `new T` sees the caller's already-minted refs and emits `newRef != callerRef`. Tested in `tests/tsymex_phase15_CR1_CR5_closure_heap.nim` (CR-5 test 1). |
| CR-6 | Med | fixed | runtime.nim:2407-2447 | `cmpFloat` `doAssert a.kind==b.kind` crashes (not sxUnknown) for float32-var vs float64-var compare (Invariant-3) | PARTIAL (narrow case holds; retBindEq path refuted). **Fixed**: mixed-FLOAT reconciliation added at the top of `cmpFloat`: when a.kind==svFloat32 and b.kind==svFloat64 (or vice versa), widen the float32 to float64 via `toFp(rmRNE(), fp32, Z3Float64)` before the comparison — mirrors Nim semantics and the mixed-integer reconciliation at ~3204-3210. The `doAssert` now checks `in {svFloat32, svFloat64}` (not `==`) for a clean classified error if a non-float somehow arrives. Tested in `tests/tsymex_phase15_CR3_CR4_CR6_float.nim` (CR-6 tests 1-3). |
| CR-7 | Med | open | runtime.nim (god module) | runtime.nim 7057 lines; 5 independent theory subsystems inlined in lower()/walk(); split into runtime_{heap,closures,exceptions,floats,strings}.nim | design judgment |
| CR-8 | Med | fixed | E7_smoke.nim / dt-bounded.sh | C/C++ backend parity is a manual workflow step, not an automated regression gate; exception witnesses not replayed against SUT at runtime. **Fixed**: (Part A) `scripts/parity-check.sh <test.nim>` runs the same test under both `c` and `cpp` via `dt-bounded.sh` and fails if either backend fails or they diverge — this is now the canonical RFC DoD "both backends" gate. (Part B) `tests/tsymex_phase15_E_roundtrip.nim` adds exception-witness runtime replay across three E-cluster shapes (condRaise/ValueError, finallyReplaces/IOError, finallyReplaces/ValueError, assertDefect/AssertionDefect): after `symexFind` reports `sxRaised`, the actual Nim proc is called at runtime with `raisedWitness[...]` and the expected exception is confirmed raised; contrasting inputs confirm no spurious raise. 7/7 tests pass on both `c` and `cpp`. | reviewer (coverage) |
| CR-9 | Med | in-progress | runtime.nim:4805-4850 | design: SymVal.signed phantom field; 5-way svInt/svBV split; ad-hoc caps → ResourceBudget; 28 threadvars → LowerCtx. **Stage 1 done**: added `lowerInExpr` (lines 4805–4830) and `lowerBoolInExpr` (lines 4832–4850) wrappers immediately after `drainPendingLowerEffects`. Each wrapper encapsulates the canonical seed→reset-transient-sinks→lower/lowerBool→drain sequence so a walk arm cannot structurally forget to drain. No call sites migrated (Stage 2). `w: var WalkCtx` param included for Stage 6 in-place migration. Nim warns on unused `var` params are not emitted in practice; no `{.used.}` pragma needed. Test: `tests/tsymex_phase15_cr9_lowerInExpr.nim` (6 tests: float→int let sxSat+hint+round-trip, plain arith let sxSat+round-trip, float→int while-guard sxSat+hint+round-trip). All 6 pass on both c+cpp backends. All Stage-0 canaries re-verified green after addition (zero verdict changes). **Stage 2 done** (commits c28ccd4…d2203f5, 2026-06-21): migrated 9 of 10 arms in scope — one arm per commit, parity-check canary after each. Arms migrated: (1) `isLet` rhs → `lowerInExpr`; (2) `isAssign` rhs → `lowerInExpr`; (3) `isWhile` cond → `lowerBoolInExpr`; (4) `isReturn` retExpr → `lowerInExpr` (proto threaded from frame retSym); (6) `isAssert` cond → `lowerBoolInExpr` (drainParseIntRaises called on returned path); (7) `isVariantReassignSymbolic` vrsRhs → `lowerInExpr`; (8) `isIndex` ixIdx seq-path → `lowerInExpr` (svInt proto); (8b) `isIndex` ixIdx array-path → `lowerInExpr`; (9) `isDerefWrite` dwValue → `lowerInExpr` (proto from allocateSym, computed before wrapper); (`isIf`) per-branch cond → `lowerBoolInExpr` (NI-1 preserved: wrapper seeds from current `cp`, not original `p`; returned path threaded back as `cp = cp2`; DES-4 comment updated). Arms migrated also received the `parseIntRaiseConds=@[]` reset that several arms previously omitted (safe net addition). Deferred: `isCall` actual-args lowering loop (arm 5) — structure incompatible: the arm seeds ONCE before the multi-arg loop then drains ONCE after; per-arg wrapper calls would reset threadvars between args, breaking multi-arg float-bound accumulation. Left as-is. Zero verdict flips; zero hangs (exit 137). All canaries green on both c+cpp: rereview_drains, CR1_CR5_closure_heap, CR3_CR4_CR6_float, F5hang_derefwrite, cr9_lowerInExpr. **Stage 3 done** (commit 252a095, 2026-06-21): added `lowerLeafInExpr(p: Path, e: IRExpr): SymVal` helper at line 4850 (immediately after `lowerBoolInExpr`). It centralises the `doAssert e.kind in {iekVar, iekField}` guard and bare `lower(p.env, e)` call (no proto, no seed/drain) with a doc-comment naming WHY no drain is needed: A-normal-form leaf operands cannot carry closure calls or float→int conversions. Four arms migrated — inline doAssert+lower replaced by `lowerLeafInExpr(p, stmt.X)`: `isIndex`/`ixArr` (line 5070), `isVariantField`/`vfRecv` (line 5466), `isDeref`/`dPtr` (line 6012), `isDerefWrite`/`dwPtr` (line 6147). No arms passed extra args to lower() — default proto only, no helper adaptation needed. Behaviour byte-identical (same assert, same bare lower, no seed/drain added/removed). Zero verdict changes. Gate: parity-check c+cpp all green: fieldcontainer (5/5), r3_deref_read (4/4), r4_deref_write (7/7), r6_refobj (5/5), S3_strindex (7/7), F9c_variant_float (3/3), rereview_drains (14/14). **Stage 4 done** (commits 4448450…5cd72cd, 2026-06-21): migrated all 6 Group-1 caches (4 commits). Nil-guard approach: since the populators (`allocRefSort`, `allocDistinctSym`, `buildClosure`) are defined before `WalkCtx` and thus cannot use `cast[ptr WalkCtx]` directly, each cache uses a forward-declared `sync*Entry` proc (defined after `WalkCtx`) that does a nil-guard + statics write. Threadvars always written at population time; `sync*Entry` additionally writes to `WalkerStatics` when `currentWalkCtxPtr != nil`. A pre-walk initial sync loop in `runSymexImpl` seeds `WalkerStatics` with any entries allocated during env setup (before `currentWalkCtxPtr` is set). Post-walk mirror loops for all 4 caches replaced with no-op comments. `currentWalkCtxPtr` moved earlier (before `allocRefSort`) since it is typed `pointer` with no `WalkCtx` forward-reference. **Migrations**: (4a) `currentRefSorts`/`currentNilConsts` via `syncRefSortEntry`; post-walk mirror removed; `nilDerefFork` reads statics via nil-guard. (4b) `currentDistinctSorts`/`distinctSortNames` via `syncDistinctSortEntry`; `WalkerStatics.distinctSorts` type widened to `Table[string, DistinctSortEntry]` (was `Z3Sort` only); `distinctSortNames: seq[string]` field added to WalkerStatics; post-walk mirror removed. (4c) `currentClosureSyms` via `syncClosureSymEntry`; post-walk mirror removed. (4d) `currentClosureBodies` via `syncClosureBodyEntry`; `closureBodies` field added to WalkerStatics; `applyClosureGround`/`lowerHofCall` axiom path read via nil-guard. All canaries green on both c+cpp after each commit: rereview_drains, r1_refsort, g4_distinct_sort, C2a_closure_capture, C2b_closure_call, CR1_CR5_closure_heap, r13_closure_ref. Zero verdict flips; zero hangs; zero nil-ptr probe crashes. **Stage 5 done** (commits fc95a70…38e7c72, 2026-06-21): migrated 8 of 11 Group-3 error/hint sinks (8 commits, one per sink). The sync-helper dual-write idiom from Stage 4 is reused throughout: threadvar is written first (probe/pre-walk fallback), then a `sync*` helper (forward-declared before the add-site, defined after `WalkCtx`) appends to the corresponding `WalkCtx` field when `currentWalkCtxPtr != nil`. Verdict-assembly drain reads `w.field & threadvar` (union covers all paths). **Migrated** (all confirmed non-silently-dropped by their gating tests): (5a) `freshnessCapHints` → `WalkCtx.freshnessCapHints` via `syncFreshnessCapHint`; gate r2_new test 4 (heFreshnessCapExceeded check). (5b) `heapDepthErrors` → `WalkCtx.heapDepthErrors` via direct `w.heapDepthErrors.add` (heapDepthExhausted has `w: var WalkCtx`); gate r9_recursive test 1 (heDepthExhausted check). (5c) `ptrFamilyHints` → `WalkCtx.ptrFamilyHints` via direct write (isDeref/isDerefWrite have `w`); gate r8_ptr test 1 (hePtrFamily check). (5d) `unknownExnWarnings` → `WalkCtx.unknownExnWarnings` via direct write (routeRaise has `w`); gate E4_hierarchy (4/4), E4a_userexn (2/2). (5e) `distinctBijectivityHints` → `WalkCtx.distinctBijectivityHints` via `syncDistinctBijectivityHint`; gate g4_distinct_sort test 1 (geDistinctBijectivitySkipped check). (5f) `convFloatToIntDomainHints` → `WalkCtx.convFloatToIntDomainHints` via `syncConvFloatToIntDomainHint`; gate CR3_CR4_CR6_float CR-4 tests (feConvDomainExcluded check). (5g) `extractionErrors` → `WalkCtx.extractionErrors` via `syncExtractionError` (4 add-sites in extractLeaf/extractFromSymVal); gate phase12_witnesses (10/10), F7_float_extract (6/6). (5h) `currentClosureCallErrors` → `WalkCtx.closureCallErrors` via `syncClosureCallError` (7 add-sites across lowerClosureCall/applyClosureGround/lowerHofCall); gate C2b (3/3), C4_hof test 2 (ceUnsupportedHof check), C6 (6/6), CR1_CR5 (5/5). **Deferred** (3 sinks — too entangled with `trySolve` mid-walk read): `parseIntGateConstraints` (appended by lower(iekParseInt), READ inside trySolve which has no WalkCtx param); `currentClosureCallAxioms`/`currentClosureCallAxiomStrs` (appended by applyClosureGround, READ inside trySolve). Migrating these three would require threading to trySolve or passing the seq by ref; left as threadvars. All 8 migrated sinks: rereview_drains canary (14/14) green on both c+cpp after every commit. Zero verdict flips; zero hangs. **Stage 6 done** (commit 1be2701, 2026-06-21): migrated all 10 Group-2 transient sinks in a single coherent commit. Threading approach: cast idiom (`cast[ptr WalkCtx](currentWalkCtxPtr)` with nil-fallback) for helpers without `w` param; direct field write via `w` for helpers that already have `w: var WalkCtx`. Four new WalkCtx fields added (`convFloatToIntBoundConds`, `parseIntRaiseConds`, `callerHeaps/HeapDepth/AllocCounters/LiveRefs`, `closureExitHeaps/AllocCounters/LiveRefs/DidMutateHeap`). Three new forward-decl+definition pairs: `syncConvFloatToIntBoundCond`, `syncParseIntRaiseCond`, `seedCallerHeapInWalkCtx`. `seedCallerHeapThreadvars` now calls `seedCallerHeapInWalkCtx` (dual-write). `drainConvFloatToIntBounds`/`drainParseIntRaises`/`drainClosureExitHeap` use cast idiom to prefer WalkCtx field when ptr != nil. `drainPendingLowerEffects` resets WalkCtx exit-heap fields. `lowerInExpr`/`lowerBoolInExpr` reset `w.convFloatToIntBoundConds` and `w.parseIntRaiseConds` directly. `applyClosureGround` reads `w.callerHeaps/*` for `descentBase` and dual-writes merged exit-heap to both threadvar and `w.closureExit*`. **Correctness checks** (all confirmed): (NI-1) `seedCallerHeapInWalkCtx` zeroes `w.closureDidMutateHeap/ExitHeaps/AllocCounters/LiveRefs` at every lower() call site — stale exit-heap cannot cross elif branches or sequential calls via WalkCtx path. (CR-5) `seedCallerHeapInWalkCtx` copies `p.liveRefs` to `w.callerLiveRefs`; `applyClosureGround` reads `w.callerLiveRefs` for `descentBase.liveRefs` — freshness seeding preserved on WalkCtx path. (S-1) `applyClosureGround` liveRefs merge is still the true set-union loop (unchanged); writes to both `currentClosureExitLiveRefs` and `w.closureExitLiveRefs`. (idempotency) `drainPendingLowerEffects` resets both threadvar and WalkCtx exit-heap fields; second call is a no-op on both paths. **Canaries**: rereview_drains (14/14), CR3_CR4_CR6_float (9/9), F5hang_derefwrite (2/2, NO HANG), CR1_CR5_closure_heap (5/5), r9_recursive (3/3, NO HANG), S10b_strconv (4/4), C6_smoke (6/6), r13_closure_ref (1/1), r2_new (4/4) — all green on both c+cpp. Full R+C+F+E+S cluster gate: all tests pass on both c+cpp (S7a_bytes/S7b_smoke fail pre-existing — iekStrBytes hits lowerLeafInExpr assert, NOT related to Stage 6). Zero verdict flips; zero hangs (no exit 137 anywhere). | design judgment |
| CR-10 | Low | open | regex_parser.nim:272-281 | `{n,m}` repetition parsed as int64 then passed cuint → silent truncation >2^32 → wrong regex bound (absurd trigger) | reviewer (security) |
| CR-11 | Low | open | runtime.nim:2775-2789 | `iekStrSplit` concrete-inline paths lack parts-count cap → compile-time DoS on huge literal | reviewer (security) |
| CR-12 | Low | open | runtime.nim:839-843 | `liveRefs` grows O(N) memory even after `maxFreshnessAssertions` stops emitting Z3 assertions | reviewer (security) |
| CR-13 | Low | open | runtime.nim:6767-6769 | `currentWalkCtxPtr` left dangling if `walk()` raises (no current deref-after path; add try/finally) | reviewer (security) |
| CR-14 | Low | open | exn_hierarchy.nim:39-66 | `EOFError`/`LibraryError`/`ResourceExhaustedError` absent from table → sxUnknown+warn instead of subtype match (conservative) | reviewer (FSE) |
| CR-15 | Low | open | dsl_parser.nim:2341-2357 | `SomeOrdinal` concept table omits user enums → spurious geConceptViolation (safe direction) | reviewer (generics) |
| CR-16 | Low | open | types.nim:743,750 | dead enums `ceUnsupportedCapture`, `geUnresolvedGeneric` never emitted (cosmetic; underlying constructs classify/crash-visibly) | downgraded by verifier |
| CR-17 | Low | open | runtime.nim:2664-2665,2068 | latent Z3 hazards: `s[i]` ordering (String+Int+BV) and `toBv64ForFp` svInt int2bv arm — no current parser trigger | reviewer (Z3) |
| CR-18 | Low | open | runtime.nim:2792 | `maxSplitParts` cap defined but unwired (symbolic split throws sxUnknown first); wire + add to cache key when implemented | verifier |
| S-1 | High | fixed | runtime.nim:~6481-6484 (applyClosureGround liveRefs merge) | `liveRefs` union-merge in `applyClosureGround` used "take longest list" heuristic: equal-length arms silently dropped one arm's ref, allowing Z3 to alias the caller's `new T` with the dropped ref. **Fix**: replaced with true set-union (dedup by Z3 AST raw pointer identity) so BOTH closure-arm refs end up in `currentClosureExitLiveRefs`. **Test note**: S-1 cannot be triggered in a black-box test with the current API — closure-body refs are not externally accessible. Tests confirm the fix is inert against CR-5 and does not regress closure freshness. Verified by code inspection + absence of CR-5 regression. Consolidated into `drainPendingLowerEffects` audit. |
| S-2 | High | fixed | runtime.nim:~4937 (isWhile), ~5729 (isAssert) | `isWhile` and `isAssert` arms were missing `seedCallerHeapThreadvars` + `drainClosureExitHeap` around their guard/predicate `lowerBool` call. A closure call in the while-guard or assert-predicate that wrote through a ref had its mutation dropped → false UNSAT for the write's post-condition. **Fix**: Added `seedCallerHeapThreadvars(p)` + `convFloatToIntBoundConds = @[]` before `lowerBool` and `drainPendingLowerEffects(p)` after in both arms. Tested in `tests/tsymex_phase15_rereview_drains.nim` (S-2a-1/2 for `isAssert`, S-2b-1/2 for `isWhile`). Note: S-2b-2 uses `!= sxSat` rather than `== sxUnsat` because while-loop exhaustion sets `sawUnknown` on dead truePaths. |
| S-3 | Med | fixed | runtime.nim:~5604-5607 (isCall arg-lowering) | `isCall`'s arg-lowering loop called `seedCallerHeapThreadvars(p)` and `drainConvFloatToIntBounds(p)` but NOT `drainClosureExitHeap`. A closure-call in an argument position that wrote through a ref had its mutation discarded → false UNSAT downstream. **Fix**: replaced `drainConvFloatToIntBounds` with `drainPendingLowerEffects` (which drains BOTH float bounds and closure exit heap). Tested in `tests/tsymex_phase15_rereview_drains.nim` (S-3-1/2). |
| S-4 | Med | fixed | runtime.nim:~5495 (isReturn retExpr) | `isReturn`'s `lower(p.env, stmt.retExpr)` call lacked `seedCallerHeapThreadvars` (so a closure in retExpr descended with stale/empty heap threadvars) and `drainClosureExitHeap` (so the write was dropped). **Fix**: Added `seedCallerHeapThreadvars(p)` + `convFloatToIntBoundConds = @[]` before `lower` and `drainPendingLowerEffects(p)` after. Tested in `tests/tsymex_phase15_rereview_drains.nim` (S-4-1/2). |
| NI-1 | Med | fixed | runtime.nim:~4867 (isIf branch loop) | `seedCallerHeapThreadvars` was called ONCE before the branch loop; closure exit-heap threadvars were not reset per-branch. The first branch's exit heap (e.g. clo1 writing p[]=11) bled into the second branch (elif where clo2 ran), making clo1's write falsely visible on the elif path. **Fix**: moved `seedCallerHeapThreadvars(cp)` + `convFloatToIntBoundConds = @[]` INSIDE the branch loop so each branch seeds independently from the current `cp`. Tested in `tests/tsymex_phase15_rereview_drains.nim` (NI-1-1/2/3). |
| NI-2 | Med | fixed | runtime.nim:~6027 (isDerefWrite dwValue); also isVariantReassignSymbolic, isIndex | `isDerefWrite`, `isVariantReassignSymbolic`, and `isIndex` (seq + static array) all called `lower(stmt.dwValue / ...)` without `seedCallerHeapThreadvars` or `drainConvFloatToIntBounds`. For float→int RHS (`p[] = int(x)`): (a) domain bounds were never drained into `p.pc`; (b) `iekConvFloatToInt` returns `svInt` (Z3Int) but the heap array expects `svBV64`, causing a sort mismatch that made Z3 return UNSAT on a satisfiable goal. **Fix**: added seed + reset + `drainPendingLowerEffects` in all three arms; added `intToBv[64]` BV coercion after drain in `isDerefWrite` for `svInt → BV64` reconciliation. Tested in `tests/tsymex_phase15_rereview_drains.nim` (NI-2-1/2). |
| D-3 | Low | fixed | runtime.nim:~2427-2443 (reconcileFloat, cmpFloat) | Mixed-float widening logic was inlined in `cmpFloat` only (`a.kind == svFloat32 and b.kind == svFloat64` → widen via `toFp(rmRNE(), fp32, Z3Float64)`). **Fix**: extracted into a standalone `reconcileFloat(a, b: SymVal): (SymVal, SymVal)` helper with its own `doAssert` and identity fast-path (same width → unchanged). `cmpFloat` now calls `var (a, b) = reconcileFloat(a, b)` at the top. The helper is exported (`*`) for potential reuse by `arithFloat` (which still has its own `doAssert a.kind == b.kind` — mixed-precision arithmetic is a separate concern tracked by CR-6/ADR-0005). No separate tests needed: CR-6 regression tests cover the widening path. |
| DES-2 | Low | fixed | runtime.nim:~2453 (cmpFloat) | `cmpFloat` had a redundant `doAssert a.kind in {svFloat32, svFloat64} and b.kind in ...` that duplicated the same check inside `reconcileFloat` (D-3). Two assertion sites for the same invariant → DRY violation. **Fix**: removed the redundant `doAssert` from `cmpFloat`; `reconcileFloat`'s own assert is the single canonical check site (with a clearer diagnostic message). No test change needed: the CR-6 regression tests continue to cover the float-kind validation path through `reconcileFloat`. |
| NEW-1 | Low | fixed | runtime.nim:~5000 (isIndex/Table ixArr) | `isIndex` lowered `stmt.ixArr` (the container expression) without asserting it is an env-resident var (`iekVar`). If the parser ever emits a non-var container expression, float→int or closure effects from lowering it would be silently dropped. **Fix**: added `doAssert stmt.ixArr.kind == iekVar` with a diagnostic message directing future maintainers to add seed+drain. Comment added for the Table-key path noting that string sub-expressions cannot carry float→int convs or closures. |
| NEW-2 | Low | fixed | runtime.nim:~5405 (isVariantField/vfRecv), ~5958 (isDeref/dPtr), ~6096 (isDerefWrite/dwPtr) | Three walk arms lowered env-resident var expressions without asserting they are `iekVar`. If the parser ever emits a non-var receiver/pointer operand, effects from lowering it would be silently dropped. **Fix**: added `doAssert stmt.vfRecv.kind == iekVar` (isVariantField), `doAssert stmt.dPtr.kind == iekVar` (isDeref), and `doAssert stmt.dwPtr.kind == iekVar` (isDerefWrite), each with a diagnostic directing maintainers to add seed+drain if the parser changes. These are latent-safety guards: no active parser violates them today. |
| DES-4 | Low | fixed | runtime.nim:~4897 (isIf arm, condBool/drainPendingLowerEffects) | The `isIf` arm computed `condBool = lowerBool(cp.env, br.cond)` then immediately called `cp = drainPendingLowerEffects(cp)`, after which `condBool` was used to build arm/else path conditions. A reader might reasonably wonder whether `drainPendingLowerEffects` invalidates `condBool`. **Fix**: added a one-line invariant comment explaining that `drainPendingLowerEffects` only extends `cp.pc` (folds domain bounds / closure heap state into the path condition) and does NOT touch `cp.env`; since `condBool` is a Z3 AST term built from `cp.env` variables, it remains valid after the drain and can safely be added to the fork conditions. No code change; comment only. |
| SEC-1 | Low | fixed | runtime.nim:4914-4943 (isLet/isAssign walk arms) | `isLet` and `isAssign` arms applied `drainConvFloatToIntBounds` + `drainClosureExitHeap` separately (not via the consolidated `drainPendingLowerEffects`). The `drainClosureExitHeap` call did NOT reset `currentClosureExitHeaps/AllocCounters/LiveRefs/DidMutateHeap` to clean state, so a stale exit-heap from one `lower()` call could leak into the next arm's lower call if the exit-heap was set (though no active bug exists today because `drainClosureExitHeap` is a conditional no-op when `currentClosureDidMutateHeap==false`, and the `isLet`/`isAssign` arms correctly call `seedCallerHeapThreadvars` which resets `currentClosureDidMutateHeap` before the lower call). **Fix**: replaced the split `drainConvFloatToIntBounds(p)` + `drainClosureExitHeap(cp)` pair with a single `drainPendingLowerEffects(p)` call (which drains both sinks AND resets all threadvars), eliminating the split-drain fragility. The `drainParseIntRaises` step preserved (it follows the unified drain on the already-drained path). Regression: all existing tests pass on both c + cpp backends. |
| CR-20 / S7-bytes-index-assert | Med | fixed | runtime.nim:lowerLeafInExpr + isIndex/ixArr | **Attribution**: REGRESSION introduced by commit `cecc36a` ("uniform drain coverage + DES-2/DES-4 cleanups"). That commit added `doAssert stmt.ixArr.kind == iekVar` in the `isIndex` arm (NEW-1) and the corresponding assert was later centralised into `lowerLeafInExpr` (commit `252a095`, Stage 3 of CR-9) as `doAssert e.kind in {iekVar, iekField}`. Both asserts are **over-strict**: the parser does NOT fully A-normalise `ixArr` — `bytes(lit)[i]` emits an `iekStrBytes` expression directly as `ixArr` (not a named binding). Commit `1d41e64` widened the assert to `{iekVar, iekField}` for the `field-container-assert` fix but did not add `iekStrBytes`. Tests `tsymex_phase15_S7a_bytes.nim` and `tsymex_phase15_S7b_smoke.nim` PASSED at baseline `26b0e82` (pre-review) and FAILED beginning at `cecc36a` (first review commit). **Root cause analysis**: `bytes(s)` returns `seq[byte]`; when immediately indexed as `bytes("A")[0]`, the parser (dsl_parser.nim, `itSeq` branch of bracket-expr handling) passes the `iekStrBytes` expr directly as `stmt.ixArr` without inserting a synthetic let-binding. **Soundness of fix**: `iekStrBytes` in `ixArr` position is side-effect-free — the `lower()` arm for `iekStrBytes` operates only on a string literal (`iekStrLit` receiver, else raises `SymexBytesSymbolicLengthError`/`SymexBytesLengthTooLargeError`), producing a Z3 const-array of BV8 via `mkString`/`store`/`intToBv`/`toCode`/`at` — no closure calls, no float→int conversions. So no seed+drain is needed. **Fix**: widened `lowerLeafInExpr`'s assert to `{iekVar, iekField, iekStrBytes}` with updated doc-comment enumerating why each admitted kind is side-effect-free; updated the `isIndex` drain-coverage comment to correctly describe `iekStrBytes` as the known parser exception. **No version bump** (no verdict change for passing programs — fix only un-crashes previously-asserting shapes; `iekStrBytes` lowering is unchanged). S7a (5/5) and S7b (8/8) now pass on both c and cpp backends. Collateral green: rereview_drains (14/14), r3_deref_read (4/4), r4_deref_write (7/7), cr9_lowerInExpr (6/6), fieldcontainer (5/5), S3_strindex (7/7). |

## Refuted (false positives — recorded, not presented)

| claim | why refuted |
|-------|-------------|
| float→int ordering HANG (FP+BV+Int) ×2 reviewers | `toSbv` consumes FP, `bv2int` lifts to Z3Int; ordering goal is pure integer arith — not the F5 pathology |
| closureInlineCount budget non-transitive (HC1) | line 6136 overwrites zero-init with `frameStack[^1].closureInlineCount + 1` — accumulates correctly |
| heapDepthExhausted in-place mutation inflates nil branch (HC4) | `forkPath` builds fresh Path objects; both branches inherit copied depth uniformly, no asymmetry |
| buildClosure capture-drop → svTupleEq doAssert crash | different env arities → different funcSym keys → svTupleEq never called cross-arity |
| `owned T` → silent verdict (heUnsupportedOwnership dead) | enum IS emitted via SymexOwnershipUnsupportedError catch → sxUnknown |
| unsupported capture → silent wrong verdict | Table[int,int] crashes visibly (ValueError); absent-name drop carries no SymVal, benign |
| unresolved generic → silent verdict | compile-time error() or geInstantiationCapped + sawUnknown → sxUnknown |
| retBindEq doAssert crash via int(float) return | both sides svInt; reconciliation guard handles it; no mismatch |
| maxInstantiationsPerProc missing from cache key | self-protected: cap changes prog.procs, which IS in the program key |
| pcImpliesNonNil ref-prefix collision | self-retracted; trailing `_` separator prevents collision |

## Verified-correct (positives confirmed by reviewers)

G1a bare-name collision fix · distinct-sort bijectivity is ground (no ∀) · freshness pairwise-ground · mapArray is not a quantifier · mkStringVar ≤0xFF byte constraint · toBv64ForFp (int→float) · string mutation/iteration classified honestly · IEEE float-eq semantics · finally on normal+exceptional paths · getCurrentException-outside-handler classified · Z3 FFI cstring lifetimes / no use-after-free / refcount leak benign · witness ≤0xFF round-trip · NaN/Inf, alias-visibility, heapDepth, nil-fork all tested.
