# Phase 15 (language fragments) — handoff

- **Stage:** 2 architect → round 2 COMPLETE → **reconciliation IN PROGRESS** (RFC drifted from real codebase; see `RFC-phase15-reconciliation.md`) → stage 3 TDD grind
- **Resume:** `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; stop when every slice is implemented`
- **Progress:** Clusters Z, L, F, S, H, E COMPLETE. **G0-ADR SHIPPED** (2026-06-15): ADR-0008 confirmed on disk (Accepted, matches preamble D1-D7); §F Cluster G reconciliation written. **G1a SHIPPED** (2026-06-15): generic instantiation key — fixed the bare-name `ctx.procs`/`parsing` collision (per locked decision: NO `isGenericCall` IR). Shared `instKeyFor`/`bodyHashPart` builds an ADR-0008 D2 key `name#<symBodyHash|lineInfo>#<sorted T=Type tuple>`; `ensureProcRegistered` returns it and all three call-emission sites pass it as the `mkCall` callee name → registration AND walker dispatch (`w.procs[stmt.callee]`) share ONE key. Non-generic procs keep the bare name (unchanged). `tests/tsymex_phase15_G1a_instkey.nim` 4/4 c+cpp (RED demonstrator `szof[T]:sizeof(T)` at int8+int64 sxUnsat→sxSat); 9/9 regression; walker version stays "7". **G1c SHIPPED** (2026-06-15): generic instantiation CAP (G1b folded into G1a; the dispatch+cache were already shipped, so G1c is purely the cap). NET-NEW `SymexSettings.maxInstantiationsPerProc` (default 64; RFC's "already in Z3" claim was FALSE) + `defaultSymexSettings` + `+`-merge ripple. PER-BASE-PROC counter in `ensureProcRegistered` keyed by the generic's DEFINITION site (`impl.lineInfoObj` file:line:col — the only identity stable across instantiations AND module-disambiguating; `symBodyHash` is per-instantiation so couldn't key it). Over-cap → instantiation NOT registered → `mkCall` key absent from `w.procs` → the G1c-hardened missing-callee walker arm binds a fresh retSym + marks the path uncertain → sxUnknown (was KeyError). `geInstantiationCapped` (sevError, REUSED from Z3a) → `ParseCtx.parseErrors` → `ParseResult`/`SymexProgram.parseErrors` → drained into `RawResult.errors` by `runSymexImpl`, which also FORCES sxUnknown on any sevError parse-error (Invariant 3). Cap threads settings→parser: macros pass `settings.maxInstantiationsPerProc` into `parseProc`→`newParseCtx`. `SymexProgram` moved below `SymexErrorInfo` in `types.nim` so its new `parseErrors` field can name that type. `withSymexSettings` sets the new field (verified). `tests/tsymex_phase15_G1c_instcap.nim` 2/2 c+cpp; 8/8 regression; walker version stays "7". **G3 SHIPPED** (2026-06-15): type-substitution through `classifyType` + auto-return safety (audit + guards). **Already worked:** the `classifyType` text-match fallthrough already maps a substitution-derived `float64`/`string`/`bool`/`int`/enum SYM to the right `IRType` and already `error`s cleanly (Invariant 3 held pre-G3), so the centerpiece PARAM classification was sound. **Had to FIX (gaps the float/sink RED surfaced — substitution sound, WALK path + `sink` AST shape were not):** (1) **float/string proc-RETURN** — the `isReturn` retConstraint raised "composite return not yet wired" for floats/strings and all FIVE call-return retSym allocations used `bvVar` (asserts `itInt`), so a value-returning generic at `float64` HARD-CRASHED before its target; added `retBindEq` (NaN-safe structural float eq `(a==b) or (both NaN)`; native int/bool/string eq) + `freshRetSym` (routes every retSym through the type-aware `allocateSym`, threading init constraints — string byte-range floor etc. — onto post-call survivors AND into the call-cache `pcDelta`); wired all five sites (`runtime.nim`). (2) **`sink T` through generics** — the typed generic AST presents `sink T` as `nnkCommand[sink,T]` (NOT the `sink[T]` bracket Z3c handled) and `gatherTypeSubst` only bound a BARE `nnkIdent` formal, so `T` never bound → body `T` un-monomorphised → `classifyType` errored on `T`; fixed `gatherTypeSubst.unwrapGenericTy` to strip `var`/`sink`/`lent` (command+bracket) before name-match, and taught `classifyType` to strip the `nnkCommand` sink/lent form BEFORE `getTypeInst` (the substituted `sink int` command carries no type → "node has no type"). **Guard ADDED (DoD):** auto-return `nnkEmpty` guard in `parseCalleeImpl` — a monomorphised proc that DECLARED a return type but resolves it to `nnkEmpty` now `error`s instead of silently treating it as void (defensive; Nim resolves `auto` pre-getImpl). Float64 instantiation symex's correctly THROUGH the generic call (sxSat float witness, not sxUnknown — Cluster F bridge via generics). String proc-RETURN extraction DEFERRED (pre-existing unwired path, not a generic concern; degrades soundly to sxUnknown). `tests/tsymex_phase15_g3_type_subst.nim` 2/2 c+cpp (float + sink); 10/10 regression (G1a_instkey, G1c_instcap, rectify_generics, F2_float_literals, F5_float_conv, F8_smoke, phase3_recursion, phase4_tuple, S3_strindex, phase1_arith); walker version stays "7". **G4 SHIPPED** (2026-06-15): `distinct T` as a fresh uninterpreted Z3 sort. New `IRTypeKind itDistinct{distinctName, distinctBase}` (ctor `tDistinct`; exhaustiveness ripple chased across `==`/`$`/canonicalize/`emitIRType`/`classifyType`/`allocateSym`/`tyOf`/`iteSV`/`coerceIntLit`/`symValHash`/`extractFromSymVal`/`extractLeaf`/`defaultZero`/the `runSymexImpl` param-loop — bounded compiles). `classifyType` maps `type Foo = distinct Bar` (the `nnkDistinctTy` in `getImpl`) → `itDistinct(name=Foo, base=classify(Bar))`, BEFORE the object/enum/alias unwrap paths. **SymVal = NEW `svDistinct{distinctAst: Z3AnyAst, distinctName, distinctBaseSym: ref SymVal}`** (NOT a reuse of E8's `svUninterpRef` — that has no base; G4 needs the boxed ejected base for the witness eject-chain + conversions). **distinctSorts placement:** live cache is a per-run threadvar `currentDistinctSorts: Table[string, DistinctSortEntry{sort, inject, eject}]` (reset at `runSymexImpl` — `allocateSym` has no `WalkCtx` access, exactly E8's threadvar idiom); `WalkerStatics.distinctSorts: Table[string, Z3Sort[stUninterpreted]]` ADDED per ADR D4 + mirrored post-walk. **inject/eject:** raw `Z3_mk_func_decl` (phantom `Z3FuncDecl` unusable — runtime-known sorts), declared once per (name, run). **Round-trip:** decidable base {int,BV,bool} → a GROUND per-occurrence pin `eject(dConst)==baseSym`, NOT a universal quantifier. **CRITICAL HANG FINDING:** the universal `∀x. eject(inject(x))==x` HANGS 180s (MBQI loop); WITH triggers it returns `unknown`; even a GROUND REVERSE `inject(baseSym)==dConst` ALSO hangs (uninterpreted-fn-over-BV). The eject-pin alone is the decidable model (QF_UFBV, sxSat/sxUnsat) and meets the DoD; inject declared but never applied. **int-base CONFIRMED non-hanging** under the bounded runner. FP/String base → round-trip SKIPPED + `geDistinctBijectivitySkipped` (sevHint, REUSED) drained on every verdict branch. Z3 4.15 footguns fixed: the uninterpreted sort needs explicit `Z3_inc_ref(Z3_sort_to_ast(...))` (else GC → `Z3_UNKNOWN_SORT` → SIGSEGV); `Z3_mk_app` args + func-decl domains must be HEAP seqs; intermediate app results must be `wrap`-ped. **emitTyAndReader** `itDistinct` renders `DistinctName(baseReader)` + `extractFromSymVal(svDistinct)` extracts the base at the same path (Breadth-CRIT-1); `ejectBase` (recursive) auto-ejects distinct operands in binop/unop sites so `float64(m)`/`int(u)` lower right. **geDistinctBarrier** ADDED to `SymexErrorKind` (net-new, sevError; implicit coercion rejected by the semchecker pre-symex — D5-style, no synthetic emission). `tests/tsymex_phase15_g4_distinct_sort.nim` 4/4 c+cpp; 10/10 regression (G1a_instkey, G1c_instcap, g3_type_subst, rectify_generics, phase4_tuple, phase5_seq, F2_float_literals, S3_strindex, phase1_arith, E8_getcurrentexn); walker version stays "7". **G5 SHIPPED** (2026-06-15): `distinct` borrow semantics. A `{.borrow.}` operator on a `distinct T` threads the op through the BASE type via the G4 BOXED-base SymVal — NOT the Z3 `inject_T(eject_T(a) op eject_T(b))` function-application chain the RFC §G5 text describes (G4 proved that uninterpreted-fn-over-BV / MBQI chain HANGS). Parser (`dsl_parser.nim`): `hasBorrowPragma`/`borrowInfoFor` classify an operator SYMBOL as a borrow shim (`nnkPragma` child holding `ident"borrow"`; typed AST presents `m1 + m2` as `nnkInfix` whose `n[0]` is the borrow proc sym, body = bare `Sym` = the base op → must NOT be body-parsed); the return type (`impl[3][0]`) classifies `itDistinct`=arithmetic-rebox else=comparison-bool; the `nnkInfix` arm intercepts a borrow sym and emits NEW `iekBorrowOp` IR (`mkBorrowOp`), exhaustiveness ripple chased across emitExpr/render/canonicalize/abstraction/probeProto. Runtime (`runtime.nim`): the `iekBorrowOp` lower arm ejects both operands (`ejectBase`→G4 `distinctBaseSym`), applies the base op (`arithInt`/`arithFloat`/BV, `cmpInt`/`cmpFloat`/`cmpString`/BV), and for arithmetic RE-BOXES via new `reboxDistinct` (fresh distinct-sort const from `currentDistinctSorts` + computed base boxed under; name uniquified by `currentBorrowReboxCounter` threadvar, reset at run entry) — comparison returns raw svBool. `Meters(10.0)` is the G4 pass-through-to-base-float-literal construction (no new code). geDistinctBarrier (Invariant 3): in `ensureProcRegistered`, a NON-borrow proc with empty body (`impl[6]==nnkEmpty`, e.g. `{.importc.}` magic — a truly forward-declared bodyless proc is rejected by Nim itself) taking a distinct param emits `geDistinctBarrier` (sevError, REUSED from G4, now with a live emission site) into `ctx.parseErrors` + is NOT registered → missing-callee arm + sevError → sxUnknown (never silent). CONFIRMED no hang (boxed-base op, inject fn never applied). `tests/tsymex_phase15_g5_distinct_borrow.nim` 3/3 c+cpp; regression 9/9 (g4_distinct_sort, g3_type_subst, G1a_instkey, rectify_generics, F2_float_literals, F3_float_arith, F4_float_compare, phase4_tuple, phase1_arith); walker version stays "7". **G6 SHIPPED** (2026-06-15): concept constraints / trust boundary. NO `isGenericCall` node — RFC §G6 GREEN (a walk-time `of isGenericCall:` guard reading `gcTypeArgs`) reconciled to a PARSE-TIME check on the RESOLVED concrete type in `parseCalleeImpl`. NET-NEW `ProcSig.conceptConstraints: seq[string]` (types.nim) captures per-generic-param type-class constraint names by walking the ORIGINAL impl's `nnkGenericParams` (`impl[2]`/`impl[5][1]`): each `nnkIdentDefs` whose constraint node (`gp[gp.len-2]`) ≠ `nnkEmpty` contributes its sym name (compound `A and B` → `.repr`, no special-casing); added to `canonicalize(ProcSig)` (`cc=[…]`), `emitProcSig` leaves it defaulted (runtime never reads it). Stdlib membership table (`stdlibConceptMembers`: SomeNumber/SomeInteger/SomeFloat/SomeOrdinal/SomeUnsignedInt/SomeSignedInt) + the single entry point `conformsToStdlibConcept(concept, resolvedType)`: stdlib concept → membership; NON-stdlib (user) concept → `true` ("no violation to assert" = TRUST THE SEMCHECKER, validator skips). In `parseCalleeImpl`, a stdlib-constrained param bound to a non-conforming resolved type emits `geConceptViolation` (sevError, REUSED — already in enum) → `ctx.parseErrors` → forced sxUnknown (G1c/G5 plumbing, Invariant 3). Belt-and-suspenders: the semchecker already guarantees conformance on REAL source, so this never fires there — it's the test-injectable guard. NEGATIVE-TEST ADAPTATION: no `isGenericCall` node to malform, so the test injects a non-conforming pair DIRECTLY into `conformsToStdlibConcept` (`("SomeNumber","string")==false`, `("SomeFloat","int")==false`), asserting the violation is classified, plus conforming-`true` + user-concept-trust cases (geDistinctBarrier-sibling pattern). `tests/tsymex_phase15_g6_concept_constraint.nim` 3/3 c+cpp (POSITIVE `clampPos[T: SomeNumber]`@int → sxSat witness>10; NEGATIVE helper injection; COMPOUND `onlyInts[T: SomeInteger]`@int → sxSat witness 42); regression 9/9 (g4_distinct_sort, g5_distinct_borrow, g3_type_subst, G1a_instkey, G1c_instcap, rectify_generics, phase1_arith, phase4_tuple, F2_float_literals); walker version stays "7". **G7 SHIPPED** (2026-06-15): `static[T]` params as instantiation-key components. AST reality: the static-param CONSTRAINT is `nnkCommand[Ident "static", T]` (NOT `nnkStaticTy` as RFC §G7 guessed); the static VALUE is NOT a call arg (`foo[3]` loses its brackets in the typed `Call`) — Nim's semchecker ALREADY bakes it into the BODY (`x[N-1]`→`x[2]`, `x>N`→`x>3`, `static bool` `B`→`IntLit 0/1`); the only residue is a FORMAL TYPE naming N as an array dimension (`array[N,int]`). The two instantiation Syms are DISTINCT and `symBodyHash` already differs. PARSER (`dsl_parser.nim`): `staticParamNames`/`genericParamsNode` detect static params; `gatherTypeSubst` recovers an array-dimension N from the arg's `array[range[lo..hi],_]` getType (→`newLit(hi-lo+1)`) + adds `subst[N]` so `monomorphize` rewrites `array[N,int]`→`array[3,int]`; `instKeyFor` includes the value in the type tuple (array case) or appends a `#<bodyHash>#static` discriminator for a SCALAR static param (empty typeSubst would otherwise collapse to the bare name = the G1a collision); `parseCalleeImpl` single-expr-RHS path emits a lifted preamble (array index A-normalises) instead of asserting it away. TYPEBRIDGE: `classifyType` matches a monomorphized synthesized `array[<IntLit>,T]` on the RAW node BEFORE `getTypeInst` (no type otherwise). RUNTIME: `coerceToBoolSV` + widened bool `==`/`!=` arm coerce a `static bool` literal arriving as `IntLit 0/1` (`!=0`). REAL KEY SHAPE vs RFC's idealized `"foo#int;static=3"`: the real key carries a per-instantiation bodyHash (`name#<bodyHash>#N=3` array / `name#<bodyHash>#static` scalar), so the TEST asserts BEHAVIOR (per-instantiation literal index `x[2]` vs `x[4]`; bool polarity), not the string. No `cacheHitsFor` accessor exists (RFC DoD item not buildable). `tests/tsymex_phase15_g7_static_param.nim` 2/2 c+cpp; regression 9/9 (G1a_instkey, G1c_instcap, g3_type_subst, g4_distinct_sort, g6_concept_constraint, rectify_generics, phase4_tuple, phase4_array, phase1_arith); walker version stays "7". **G8 SHIPPED** (2026-06-15): multi-parameter generics (`proc foo[T, U](a: T, b: U)`) — mostly AUDIT + REGRESSION-GUARD (RFC §G8: "no structural changes beyond bug fixes the RED test surfaces"). Multi-param ALREADY worked: `gatherTypeSubst` binds ONE entry per generic-named param from the matching arg's `getType` (`argIx` advances once per formal name — NO conflation, NO off-by-one), and G1a's `instKeyFor` sorts the type tuple BY param name (ADR-0008 D6 order-independence), so reversed tuples (`int,string` vs `string,int`) get DISTINCT keys and never collide. The RED test CONFIRMS: `T`+`U` resolve INDEPENDENTLY (int arith on `a:int`, string eq on `b:string` in one proc, witness `(1,"ok")`); `bar[T,U]` at `T=bool,U=int` → `(true,6)` (no param-order dependency); the SAME `pick[T,U]` at reversed tuples dispatches to CORRECTLY-typed bodies simultaneously (witness `(7,"x","y",9)`) — behavioral proof the sorted key keeps distinct tuples distinct (no `cacheHitsFor` accessor exists). BUG FIXED (surfaced by RED, ORTHOGONAL to generics): a call whose FIRST arg is `itString` was unconditionally claimed by the Cluster-S `itString`-receiver call-routing guard (`dsl_parser.nim`), so a USER PROC with a `string` FIRST param (`proc foo(a:string,…)`, e.g. `solo(s)`) was mis-classified `seUnsupportedStringOp`→sxUnknown (proven minimal: even a never-compared string-first param failed; string-SECOND always worked). Fix: when the string-receiver model is `smkUnregistered` AND the callee resolves to a real user `nnkProcDef`, fall THROUGH to the user-proc call path instead of `iekStrUnsupported` (an unsupported stdlib string call with no user impl still → `iekStrUnsupported`, Invariant 3). Generic/multi-param machinery untouched. `tests/tsymex_phase15_g8_multi_param.nim` 3/3 c+cpp; regression 10/10 (G1a_instkey, G1c_instcap, g3_type_subst, g4_distinct_sort, g6_concept_constraint, g7_static_param, rectify_generics, S3_strindex, phase1_arith, phase1_bool) + extra S-cluster (S1/S4/S5) green; walker version stays "7". **G10 SHIPPED** (2026-06-15): **Cluster G COMPLETE, walker version now "8".** Hermetic in-process G-cluster smoke (`tests/tsymex_phase15_g10_smoke.nim`, 7/7 c+cpp) composing the G1a-G8 machinery TOGETHER — multi-param at int+string (G8); the same `szof[T]=sizeof(T)` at int8+int64 in ONE SUT (G1a collision); a `distinct float64` with borrowed `+`/`<` (G4+G5); a `static[int]` `array[N,int]` at N=3+5 (G7); a `T:SomeNumber` concept generic at int + the `conformsToStdlibConcept` table (G6); the cap via `withSymexSettings(maxInstantiationsPerProc=2)` → `geInstantiationCapped`/sxUnknown (G1c). No state-threading regression found (the multi-file G edits thread state cleanly). Walker version bumped "7"→"8" single-sourced in `canonicalize.nim:symexWalkerVersion` (Invariant 6, confirmed no duplicate); the bump orphans every "7"-era cache key. Four version-pin tests (F8_smoke, S7b_smoke, S11_mutation, E7_smoke — all pinned "7") advanced to "8". Broad regression sweep all green c, no hangs: all G tests (G1a_instkey, G1c_instcap, g3-g8), rectify_generics, Cluster E (E3_try, E7_smoke), Cluster S (S3_strindex, S5_strops, S9_caseconv, S11_mutation), Cluster F (F2, F6, F8_smoke), H1, earlier phases (phase1_arith, phase3_recursion, phase4_tuple, phase4_array, phase11_walker, phase13_unsat_roundtrip). No production-code change beyond the bump. **C0-ADR SHIPPED** (2026-06-15): authored `docs/symex/ADR-0009-closure-encoding.md` (Status Accepted; closure = `(funcSym, envRecord)` pair, new `svClosure`; `funcSym` = per-lambda-site Z3 uninterpreted fn keyed by `(symBodyHash(lambdaBody), declOrderIndex)` NOT file:line:col; D1–D8 + 5 rejected alts) + `docs/symex/closures.md` skeleton (8 sections, each pointing to its filling cycle). **§F-C reconciled** in `RFC-phase15-reconciliation.md`. **★ HEADLINE — the G4 hang lesson applies to closures:** ADR-0009's closure-call axiom MUST be **GROUND** (per call site, concrete `env`/`args`: `implies(path.pc and pc_i, funcSym(env,args)==v_i)` per body sub-path, `ite`-merge) — **NEVER `∀env,args`**. A universal axiom over the uninterpreted `funcSym` would hang exactly as G4's `∀x.eject(inject(x))==x` did (180s MBQI; even a ground *reverse* application hangs — `runtime.nim:2440-2444`). The round-2 CRIT-C4 text is already ground/per-call-site (round2 line 14); ADR-0009 § D6 + Rejected Alt E pin it explicitly. **C2b must implement it ground.** Reconciliation findings: (1) `envRecord` is the engine's **Nim-side `svTuple`** (`runtime.nim:170-172`), NOT a Z3 record sort — there is NO Z3 tuple/datatype sort anywhere; the RFC's `funcSym:(envRecord_sort,…)` signature is reconciled by FLATTENING the env to leaf Z3 args (`Z3_mk_tuple_sort` exists at `ffi.nim:781` but is deliberately unused). `Z3_mk_app`/`Z3_mk_func_decl` (ffi.nim:836/842) are buildable — reuse the G4 raw-FFI pattern. (2) `ce*` kinds (`ceNotImplemented`/`ceUnsupportedCapture`/`ceUnsupportedHof`) **already exist** (`types.nim:611`, Z3a) — REUSE. `inlinePolicy` **already exists** (`types.nim:731`) — consume, don't redefine. (3) **NET-NEW:** `seqInlineThreshold` (C4), `svTupleEq` (C5 — no `svTuple` `==` arm today), `WalkerStatics.closureSyms` + `CallFrameCtx.closureInlineCount` (C2a — comment-only at `runtime.nim:2985/3013`, fields don't exist), `maxClosureInlineCount`. (4) `symBodyHash`-on-lambda needs a C1 node-plumbing check (G1a applies it to a proc SYMBOL + lineInfo fallback; a lambda is an `nnkLambda`/`nnkProcDef` node). Closures are **genuinely net-new** (UNSUPPORTED today: `parseExpr` has no `nnkLambda` arm → `error` at `dsl_parser.nim:1144`; B67 at `:1620-1648`) — **NO redundancy fork like Cluster G**. Doc + reconciliation cycle: no production code, no test, no build. **C1 SHIPPED** (2026-06-15): closure IR + parser foundation (purely STRUCTURAL — walker STUBS, no semantics). Net-new `iekLambda`/`iekClosureCall` IRExprKind (+`iek`/`is`/`it` prefix comment block, M2) + node shapes/`mkLambda`/`mkClosureCall`/render/canonicalize/emitExpr; `svClosure` SVKind stub `(closureSite: (siteHash,declOrder), closureEnv: ref svTuple)`. **`symBodyHash`-on-lambda REALITY (the flagged C1 verification): it does NOT apply** — a lambda is a NAMELESS `nnkLambda`/expr-position `nnkProcDef`/`nnkFuncDef` NODE, and `symBodyHash` is a `std/macros` builtin over a proc SYMBOL. So C1 uses the **ADR-0008 D2 lineInfo fallback directly**: `siteHash = hash("file:line:col")`, `declOrder` from a new `ParseCtx.lambdaCounter`. (D8's concrete param types in the canonical key disambiguate `T=int`/`T=string` — RED test asserts distinct keys.) **Free-var capture = scope-stack diff:** body `nnkSym` refs with `symKind ∈ {nskParam,nskLet,nskVar,nskForVar}` (value bindings; excludes top-level procs/types/consts by symKind) minus the lambda's params++body-locals (`let`/`var`/`for` LHS); first-seen source order preserved. RED test: outer local captured, inner body-local + param NOT. (Body read at `body(n)`, the routine-body index — `n.len-1` is a trailing synthetic `result` sym; a C1 gotcha found+fixed.) **Proc-valued-VARIABLE call** (`getImpl` not a routine def + `getTypeInst==nnkProcTy`) → `iekClosureCall`; top-level procs-as-values stay C3; proc-typed `let` placeholder-typed. **svClosure stub rep:** site key + boxed svTuple env placeholder (per-site funcSym decl is C2a's `closureSyms`). Net-new **`sortOfTuple`** (flattened per-leaf Z3 sorts of env svTuple, D5) + **`c1ClosurePoCApply` Z3_mk_app PoC** — declares a funcSym over runtime-constructed sorts (`Z3_mk_func_decl`) + applies with HEAP-seq args (`Z3_mk_app`, G4 raw-FFI discipline); **round-trips (result sort == range) → C2b application path DE-RISKED**. Walker STUBS both → `SymexClosureUnimplementedError` caught at the runSymex boundary → `ceNotImplemented`/sevError → `sxUnknown` (Invariant 3 — never silent/crash). `nnkIteratorDef` (closure iterator) → ceNotImplemented stub (detail "closure iterators not yet supported"), deferred. **DUAL exhaustiveness ripple: IRExprKind 6 arms** (render, canonicalize, emitExpr, probeProto, lower-stub, abstraction tryEvalInterval+collectVarRefs) **+ SVKind 5 arms** (tyOf, iteSV, coerceIntLit, extractLeaf, symValHash); `allocateSym` untouched (IRType-keyed, no `itClosure`). `tests/tsymex_phase15_C1_ir.nim` 7/7 c+cpp; 11-file regression green (phase1_arith/let, phase3_recursion, phase4_tuple, phase5_seq, phase11_walker, g4_distinct_sort, E3_try, S11_mutation, F8_smoke, g10_smoke), no HANG. Walker version stays "8" (C6 bumps). **C2a SHIPPED** (2026-06-15): closure CONSTRUCTION. `lower(iekLambda)` replaces the C1 `ceNotImplemented` stub with real construction — (1) ENV SNAPSHOT: each `lambdaCaptures` name looked up in the CURRENT env, SymVals collected → an `svTuple` `closureEnv` (NO body descent — the body lowers only at APPLICATION, C2b); (2) PER-SITE funcSym: get-or-create the uninterpreted decl over runtime-known sorts (domain = flattened env-leaf sorts via `sortOfTuple` ++ param sorts; range = `lambdaRetTy` sort) via raw `Z3_mk_func_decl` + `incRefFD` (G4/C1 raw-FFI discipline, heap domain seq); (3) assemble `svClosure{closureSite, closureEnv, closureRawFD}`. **closureSyms placement = net-new `currentClosureSyms` THREADVAR** (`Table[ClosureSymKey, RawZ3FuncDecl]`, reset at `runSymexImpl` entry) — NOT a directly-populated WalkerStatics field, because `lower` has no `WalkCtx` (the G4 `currentDistinctSorts` idiom); the **net-new `WalkerStatics.closureSyms` field** MIRRORS it after the walk (so the drift-table "C2a adds the field" is honoured: real field + live threadvar populator). Key `ClosureSymKey = ((siteHash, declOrder), envSortId, paramsSortTupleId)` with `Z3_get_sort_id` fingerprints of env-leaf + param sorts (same site, two monomorphizations ⇒ distinct funcSyms, D8). **Net-new `svClosure.closureRawFD: RawZ3FuncDecl`** (verified the real nim-z3 raw func-decl type; nil in the C1 stub). `extractFromSymVal(svClosure)` → classified `ceNotImplemented`/sevError (closure as top-level SUT RESULT unsupported — classified, Invariant 3). `symex.nim emitTyAndReader`: proc/closure type (`classifyType(nnkProcTy)` → `tUninterp("__closure")`) → `proc` placeholder + `{.warning.}` (Invariant 3; note `emitTyAndReader` renders only INPUT-PARAM witnesses, so this is the classified-rejection guard, never reached for the C2a RED SUT which returns `int`). **`CallFrameCtx.closureInlineCount` DEFERRED to C2b** (an application-descent budget; no descent in C2a). **`iekClosureCall` arm STAYS `ceNotImplemented`** (C2b). Test hook: `c2aClosureProbe`/`c2aClosureProbeRelowered` (runtime.nim) lower a hand-built `iekLambda` against a constructed env at the `let f = …` binding point and return an introspectable `C2aClosureProbe` record (keeping `Env`/`lower`/`symValHash` encapsulated). `tests/tsymex_phase15_C2a_closure_capture.nim` 3/3 c+cpp; 10-file regression green (C1_ir, phase4_tuple, phase1_let, phase1_arith, phase3_recursion, g4_distinct_sort, E3_try, S11_mutation, F8_smoke, g10_smoke), no HANG. Walker version stays "8" (C6 bumps). **C2b SHIPPED** (2026-06-15): closure CALL dispatch (the cluster CORE). `lower(iekClosureCall)` (a C1/C2a `ceNotImplemented` stub) → net-new `lowerClosureCall` (defined after `walk`, fwd-declared, reached via the net-new `currentWalkCtxPtr` threadvar — a `ptr WalkCtx` published in `runSymexImpl` just before the top-level `walk` so the `lower` evaluator can drive a body descent through `walk`). Steps: (1) resolve `ccCallee` in the env → an `svClosure` (a C2a-constructed lambda OR a proc-valued PARAM — SAME dispatch, no separate path); unresolved → net-new `ceClosureUnknownCallee`/sevError → sxUnknown (Invariant 3). (2) **Reach the lambda BODY** via the net-new `currentClosureBodies` site→body map: `svClosure` carries `closureSite`+`closureEnv`+`closureRawFD` but NOT the body IR, so `buildClosure` (the C2a path) now ALSO stashes `lambdaBody`+params+captures+retTy keyed by `(siteHash, declOrder)`; the call keys back in. (3) Build the funcSym application at THIS occurrence — raw `Z3_mk_app` (C1 PoC pattern, heap arg seq) over the flattened env-leaf asts ++ flattened call-arg asts (net-new `flattenLeafAsts`, the ast mirror of `sortOfTuple`); the `Z3AnyAst` result `wrap`-ped to a typed SymVal per `lambdaRetTy` (net-new `symValFromRawAst`) — **the funcApp IS the call's result SymVal** (ADR-0009 D6). (4) Descend the body ONCE (fresh env: params←concrete args, captures←closureEnv tuple fields) via `walk` on a pushed CallFrame whose retSym = the funcApp; harvest return sub-paths from BOTH the explicit-`return` channel (`frame.returnedPaths`) AND the implicit-`result`/fall-through channel (a value-returning lambda whose last stmt is `result = EXPR` leaves the value in `cp.env["result"]`). (5) For EACH sub-path `(branch_conds_i, v_i)` assert the **GROUND** `implies(and(branch_conds_i), funcApp == v_i)` into the net-new `currentClosureCallAxioms` threadvar, drained into EVERY `trySolve` (the `parseIntGateConstraints` idiom — each is a closed implication, vacuously true off its branch). **★ NO `∀env,args` axiom anywhere — confirmed NO HANG** (the funcSym applied at the GROUND occurrence and equated to a value is structurally G4's decidable eject-pin; QF_UF…, no MBQI loop). **Inline budget:** net-new `CallFrameCtx.closureInlineCount` (C2a-deferred) bumped per nested descent, checked vs net-new `SymexSettings.maxClosureInlineCount` (default 64; + default + `+`-merge clause); overflow → net-new `ceInlineBudgetExceeded`/sevError → sxUnknown. **Parser fixes for proc-valued params:** (a) a monomorphized `proc(x:T):T` formal (`applyTwice[T]`) arrives as a synthesized `nnkProcTy` carrying no type → `classifyType` matches it structurally BEFORE `getTypeInst` (→ `__closure` placeholder); (b) the closure CALL is now detected EARLY in `parseExpr` (`earlyClosureCallDetect`, before the string-builtin routing, which would `classifyType(n[1])`-crash on a closure-call arg like `f(f(v))`). `tests/tsymex_phase15_C2b_closure_call.nim` 3/3 c+cpp: (1) captured-value call `f(3)==3+x*2` → sxSat, witness x==5; (2) proc-valued param `applyTwice` (+ Cluster G generics) → sxSat, witness n==40 (resolved via svClosure, NOT `ceClosureUnknownCallee`); (3) 2-branch lambda — both ground arms `(=> cond (= fs a))` / `(=> (not cond) (= fs b))` present (introspected via net-new `currentClosureCallAxiomStrs` hook — SMT-LIB strings captured while the ctx is live), no `forall`. Regression green, no HANG (C1_ir [its stale "walker STUBS a lambda SUT" test updated to the now-modeled sxSat], C2a_closure_capture, phase4_tuple, phase3_recursion, g4_distinct_sort [the other raw-funcSym user — no interference], g8_multi_param, rectify_generics, E3_try, S11_mutation, F8_smoke, g10_smoke). Walker version stays "8" (C6 bumps). **C3 SHIPPED** (2026-06-15): top-level procs as VALUES (unit-env), MOSTLY PARSER — reuses the C2a construction + C2b call-dispatch wholesale (NO new walker semantics, NO new IR table, NO canonicalize change). A module-scope proc referenced in EXPRESSION (value) position — `let g = double`, or `double` passed as a proc-valued ARG — is encoded as an `iekLambda` with `lambdaCaptures = @[]` (a unit-env closure: a ZERO-field svTuple env). **Parser (dsl_parser.nim):** the bare-`nnkSym` VALUE-position branch of `parseExpr` (the `mkVar` fall-through) detects `symKind(n) == nskProc` with a resolvable `nnkProcDef` `getImpl` and routes to the net-new `parseProcAsValue` → `iekLambda`; body/params/retTy come from the proc's `getImpl` via the net-new **shared `parseRoutineToLambda` core** (factored out of `parseLambda`; a `forceNoCaptures` flag skips the free-var scan — a module-scope proc has no enclosing runtime scope to capture). **★ The value-vs-callee distinction (the headline regression risk) is STRUCTURAL, not heuristic:** a proc in CALLEE position is `n[0]` of an `nnkCall`, parsed STRUCTURALLY and NEVER routed through `parseExpr`, so `double(n)` stays a normal Phase-3 `isCall`; a call THROUGH a proc-valued LOCAL (`g(n)`) is C2b's `earlyClosureCallDetect` → `iekClosureCall`; a proc-valued PARAM is `nskParam` (≠ `nskProc`) → stays C2b's svClosure path. ONLY a bare module-proc symbol in value position reaches the new branch — verified by the proc-call-heavy regression (recursion/mutual/summarization) staying green. **`symBodyHash` on a top-level proc WORKS** (resolving the C1 open question for the top-level case): unlike C1's nameless lambda — where `symBodyHash` does NOT apply (no symbol) and C1 fell back to a lineInfo hash — a top-level proc HAS a symbol, so the site key reuses the existing `bodyHashPart` helper (fwd-declared; `symBodyHash(procSym)` with the ADR-0008 D2 lineInfo fallback), `declOrder = 0` (D3). **Runtime (runtime.nim):** no new walker code — `buildClosure` already materialises a zero-field svTuple unitEnv on the empty-capture path; added a `doAssert envRecord.fields.len == 0` guard on the `lambdaCaptures.len == 0` path (the spec's C3 walker assertion). **The proc-as-value call gives the SAME witness AND verdict as a direct call** (the encoding is semantically transparent): `let g = double; g(n)` gated at `result == 10` → sxSat witness n==5, EQUAL to a `double(n)` direct-call SUT. `tests/tsymex_phase15_C3_proc_as_value.nim` 2/2 c+cpp: (1) proc-as-value == direct-call witness/verdict; (2) `applyOnce(double, n)` (top-level proc as proc-valued arg) → sxSat n==5. 10-file regression green, no HANG (C1_ir, C2a_closure_capture, C2b_closure_call, phase3_recursion, phase3_mutual, phase3_summarization, g8_multi_param, rectify_generics, phase1_arith, F8_smoke). Walker version stays "8" (C6 bumps). **C4 SHIPPED** (2026-06-15): DSL HOFs filter/map/fold over seq[T]. Net-new walker HOF dispatch: parser `hofDispatch` block (in the `nnkCall` arm of `parseExpr`) intercepts `filter`/`map`/`fold` GUARDED on `calleeSym.owner.strVal == "sequtils"` -> net-new `iekHofCall`; a non-sequtils same-named proc owns to its own module and FALLS THROUGH to normal isCall (the Des-LOW-L3 regression guard -- verified: user `filter` not hijacked). Net-new `iekSeqLit` (`@[a,b,c]`/`@[]` -> concrete-length svSeq) feeds the inline path its concrete length. **mapArray reality:** `Z3_mk_map` is a DECIDABLE array-map (NOT a universal-forall) -> symbolic-map TERMINATES, no hang (closure funcSym left opaque, sound over-approx). **fold reality:** sequtils `foldl`/`foldr` are TEMPLATES the typed macro EXPANDS to a `for...items` loop before the parser runs, so fold never reaches the HOF handler through std/sequtils; `walkHofFold` exists/wired but the realised closure HOFs are map/filter (real {.closure.} procs). INLINE path (concrete simplify(seqLen) <= seqInlineThreshold, under ipHybrid/ipAlwaysInline): unroll 0..<N, apply closure per element via net-new `applyClosureGround` (factored from C2b `lowerClosureCall` -- ground funcSym-app + body descent + GROUND axiom, reused per element); map = per-element store; filter = compacted keep-mask (keptLen += ite(pred_i,1,0)); quantifier-free, bounded N<=8 (no fan-out). AXIOM path (symbolic length): map -> mapArray; **filter -> ceUnsupportedHof (sevError -> sxUnknown), axiomatize-filter DEFERRED to Phase 16** (no Z3 seqFilter); fold(symbolic) -> opaque ground + ceUnsupportedHof. Net-new `SymexSettings.seqInlineThreshold` (=8) + defaultSymexSettings + `+`-merge ripple + net-new `validateSymexSettings` warning (set without ipHybrid); inlinePolicy + seqInlineThreshold added to the canonicalize settings digest. `tests/tsymex_phase15_C4_hof.nim` 5/5 c+cpp; 10-file regression green no HANG (C1_ir, C2a/b, C3, phase5_seq, F9b_seq_float, g8_multi_param, phase3_recursion, F8_smoke, S11_mutation). Walker version stays "8" (C6 bumps). **C5 SHIPPED** (2026-06-15): closure EQUALITY (nominal-for-site + structural-for-env, ADR-0009 D7). `bEq`/`bNe` on two `svClosure` operands dispatch via net-new `closureEq` (in the comparison arm of `lower(iekBinop)`, both probe-hit and probe-miss branches; `ejectBase` passes svClosure through). DIFFERENT site `(siteHash,declOrder)` int-pair -> always unequal (Nim-side, NO Z3; `==`->mkBool(false), `!=`->mkBool(true)). SAME site -> structural env equality via net-new `svTupleEq(a,b):Z3Bool` (FIRST `svTuple` `==` arm; field-by-field conj over net-new `svLeafEq` -- int/bool/BV/float/string, svDistinct ejects+recurses; recurses nested tuples+concrete arrays; zero-field unit-env vacuously true). svTupleEq confirmed NOT to break tuple tests (phase4_tuple/phase11_walker green). **Sub-test-2 ADAPTED:** RFC canonical same-site shape is a closure-RETURNING closure (`mk` returns a proc) -- OUT OF REACH (closure-call result wrapped by `symValFromRawAst`, scalar return kinds only); adapted to `let f=proc(y:int):int=y+x; let g=f` (same site+env -> structural-env branch -> sxSat). **Sub-test-4 reconciliation:** RFC assumes formatting-stable `symBodyHash`, but C1 keys nameless lambdas by lineInfo (POSITION-based, not formatting-stable across positions -- different positions = different sites, CORRECT); adapted to test what lineInfo guarantees (a comment inside the body does not move the declaration line:col -> same-position lambda keys stably -> sxSat; deterministic across runs). NOT asserting formatting-stability lineInfo cannot provide. `tests/tsymex_phase15_C5_closure_eq.nim` 5/5 c+cpp (distinct-site `==`->sxUnsat, distinct-site `!=`->sxSat, same-site alias `==`->sxSat, runtime-divergence doc test, lineInfo stable+deterministic). 10-file regression green no HANG. closures.md C2a/C2b/C3/C4/C5 subsections completed + lineInfo-vs-symBodyHash reconciliation; determinism.md closure section added.  **C6 SHIPPED** (2026-06-15): **Cluster C COMPLETE; walker version now "9".** Hermetic in-process C-cluster regression smoke (`tests/tsymex_phase15_C6_smoke.nim`, 9/9 c+cpp) composing C1-C5 TOGETHER: closure construction+call observing a captured value (C2a/C2b); a proc-valued PARAMETER `applyTwice[T]` (also exercises Cluster G generic instantiation -- the C+G composition); a top-level proc as a VALUE (C3) matching the direct call; bounded filter/map HOFs with a closure (C4 inline path); closure equality (C5 -- distinct sites unequal->sxUnsat, same-site alias+same env->sxSat via svTupleEq); plus the DoD `maxClosureInlineCount=8` withSymexSettings override still witnessing a closure call. NO state-threading regression found -- the instantiation-cache / closureSyms / currentClosureBodies scopes are already cleanly separated, so NO production code changed beyond the bump. Walker version bumped "8"->"9" single-sourced in canonicalize.nim (Invariant 6, no duplicate); five prior version-pin tests (F8_smoke, S7b_smoke, S11_mutation, E7_smoke, g10_smoke) advanced to "9". Broad regression sweep all green c, no hangs (all C tests, Cluster G sample, E3_try/E7_smoke, S3_strindex/S11_mutation, F2/F8_smoke, H1, earlier phases). Registered in proptest.nimble; SYMEX_PLAN.md C6->SHIPPED + Cluster C COMPLETE; closures.md "Generics interaction" subsection completed (cross-refs ADR-0008); determinism.md "9" version-history row added. **R1a SHIPPED** (2026-06-15): **Cluster R OPENS (ref/ptr aliasing via logical heap -- the FINAL cluster).** Purely STRUCTURAL per RFC §R1a / ADR-0010 -- IR + SVKind + exhaustive dispatch STUBS; the walker STUBS itRef/itPtr/isDeref/isNew with a classified `heUnresolvedRef`->`sxUnknown` (Invariant 3), NO heap semantics (R1-R13). **types.nim:** `IRTypeKind += itRef(refPointeeTy)/itPtr(ptrPointeeTy)`; `IRStmtKind += isDeref(dRetName/dPtr/dElemTy/dPtrFamily)/isNew(nRetName/nRefTy)`; ctors tRef/tPtr/mkDeref/mkPtrDeref/mkNewT; ==/$/render arms; `SymexErrorKind += heUnresolvedRef` (**NET-NEW** -- the other he* kinds heUnsupportedOwnership/heDepthExhausted/heUnsafeCast/hePtrArith/hePtrFamily/heFreshnessCapExceeded/heUnsupportedVarRef/heRefVariantUnsupported ALREADY EXISTED from the Z3-Enum cycle); `SymexSettings.maxHeapDepth` (default 8; 0=unlimited) + default + +-merge (field-ripple). **runtime.nim:** `SVKind += svRef(refAst)/svPtr(ptrAst,ptrFamily)`; net-new SymexRefUnresolvedError/SymexOwnershipUnsupportedError caught at the runSymex boundary -> heUnresolvedRef/heUnsupportedOwnership (sevError) -> sxUnknown; walk(isDeref/isNew) + allocateSym(itRef/itPtr) (the ref/ptr PARAM path) STUB the classified halt. **dsl_typebridge.classifyType:** INLINE nnkRefTy->tRef(pointee) / nnkPtrTy->tPtr(pointee) (REPLACING the pre-R unwrap, reconciliation §A:128) -- the NAMED `type Foo = ref object` getImpl-unwrap path (:178) is **UNCHANGED so tsymex_rectify_refs is NOT regressed** (re-ran green); owned T/WeakRef[T]/Atomic[T] -> __ownership:* placeholder -> heUnsupportedOwnership (Breadth-LOW-L4). **dsl_parser.nim:** emitIRType/emitStmt arms (round-trip the runtime IR literal). **H1 heaps/heapDepth/allocCounters + forkPath deep-copy CONFIRMED present, INERT through R1a.** **★ TRIPLE exhaustiveness ripple: IRTypeKind 9 arms** (emitIRType, canonicalize(IRType), ==, $, allocateSym, tyOf, defaultZero, runSymex param-alloc, emitTyAndReader) **+ IRStmtKind 8 arms** (emitStmt, canonicalize(IRStmt), render, collectBan, collectSetLitMembers, collectTableLitKeys, scanStmt, walk) **+ SVKind 5 arms** (tyOf, iteSV, coerceIntLit, extractLeaf, symValHash). `tests/tsymex_phase15_R1a_ir.nim` 9/9 c+cpp. **No walker version bump** (Cluster R bumps "9"->"10" at R12; stays "9"). Regression all green no HANG: phase1_arith, phase1_let, phase3_recursion, phase4_tuple, phase5_seq, phase11_walker, g4_distinct_sort, C6_smoke, E7_smoke, S11_mutation, F8_smoke, g10_smoke, **rectify_refs** (the ref-unwrap-dependent test). Registered in proptest.nimble; SYMEX_PLAN R1a->SHIPPED; reconciliation §F "Cluster R" subsection added. **R1 SHIPPED** (2026-06-15): the FIRST real heap semantics (ADR-0010) -- a logical heap modelled as a per-type `Z3Array[Ref_T, T]` over an UNINTERPRETED address sort. `WalkerStatics.refSorts: Table[string,RawZ3Sort]` (per-WALKER `Ref_<typeId>` sorts via net-new `allocRefSort`->`mkUninterpretedSort("Ref_"&typeId)`, **`Z3_inc_ref(Z3_sort_to_ast)`**-pinned per the G4 SIGSEGV footgun) + `WalkerStatics.nilConsts: Table[string,Z3AnyAst]` (the `nil_<typeId>` distinguished const). LIVE populators are net-new `currentRefSorts`/`currentNilConsts` threadvars (the G4/C2a idiom -- `allocateSym` has no WalkCtx), reset at `runSymexImpl` entry, mirrored into WalkerStatics post-walk. `allocateSym(itRef/itPtr)` builds the param's fresh `Ref_T` const -> `svRef(refAst,refPointee)`/`svPtr(ptrAst,ptrFamily,ptrPointee)` (NET-NEW `refPointee`/`ptrPointee:IRType` fields so `tyOf`/the witness reader resolve the pointee without re-deriving). `walk(isDeref)`: per path, resolve the ref/ptr SymVal, lazily materialise `path.heaps[typeId]` to a FREE `Z3Array[Ref_T,T_sym]` var (NET-NEW `mkHeapArrayVar` via raw `Z3_mk_array_sort`+`Z3_mk_const` -- the key sort `Ref_T` is a RUNTIME uninterpreted sort the typed generic `mkArrayVar[K,V]` can't express), then a **GROUND `Z3_mk_select(heap,p)`** (NET-NEW `heapSelect`+`liftHeapValue`) lifted to a SymVal + bound to the deref let; the surviving path carries the heap forward so a 2nd deref of the SAME ref reads the SAME array. **★ NO ∀-axiom over the uninterpreted sort** (the G4 MBQI hang lesson) -- the ground select is decidable (QF_AUFLIA-ish) and **CONFIRMED not to hang** (the sat witness AND the same-ref contradiction `42∧43`->sxUnsat BOTH resolve under the bounded runner). `dsl_parser`: NET-NEW `nnkDerefExpr`/`nnkHiddenDeref` parse arm A-normalises a ref/ptr deref into an `isDeref` stmt (fresh let binds `p[]`); a non-ref hidden-deref keeps the pre-R unwrap. `symex.emitTyAndReader(itRef)` (C7/Breadth-CRIT-1) renders `(var c=new(T); c[]=<pointeeReader>; c)` so the witness shows `p[]==42`; `extractFromSymVal(svRef/svPtr)` writes the dereffed value (recorded under the param name in the NET-NEW `currentHeapDerefVals` hook at deref time) at the param's witness path, or a default-zero leaf when never dereffed (no KeyError); `ptr T` -> `nil` placeholder + classified `{.warning.}` (full pointer-family witness lands R8; heap-snapshot witness format lands R11b/R12). `tests/tsymex_phase15_r1_refsort.nim` 2/2 c+cpp (sat witness `p[]==42`; same-ref `42∧43`->sxUnsat); two obsoleted R1a stub-assertions (ref/ptr param -> sxUnknown + heUnresolvedRef) updated in `tsymex_phase15_R1a_ir.nim` to the R1 promotion (-> sxSat, no classified ref halt). **No walker version bump** (stays "9"; Cluster R bumps at R12). Regression all green c no HANG: R1a_ir, rectify_refs, phase4_tuple, phase5_seq, g4_distinct_sort, phase1_arith, C6_smoke, F8_smoke; cpp parity on the array/sort-touching tests + R1. Registered in proptest.nimble; SYMEX_PLAN R1->SHIPPED; reconciliation §F "Cluster R" R1 note added; determinism.md "Heap depth budget" subsection added. **Next = R1b (inter-procedural heap threading).** **R1b SHIPPED** (2026-06-15): inter-procedural heap threading at every call boundary (ADR-0010 R1b). The `isCall`/`isGenericCall` arms were ALREADY threading structurally — once R1 made `heaps` live, the `calleePath = forkPath(p,...)` entry clone deep-copies the caller's `heaps`/`allocCounters`/`heapDepth` IN, and the return survivor `forkPath(cp,...)` (forked from the returned CALLEE path) gives `heaps` REPLACEMENT + `heapDepth` OUT. The ONE R1b code-change on this arm: `allocCounters` is merged by **`max(caller[T],callee[T])` per type key** (NOT the plain replacement `forkPath` gives) — the freshness invariant so a post-call caller `new T` can't collide with a callee-allocated ref (inert until R2 wires the increments). The **closure arm** (`iekClosureCall`, lowered inside `lower` with NO `Path` in scope): NET-NEW `currentCallerHeaps`/`currentCallerHeapDepth`/`currentCallerAllocCounters` threadvars (the `currentWalkCtxPtr`/E8 idiom; reset at `runSymexImpl` entry) seeded per-path by NET-NEW `seedCallerHeapThreadvars(p)` at the `isCall`/`isIf`/`isLet`/`isAssign` arms before expression lowering; `applyClosureGround` builds the closure `descentBase` from them instead of a fresh-empty heap — so a deref inside a closure body reads the caller's threaded heap. Closure heap WRITES back out are inert until R4 (closures can't mutate the heap yet) — R1b threads the closure READ (entry) direction. **★ Test reconciliation (no-write approach (a)):** RFC §R1b's literal SUT does `p[]=7` (a heap WRITE -> R4, NOT implemented), so the test proves threading WITHOUT a write via SAME-REF deref consistency: POSITIVE caller `p[]==7` + callee `inner(p)` reads `q[]==7` on the SAME threaded heap -> sxSat; NEGATIVE (the THREADING PROOF) caller `p[]==7` + callee `inner2(p)` reads `q[]==8` on the same heap -> **sxUnsat** (unthreaded the callee gets a fresh heap and this would be sxSat — so sxUnsat PROVES the heap crosses the boundary; R4 reproves it through a real write). `tests/tsymex_phase15_r1b_callheap.nim` 2/2 c+cpp, **CONFIRMED not to hang**. **No walker version bump** (stays "9"; Cluster R bumps at R12). Regression all green c no HANG: r1_refsort, R1a_ir, rectify_refs, phase3_recursion, phase3_mutual, phase3_summarization, C2b_closure_call (closure-arm threadvar change — sound), E3_try, phase4_tuple, C6_smoke (HOF map/filter closures), F8_smoke; cpp parity on r1_refsort + C2b_closure_call. Registered in proptest.nimble; SYMEX_PLAN R1b->SHIPPED; reconciliation §F "Cluster R" R1b note added. **R2 SHIPPED** (2026-06-15): `new T` allocation semantics (ADR-0010 R2), replacing R1a's `isNew` stub. **`freshRef(ctx,refSort,typeId,path)`** increments `path.allocCounters[typeId]` (per-path; R1b already threads + `max`-merges it) and mints a FRESH `Ref_T` const `ref_<typeId>_<n>` (n = the new counter value) via raw `Z3_mk_const` (G4 raw-const discipline). **`assertFreshness(ctx,path,typeId,newRef,settings)`** asserts GROUND inequalities into `path.pc`: `newRef != nil_<typeId>` (always — fresh refs are never nil) AND `newRef != prior` for every PRIOR LIVE ref of this sort on THIS path. **★ NO universal-∀ over the uninterpreted ref sort** (the G4 MBQI hang lesson) — all ground/decidable, CONFIRMED not to hang (the `p == q` alias case resolves to sxUnsat). **Prior live refs (the question R1/R1b left open):** R1/R1b kept `allocCounters` (a COUNT) but not the actual consts, so R2 adds NET-NEW `Path.liveRefs: Table[string, seq[Z3AnyAst]]` (threaded through `deepCopyHeapState`/`forkPath` like `heaps`/`allocCounters`). **Per-path counter isolation:** because `liveRefs`/`allocCounters` are VALUE-copied at every fork, DISJOINT forked branches do NOT share priors — a `new T` on branch B restarts from the fork snapshot (no cross-path `ref_T_i != ref_T_j`); tested observably (armA + armB both sxSat). **Cap:** NET-NEW `SymexSettings.maxFreshnessAssertions: int = 256` (0 = unlimited, the `maxFrontierSize` convention) + default + `+`-merge; per-path `Path.freshnessAssertCount` counts emitted inequalities — over-cap SKIPS the pairwise `newRef != prior` inequalities and emits `heFreshnessCapExceeded` (sevHint, already in enum) via NET-NEW `freshnessCapHints` threadvar (the G4 `distinctBijectivityHints` drain idiom, dedup'd). **SOUND over-approximation** — skipping a distinctness inequality lets Z3 allow aliasing beyond the cap (MORE models), NEVER a false UNSAT; the `newRef != nil` pin stays uncapped. **Supporting wiring:** NET-NEW `refEq(a,b,op)` lowers `svRef`/`svPtr` `==`/`!=` to a ground `Z3_mk_eq` over the address consts (both comparison-binop branches), so `if p == q` over two fresh allocs is a provably unreachable branch; the walker `of isNew:` arm forks per path, `freshRef` + `assertFreshness`, binds `svRef`/`svPtr` under `stmt.nRetName`. **Parser gap closed:** R1a wired the `mkNewT` IR ctor + emit but NEVER taught the parser to recognise `new T` from a SUT — the let-section arm now detects a `new T` RHS (NET-NEW `isNewCall` — `nnkCommand`/`nnkCall` headed by `new`) and lowers to `mkNewT(name, classifiedRefTy)`. `tests/tsymex_phase15_r2_new.nim` 4/4 c+cpp (alias → sxUnsat; disjoint armA/armB both sxSat; over-cap 5-alloc-under-cap-3 → sxSat + hint, sound), CONFIRMED not to hang. **No walker version bump** (stays "9"; Cluster R bumps at R12). Regression all green c no HANG: r1_refsort, r1b_callheap, R1a_ir, rectify_refs, phase3_recursion, phase4_tuple, C6_smoke, F8_smoke (the `maxFreshnessAssertions` settings-ripple), S11_mutation. Registered in proptest.nimble; SYMEX_PLAN R2->SHIPPED; reconciliation §F "Cluster R" R2 note added. **R3 SHIPPED** (2026-06-15): completes the `p[]` deref READ path + adds the **seq[ref T] element path** (the headline). **Read select confirmed per-path:** the R1 `of isDeref:` arm threads `path.heaps[typeId]` (per-PATH, NOT a global/`WalkerStatics` field) for the ground `select` → a fully-typed dereffed SymVal; the read does NOT modify the heap. **seq[ref T] path:** `allocateSeqDataRaw(itRef/itPtr)` builds a FREE `Z3Array[Z3Int, Ref_T]` via raw FFI (`Z3_mk_array_sort`/`Z3_mk_const` — `Ref_T` is a runtime uninterpreted sort the typed `mkArrayVar` can't express; mirrors `mkHeapArrayVar`; two fwd-decls `allocRefSort`/`refPointeeTypeId`); the `isIndex` seq arm gains an `itRef`/`itPtr` branch that raw-`Z3_mk_select`s the element at `idxZi.raw` → `svRef`/`svPtr`; a later `[]` (isDeref) derefs THAT element through `path.heaps[T]`. So `xs[0][]` = seq-elem select (→svRef) → heap select (→pointee). Both GROUND — NO ∀ over `Ref_T` (G4 hang lesson); CONFIRMED not to hang. **Witness:** `extractSeqElements(itRef/itPtr)` records only the LENGTH; the `emitTyAndReader` itSeq `itRef` reader renders a `seq[ref T]` of model length with `new(T)` default cells (pointee reader NOT invoked — would KeyError on the absent leaf; full per-element heap-snapshot witness lands R11b/R12). NET-NEW `readSeqLen` helper. **`isDerefWrite` IRStmtKind (`p[] = v`)** added (ctor `mkDerefWrite` + parser detection of `nnkAsgn` LHS `nnkDerefExpr`/`nnkHiddenDeref` over a ref/ptr operand, checked BEFORE the hidden-deref `unwrap`; + exhaustiveness ripple render/canonicalize/collectBan/collectSetLitMembers/collectTableLitKeys/scan/emitStmt/walk) — **STUBBED no-op at R3** (real `store` lands R4). **Free-heap reconciliation:** the `p[] = 99; p[] == 99` SUT reaches sxSat via the FREE heap (R1) — the no-op write is irrelevant; NOT real read-after-write (R4). The "unwritten branch doesn't see the update" isolation DoD needs the store → DEFERRED to R4; R3 tests isolation via INDEPENDENT free heaps (two forked branches deref `p==11`/`p==22`, each sxSat on its own per-path heap). `tests/tsymex_phase15_r3_deref_read.nim` 4/4 c+cpp, CONFIRMED not to hang. **No walker version bump** (stays "9"; Cluster R bumps at R12). Regression all green c no HANG: r1_refsort, r1b_callheap, r2_new, R1a_ir, rectify_refs, phase5_seq, F9b_seq_float, phase4_tuple, C6_smoke, F8_smoke. Registered in proptest.nimble; SYMEX_PLAN R3->SHIPPED; reconciliation §F "Cluster R" R3 note added. **Next = R4 (`p[] = v` write — `store(heap_T, p, v)`; promotes the R3 `isDerefWrite` no-op stub to a real heap store → read-after-write + the write-based per-path isolation deferred here).**
- **MAJOR DRIFT FLAGGED at G0-ADR (needs human confirm before G1a):** generic procs ALREADY symex end-to-end via **parse-time monomorphization** (`ensureProcRegistered`→`gatherTypeSubst`→`monomorphize`→`parseCalleeImpl` in `dsl_parser.nim:1618/1588/1574/1645`; proven green by `tests/tsymex_rectify_generics.nim`). There is **NO `isGenericCall` IR** and the basic case needs none — so the RFC's G1a/G1b/G1c net-new `isGenericCall` IR + walk-time `instCache` is **redundant scaffolding**. RECOMMENDATION: repurpose G1a-c to *formalize/harden the existing monomorphization* — the real gap is the bare-name `ctx.procs` collision (Feas-H5: same-name generics across modules, and the same proc at different `T`, collide on `calleeSym.strVal`) + the instantiation cap. Details in `RFC-phase15-reconciliation.md` §F Cluster G. Also corrected: `ge*` kinds already in `types.nim:575`; `maxInstantiationsPerProc` is **net-new** (not in SymexSettings, RFC wrong); `mkUninterpretedSort` exists+used (`sort.nim:113`); no `he*`→`ge*` rename needed.
- **Reconciliation:** global drift resolved in `RFC-phase15-reconciliation.md` §A–§E; per-cluster specifics filled into §F just-in-time before each cluster. Z0 fully reconciled (§C). Item-1 named tuples resolved as a `map`-keyword generalization (§D). Z0 item-3 `:unk` guard struck (would break live caching).
- **Toolchain (established at Z1):** dev/test runs use `localhost/proptest-dev:latest` (built from `ghcr.io/coreyleavitt/nim:latest`, Nim 2.2.10, + `z3-devel`) via `scripts/build-dev-image.sh`. **nim-z3 v2.0.0 needs Nim >= 2.2.10** (2.2.0/2.2.4 fail on a funcdecl tuple-type parser bug). Single test: `scripts/dt.sh <c|cpp> tests/<file>.nim`.
- **Shipped:** Z0 (carryover), Z1 (nim-z3 v2.0.0 pin bump + canary), Z2 (regression smoke, 17 tests, 0 drift), **Z3 complete** (a: error/severity/settings enums; b: svUninterpRef/itUninterp; c: classifyType char + sink/lent; d: withSymexSettings/+ merge; e: cacheKeyRaised + suffix rename; f: SYMEX_PLAN.md). **Z4 done** (`WalkCtx.found` Option→seq + WalkerStatics/CallFrameCtx empty records + ADR-0007). **Cluster Z COMPLETE (Z0–Z4).** **Cluster L COMPLETE** (L1 boundary audit, L2 untyped templates, L3 getAst/quote — all verification-only; templates-macros.md authored). **Cluster F (float) in progress:** F0-ADR (on disk), **F1 SHIPPED** (type-bridge: itFloat32/64 + svFloat32/64 + 12-arm ripple). **F2 SHIPPED** (literals + IEEE ==/!=). **F3 SHIPPED** (arith). **F4 SHIPPED** (ordering compare). **F5 SHIPPED** (int<->float conv: `float(x)`/`float32(x)` via toFpFromSigned/rmRNE, `int(f)` via toSbv/rmRTZ; **fix:** int→float takes the BV pattern directly via `toBv64ForFp` — the `int2bv(bv2int(x))` round-trip hung Z3 forever on ordering goals). **F6 SHIPPED** (std/math float ops + FP predicates, all Z3-FP-native: new `iekMathCall` IR; parser intercepts float-receiver calls by name + `isStdMathProc` file-path catch-all; `lowerMathCall` does abs / sqrt(rmRNE) / min / max / floor(rmRTN) / ceil(rmRTP) / round(rmRNE) / trunc(rmRTZ) / signbit→isNegative / isNaN; deferred classify/copySign/unmodeled `math.*` raise `SymexUnsupportedOpError` → `sxUnknown`+feUnsupportedOp via new `SymexResult.errors` field. **Deviations:** nim-z3 wrappers are `isNaN`/`isInf`/`isNegative`/`isNormal`/`isFinite`, NOT the RFC's `fpIs*`; Nim std/math 2.2.x lacks `isInf`/`isFinite`/`isNormal`/`nextafter`, so those are modeled-but-untested; `fcNan` not `fcNaN`). **F7 SHIPPED** (eval-side bit-exact float witness round-trip: F1 `extractLeaf` stub replaced — `svFloat64`/`svFloat32` evaluated into new `RawWitness.float64Vals`/`float32Vals` tables, real `readFloat`/`readFloat32`, `renderAsChoices` `SomeFloat` arm via `floatChoice(-Inf,Inf,allowNan)`; new `feExtractionFailed` kind + threadvar error sink drained into `RawResult.errors`. **Deviations:** call `evalFloat64Opt(sv.fp64, modelCompletion=true)` directly — already does the `m.eval` internally, no `Z3Float64(raw:)` reconstruct; **NaN needs a special path** — Z3's `to_ieee_bv(NaN)` is unspecified so `evalFloat*Opt` returns none, so test `m.evalBool(isNaN(sv.fpXX))` first and emit Nim canonical `NaN`; float choice-render lives in `renderAsChoices`@symex.nim not canonicalize.nim). **F8 SHIPPED** (closes Cluster F: regression smoke + 23-shape arbitrary-float64 round-trip property — each `symexFind` SAT witness, read bit-exactly from `r.witness[i]`, is plugged back into the identical-condition Nim predicate at runtime and asserted true; `withSymexSettings() do (s): s.inlinePolicy = ipAlwaysAxiomatize` wired through `symexFind` on a float SUT → `sxSat`; broken `ln(x)` SUT → `sxUnknown` with only `feUnsupportedOp`; **walker version now `"5"`** — bumped `"4"→"5"` as the final edit, single-sourced in `canonicalize.nim:symexWalkerVersion` (no duplicate in runtime.nim), asserted `== "5"` in the smoke. No new source files. **Deviations:** base ctor is `defaultSymexSettings()` not `defaultSettings()` (latter is the engine's); `withSymexSettings(f, base=...)` — the `do`-block binds `f`, so the faithful form is `withSymexSettings() do (s): ...`, NOT `withSymexSettings(defaultSettings()) do ...`; round-trip `int(x)==3` windowed to `x>3.0 and x<4.0` since out-of-range float→int is unconstrained per F5 deferral. Green c+cpp 4/4; regression subset (F1–F7, l1/l2/l3, phase1_arith/bool, phase2_overflow/abstraction, phase3_recursion, phase4_tuple, phase5_seq/table, phase14_disc_promotion/frontier_pruning) all green, no hangs.) **Cluster F COMPLETE (F1–F8). F9a SHIPPED** (array[N, float] element type-bridge audit + array-NaN extraction; mostly a completeness confirmation — array elements allocate/classify/extract via existing recursion — with one GREEN fix: `iteSV`'s float arm was an F3/F4-era `raise` stub and is now real Z3 FP `ite`, needed by literal array-index ite-chain merge. Green c+cpp 3/3.) **F9b SHIPPED** (seq[float32/64] SUT param — real GREEN extension of Phase-5 dynamic-seq machinery at 4 sites: allocateSeqDataRaw float arms, seq-index walker float arms, extractSeqElements delegating to extractLeaf for NaN reuse, emitTyAndReader→readSeqFloat64/readSeqFloat32. Green c+cpp 3/3.) **F9c SHIPPED — Cluster F COMPLETE (F1–F8, F9a/b/c).** Object-variant float32/64 arm fields. The float arm fields were already supported transitively (allocate/access/extract recursion); the real GREEN work was `bool`-discriminator support (enum discs lift to BV but `bool` was skipped, so it stayed svBool — never previously a symex variant disc): added `svBool` arms to both `discEq` sites + **skipped the Phase-14 A6 Z3Int disc-promotion for itBool discs** (promoting a bool disc to svInt made `if v.k:` trip `lowerBool`'s svBool assert). Green c+cpp 3/3; variant + F regression clean, enum-disc promotion path untouched. **Cluster S (full strings) STARTED. S0-ADR SHIPPED + REWORKED to BYTE-FAITHFUL (Corey-locked)** (doc + reconciliation cycle, no production code): `ADR-0006-string-codepoint-indexing.md` now records the **byte-faithful** string model (the project owner locked this; the earlier codepoint-indexed draft was internally contradictory and is superseded). **The model:** free `string` vars constrain every char ≤ 0xFF, so Z3 position == Nim byte index and the Z3 positional model coincides with Nim's byte model — **no divergence.** The ≤0xFF char-range constraint (asserted at `mkStringVar` allocation, S1/S3) is the **soundness mechanism** (without it Z3 picks full-Unicode codepoints that don't round-trip to Nim bytes). **Reshaped scope:** `s.len`/`s[i]` read/`s[a..b]`/`s.high`/`for c in s` are now **byte-faithfully SUPPORTED** (moved OUT of out-of-scope); only `s[i]=c`/`s.add` (Z3-string **immutability**) and `toLower`/`toUpper` (**no Z3 case-fold**) stay unsupported → `seUnsupportedStringOp`. `bytes(s)` (S7a) shrinks to a **trivial identity byte-view** (chars already 0..255), not a UTF-8-decode subsystem. `seByteIndexUnsupported`/`seByteIterUnsupported` are no longer needed for `s.high`/`for c in s` (kept in the enum, unused for those). **Coverage boundary (documented):** free-var witnesses are one byte per char (no synthesized multi-byte runes); multi-byte *literals* like `"é"` still work (lower to bytes `[0xC3,0xA9]`, len 2, free var matches). Also reconciled `RFC-phase15-reconciliation.md` § F — Cluster S against the real codebase. **Key drifts found:** (1) `mkString`→`Z3_mk_lstring` is **byte-faithful**, so `mkString("é").len == 2` (byte count), NOT `== 1`/runeLen as the RFC claimed — the S2 multi-byte DoD test must assert `len==2`; (2) `seZ3VersionMissing`/`seParseIntPreE`/`seBytesBeyondBMP` error kinds and `maxSplitParts`/`maxBytesEncodingLen` settings fields are **not yet in the type** (net-new for S5/S7a/S10a, not pre-existing); (3) no `symex_settings.nim` (SymexSettings lives in `types.nim:518`); `regex_parser.nim` is net-new for S6a; (4) test layout is flat `tests/tsymex_phase15_<CYCLE>_*.nim` (no `tests/smt/`/`tests/symex/`); (5) real nim-z3 string ops live in `sequence.nim` — `indexOf` (not `find`), `at`/`substr(off,len)`/`contains`/`startsWith`/`endsWith`/`replace`, regex `mkRegex`/`matches`/`star`/`plus`/`loop`/`range`; int interop `toInt`/`toStr`; codepoint `toCode`/`fromCode`; (6) `replaceAll`/regex-replace version gates `z3WithSeqReplaceAll`/`z3WithSeqReplaceRe` confirmed present (real FFI symbol is `Z3_mk_seq_replace_re`, RFC's `Z3_mk_seq_re_replace_all` is wrong); (7) `Table[string,V]` V≠int is a runtime `raise ValueError` today, not the parse-time `seUnsupportedTableValType` the preamble assumes — S1 must move it. Walker version unchanged at `"5"`. **S1 SHIPPED** (string type-bridge): **17 `iekStr*` IR variants** with a uniform `strArgs`/`strOp` payload + `StrOpKinds` set + `mkStrOp` (so the exhaustiveness ripple was just **6 arms** — one `of StrOpKinds:` per dispatch in render/canonicalize/tryEvalInterval/collectVarRefs/probeProto/lower, plus the emitExpr round-trip arm); `smkStr*` `StdlibModelKind` family + `of itString:` dispatch in `getStdlibModelFor`; an **`itString`-receiver call guard** in `dsl_parser.nim` routing every string call to its `iekStr*` kind (unrecognised → `iekStrUnsupported`) — this is the `string.len` routing guard and it fixes a pre-existing `getImpl`-on-`len` compile crash; walker `lower(StrOpKinds)` raises new `SymexUnsupportedStringOpError` → caught at the `runSymex` boundary → `sxUnknown`+`seUnsupportedStringOp` (Invariant 3). **Bonus fix:** free-`string` `s == "lit"` equality *did not actually work* in Phase 5 (only table-key string eq was wired; bare `s == "hello"` crashed on `eqBV on non-BV SymVal`) — added a real `cmpString` (`svString` arm in both `lower` compare branches via Z3 `Z3String ==`/`!=`); `<`/`<=` raise the classified string-unsupported error (deferred to S3). **≤0xFF byte-faithful constraint DEFERRED to S3** (S1 SUTs only use equality which pins bytes; `allocateSym` has no constraint sink; the assertion machinery + first positional op land together in S3; a regex/char-range assert now risks a Z3 string-solver hang) — marker left at `runtime.nim:allocateSym(itString)`. **`Table[string,V]` V≠int** left as the existing runtime `ValueError` (not exercised by S1; folded into a later S-cycle). Test `tsymex_phase15_S1_typebridge.nim` (2 tests) green c+cpp; regression clean (phase5 seq/table/models/hashset, phase14 multivariant, F2/F6, canonicalize, phase1_dsl), no hangs. Registered. Walker version unchanged at `"5"`. **S2 SHIPPED** (string literal lifts): **no production source change needed** — S1 had already wired the full literal path (`nnkStrLit`/`nnkRStrLit`/`nnkTripleStrLit` → `mkStrLit(n.strVal)` → `iekStrLit` → `mkString(e.sval)`/`Z3_mk_lstring`; `cmpString` for `==`/`!=`; `evalStr` witness extraction). S2 = confirmation + byte-faithful coverage. Test `tsymex_phase15_S2_strlit.nim` (6 tests): `s == "hello"`/`""`/`"\n"`(1 byte)/`"\x61"`→`"a"` all `sxSat` with the exact witness; `s == "hello" and s == "world"` → `sxUnsat`; **byte-faithful** `s == "é"` → `sxSat` with the extracted Nim witness asserted `== "é"` **and `.len == 2`** (confirms `Z3_mk_lstring` maps UTF-8 bytes `[0xC3,0xA9]` to one Z3 char each — corrects the RFC's `==1` claim). **No symex `s.len` constraint used** — that op is deferred to S3 (`iekStrLen` still raises `seUnsupportedStringOp`); byte-faithfulness is asserted on the extracted Nim witness instead. Escapes decoded by the Nim semchecker pre-`strVal` (no parser escape handling). Green c+cpp 6/6; regression clean (S1 typebridge, phase5 seq/table, F2 float literals), no hangs. Registered after S1. Walker version unchanged at `"5"`. **S3 SHIPPED** (string len/index/slice + the ≤0xFF byte-faithful constraint — the deferred-from-S1 soundness mechanism, now load-bearing). **≤0xFF constraint = regex membership, does NOT hang:** `allocateSym(itString)` asserts `matches(s, star(range(mkString("\x00"), mkString("\xff"))))` (i.e. `s ∈ (re.range '\x00' '\xff')*`) threaded into the path condition via the existing `pcOut` sink (same path as `seqLen>=0`); `range(lo,hi:string)` is the nim-z3 single-byte-endpoint overload, `star`/`matches` the `regex.nim` combinators. This is the cluster's highest hang-risk code but every S3 test + full regression completed well within the bounded-runner budget — no timeout raise, no exit-137, no fallback needed. **`s.len`** → Z3 `str.len` (svInt). **`s[i]` read** → a Nim `char` (svBV8 unsigned) via the at→toCode→BV8 bridge (`toCode(at(s,i))` is the byte value 0..255 under ≤0xFF; `intToBv[8]`); so `s[i] == 'c'` compares two BV8s through the existing mixed-int path; out-of-range `i` → `at` empty → `toCode` −1 → BV8 0xFF, no crash. **`s[a..b]`** → `iekStrSubstr` = Z3 `substr(s, lo, hi-lo+1)`; parser folds `..<` to inclusive `hi`; handled in both the `[]`-call and `nnkBracketExpr` shapes. **`s.high`** = parser-level `len(s)-1` (supported, not classified). **`for c in s` — honestly classified unsupported** (NOT a byte/codepoint reason — *unbounded symbolic iteration length*, no sound bounded encoding): classified `mkUnsupported` at parse time BEFORE the loop body is parsed (so the body's loop-var refs don't trip user-proc registration) → `sxUnknown` (Invariant 3); does NOT emit `seByteIterUnsupported`; **the reconciliation §F-S over-promise that `for c in s` is supported is corrected in the doc.** Exhaustiveness: `lower(StrOpKinds)`/`probeProto(StrOpKinds)` split into `iekStrLen`/`iekStrAt`/`iekStrSubstr` arms + a `StrOpKinds - {those}` residual-raise arm. **S1 test updated:** `s.len > 3` flipped `sxUnknown`→`sxSat` (op live as of S3). Test `tsymex_phase15_S3_strindex.nim` (7 tests) green c+cpp 7/7; regression clean (S1/S2, phase5 seq/table/hashset, F2/F6, canonicalize, phase14 multivariant), no hangs. Registered after S2. Walker version unchanged at `"5"`. **S4 SHIPPED** (string substring predicates + search — `contains`/`startsWith`/`endsWith`/`find`). **Parser + stdlib_models were already scaffolded in S1**, so S4's only production change is the **runtime lowering**: `iekStrContains` → `contains(recv.str, sub.str)` (`Z3_mk_seq_contains`, svBool), `iekStrStartsWith` → `startsWith(recv.str, prefix.str)` (`Z3_mk_seq_prefix`; nim-z3 arg order `(a, prefix)` already matches Nim), `iekStrEndsWith` → `endsWith(recv.str, suffix.str)` (`Z3_mk_seq_suffix`), `iekStrFind` → `indexOf(recv.str, sub.str)` (svInt; the no-start overload at pos 0, `Z3_mk_seq_index`; byte offset == position offset under ≤0xFF, **−1** when absent, no crash). **Real nim-z3 name is `indexOf`, not `find`.** **`sub in s` routing CONFIRMED → `iekStrContains` (string path), not `iekContains`:** typed AST desugars `sub in s` to `contains(s, sub)` (an `nnkCall` — `binopForInfix` has no `in`), which for an `itString` receiver hits the string-call guard BEFORE the `itTable`/`itSet`-only `contains`/`hasKey` guard; seq/table/set `in` membership untouched (phase5_seq/table/hashset all green). Exhaustiveness: the 4 new arms split out of the `lower`/`probeProto` `StrOpKinds` residual-raise arm (3 svBool protos + 1 svInt proto). Test SUTs need `import std/strutils`. Test `tsymex_phase15_S4_strpred.nim` (8 tests) green c+cpp 8/8; regression clean (S1/S2/S3, phase5 seq/table/hashset, F2_float_literals), no hangs. Registered after S3. Walker version unchanged at `"5"`. **S5 SHIPPED** (replace / replaceAll / split / join — densest, highest-hang-risk cycle; **NO hang**). New `seZ3VersionMissing` error kind (`seZ3StringIncomplete` already existed) + `maxSplitParts:int=8` `SymexSettings` field (+ default + `+` merge; the ripple did NOT break the withSymexSettings tests). **replace** → `replace(recv,old,new)` first-occurrence (`Z3_mk_seq_replace`) svString. **replaceAll** version-gated `when defined(z3WithSeqReplaceAll):` — gate is NOT defined on this Z3 4.15.0 build, so the `else` raises new `SymexZ3VersionMissingError` → `sxUnknown` + `errors[0].kind==seZ3VersionMissing` (the test path on this build; the `when` guard is load-bearing — the symbol doesn't exist unguarded). Nim has no `replaceAll` stdlib proc → test SUT defines a local shim (parser dispatches on callee NAME for itString receiver). **join** → `joinStrSeq` Z3 concat chain with sep interleaved, over a CONCRETE-length `svSeq[string]` (symbolic-length → seZ3StringIncomplete); new parser guard routes `xs.join(sep)` (seq[string] receiver) to `iekStrJoin` before the itString classify. **split** — special cases only, NO quantifier, NO hang: (a) empty-sep `split("abc","")`→`@["a","b","c"]` byte-faithful; (b) concrete-inline (receiver+sep are string LITERALS, detected at IR level via `iekStrLit` — NOT Z3 `isStringValue`, which is absent) → Nim-computed concrete `svSeq`; (c) GENERAL symbolic path → CONSERVATIVELY classified `seZ3StringIncomplete` → `sxUnknown` via new `SymexZ3StringIncompleteError` (the RFC's universal-quantifier-over-symbolic-seq encoding is NOT emitted — biggest hang risk in the cluster). **seq[string]** support PARTIALLY added (only what S5 exercises): `mkConcreteStrSeq` (mkConstArray+store), `allocateSeqDataRaw` itString arm, statement-level seq-index itString arm (`parts[i]`→svString via cmpString); `.len` works via existing iekSeqLen. **NOT added: seq[string] witness extraction** — split results live only in env locals, never a SUT param (extraction iterates params only), so unreachable in S5; deferred (no untested gold-plating). Two new boundary catches before the generic Z3Error. Test `tsymex_phase15_S5_strops.nim` (7 tests) green c+cpp 7/7; regression clean (S1–S4, phase5_seq/table, F2, phase13_verdict_primitives + F8_smoke as withSymexSettings exercisers), no hangs. Registered after S4. Walker version unchanged at `"5"`. **S6a SHIPPED** (standalone Nim-regex → `Z3Regex[Z3String]` parser — net-new `src/proptest/smt/regex_parser.nim`, NO walker/symex import, NO Z3 solving; pure recursive-descent over the `z3/regex` combinators). **Result idiom:** repo has no `results` dep, so it matches the `isOk`-duck-typed convention → `RegexParseResult{isOk:bool, regex:Z3Regex[Z3String], error:string}`. **Real nim-z3 combinators used** (all confirmed in `regex.nim`): `mkRegex(Z3Seq)`, `star`/`plus`/`option`/`complement`, `concat`/`union`/`intersect` (varargs), `loop(r,lo,hi)`(={n,m}), `power(r,n)`(=exact {n}), `range(lo,hi)`(=[a-z]). **Import gotcha (logged):** the `union`/`intersect`/`concat` varargs templates expand `checkErr`+raw FFI in the caller scope, so the module must also `import z3/error` + `import z3/ffi` (z3/regex+strings+context alone fail to compile). **Byte-faithful (ADR-0006, ≤0xFF):** `.` = `range('\x00','\xFF')` (NOT `mkRegexAllChar` — its full-Unicode basis admits codepoints >0xFF that don't round-trip to a Nim byte); `\d`=`range("0","9")`; `\w`=union `[A-Za-z0-9_]`; `\s`=union `' '\t\n\r\f\v`; `[a-z]` uses the `Z3String`-typed `range(mkString,mkString)` (the `(string,string)` overload rejects 0x80..0xFF); `[^…]`=`intersect(complement(class), range('\x00','\xFF'))` (so it matches exactly one byte); `(...)`/`(?:...)` transparent; `{n,}`=`power++star`. **Rejected → isErr** (message embeds `seUnsupportedRegex`): backreferences `\1`..`\9`, lookahead `(?=…)`/`(?!…)`, named groups `(?P<n>…)`/`(?<n>…)`/`(?'n'…)`. Test `tsymex_phase15_S6a_regex_parser.nim` (21 tests: 17 supported + 4 rejected) green c+cpp 21/21; regression S4/S5 clean. Registered after S5. Walker version unchanged at `"5"` (no walker touched). **S6b SHIPPED** (regex match walker integration — wires S6a's `parseNimRegexToZ3Regex` into `runtime.nim`). **NO HANG** (the key risk): regex membership on a FREE string under the ≤0xFF constraint completed well inside the bounded-runner budget for every test + full regression (no exit-137). **`rePatternStr` carried in the existing `strOp` field** (option a, least disruption): reuse the uniform-payload `strOp: string` for `iekStrMatch`/`iekStrFindRe`/`iekStrReplaceRe` (NO new field, NO recursive `IRRegex`); `canonicalize` already folds `strOp` into the content-addressed cache key, so distinct patterns content-address distinctly for free. **`matches` confirmed; `iekStrFindRe` DEFERRED:** `matches(Z3String, Z3Regex): Z3Bool` (`regex.nim:93`, `Z3_mk_seq_in_re`) → `iekStrMatch` svBool; nim-z3's `indexOf` takes a `Z3Seq` sub ONLY (no regex overload), so `s.find(re"…")` has no sound Z3 primitive → classified `seUnsupportedRegex` (sxUnknown) via new `SymexUnsupportedRegexError` (documented deferral; pattern still parsed first so a rejected pattern reports the precise S6a reason). **Pattern extraction:** a typed `re"…"` is `nnkCallStrLit(Sym "re", RStrLit "<pat>", Curly(reStudy))`; match/find/contains carry a trailing default `start` `nnkIntLit 0` (dropped), replace carries the replacement string arg; the parser scans args ≥2 for the `re`/`rex` CallStrLit (BEFORE the uniform `sArgs` parse that chokes on it) and lifts `[1].strVal` into `strOp`. `contains(s, re"…")` routes to `iekStrMatch`. A non-literal Regex falls through → `iekStrUnsupported`. **`replaceRe` version-gate (OFF):** `when defined(z3WithSeqReplaceRe):` `replaceRe(recv,re,repl)` (`Z3_mk_seq_replace_re`); `else:` `SymexZ3VersionMissingError` → sxUnknown + `seZ3VersionMissing` (gate confirmed absent on this Z3 4.15.0 build → else exercised; the `when` is load-bearing). Exhaustiveness: `lower`/`probeProto` split the 3 kinds out of the `StrOpKinds` residual; `abstraction.tryEvalInterval`'s explicit list gained them; canonicalize/render/emitExpr use set arms (no edit). New `SymexUnsupportedRegexError` catch at the `runSymex` boundary → `seUnsupportedRegex`. Test `tsymex_phase15_S6b_regex.nim` (5 tests: `[a-z]+` SAT all-lowercase, `\d+` SAT numeric, `re""` vs non-empty UNSAT, backref → sxUnknown+seUnsupportedRegex, regex replace → sxUnknown+seZ3VersionMissing) green c+cpp 5/5; regression clean (S1–S5, S6a_regex_parser, phase5_seq, F2_float_literals), no hangs. Registered after S6a. Walker version unchanged at `"5"`. **S7a SHIPPED** (`bytes(s)` byte-faithful **trivial identity byte-view** — NOT the RFC §S7a multi-byte UTF-8 BMP `ite`-encoding, which is rejected under byte-faithful, ADR-0006). Every Z3 string char is ALREADY one byte (≤0xFF, S3), so `bytes(s)` is an `svSeq` of `svBV8`, `seqLen == len(s)` (**EQUAL**, not `>=` — byte count == char count), each `bytes[i] == intToBv[8](toCode(at(s, i)))` (reuses S3's exact at→toCode→BV8 bridge). **Concreteness detected at the IR level (mirrors S5 split):** a string LITERAL receiver (`iekStrLit`) has a known byte count (`recvIR.sval.len`); a bare `string` param (symbolic length) → `SymexBytesSymbolicLengthError` → sxUnknown + `seBytesSymbolicLength` (no Z3 numeral-extraction needed — concrete cases call `bytes` on a LITERAL while the param is pinned, the S5 split idiom). **New setting `maxBytesEncodingLen:int=32`** added to `SymexSettings`/`defaultSymexSettings()`/`+` merge (same ripple as S5's `maxSplitParts`); threaded into `lower` via a per-run `currentMaxBytesEncodingLen` threadvar (set in `runSymexImpl`, mirroring F7's `extractionErrors`, since `lower` has no settings param); caps the concrete CHAR count directly (1 byte/char, NOT `/3`); a concrete length above it → `SymexBytesLengthTooLargeError` → sxUnknown + `seBytesLengthTooLarge`. **`seBytesBeyondBMP` UNREACHABLE — OMITTED** (a free char is ≤0xFF by construction, a literal char is a raw byte 0..255, so toCode always fits BV8; the error kind is NOT added; the RFC's "codepoint > 0xFFFF" test is dropped). **One supporting fix:** `classifyType` gained a `"byte"` arm (`= uint8` → `tInt(8,unsigned)`) — `bytes` returns `seq[byte]` and the seq-element classify previously errored on the `byte` alias (only `uint8`/`char` mapped); `byte` IS `uint8` (real gap, not scope-creep). Exhaustiveness: `iekStrBytes` split out of `lower`'s `StrOpKinds - {…}` residual-raise (added to the exclusion set); `probeProto`'s `none(SymVal)` residual already covers it (svSeq, consumed only via `.len`/index — no comparison proto); two new boundary catches before the generic Z3Error. Test `tsymex_phase15_S7a_bytes.nim` (5 tests: `bytes("A")` len-1 `[0]==65` SAT; `bytes("é")` len-2 `@[0xC3,0xA9]` byte-faithful SAT; `len(bytes(s))==len(s)` EQUAL SAT; symbolic-length → seBytesSymbolicLength; >32 → seBytesLengthTooLarge) green c+cpp 5/5; regression clean (S1–S6b, phase5_seq, F8_smoke, F2), no hangs. Registered after S6b. Walker version unchanged at `"5"`. **S7b SHIPPED** (Z3-string-theory regression smoke + determinism doc — **verification-only, no new source files, no fixes needed**). New hermetic in-process test `tsymex_phase15_S7b_smoke.nim` (8 tests) green c+cpp 8/8, exercising MULTIPLE string ops TOGETHER on the same/related SUTs to catch cross-op state-threading bugs from the S1–S7a multi-file edits: a multi-op SUT `s.len==5 and s[0]=='h' and s.contains("ell") and s.startsWith("he")` → sxSat with a witness round-tripped through the runtime predicate; a `len+slice+endsWith+find` SUT pinned to "world"; a concrete split+join round-trip AND `bytes(literal)` in ONE SUT; a regex `match(re"[a-z]+")` SUT + a string-equality SUT; a `withSymexSettings` exerciser on a STRING SUT (confirms the settings builder threads through for strings too, mirroring F8); the ≤0xFF byte-faithful invariant (free `s` with `s.len==1` → single Nim byte); and `symexWalkerVersion == "5"` asserted (NOT bumped — that is S11). **Finding (logged):** a bool-returning *string* helper proc does NOT inline under symex — even a trivial `proc p(s:string):bool = s=="hello"` called as `if p(s)` yields sxUnknown (call not inlined into the path condition; outside S7b's scope); the S-cluster convention of inlining the condition directly in the SUT body sidesteps it, and the smoke uses a textually-identical runtime predicate only for the witness round-trip (the F8 (symex SUT, runtime predicate) split). **Broad regression sweep (representative subset, all green c, no hangs / no exit-137):** ALL Cluster S (S1–S7a), the L-cluster (l1/l2/l3), a Cluster-F sample (F2/F6/F8), and pre-existing string-bearing tests (phase5_seq/table/hashset/models, phase14_multivariant_walker/frontier_pruning). **determinism.md updated** with a STRING section (byte-faithful ≤0xFF model per ADR-0006; byte-indexed len/[i]/[a..b]/.high; supported-op list; classified-unsupported ops + error-kind table; Latin-1 witness-coverage limitation). Registered after S7a. Walker version unchanged at `"5"`. **S8 SHIPPED** (`&` string concatenation → `Z3_mk_seq_concat`). `iekStrConcat` (a StrOpKinds stub since S1) given its real lowering. **Parser `&` itString-guard (load-bearing):** `&` is an INFIX operator, not a named call, so it reaches `parseExpr`'s `nnkInfix` arm where `binopForInfix` has no `&` case and would `error` (the RED failure). S8 intercepts `&` THERE, BEFORE `binopForInfix`, gated on `n[0].strVal=="&" and classifyType(n[1]).ty.kind==itString and classifyType(n[2]).ty.kind==itString` → `mkStrOp(iekStrConcat,"&",@[lhs,rhs])`. Purely ADDITIVE: a non-itString operand (seq concat, other types) fails the itString check and falls through to `binopForInfix` as before — and symex never wired a parser `&` for SEQ concat at all (`binopForInfix` has no `&`; phase5_seq uses none), so there was no seq-`&` path to break. Chained `a&b&c` is left-assoc (`(a&b)&c`, each `&` its own binary node) → operand recursion handles it, no special casing. **runtime `lower`:** split `iekStrConcat` out of the StrOpKinds residual → `SymVal(svString, concat(l.str, r.str))`. **nim-z3 concat API: `concat(a,b)`** (`sequence.nim:141`, `Z3_mk_seq_concat`; equivalent to the `&` sugar at `:143`/lifted `&` at `strings.nim:197` — `concat` chosen per spec text). Both operands lower to svString (a literal via the existing `iekStrLit`→`mkString` path; no separate coercion). `probeProto` svString arm (so `(a&b)=="lit"` lowers its literal through `cmpString`) + residual exclusion. Exhaustiveness: 2 edited dispatches (`lower`, `probeProto`) in runtime.nim; no types/canonicalize/abstraction edit (uniform-payload StrOpKinds set arms already cover it). Byte-faithful: `(a&b).len==a.len+b.len` holds (S3 `iekStrLen`). Test `tsymex_phase15_S8_concat.nim` (5: literal→"foobar", var concat→b=="llo", chained "abc", additive len, UNSAT contradiction) green c+cpp 5/5; regression S1–S7b + phase5_seq + F2 clean, no hangs. Registered after S7b. Walker version unchanged at `"5"`. **S9 SHIPPED** (`toLower`/`toUpper` classified unsupported — small classify-only cycle, **NO Z3 modeling, NO new IR kind, NO new error kind**). Per ADR-0006 Z3 has NO native case-folding primitive (regex-range approximation deferred to Phase 16), so the case-folding ops must yield a CLASSIFIED `seUnsupportedStringOp` → `sxUnknown` (Invariant 3 — never silent UNSAT, never crash). `dsl_parser.nim` adds ONE explicit guard in the `itString`-receiver call arm (alongside S3's `high` guard, BEFORE the `getStdlibModelFor` dispatch): a callee in `["toLower","toUpper","toLowerAscii","toUpperAscii"]` on an `itString` receiver → `mkStrOp(iekStrUnsupported, <callee>, args)` carrying the REAL surface op name. This reuses the existing pipeline end-to-end: `iekStrUnsupported` ∈ `StrOpKinds` falls into runtime `lower`'s residual `StrOpKinds - {modeled}` arm → raises `SymexUnsupportedStringOpError` → the `runSymex` boundary maps it to `sxUnknown` + `seUnsupportedStringOp`. (These four names would ALSO have fallen through `getStdlibModelFor`'s `else: iekStrUnsupported`; the explicit guard makes the classification intentional + self-documenting + carries the precise op name into the diagnostic — same idiom as the `high` guard.) **Covers both stdlib homes:** `toLower`/`toUpper` from std/unicode AND ASCII `toLowerAscii`/`toUpperAscii` from std/strutils; test SUTs `import std/[strutils, unicode]`. Test `tsymex_phase15_S9_caseconv.nim` (5 tests: 4 case-conv ops → sxUnknown + `errors[0].kind == seUnsupportedStringOp`; plain `s == "abc"` still sxSat — proves S9 ONLY classifies case-conv, doesn't break the string path) green c+cpp 5/5; regression clean (S1–S5, S8, S7b), no hangs. Registered after S8. Walker version unchanged at `"5"`. **S10a SHIPPED** (`$int`/`parseInt` **digits-path only** — S10b raises-path still DEFERRED post-E1; exceptions not built). **No hang:** `str.to_int` + prefix/substr/len on a free string are decidable under the ≤0xFF byte-faithful constraint (S3); every test + full regression inside the bounded-runner budget. **New error kind `seParseIntPreE`, severity `sevHint`** (NOT sevError) added to `SymexErrorKind` — a path carrying it stays **sxSat**, exercising the Invariant-7 severity contract (sxSat with non-empty errors ⇒ all sevHint/sevWarning). **`$n` arrives as `nnkPrefix` `$` (NOT nnkCall)** — the RED failure was "unsupported prefix operator `$`"; intercepted in the `nnkPrefix` arm (itInt operand → `iekIntToStr`; non-int `$` errors at parse time, deferred). `parseInt(s)` on an itString → `iekStrToInt`, intercepted in the nnkCall arm before the itString-receiver classify. **runtime:** `iekIntToStr` → `SymVal(svString, toStr(toZ3Int(op)))` (`toStr` on Z3Int = `Z3_mk_int_to_str`; operand coerced via `toZ3Int` because int params are BVs under the abstraction layer — the `==`-goal keeps the bv2int mix at low F5 hang risk, F5's pathology was ORDERING goals). `iekStrToInt` → `ite(startsWith(s,"-"), -toInt(substr(s,1,len-1)), toInt(s))`. **nim-z3 name/semantics corrections (verified _deps/z3/src/z3/):** the RFC's `prefixOf` is really **`startsWith(a, prefix)`** (`sequence.nim:171`, `Z3_mk_seq_prefix`); and `toInt` (`Z3_mk_str_to_int`) returns the fixed value **−1** for a non-digit string (`strings.nim:126-128`), **NOT** "unconstrained" as the RFC premised — that reshapes the gate. **Digits gate on the NEGATIVE branch ONLY** (`isNeg ⇒ negInner>=0`): the positive branch needs no gate (Z3's toInt is already faithful — true digits or −1), and gating it would wrongly force non-digit → UNSAT and erase the window; the negative branch is gated so a non-digit suffix can't make `-(-1)=+1` a false positive. Gate clauses accumulate in a `parseIntGateConstraints` threadvar (lower has no pc sink — Env is a pure value table) drained into EVERY `trySolve` check (sound: each clause references the param string's Z3 AST, identical across paths). **seParseIntPreE sevHint** emitted whenever parseInt is lowered on a not-provably-digit string (conservative HINT over-emission, spec-permitted), collected in `parseIntPreEHints` threadvar, surfaced (deduped to one) on sxSat results in `runSymexImpl`. **The window:** `parseInt(s) == -1` is sxSat for a non-digit `s` (Z3's faithful −1) where Nim would RAISE `ValueError` — modeling the raise needs E1 → S10b. Exhaustiveness: `lower` splits the 2 kinds out of the residual; `probeProto` adds `iekStrToInt`→svInt and `iekIntToStr`→svString protos; `trySolve` drains the gate, `runSymexImpl` resets both threadvars + surfaces the hint. Test `tsymex_phase15_S10a_strconv.nim` (4 tests: `$n=="42"`→n=42; `parseInt(s)==42`→s="42"; `parseInt("-42")`→ −42; non-digit `parseInt(s)==-1`→sxSat+seParseIntPreE hint) green c+cpp 4/4; regression clean (S1–S5,S8,S9,S7b,phase1_arith,F2), no hangs. `determinism.md` string section gained the S10a note. Registered after S9. Walker version unchanged at `"5"`. **S11 SHIPPED — CLOSES Cluster S; walker version now `"6"`.** Immutable-string mutations (`s[i] = c`, `s.add(c)`/`s.add(otherStr)`) classified `seUnsupportedStringOp` → `sxUnknown` (ADR-0006 — reason is **immutability**, NOT byte/codepoint; Invariant 3, never silent UNSAT/crash). Two dsl_parser interception points reusing the S9/S3 idiom (route to `iekStrUnsupported` carrying the surface op name → residual `lower` arm raises `SymexUnsupportedStringOpError` → runSymex boundary maps to `seUnsupportedStringOp`; **no new IR kind, no new error kind**): (1) `s[i] = c` detected in the **`nnkAsgn`** arm when the LHS `nnkBracketExpr` receiver is `itString` (was falling to the generic `mkUnsupported` → a SILENT sxUnknown) — **this path IS reachable in the SUT grammar** (a local `var s: string` + `s[0] = c` parses cleanly, confirmed RED→GREEN); (2) `s.add(…)` intercepted in the statement-`nnkCall` arm with an explicit `itString` guard BEFORE the `itSeq` `add` arm (else it fell through to user-proc registration → a spurious sxUnsat). **Walker version bumped `"5"→"6"`** as the final edit, single-sourced in `canonicalize.nim:symexWalkerVersion` (re-exported via symex.nim; confirmed no duplicate — Invariant 6). The bump orphans all "5"-era cache keys → broad regression re-solves from scratch. Two prior version-pin assertions (F8_smoke, S7b_smoke, both pinned "5") advanced to "6" — the intended consequence of the close-out bump (S7b's comment said "the Cluster-S bump is S11"). `determinism.md`: unsupported-op table row updated + "Cluster S op-table COMPLETE (closed at S11)" note. Test `tsymex_phase15_S11_mutation.nim` (5 tests: s[i]=c, s.add(c), s.add("x") → sxUnknown+seUnsupportedStringOp; plain read → sxSat; version=="6") green c+cpp 5/5. Broad regression under v6, all green no hangs: S1–S10a (incl. updated S7b/F8 version assertions), F2, F6, F8, phase1_arith, phase5_seq, phase14_multivariant_walker/disc_promotion/frontier_pruning. Registered after S10a. **S10b SHIPPED — Cluster S FULLY COMPLETE (S1–S11 + S10b).** S10b closed the S10a pre-E1 window: non-digit `parseInt` now RAISES `ValueError` (expression-level raise forked at the enclosing statement walk via a `parseIntRaiseConds` threadvar + `drainParseIntRaises` helper, reusing E3's `routeRaise`); `seParseIntPreE` no longer emitted; `$float`/`parseFloat` classified `seUnsupportedStringOp`. S10a's test updated to the raises behavior. No walker version bump (Cluster S bumped at S11→"6"; stays "6"). **H1 SHIPPED — Cluster H COMPLETE.** Pure infrastructure (no heap semantics — those are Cluster R): `Path` (a **private `ref object`** in `runtime.nim`, NOT a value object, NOT exported) gains three empty-default fields — `heaps: Table[string, Z3AnyAst]`, `heapDepth: int`, `allocCounters: Table[string, int]` — inert until Cluster R fills them. New `deepCopyHeapState(src)` helper + `forkPath(parent, pc, env, uncertain)` template route the three fields (Nim `Table` assignment = value copy → fork isolation) through **all 26 CHILD fork sites** (isIf arm/else, isLet, isAssign, isWhile body/exit/unwind, isIndex table/seq-oob/seq-surv/array-oob/array-surv, isVariantReassign, isVariantReassignSymbolic ×2, isVariantField fd/surv, isReturn, isCall opaque/depth-bail/cycle/cache-hit/descent/return-merge, isAssert viol/holds); the **1 ROOT** `Path(` in `runSymex` keeps empty defaults (no parent). Fork-site registry comment block added above `walk`. `Z3AnyAst` confirmed as the erased-AST type (same handle as `seqDataRaw`/`tabDataRaw`/`uninterpAst`; `std/tables` already imported). ADR-0010 (logical-heap model: per-type `Z3Array[Ref_T, T]`, freshness via allocCounters, nil sort-constant, per-path deep-copy; region/Andersen/Steensgaard rejected) authored on disk. `Path` is private so fork isolation is tested via two exported hooks (`h1PathHasHeapFields`/`h1ForkIsolation`) — black-box was insufficient (fields inert until R, no observable behaviour). **NO walker bump, NO rendering bump — walker stays `"6"`** (re-asserted by S11/F8 regression). `tsymex_phase15_H1_path_heap_fields.nim` (2 tests) green c+cpp 2/2; regression (phase1_arith, phase3_recursion, phase4_tuple, phase5_seq, S3_strindex, S11_mutation, F8_smoke) clean, no hangs, version still "6". Registered after S11. **E1 SHIPPED — Cluster E (exceptions) opened.** Purely STRUCTURAL foundational cycle (IR + parser + handler-stack scaffolding; walker STUBS both new kinds — no exception semantics, those land E2b+). types.nim: `IRStmtKind` += `isRaise`/`isTry` + new `ExceptHandler` object (`typeIds: seq[string]` (empty = bare `except:`), `body: IRStmt`); IRStmt fields (isRaise: `raiseTypeId`/`raiseMsg`/`raiseIsReraise`; isTry: `tryBody`/`tryHandlers`/`tryFinally`); ctors `mkRaise`/`mkReraise`/`mkTry`; `SymexErrorKind` += `eeRaiseUnimplemented`/`eeTryUnimplemented`. render: `raise(ValueError,"x")` / `raise()` (bare) / `try{…}except[ValueError=>…][finally=>…]`. dsl_parser.nim: `nnkRaiseStmt` (newException form — typed AST `RaiseStmt[StmtListExpr[Empty, ObjConstr[Par[RefTy[Sym T]], ExprColonExpr[msg,…]]]]`, type unwrapped through Par/RefTy/PtrTy to the Sym/Ident; bare `raise`/`RaiseStmt[Empty]` → mkReraise) + `nnkTryStmt` (`nnkExceptBranch` leading Type-nodes → `ExceptHandler.typeIds`, last child = handler body; `nnkFinally` → tryFinally) + emitStmt branches for both. runtime.nim: `HandlerFrame`/`ExnRecord` types; `WalkerStatics` += `exnTable`/`userExnHierarchy` (both empty until E4a); `CallFrameCtx` += `handlerStack`/`inFlightExn`; `WalkCtx` += `frameStack`; `pushFrame`/`popFrame` helpers wired into the **isCall descent arm** (per-frame handler stack — a try opened in a callee is invisible to the caller after return; inert in E1, wired so E3/E5 are correct by construction). **Z4's WalkerStatics/CallFrameCtx split already existed and MATCHED the RFC's EffectCtx→statics+frame mapping** — §B.3's "monolithic" premise predates Z4; only field-population was net-new. **RawResult/verdict reality:** RawResult is a flat object with `status ∈ {sxSat,sxUnsat,sxUnknown}` + an `errors` field — **NO sxRaised/InternalVerdict yet** (those land E2a). **Before E1, raise/try parsed to a SILENT `isUnsupported` sxUnknown** (`mkUnsupported` fallthrough); now the walk STUBS raise `SymexRaiseUnimplementedError`/`SymexTryUnimplementedError` → caught at the runSymex boundary (the established classified-error idiom, since the silent-sawUnknown tail emits empty errors) → sxUnknown + `eeRaiseUnimplemented`/`eeTryUnimplemented` (sevError, Invariant 3 — never silent/crash). **Exhaustiveness ripple = 5 compiler-required arms across 3 files** (types.render, canonicalize, runtime.collectSetLitMembers, runtime.collectTableLitKeys, abstraction.collectBan, scan.scanStmt-1st — all exhaustive; emitStmt is exhaustive too but those branches are deliverables; collectAssertRanges/scan-2nd/the IRExprKind dispatches have `else`). **No walker version bump** (E-cluster bumps at E7; stays `"6"`). Test `tsymex_phase15_E1_ir.nim` (4 tests: parser emits isRaise + canonical render; hand-built isTry render; bare-raise render; walker stub → sxUnknown+eeRaiseUnimplemented+sevError) green c+cpp 4/4; regression (phase1_arith/let/assert, phase3_recursion (call-descent push/pop), phase4_tuple, phase5_seq, S3_strindex, S11_mutation, F8_smoke, phase11_walker) all green, no hangs. Registered after H1. **S10b (parseInt raises-path) now UNBLOCKED** — can land after any E-cycle (it carries its own walker bump). **E2a SHIPPED — structural sxRaised cascade.** Mechanical wiring of `sxRaised` through the whole type/dispatch surface, **NO handler matching / NO propagation / NO witness / NO Z3** (those land E2b). **Z4 already provided `WalkCtx.found: seq[RawResult]`; Z3e already provided `cacheKeyRaised(typeId)`** — both reused (Invariant 6), NOT re-added. **Added:** `sxRaised` to `SymexStatusKind` + the `RawResult` variant branch (`raisedTypeId` live; `isDefect`/`raisedMsg: Option`/`raisedWitness` inert until E6/E2b) (runtime); `sfRaised` to `SymexFindingStatus` (engine/types); `stkRaisedExn{typeFilter}` to `SymexTargetKind` + `tRaisedExn()` ctor — **`SymexTargetKind` lives in `smt/types.nim`, NOT engine/types** (prompt hedged; confirmed). **Walker `isRaise` arm: REPLACED E1's `eeRaiseUnimplemented` classified stub with a STRUCTURAL per-feasible-raise-path `sxRaised` emission into `w.found`** (uncertain paths → sawUnknown; path terminates `@[]`); `shouldStop` stop-set now `{sxSat, sxRaised}`; `isTry` keeps its E1 `eeTryUnimplemented` stub. **Multi-`sxRaised` cache — the one real RFC-GREEN deviation:** the RFC specced `loadSymexVerdictImpl`+`loadAll(:raised:*)`, but the general example-DB (`db.nim`) has **NO key-prefix scan** (`loadPrimary` is exact-key only). Realised instead as a dedicated `saveSymexRaisedImpl`/`loadSymexRaisedImpl` pair (symex.nim): each distinct raised type → a per-type sentinel under `cacheKeyRaised(typeId)` PLUS an index slot (`:raised`) enumerating the type ids (each `bytesChoice`-encoded); load reads the index and reconstructs the full `seq[RawResult]`. A two-raise SUT (ValueError, IOError) round-trips both findings without Z3. `toFindingStatus(sxRaised)==sfRaised`; `saveSymexVerdictImpl` gets `of sfRaised: return`; `symexFindAllWitnesses` gets a cold-path save + a 4th cache-cascade load level. **Exhaustiveness ripple = 11 compiler-required arms across 4 files** (chased via bounded compiles; phase13_layer1_wire regression surfaced the `symexFindAllWitnesses` cold-path arm the E2a test alone didn't reach): SymexStatusKind → `SymexResult[T]` variant, `symexFind` `case raw.status`, 5 walker `trySolve`-dispatch sites (unreachable discard), `symexFindAllWitnesses` cold-path; SymexTargetKind → describeTarget, canonicalize, 4 macro-time `case target.kind` rebuild/cover sites. **E1 test UPDATED** (4th case asserted the now-removed `eeRaiseUnimplemented` stub → now asserts `sxRaised` + `raisedTypeId=="ValueError"`). **No walker version bump** (E-cluster bumps at E7; stays `"6"`). Test `tsymex_phase15_E2a_cascade.nim` (4 tests) green c+cpp 4/4; registered after E1. Regression (phase13 verdict/cache round-trip ×5, phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **E2b SHIPPED — real `walk(isRaise)` semantics + `InternalVerdict` boundary.** **Walk-return structure found: ACCUMULATOR-based, NOT a `WalkResult` type** — `walk(stmt, paths, w): seq[Path]` returns the SURVIVING continuation paths and side-effects findings into `w.found: seq[RawResult]` (Z4); the Des-H3 "WalkResult→InternalVerdict rename" does NOT apply. So `InternalVerdict` is a **LOCALIZED raise-path helper**, not a walk-return refactor: a private union (`ivSat`/`ivUnsat`/`ivUnknown`/`ivRaised`) built inside the `isRaise` arm and converted by `toPublic(iv): RawResult` — the **SINGLE boundary conversion (Invariant 9)**, called once per finding immediately before `w.found.add`. (Variant field names `satWitness`/`raisedWitness` differ across branches because Nim forbids a repeated field name across case arms — the same rule forced `SymexResult[T].sxRaised` to use `raisedWitness*: T`, not reuse `witness`.) **Raise witness extraction = the EXACT `sxSat` mechanism:** the `isRaise` arm calls the existing `trySolve(w.z3, p, w.params, …, w.initialEnv)` on the already-forked non-uncertain raise path; on `sxSat` it takes the returned `RawWitness` (extracted by `extractWitness` from `initialEnv`/`path.env`, identical to the assert/label arms) and wraps it as `ivRaised(typeId, msg, witness)`. `raiseMsg` evaluated by `evalRaiseMsg`: string-literal `iekStrLit`→`some(sval)`, nil/non-literal→`none` (no exact value for a non-literal without a string-sort Z3 model — deferred, never guessed; Invariant 3). **Target-gated emission (the real behavior change from E2a's untargeted structural stub):** the raise surfaces a finding ONLY under `stkAssertionViolation` (reachable raise = violation) or `stkRaisedExn` (matching `typeFilter`, empty=any); under `stkLabel`/other the raise just terminates the path (`@[]`) so a post-raise label is correctly `sxUnsat`. **Bare-raise handling:** `raiseIsReraise` with `w.frame.inFlightExn.isSome` → re-raise that `ExnRecord` (typeId+msg propagated); empty handler stack + no in-flight → `SymexRaiseOutsideHandlerError` → boundary `eeRaiseOutsideHandler` (sevError, Invariant 3); non-empty handler stack without a recorded in-flight exn → `sawUnknown` (handler-stack re-raise is E3). **isTry STAYS the E1 `eeTryUnimplemented` stub — E2b does NOT touch try/except (E3 does).** types.nim: new `eeRaiseOutsideHandler` `SymexErrorKind`; `SymexResult[T].sxRaised` += `raisedWitness*: T`. runtime.nim: new `SymexRaiseOutsideHandlerError` + its `runSymex` boundary clause. symex.nim: `symexFind` macro `sxRaised` arm reconstructs the public witness tuple from `raw.raisedWitness` via the shared `witnessTup` reader. **E1 test UPDATED** (4th case re-targeted: `tLabel("after")`→`sxUnsat` + `tRaisedExn("ValueError")`→`sxRaised`). **No walker version bump** (E-cluster bumps at E7; stays `"6"`). Test `tsymex_phase15_E2b_raise.nim` (5 tests, isExact+isOptimised) green c+cpp 5/5; registered after E2a. Regression (E1_ir updated, E2a_cascade, phase13 satsuffix/unsat_roundtrip/layer1_wire, phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **E3 SHIPPED — try/except matching by type (first-match, catch-all) + inter-proc propagation (the exception-control-flow CORE).** Accumulator-based walker preserved (no walk-return refactor); E3 = a shared `routeRaise(p, typeId, msg, w): seq[Path]` primitive + two per-frame channels (`caught`, `escaped`). **`walk(isTry)`** (replaces E1's `eeTryUnimplemented` stub): push `HandlerFrame{handlers, finallyBlock}` at `myDepth = handlerStack.len`, walk `tryBody`, pop to `myDepth`, then CLAIM `caught` entries tagged `depth == myDepth` and merge with the body's fall-through. **Key insight — the depth-tagged `caught` channel:** a caught raise's handler continuation must EXIT the try, not resume the try body at the raise site (first attempt let a post-raise `done` label be reached on the raising input). So `routeRaise` records handler continuations on `w.frame.caught` and returns `@[]`; only the owning `isTry` (matched on depth) re-introduces them. **`routeRaise`** searches `handlerStack` top-down for the first `ExceptHandler` whose `typeIds` contains `typeId` — **EXACT-STRING** (`typeId in h.typeIds`; empty = bare `except:` catch-all). MATCH → truncate below the matched frame, set `inFlightExn` for the handler body (outward re-raise), walk handler body, restore, record on `caught`. NO MATCH in callee frame (`frameStack.len > 0`) → push `EscapedRaise{path, typeId, msg}` onto `w.frame.escaped`. NO MATCH in root frame → E2b boundary (target-gated `trySolve`→`toPublic(ivRaised)`). **`isRaise`** delegates each path to `routeRaise` (re-raise via `inFlightExn` unchanged). **Inter-proc rides the `isCall` arm:** capture callee `escaped` BEFORE `popFrame`, then after the pop re-route each via `routeRaise` against the CALLER's handler stack (helper's raise caught by f's surrounding try). Escaped callees are NOT cached (`calleeEscaped.len == 0` guard — incomplete summary). **`tryFinally` DEFERRED to E5** (stub: walked on normal fall-through only; no finally in E3 tests). **Exact-string (NOT subtype) confirmed** by the negative test: `except CatchableError:` does NOT catch `ValueError`→propagates `sxRaised`. **No hang** (routeRaise always returns `@[]` on the straight-line raise; escaped drained once per descent). No walker version bump (stays "6"). `tsymex_phase15_E3_try.nim` (10 tests) green c+cpp 10/10. Regression (E1_ir, E2a_cascade, E2b_raise, phase3_recursion (call-descent — inter-proc rides this), phase3_mutual, phase1_arith/assert, phase11_walker, S11_mutation, F8_smoke, phase13_satsuffix) all green, no hangs. **E4 SHIPPED — exception subtype catch via static `ExnTypeTable`.** New file `src/proptest/smt/exn_hierarchy.nim`: compile-time `const exnTypeTable: Table[string, seq[string]]` (type name → FULL ancestor chain) encoding Nim's REAL hierarchy (verified vs Nim 2.2.10 system.nim/exceptions.nim): Exception(root)→{Defect, CatchableError}; CatchableError→{ValueError, IOError, OSError}; **KeyError is-a ValueError** (NOT directly CatchableError); Defect→{IndexDefect, FieldDefect, AssertionDefect, RangeDefect, OutOfMemDefect, StackOverflowDefect}. **Naming:** the real type is `OutOfMemDefect`; the RFC/checklist `OutOfMemoryDefect` is aliased to the same chain. `isSubtypeOf(raised, ht, exnTable, userExnHierarchy)` = `ht == raised or ht ∈ ancestorsOf(raised)`; `ancestorsOf` resolves the static chain else walks `userExnHierarchy` (child→parent, E4a-populated, EMPTY in E4) splicing the first standard ancestor's static chain. `isDefect(exnTable, typeId, …)` = `"Defect" ∈ ancestorsOf` (dkOther fallback for unknowns). **runtime.nim:** `WalkerStatics.exnTable` retyped `Table[string,string]`→`Table[string, seq[string]]` + populated from `exnTypeTable` in the WalkCtx ctor; `routeRaise`'s exact `typeId in h.typeIds` swapped for a per-`ht` `isSubtypeOf` loop (bare `except:` still catches all). **Unknown raised type** (in neither table) → records `eeUnknownExnType{sevWarning, msg: typeId}` on a new dedup'd `unknownExnWarnings` threadvar (reset at runSymex entry, drained into `RawResult.errors` on EVERY verdict branch since sevWarning never halts — Invariant 7) + matched ONLY by a bare `except:` (Invariant 3, no silent false-negative). `eeUnknownExnType` added to `SymexErrorKind`. **Dynamic `userExnHierarchy` capture DEFERRED to E4a** (per RFC §E4a — empty in E4); test 3 supplies the `{MyDefect: Defect}` chain E4a will capture, proving the dkOther-fallback `isDefect`/`isSubtypeOf` logic + the observable `sxRaised{raisedTypeId:"MyDefect"}` (public `SymexResult` has no `isDefect` field — only the inert `RawResult` one, E6). **E3's test 5 (2 cases) UPDATED** to post-E4 (CatchableError now CATCHES ValueError: handler body→sxSat, nothing escapes→sxUnsat), keeping the E3 file green under E4 (same pattern E2a/E2b used on E1). **No walker version bump** (stays "6"). Test `tsymex_phase15_E4_hierarchy.nim` (4 tests) green c+cpp 4/4; regression (E1_ir, E2a_cascade, E2b_raise, E3_try updated, phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **E4a SHIPPED — dynamic user-exception hierarchy (`getImpl` ancestor walk → `userExnHierarchy`; closes the user-subtype soundness gap).** E4 already shipped the CONSUMER (`isSubtypeOf`/`ancestorsOf` walk `userExnHierarchy` child→parent and splice into the static chain at the first known base) but left the table EMPTY; E4a is the parse-time PRODUCER. **dsl_parser.nim `collectUserExnAncestors(typeSym, ctx)`:** `typeSym.getImpl` → `nnkTypeDef[Sym, Empty, ObjectTy[…]]` (Ref/PtrTy-unwrapped); the inherit clause is an `nnkOfInherit[Sym Parent]` CHILD of the ObjectTy — **NOT at a fixed index** (confirmed Nim 2.2.10 shape is `ObjectTy[Empty, OfInherit[Sym Parent], Empty]`, OfInherit at child 1 after an Empty pragma slot; a bare Empty there = `of RootObj` = chain end), so the helper SCANS the children for the OfInherit. Records `child→parent` into `ctx.userExnHierarchy`, recurses to a static-`exnTypeTable` base or no-parent terminus (depth-cap 64 + already-recorded cycle guard). **RECONCILIATION beyond the RFC (the spec under-specifies):** the RFC says walk only `nnkExceptBranch` handler types — INSUFFICIENT for its OWN test 1 (raises `MyError`, catches `ValueError`; the `MyError→ValueError` link appears ONLY at the `raise newException(MyError,…)` site, never in an except branch). So E4a captures at BOTH the RAISE type node (`tn`, after Par/Ref/PtrTy unwrap) AND each `nnkExceptBranch` handler type node. (In the typed AST a stdlib handler type like `ValueError` arrives as `nnkType`, not `nnkSym`, so the helper no-ops on it — only user `nnkSym`/`nnkIdent` types are walked; stdlib types need no dynamic link.) **Threading:** `ParseCtx.userExnHierarchy: Table[string,string]` → `parseProc` emits it via new `emitStrStrTable` (block + per-entry assign, mirroring `emitProcs`) into `ParseResult.userExnHierarchyNimNode` → `symexFind`'s `quote do` adds `userExnHierarchy: <emitted>` to the `SymexProgram(…)` construction (new `SymexProgram.userExnHierarchy` field, types.nim) → `runSymexImpl`'s `WalkCtx` ctor passes `userExnHierarchy: prog.userExnHierarchy` into `WalkerStatics` (was the bare `WalkerStatics(exnTable: exnTypeTable)`). **dkOther now DYNAMIC** — E4's deferred user-Defect dkOther (E4 test 3 hand-supplied the `{MyDefect: Defect}` chain) is captured automatically: a user type whose chain reaches `Defect` makes `isDefect` true with no manual chain. Unknown types (neither table) still emit `eeUnknownExnType{sevWarning}` + bare-except-only match (Invariant 3). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E4a_userexn.nim` (2 tests: user subtype caught by base handler → sxSat; unrelated user type → sxRaised{SomeError}) green c+cpp 2/2; regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, phase1_arith, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **E5 SHIPPED — `finally` semantics (both exit paths; finally-raises-replaces).** Completes `walk(isTry)`: the `finally` runs on BOTH the normal AND the raised exit path (E3 stubbed it to the normal fall-through only) and composes per Nim's documented rules (normal+finally-normal=normal; raised+finally-normal=re-raise the original; any+finally-raise=finally wins). **Extends E3's depth-tagged channel mechanism with a new per-frame `pendingRaise` channel** (the raised-path analogue of `caught`): in `routeRaise`, AFTER the `except`-arm search fails but BEFORE the escape/boundary fall-through, the handler stack is scanned top-down for the deepest frame with a non-nil `finallyBlock`; if found, the raise is recorded on `pendingRaise` tagged at that depth and `routeRaise` returns `@[]` — so a `try: raise … finally: …` (no except) runs its finally before the raise propagates. **The `isTry` arm** claims `pendingRaise` at `myDepth` (alongside the E3 `caught` claim) and composes: (a) NORMAL exits → walk finally on the merged normal set; fall-through survives, a finally raise is routed by `routeRaise` (REPLACES — no in-flight exn, so it is a fresh raise); (b) RAISED exits → set `w.frame.inFlightExn` to the original exn for the finally's duration (so a bare re-raise inside finally sees it; restored after), walk finally on the raised path, the fall-through survivors RE-RAISE the ORIGINAL (re-routed outward via `routeRaise`) while any sub-path where the finally itself raised had that new exn already routed (REPLACES). The conditional `if x>100: raise IOError` finally splits naturally into x>100 (IOError replaces) / x<=100 (ValueError re-raised). **Path-explosion tamed:** finally runs ONCE per exit continuation (once on the merged normal set; once per raised continuation) — never combinatorially; the handler stack is popped to `myDepth` BEFORE the finally walk so a raise inside the finally routes to the NEXT-OUTER try and can NEVER re-enter this same finally (no loop). **Test 3 (ptr-deref heap-write visibility through finally) DEFERRED to Cluster R** — RFC §E5 test 3's `p[]=7`/`q[]=99` SUT is logical-heap (deref/assign) semantics; `path.heaps` exists (H1) but is INERT until Cluster R, so the engine cannot yet PRODUCE ptr-write witness values and faking the assertion would be unsound. Tests 1 & 2 (pure control flow) ship as the E5 deliverable; each exit continuation's path state (heaps/heapDepth/allocCounters, carried on `Path`) is threaded into the finally walk structurally and will exercise once Cluster R makes `path.heaps` live (test marked `skip()` with a `# deferred to Cluster R: ptr-deref heap writes` note — the S10b→E1 deferral pattern). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E5_finally.nim` (5 tests + 1 deferred skip) green c+cpp 5/5; regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, E4a_userexn, phase3_recursion, phase1_arith, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **E6 SHIPPED — `Defect` modeling (`sxRaised{isDefect: true}`; `defectExclusions`; OQ4 CLOSED).** **DefectKind/defectExclusions REALITY (reused, not re-added — Invariant 6):** both `DefectKind` enum + `SymexSettings.defectExclusions: set[DefectKind]` (default `{dkOutOfMemoryDefect, dkStackOverflowDefect}`) already shipped in **Z3a** and live in **`smt/types.nim`, NOT `engine/types.nim`** as RFC §E6 GREEN says (prompt hedge CONFIRMED); the enum also has an extra `dkRangeDefect` beyond the RFC list (Z3a, kept). `RawResult.sxRaised.isDefect: bool` already existed from **E2a** (init false). **E6 added:** `SymexFinding.defectTypeId: string` (engine/types) for display; `typeIdToDefectKind(typeId): DefectKind` (runtime — both `OutOfMemDefect`/`OutOfMemoryDefect`→`dkOutOfMemoryDefect`, user defects→`dkOther` excludable all-or-none); `InternalVerdict.ivRaised.raisedIsDefect` + `toPublic` copies it to `RawResult.isDefect`, populated at the routeRaise SUT boundary via the E4 `isDefect(...)` helper. **assert→implicit-AssertionDefect raise = PARSER (not walker):** a raw `assert cond, msg` lowers (after semcheck) to gensym scaffolding + `PragmaBlock[Pragma, IfStmt[ElifBranch[not (cond), Call failedAssertImpl]]]`; new `callsFailedAssertImpl`/`findAssertFailsCond` (dsl_parser) recognise this at the StmtList/PragmaBlock head and lower it to `mkIf(@[mkBranch(<not cond>, mkRaise("AssertionDefect", nil))])` — the assert-FAILS branch raises an implicit AssertionDefect. The `symexAssert(...)` MARKER (→`mkAssert`/`isAssert`) + its `tAssertionViolation` semantics are UNCHANGED (separate path). **defectExclusions filter (routeRaise boundary):** an EXCLUDED defect (DefectKind ∈ `settings.defectExclusions`) is SUPPRESSED (no finding, any target); a NON-excluded defect ALWAYS surfaces as `sxRaised{isDefect:true}` EVEN under a label/non-raise search (a reachable contract violation is never silently dropped); a non-defect `CatchableError` keeps E2b target-gating. **Auto-discovery:** new `irHasAssertDefect`/`scanAssertDefect` (scan.nim) detect the implicit AssertionDefect raise so `symexFindAllWitnesses` adds a `tRaisedExn("AssertionDefect")` target (+ `tRaisedExn` exclude arm); the sxRaised recording path sets `f.defectTypeId` when `raw.isDefect`. **Existing `tAssertionViolation` (phase1_assert via `symexAssert`), `stkIndexError` (phase4_tuple), `stkFieldDefect` (phase11_fielddefect) CONFIRMED UNCHANGED** (green c+cpp — produce sxSat under their own semantics; E6 only adds the sxRaised-Defect path). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E6_defect.nim` (4 tests) green c+cpp 4/4; registered after E5. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, E4a_userexn, E5_finally, phase1_assert, phase4_tuple, phase11_fielddefect, phase1_arith, phase7_assertcovered, phase11_walker, phase13_layer1_wire, F8_smoke) all green, no hangs. **E7 SHIPPED — Cluster-E regression smoke + walker version now `"7"`. **CLUSTER E COMPLETE through E7** (E8 remains, additive under "7"). Hermetic E-cluster smoke `tsymex_phase15_E7_smoke.nim` (12 tests) green c+cpp 12/12 — full exception machinery (E1–E6) composed in one file: inter-proc raise, finally re-raise/replaces, multi-frame bare re-raise (inner no-match pops to outer handler via `inFlightExn`), subtype + user-exn-by-base catch, assert→`sxRaised{AssertionDefect,isDefect}`, semantically-complete multi-finding `sxRaised` cache round-trip. NO state-threading regression found; no production change beyond the bump. Walker `"6"→"7"` single-sourced in `canonicalize.nim` (Invariant 6, no duplicate); F8_smoke/S7b_smoke/S11_mutation version pins advanced "6"→"7". Broad regression sweep (all E + S sample + F + H1 + earlier phases) green c, no hangs. **E8 SHIPPED — Cluster E COMPLETE.** `getCurrentException()`/`getCurrentExceptionMsg()`: two no-arg magic intrinsics recognised by callee symbol name in `parseExpr`'s `nnkCall` arm BEFORE the user-proc fall-through → new no-payload IR kinds `iekGetCurrentExn`/`iekGetCurrentExnMsg`. **inFlightExn-in-handler-body prerequisite was ALREADY satisfied** — E3/E5's `routeRaise` match arm sets `w.frame.inFlightExn = some(...)` around the WHOLE `walk(h.body)`, not just bare re-raise; no E3/E5 change needed, both stay green. `lower` (pure Env→SymVal, no WalkCtx) reads the in-flight type/msg via threadvars `currentInFlightTypeId`/`currentInFlightMsg` mirrored from `w.frame.inFlightExn` by `setInFlightThreadvars` at every set/restore site. Msg → `svString(inFlightMsg.get(""))` ("" for zero-arg `(ref T)()` raise). Exn → fresh `mkUninterpretedSort("Exn_"&typeId)` + `Z3_mk_const` erased to `Z3AnyAst`, wrapped in `svUninterpRef(uninterpAst, sortName:"Exn_"&typeId, typeTag:typeId)` (the real Z3b fields). Out-of-handler → `SymexNotInHandlerError` → boundary → sxUnknown + **`eeNotInHandler`** (NEW sevError kind). `extractFromSymVal(svUninterpRef)` emits **`eeUninterpRefExtraction`** (pre-existing sevHint). `discard getCurrent*()` now lowered (synthetic sink `let`). NO walker version bump — stays "7". Test `tsymex_phase15_E8_getcurrentexn.nim` (4) green c+cpp 4/4; 13-test regression green c, no hangs (E3/E5 green). **Next = G0-ADR (Cluster G — generics).****

## Status
- RFC v3 on disk: `RFC-phase15-language-fragments.md` (7,795 lines, 82 cycles, **9 clusters**: Z, L, F, S, H, E, G, C, R).
- Round 2 findings preserved at `RFC-phase15-language-fragments.round2-findings.md` (85 raw findings deduped to 67 across 4 lenses; reference for any future architect rounds).
- Round 2 bake-in complete. Zero open forks survived the fork-filter. Every fix is PhD-CS clear-best applied directly.
- ADR-0005 (float NaN/Inf) and ADR-0008 (generic instantiation) authored ahead of TDD by F/G bakers; the rest (0006/0007/0009/0010 + SYMEX_PLAN.md + witness-format-v3.md + closures.md) ship via their explicit doc-authoring cycles at TDD time.

## Major structural changes in v3
- **New Cluster H** (heap preparation) inserted between S and E. Single cycle H1 promotes the former R0 forward so E3/E5/E7 can reference `path.heaps` at compile time. Cluster R now starts at R1a.
- **Cluster Z expanded to 5 cycles**: Z0 (carryover) → Z1 (pin) → Z2 (smoke) → **Z3** (cross-cutting infra: `SymexErrorKind` enum, `severity` field, `DefectKind`, `InlinePolicy`, `svUninterpRef` in `SVKind`, `withSymexSettings`, `char` classification, sink/lent strip, `cacheKeyRaised` proc, SYMEX_PLAN.md authored) → **Z4** (`WalkCtx.found: Option → seq`, `EffectCtx → WalkerStatics + CallFrameCtx` split, ADR-0007 authored).
- **`EffectCtx` split into `WalkerStatics` + `CallFrameCtx`** with lifetime-tagged separation; closes round-1's god-object regression (Des-CRIT-D1). Invariant 7 codifies the split.
- **`WalkResult → InternalVerdict`** rename; `toPublic(iv): RawResult` is the sole conversion at the `runSymex` boundary. Invariant 9.
- **Walker/rendering version constants** single-sourced in `canonicalize.nim` (not `runtime.nim`/`walker.nim`); every closing cycle's GREEN file list corrected. Invariant 6.
- **Cycle splits applied**: R1 → R1a/R1; S6 → S6a/S6b; S10 → S10a/S10b (S10b deferred to post-E); F9 → F9a/F9b/F9c; new R8b for `var ref T`.
- **ADR-authoring cycles** named explicitly: F0-ADR, S0-ADR, G0-ADR, C0-ADR; E0-ADR folds into Z4; R0-ADR folds into H1.

## Key decisions locked in round 2
- **Lifetime-tagged walker state** — three buckets: `WalkerStatics` (per-walker immutable post-parse), `CallFrameCtx` (push/pop on call descent), `Path` (per-path; deep-copied at every fork). No mutable per-walker state outside `statics`/`frame`/`errors`/`found`.
- **Severity contract** — `sxUnknown` ⇒ ≥1 `sevError`; `sxSat`/`sxUnsat` with non-empty errors ⇒ all `sevHint`/`sevWarning` (annotations only).
- **`withSymexSettings`** composition primitive added; every cluster exercises it via a confirmation test.
- **Multi-`sxRaised` cache** — serializer writes every finding under its own `:raised:<typeId>` key; `loadAll(sutKeyPrefix)` reconstructs.
- **Closure axiom is multi-return-path** — body descent collects sub-paths; per-sub-path `implies(path.pc and pc_i, funcSym(env, args) == v_i)`; Z3 ITE-merge for main axiom.
- **`allocCounters` merge is `max`**, not replace (preserves freshness across nested calls).
- **`lambdaSite` keyed by `(symBodyHash(body), declOrderIndex)`** — formatting-stable, NOT `"file:line:col"`.
- **`distinct T` bijectivity axioms only for decidable base sorts** {int, BV, bool}; FP/String base ⇒ `geDistinctBijectivitySkipped` hint.
- **`split(s, "")`** special-case: skip `contains` constraint; assert one-codepoint-per-element.
- **`parseInt` raises-path** moves to S10b (post-E); S10a documents explicit unsoundness window.
- **`maxHeapDepth = 0`** falls back to `maxCallDepth` then hard cap 256.
- **Settings family**: `maxInstantiationsPerProc=64`, `maxClosureInlineCount=64`, `maxSplitParts=8`, `maxHeapDepth=8`, `maxFreshnessAssertions=256`, `maxBytesEncodingLen=32`, `inlinePolicy=ipHybrid`, `seqInlineThreshold=8`, `defectExclusions={dkOutOfMemoryDefect, dkStackOverflowDefect}`.

## Cluster summary (post-round-2)
| Cluster | Cycles | Walker bump | Notes |
|---------|--------|-------------|-------|
| Z | 5 (Z0..Z4) | — | Z3 + Z4 land all cross-cutting infra |
| L | 3 | — | Mostly verification; `char` + sink/lent regression here |
| F | 12 (F0-ADR, F1..F8, F9a/b/c) | 4→5 at F8 | F9 split; F6 FP-native predicates |
| S | 15 (S0-ADR, S1..S11 with S6a/b, S7a/b, S10a/b splits) | 5→6 at S11 | S10b deferred to post-E |
| H | 1 (H1) | — | Promoted R0 + ADR-0010 |
| E | 10 (E1..E8 with E2a/b, E4a splits) | 6→7 at E7 | ADR-0007 in Z4; E7 SUT rewritten |
| G | 11 (G0-ADR, G1a/b/c, G3..G8, G10) | 7→8 at G10 | G2/G9 folded |
| C | 8 (C0-ADR, C1, C2a/b, C3..C6) | 8→9 at C6 | `itLambda → iekLambda` |
| R | 16 (R1a/R1, R1b, R2..R8, R8b, R9..R11, R11b, R12, R13) | 9→10 at R12; rendering 2→3 | R0 removed; R8b new |
| **Total** | **82** | | (+12 vs round 1's 70) |

## Open forks for round 3
None. All round 2 findings closed with PhD-CS clear-best fixes. No genuine forks surfaced.

## ADR / doc files to author at TDD time
Already on disk (ahead of TDD by F/G bakers):
- `docs/symex/ADR-0005-float-nan-inf.md` ✓
- `docs/symex/ADR-0008-generic-instantiation.md` ✓

Authored at their TDD cycle:
- `docs/symex/ADR-0006-string-codepoint-indexing.md` (S0-ADR)
- `docs/symex/ADR-0007-exception-flow.md` (Z4)
- `docs/symex/ADR-0009-closure-encoding.md` + `docs/symex/closures.md` skeleton (C0-ADR)
- `docs/symex/ADR-0010-logical-heap.md` (H1)
- `docs/symex/SYMEX_PLAN.md` 78-row plan table (Z3)
- `docs/symex/witness-format-v3.md` (R11b DoD)

## Next steps
1. Safe `/compact` point — RFC v3 on disk, round-2 findings preserved, handoff current.
2. `/loop implement the next unimplemented RFC slice with /tdd, following the standing rules; stop when every slice is implemented`
3. Eventually: `/code-review` cycles.
