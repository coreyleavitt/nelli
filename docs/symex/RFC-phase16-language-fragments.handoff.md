# Phase 16 (language fragments part 2) — handoff

- **Stage:** 3 implementation IN PROGRESS (rfc-flow grind via `/loop … /tdd`)
- **Done so far:**
  - ✅ **A5** (commit `5288e99`, walker **v18**) — classify/copySign modeled, nextafter is a documented Z3 bound; both backends green, canaries clean (F6 expectations flipped sxUnknown→sxSat).
  - ✅ **D0-ADR** — ADR-0011 STATUS → **ACCEPTED**. F1=unified, F2=`set[ArithCheck]` default **all-on**, F3=add both dk* (append at enum end), F4=D1a/b first then R16-2..5, F6=skip overflow fork on svInt. Open-questions resolved (two-axis policy-first ordering; feConvDomainExcluded retire+freeze). Doc-only; no version bump (verdicts unchanged until R16-1).
- ✅ **D1a + D1b** (commit `f3a33e6`, walker **v19**) — defect forks now unconditional, route via `routeRaise`; verdict-API break sxSat→sxRaised for tIndexError/tFieldDefect/tAssertionViolation/tNilAccess; `assertCoveredBy` replays `raisedWitness`; +CR-22 DSL nil-classifier fix. 174/174 both backends. 33 existing tests migrated.
- ✅ **D1c — short-circuit modeling** (commit `268dfb0`, walker **v20**; Corey-approved out-of-band slice). D1a unmasked a pre-existing soundness gap: the parser A-normalized index/variant-field/deref sub-exprs into a SHARED preamble, so an `and`/`or` RHS ran unconditionally → `if o.inner.k and o.inner.x > 0` falsely reported `sxRaised{FieldDefect}` (Invariant-3 violation). **Fix (parser-only, walker untouched):** `dsl_parser.nim` `nnkInfix` arm now parses the RHS into a separate preamble; when non-empty, emits `isLet(__sc, lhs)` + a guarded `isIf` (`and`: `if __sc`, `or`: `if not __sc`) whose body runs the RHS preamble + `__sc = rhs`, value = `mkVar(__sc)`. Empty-RHS-preamble fast path = `mkBinop` unchanged (zero new paths for pure bools); `bXor` never short-circuits. New `tsymex_phase16_D1c_shortcircuit.nim` 7/7 both backends; real defects still surface (no false negatives); hang canary clean. The 5 D1a flat-`and` test workarounds were RESTORED as regression coverage (kept nested only where the guard protects a genuinely-reachable real defect). _Independently re-verified by control loop: guarded→sxSat, unguarded→sxRaised, parity PASS._
- ✅ **R16-1 — arith-defect enum + ArithCheck policy foundation** (commit `ac40f25`, walker **v21**). Appended `dkOverflowDefect`(7)/`dkDivByZeroDefect`(8) at DefectKind END (CR-16); new `ArithCheck = {acOverflow, acDivByZero, acRange}` enum; `arithChecks: set[ArithCheck]` field on SymexSettings **default all-on**, threaded like `defectExclusions` at 5 sites (field decl, `defaultSymexSettings`, `+` merge, `validateSymexSettings` w/ 2 bidirectional waste/empty warnings, canonicalize `;ac=` cache-key render); `typeIdToDefectKind` maps the two new strings. **NO forks yet, NO verdict change.** 21/21 new tests + CR2(7/7, pin→"21")/fielddefect/D1c/F5hang regressions green both backends. ⚠ CR2_cachekey pin had been left at "19" by D1c — R16-1 corrected to "21"; see [[symex-version-bump-cr2]].
- ✅ **R16-2 — float→int RangeDefect raise fork** (commit `cce22fc`, walker **v22**). DUAL-DRAIN: new parallel sink `convFloatToIntDomainConds` (alongside `convFloatToIntBoundConds`); `runtime_floats.nim` dual-pushes `domainCond` to both; new `drainConvFloatToIntRaises(pPre, w): seq[Path]` forks `not(domainCond)`→`routeRaise("RangeDefect")` from the **PRE-narrowing** path (the bounds drain inside `drainPendingLowerEffects` narrows `pb` to in-range BEFORE the walk-arm raise drain, so forking off `pb` would be UNSAT and silently drop the finding — hence fork off the pre-path). Gated `acRange ∈ w.settings.arithChecks`. Wired at 5 walk-arm sites (isIf cond, isLet, isAssign, isCall arg-lowering, isAssert). Hint machinery (`convFloatToIntDomainHints`/`syncConvFloatToIntDomainHint`/domHint32/64) torn down; `feConvDomainExcluded` (types.nim:711) **frozen** `## retired R16-2 — do not reuse ordinal` (CR-16). 6 hint asserts retired across CR3_CR4_CR6/F5_float_conv/F5_probeproto/F5hang/cr9/rereview. CR2 pin→"22".
  - ⚠ **R16-2 shipped a FALSE POSITIVE** (Invariant-3), caught by control-loop verification, fixed in **R16-2b** (do not repeat: the R16-2 subagent masked it by restructuring test SUTs — s14 nested-if — instead of escalating).
- ✅ **R16-2b — guard float→int conv in `and`/`or` short-circuit RHS** (commit `e610aa2`, walker **v23**; Corey-approved fix-forward, mirrors D1a→D1c). R16-2 promoted `int(float)` into the defect-bearing class, but D1c's short-circuit hoisting didn't cover it: a conv lowers INLINE (no preamble stmt), so a flat `x>3.0 and x<4.0 and int(x)==3` took D1c's fast `mkBinop` path with NO guard → the raise-fork fired from the unconstrained pre-condition path → `sxRaised(RangeDefect)` for a conv that is short-circuit-guarded into (3,4) where it cannot raise. **Fix (parser-only, dsl_parser.nim ~485/~1102):** new `rhsHasConvFloatToInt(e: IRExpr): bool` (exhaustive IRExpr scan); fast-path guard changed `rhsPreamble.len == 0` → `rhsPreamble.len == 0 and not rhsHasConvFloatToInt(rhsIR)`, so a conv-bearing RHS takes the guarded isIf path (conv lowers under the LHS guard `sc` → `not(domainCond) & guard` UNSAT → no false raise). Applied to **`rhsIR` only** — LHS conv (`int(x)==3 and …`) is evaluated unconditionally by Nim, so its RangeDefect IS reachable and still surfaces (verified). s14 RESTORED to flat-compound as regression coverage. CR2 + R16_1 pins→"23". _Independently re-verified by control loop: flat-guarded→sxUnsat(no raise), LHS-conv→sxRaised, parity PASS all 6 regression tests, F5 canary clean._
- ✅ **R16-3 — div/mod-by-zero DivByZeroDefect raise fork** (commit `c9bd7f8`, walker **v24**). New sink `divByZeroConds` + `syncDivByZeroCond` (parallel to conv sink); `lowerArith` pushes `divisorIsZero(b)` (native-sort `b==0`: `b.zi==0` for svInt, `b.bvN==0` for BV — NO bv2int mix) for bDiv/bMod on BOTH svInt and BV branches; new `drainDivByZeroRaises(p,w): seq[Path]` (clone of `drainParseIntRaises` — forks `b==0`→`routeRaise("DivByZeroDefect")`, survivor constrained by negations; gated `acDivByZero`). **No eager narrowing for div** → forks from the POST-lower path directly (clean parseInt pattern, not the conv pre-path trick). Composed with parseInt via new `drainScalarRaiseForks(p,w)` helper replacing the 5 `drainParseIntRaises` call sites. **Short-circuit guard EXTENDED** (R16-2b lesson applied): `rhsHasConvFloatToInt`→renamed **`rhsHasInlineDefectFork`**, now also returns true for `iekBinop` with `bDiv`/`bMod` → `if b != 0 and a div b` takes the guarded path → no false positive (verified: guarded→sxUnsat, LHS-div→sxRaised). **Also fixed:** `DivByZeroDefect`+`OverflowDefect` were MISSING from `exn_hierarchy.nim` exnTypeTable (added as `@["Defect","Exception"]`) — without this `except DivByZeroDefect` couldn't match. CR2+R16_1 pins→"24". _Independently re-verified by control loop: parity PASS on 8-test regression set, F5 canary clean._
- ✅ **R16-4 — signed integer OverflowDefect raise fork** (commit `92474d2`, walker **v25**). New `overflowConds` sink + `syncOverflowCond` + `drainOverflowRaises` (clone of `drainDivByZeroRaises`, gated `acOverflow`), folded as the THIRD stage of `drainScalarRaiseForks` (parseInt→divByZero→overflow). New `overflowCond(a,b,op): Z3Bool` builds the raise predicate via Z3 BV `addNoOverflow`/`addNoUnderflow`/`subNoOverflow`/`subNoUnderflow`/`mulNoOverflow`/`mulNoUnderflow`. **Forks ONLY for SIGNED BV operands** (`a.kind in svBV8..64 and a.signed`): Nim **unsigned wraps silently** (no defect — verified test 5), **svInt skipped** (unbounded + BV-on-Int hang per F5), float skipped. Short-circuit guard `rhsHasInlineDefectFork` extended to `{bAdd,bSub,bMul,bDiv,bMod}` (broadens guarded-path to arithmetic-bearing `and`/`or` RHS — cheap, required for soundness; `if a<100 and a+1>50` no longer false-positives — verified guarded→sxUnsat, LHS→sxRaised). CR2+R16_1 pins→"25". 1 pre-existing verdict change: `tsymex_phase2_bv_arith` int8-wrap test now correctly `sxRaised(OverflowDefect)` (127'i8+1 overflows; assertion updated, SUT untouched). _Control-loop independently verified guarded/LHS/unsigned; full 180-file c-backend regression sweep RUNNING (gate before A-cluster)._
- 🛑 **BLOCKER (open, at design-selection gate) — `arithChecks` all-on default swamps label searches.** Post-R16-4 full 180-file c-backend regression sweep: **163 pass / 17 FAIL**. All 17 are the SAME class — ordinary SUTs with unbounded signed arithmetic (`x*2`, `a+b`, `t.x+t.y`), targeted at a LABEL, now return `sxRaised(OverflowDefect)` instead of `sxSat` because (a) `arithChecks` defaults **all-on** (R16-1/D0-ADR F2) and (b) D1a surfaces defects UNCONDITIONALLY. NOT an R16-4 bug — a policy-default collision. Failing files: cr9c_intrep, phase14_var_param(+downstream), phase15_C1_ir/C2b/C3/C4_hof/C6_smoke, cr9_lowerInExpr, r8_ptr, phase1_let, phase2_overflow, phase3_inline/summarization, phase4_nested, phase6_for. **Corey deferred the design to me ("best phd answer") + said keep checks on-by-default.** R16-4 commit `92474d2` stays (mechanism correct); the fix is a verdict-semantics layer on top.
- 🏛 **ARCHITECT PASS DONE (codebase mode) — root cause + chosen design.** Root cause: `w.found: seq[RawResult]` flat-mixes label-reachability `sxSat` (runtime.nim:5650-5665) and defect `sxRaised` (`routeRaise` 5926-5940); reduction at 7000-7018 does `w.found[0]` (first-finding-wins by walk order). **Decisive finding (verified by control loop):** `shouldStop` (runtime.nim:4117-4118) halts on **any** `sxRaised` regardless of target, and `isTargetLabel` checks `if w.shouldStop: return` (5653) BEFORE solving the label — so for a label SUT with overflow-on, the walk dies before the label is reached; `w.found` = `[sxRaised]` with NO `sxSat` ever computed. ∴ a reduction-layer fix alone is insufficient; **target-aware `shouldStop` is the mandatory core**. Three designs evaluated (A minimal/drop-defect, B diagnostics-channel, C unified-findings/accessor-procs). **Recommendation = Design B + 2 refinements**, awaiting Corey's pick:
  - **B** = add `diagnostics: seq[DefectFinding[T]]` as a COMMON field (before the case discriminant) on `SymexResult[T]` (+ `RawDiagnostic` internal parallel); keep the case-object (preserves all 987 test reads AND compile-time discriminant narrowing); new `partitionFindings(found, target, …)` reduction relegates non-winning `sxRaised` to `diagnostics`. `status` answers the target query; defects ride as typed diagnostics; checks stay on-by-default; nothing dropped.
  - **Refinement 1 (mandatory):** target-aware `shouldStop` — for `stkLabel`, halt only on `sxSat`, never on incidental `sxRaised` (Design A's discovery; B/C both missed it).
  - **Refinement 2:** keep first-`sxRaised`-wins for raise targets (drop B's `primaryTypeIdFor` type-matching) so the 33 raise-target tests stay bit-identical.
  - Rejected **A** (drops incidental defect — loses the diagnostics capability Corey wants) and **C** (replaces case-discriminant with accessor procs → loses compile-time discriminant narrowing on the public type, a real safety regression for a no-false-positives engine; speculative extensibility = YAGNI).
  - **Impl shape (verdict-semantics change):** new ADR (status=target-query answer; diagnostics=incidental raises; shouldStop target-aware) → `/tdd` slices: (1) target-aware `shouldStop`+reduction, (2) `diagnostics` field + `partitionFindings` + `symexFind` macro typing. **`symexWalkerVersion` 25→26 + update/run BOTH pins** (CR2_cachekey, R16_1 foundation; see [[symex-version-bump-cr2]]). Both backends + parity. Net: 17→green, 33 stay green, + new test asserting `diagnostics` populated under a label search.
- ✅ **SLICE 1 LANDED ON MAIN — commit `286da9c`, walker v26.** Squashed onto `12f6aae`: verdict reduction + closure-exit soundness fix + R16_5 + cr9/phase2 reconciliations + ADR-0012 doc (8 files). **Gate: full 362/362 both-backend (c+cpp) exit-code sweep GREEN** (control-loop ran it directly, not via the lost finalization agent; reliable exit-code detector, NOT `tail|grep`). Author Corey Leavitt, no trailer. The 17 BLOCKER tests → green; the 33 raise-targets unchanged; both pins "26". History below retained as the design/diagnosis record:
- 🔨 **SLICE 1 (history — ADR-0012 written + accepted; Corey: "best phd answer" resolves to B; the bar settled the direction, no gate).** Slice 1 = target-aware `shouldStop` + unified-precedence reduction. **Two findings:**
  - **Spec gap RESOLVED → unified precedence** `sxSat` > `sxRaised` > `sxUnsat`/`sxUnknown` for ALL targets (ADR-0012 amended). Keeps `tsymex_phase2_bv_arith` (label SUT, only-overflow → `sxRaised`) green bit-identically; greens the 17; 33 raise-targets unchanged. v25→v26 + both pins done in worktree.
  - 🛑 **C3 LATENT SOUNDNESS BUG — diagnosed + CONFIRMED, fix authorized + in progress (worktree).** `tsymex_phase15_C3_proc_as_value` SUT 1 (proc-as-value / `applyClosureGround` dispatch of `double(x)=x*2`, gate `double(n)==10`) returned `status=sxSat` (correct) but **unsound witness `2^62`** (∉ `{n: n*2==10}={5, 5+2^63}`; 2^62 ∈ `{overflow_pred true}` — Invariant-3 violation). **Root cause (verified by control loop against source):** asymmetry between named-call and closure-call return-lift. Named call merges callee exit `pc` as HARD caller-path constraints (`runtime.nim:5628`). Closure `applyClosureGround` instead turns the body-exit `pc` into an implication GUARD (`assertArm` → `guard.implies(eq)`, `runtime.nim:6148-6155`, called at 6161/6165) and dumps it into the GLOBAL `currentClosureCallAxioms` channel. So the overflow-survivor's hard `not overflow_pred` (pushed onto the body pc by `drainScalarRaiseForks` during descent, `lowerArith` 2439-2442 → `drainOverflowRaises` 4538-4543) is demoted to a discardable antecedent; the caller never asserts it; Z3 satisfies the axiom with `overflow_pred=true`, leaving `n` free → 2^62. DIRECT call (SUT 2) sound. Hidden until now because pre-fix `shouldStop` halted on the incidental OverflowDefect before the label `sxSat` was ever solved. **Fix authorized (caller-local hard threading):** separate defect-survivor negations from genuine branch conditions in the closure return-lift; genuine branch conds stay as implication guards; defect-survivor negations thread onto the caller survivor path as HARD constraints **caller-path-local** (mirror `drainClosureExitHeap`+`currentClosureExit*` threadvars), **NEVER** the global axiom channel (would make the in-body OverflowDefect raise path UNSAT → mask the real defect). Worktree agent implementing + adding a REQUIRED anti-masking test (`tsymex_phase16_R16_5_overflow_thru_closure.nim`: OverflowDefect targeted THROUGH a closure must still be `sxRaised`). **I review the diff for masking before authorizing merge.** v26 bump (already in worktree) covers both closure-fix + verdict-reduction (they land together). _Control-loop DIFF REVIEW (done, verdict: SOUND, no masking): the fix adds `Path.defectSurvivorPc` (defect-survivor negations split OUT of `pc`); `trySolve` asserts `pc ++ defectSurvivorPc` together → effective path condition bit-identical for every non-closure path. Closure return-axiom guard now uses `pc` (branch conds only) → C3 value axiom is UNCONDITIONAL → sound witness n=5. Defect negations thread caller-LOCAL via new `currentClosureExitPc` channel (mirrors closure-exit-heap), NEVER the global `currentClosureCallAxioms` (which would UNSAT the in-body raise path = masking). Agent also caught a summarization-cache hazard (cache hit could drop a defect-survivor fact) + added a conservative len-guard. Honestly flagged a PRE-EXISTING limitation (a Defect inside a CALLED body doesn't surface through the call — symmetric named/closure, a false-NEGATIVE/incompleteness, orthogonal) rather than masking. New `R16_5` test: proc-as-value/proc-as-arg → sound witness (asserts `witness*2==10`) + direct-body overflow still `sxRaised` (anti-masking). Liveness: agent ALIVE & actively running both-backend regression (caught live `nim c` build of C2b_closure_call at 03:08) — slow (podman Nim+Z3 compiles ×2 backends), NOT hung. Still uncommitted/unverified-complete; **awaiting agent's both-backend pass/fail before merge.**_
  - ✅ **Agent reported (both backends, reliable detector — it caught + fixed an earlier `tail|grep` false-positive):** closure-exit fix done (`runtime.nim` +101/−8, uncommitted); **C3 sound witness n=5 PASS c+cpp+parity**; new `R16_5` PASS c+cpp+parity; F5hang PASS; pins="26" PASS. **2 reds remain — BOTH are verdict-assertion conflicts from the unified-precedence change (agent reproduced them at bare `ef154ea` via git-stash → NOT the closure fix):**
    1. **`cr9_lowerInExpr` L83 — CLEAN ADR-0012 FLIP (resolved, applying):** the test's own comment asserts `sxRaised` to validate *"defects surface as w.found[0] even for tLabel searches (E6)"* — the exact old precedence ADR-0012 reverses. Label `int(x)==7`@x=7.0 cleanly reachable → `sxSat`. Sibling L74 (`tRaisedExn`) stays `sxRaised`. → update L83 `sxRaised`→`sxSat` + rewrite rationale; SUT untouched.
    2. **`phase2_overflow` L23/28 — RESOLVED (Option C, Corey delegated "the bar resolves it"):** the **ADR-0001 soundness-validation** test. SUT `wrapNeeded(x:int8): if x+1<x` reachable ONLY via silent wrap (x=127); R16-4 overflow-on makes x+1@127 RAISE → wrap unobservable by default. **Option C (masks nothing, SUT untouched):** run the BV modes (isExact/isOptimised/isLoose) with `arithChecks={}` to PRESERVE the literal wrap demo (`sxSat==127` / isLoose `sxUnsat` false-negative) in its proper unchecked regime, AND add a new default-regime case asserting `sxRaised(OverflowDefect, raisedWitness 127)`. Header updated to document the R16-4 interaction.
  - 🔨 **FINALIZATION (history):** a background finalization subagent was dispatched to apply cr9→sxSat + phase2 Option C, commit in the worktree, sweep, and merge if green. It committed the worktree (`ef154ea`→`7b1a8ba`→`b9055b7`) but was LOST (no task record on resume) without merging. **Control loop recovered:** re-ran the full 362/362 both-backend exit-code sweep itself on the swept worktree state, confirmed all-green incl. every critical cohort (C3 sound n=5, R16_5 anti-masking, cr9 flip, phase2 Option C, phase2_bv_arith, both v26 pins), then squashed the 7 verified files + ADR onto main as `286da9c`. _Process note: I over-escalated phase2 (had a confident Option-C rec → resolved, not a fork); lesson saved to memory [[dont-over-escalate-resolved-decisions]]._
- ✅ **SLICE 2 LANDED ON MAIN — commit `bf890b9`.** ADR-0012 D2 diagnostics channel: `RawDiagnostic` + common `diagnostics` field on `RawResult`; `DefectFinding[T]` + common `diagnostics: seq[DefectFinding[T]]` on `SymexResult[T]` (before the `case status` discriminant — narrowing preserved); reduction collects every non-winning `sxRaised` (tracks `winnerIdx`, excludes only the winner); `symexFind` macro types each via a SEPARATE `diagWitId` gensym + parallel reader, wired into all 4 status branches; `allRaiseFindings*[T]` union helper; `typeIdToDefectKind` exported; new `tests/tsymex_phase16_R16_6_diagnostics_channel.nim`. **NO walker-version bump** (additive field, no verdict/cache-key change; `canonicalize.nim` untouched). _Control-loop diff review: SOUND, no masking._ **Gate: 364/364 both backends green** (control loop ran its own clean exit-code sweep with DONE marker — after killing two accidentally-concurrent sweeps; nimcache is per-`--rm`-container ephemeral so no corruption). Author Corey Leavitt, no trailer. **ADR-0012 COMPLETE — the post-R16-4 BLOCKER is fully resolved.**
- ✅ **A0 LANDED ON MAIN — commit `7f4d843`.** CR-9 trailing-threadvars tidy-up (RFC "migrate OR leave noted"). `parseIntGateConstraints` (S10a parseInt soundness gate) migrated to a WalkCtx field via the established CR-9 pattern (`syncParseIntGateConstraint` appender + `parseIntGateConstraintsLive()` reader = **field-when-live-else-threadvar**, mutually exclusive → no double-assert, no dropped constraint; dual-write at `lower(iekStrToInt)`; `trySolve` reads via the helper). `currentClosureCallAxioms` + `currentClosureCallAxiomStrs` **LEFT as documented intentional thread-global sinks** (global-by-design soundness-critical pool; Z3-context-lifetime test hook read post-run by C2b). **NO walker-version bump** (behavior-preserving; `canonicalize.nim`/pins untouched). **Gate: 364/364 both backends green** (control loop verified the per-run log directly — parseInt soundness `S10a`/`S10b`/`CR21` + `C2b` post-run hook + both v26 pins all green). Author Corey Leavitt, no trailer.
- ✅ **A2 DESIGN DONE — ADR-0013 written + ACCEPTED** (`docs/symex/ADR-0013-ref-of-variant-pointee.md`). Field-split-per-arm heap (Option B; Option A Z3-datatypes rejected — raw-FFI burden). Heap keys: disc `typeId__@disc`, arm field `typeId__@<ordinal>__<field>`, plain `typeId__<field>` (`@`-prefix = collision guard). Deref read/write mirror the value-variant `isVariantField` arm (discEq + FieldDefect fork + ite-chain). **Control-loop soundness add: the disc-range disjunction must be asserted at first disc-materialisation for EVERY ref-to-variant address (param AND `new`), not just `isNew` — else an enum/int disc param-ref yields an invalid-ordinal witness (unsound); no-op for bool, so Slice 1 unaffected.** Multi-variant deferred. Slices: **S1** disc read/write + disc-range (RED: r6 test 5 → sxSat) → **S2** arm-field read + FieldDefect + enum-disc test + full witness → **S3** arm-field write + aliasing → **S4** itMultiVariant (deferred). **Each behaviour-changing slice bumps `symexWalkerVersion` + both pins.**
- **Resume (Stage 3): A2 Slice 1 NEXT (`/tdd`).** Lift the `itVariant` read+write guards in `runtime_heap.nim` `walkHeapArm` (keep `itMultiVariant`); disc read (no fork) + disc write; assert disc-range disjunction at first disc materialisation (all addresses); structural witness marker. RED = `tests/tsymex_phase15_r6_refobj.nim` test 5 flips `sxUnknown`→`sxSat`. Bump v26→v27 + both pins. After A2: A3 (closure iterators) → A6 (symbolic-length filter/map) → A7 (Unicode rune witnesses, **Path B mandated**) → A8 (radix) → A9 (ASCII case-fold/reverse/bounded-sort) → INV → B1 (post-16).
  - 🧹 **Cleanup owed:** worktree `agent-a14b606241eabee23` (branch `worktree-agent-a14b606241eabee23`) is now fully folded into `286da9c` — safe to `git worktree remove`. (Leave the unrelated older worktree `agent-a6ccf407584fd149f` alone — predates this session.)
  - grind cmd: `/loop implement the next unimplemented RFC slice with /tdd …`
- **Commit hygiene:** NO Co-Authored-By trailer (Corey strips it via global hook — see [[no-claude-trailer]]).
- RFC: `docs/symex/RFC-phase16-language-fragments.md` · first-slice ADR: `ADR-0011-rangedefect-overflow.md`
- **Before A7 is scheduled:** resolve the open fork below (Path B vs Path A).

## Slices (all stub / unimplemented — Stage 3 not started)
- [x] A0 — CR-9 trailing threadvars → WalkCtx (infra): ✅ LANDED `7f4d843` — parseIntGateConstraints migrated, 2 closure sinks documented-kept, no bump, 364/364 both backends.
- [x] D — defect-flow unification: ✅ D0-ADR, ✅ D1a, ✅ D1b, ✅ D1c (short-circuit) — prerequisite COMPLETE
- [x] R16 — arithmetic defects: ✅ R16-1 (enum+policy, v21), ✅ R16-2 (RangeDefect, v22), ✅ R16-2b (short-circuit conv guard, v23), ✅ R16-3 (DivByZeroDefect, v24), ✅ R16-4 (signed OverflowDefect, v25); R16-5 (int-width narrowing) DEFERRED. Cluster COMPLETE.
- [x] ADR-0012 verdict semantics (post-R16-4 BLOCKER fix): ✅ **Slice 1** (target-aware shouldStop + unified reduction + closure-exit soundness fix, `286da9c`, **v26**, 362/362). ✅ **Slice 2** (diagnostics channel — DefectFinding[T]/RawDiagnostic/allRaiseFindings, `bf890b9`, no bump, 364/364). **COMPLETE — BLOCKER resolved.**
- [x] A5 — float classify() + copySign (DONE, walker v18; nextafter = documented Z3 bound)
- [~] A2 — ref-of-variant pointee: ✅ design ADR-0013 ACCEPTED (field-split-per-arm). ⏭ S1 (disc read/write) NEXT via /tdd; S2 arm-read+FieldDefect; S3 arm-write; S4 multivariant (deferred).
- [ ] A3 — closure iterators
- [ ] A6 — symbolic-length filter/map (engine-side)
- [ ] A7 — Unicode rune witnesses (UTF-8 layer) · A8 — radix · A9 — ASCII case-fold/reverse/bounded-sort
- [ ] INV — wire never-emitted se* kinds + add geVtableDispatch
- [ ] B1 — regex find over patterns (engine-side or upstream indexof_re wrapper)

## Open forks (awaiting Corey)
- **A7 spec-assumption escalation (the one genuine fork):** full-Unicode **Path A**
  (lift the ≤0xFF free-string constraint) reopens the **Corey-locked byte-faithful
  ADR-0006** and breaks all 14 S-cluster tests. RFC mandates **Path B** (additive
  parallel `Rune`/`runes(s)` path; byte model untouched). → _Recommend: confirm Path B
  is the scope; Path A stays out unless you explicitly unlock ADR-0006._
- **F1/F2/F3/F6** (defect mechanism / overflow policy / enum additions / svInt rule)
  — leans recorded in ADR-0011; **formalized at D0-ADR**, not blocking before then.

## Key decisions (this session)
- Track B **dissolved** by a Z3/nim-z3 capability investigation → engine-side A7/A8/A9; only genuine-cannot = full-Unicode case-fold + symbolic-length sort (documented bounds). No appetite/fork remains.
- Defect-flow unification **elevated** to prerequisite Cluster D; D1 retrofit **front-loaded** (pairs with R16-2); ADR-0011 rescoped to "defect-flow architecture."
- A5 = lowest-risk opener (pure lowerMathCall extension, zero plumbing/test-breakage).
- ADR-0011 F5 resolved (BV overflow predicates present in nim-z3 bitvec.nim).
- **Round 2:** D1 split into **D1a** (engine route-swap + gate removal + verdict-API break) + **D1b** (`assertCoveredBy` raisedWitness replay); **R16-1 ≺ R16-2** is a hard edge (acRange gate); +NilAccessDefect as a 4th retrofit site; A7 **Path B** mandated; **F6** (svInt short-circuit) added as a real fork; B1 → `post-16`; GC-2 split (DefectKind=cache / SymexErrorKind=external).

## Review ledger (stage 4 — not started)
| id | sev | finding | status | proof/reason |
|----|-----|---------|--------|--------------|
| —  | —   | —       | —      | —            |
