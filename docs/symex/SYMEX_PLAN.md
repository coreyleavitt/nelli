# SYMEX_PLAN — Phase 15 (language fragments) cycle tracker

> Authored in cycle **Z3f** (RFC-phase15-language-fragments.md; reconciled per
> RFC-phase15-reconciliation.md). One row per implementation cycle across the 9
> clusters (Z, L, F, S, H, E, G, C, R), ordered cheapest-infra → deepest.
>
> **Status** is one of `SHIPPED` (committed + green on `nim c`/`nim cpp`),
> `pending`, or `folded` (absorbed into another cycle). `commit` is the short SHA.
>
> **Reconciliation note:** the RFC's single `Z3` cycle was sub-sliced into
> `Z3a`–`Z3f` (8 mutually-referential changes split for TDD testability); those
> rows replace the monolithic `Z3` row. All cycle homes/paths are corrected per
> the reconciliation doc — consult it before implementing any row.

## Cluster Z — nim-z3 v2.0.0 pin + carryover + cross-cutting infra

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| Z | Z0  | Phase 14 carryover close-out (named-field `map`, constraintDigest) | SHIPPED | 9dd4181 |
| Z | Z1  | nim-z3 v2.0.0 pin bump + grep verification + canary | SHIPPED | 9e7deea |
| Z | Z2  | regression smoke under v2.0.0 (0 drift) | SHIPPED | 9e7deea |
| Z | Z3a | error/severity/settings enum scaffolding | SHIPPED | f195a1f |
| Z | Z3b | `svUninterpRef` + `itUninterp`/`tUninterp` | SHIPPED | b523345 |
| Z | Z3c | `classifyType` char branch + sink/lent strip | SHIPPED | 308a8e7 |
| Z | Z3d | `withSymexSettings` builder + `+` merge | SHIPPED | a473666 |
| Z | Z3e | `cacheKeyRaised` + cache-suffix standardization | SHIPPED | fcecd96 |
| Z | Z3f | author this plan doc | SHIPPED | (this cycle) |
| Z | Z4  | `WalkCtx.found` Option→seq + WalkerStatics/CallFrameCtx records + ADR-0007 | SHIPPED | (this cycle) |

## Cluster L — templates and macros

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| L | L1 | boundary audit — template/macro/`{.dirty.}` SUTs symex soundly | SHIPPED | (this cycle) |
| L | L2 | `untyped` template params — faithfully walked (constraint honored) | SHIPPED | (this cycle) |
| L | L3 | `getAst`/`quote do` — round-trip identity; generic monomorphization; Z smoke | SHIPPED | (this cycle) |

## Cluster F — float

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| F | F0-ADR | author ADR-0005-float-nan-inf.md | SHIPPED | (on disk) |
| F | F1 | type-bridge: float/float32/float64 IR kinds, svFloat32/64 | SHIPPED | (this cycle) |
| F | F2 | float literal lifts (Inf, NaN, -0.0, finite) + IEEE ==/!= | SHIPPED | (this cycle) |
| F | F3 | arithmetic ops (+ - * / unary-) | SHIPPED | (this cycle) |
| F | F4 | ordering compare (< <= > >=) | SHIPPED | (this cycle) |
| F | F5 | int↔float conversions with range overflow | SHIPPED | (this cycle) |
| F | F6 | math-module ops + FP predicates | SHIPPED | (this cycle) |
| F | F7 | eval-side bit-exact float witness round-trip | SHIPPED | (this cycle) |
| F | F8 | regression smoke; walker `"4"→"5"`; withSymexSettings wiring | SHIPPED | (this cycle) |
| F | F9a | array[N, float32/64] element type-bridge audit | SHIPPED | (this cycle) |
| F | F9b | seq[float32/64] param: allocate → extract → emitTyAndReader | SHIPPED | (this cycle) |
| F | F9c | object-variant arm fields of float32/64 | SHIPPED | (this cycle) |

## Cluster S — full strings

> **Model: byte-faithful per ADR-0006 (Corey-locked).** Free `string` vars
> constrain every char ≤ 0xFF, so Z3 position == Nim byte index. `s.len`/`s[i]`/
> `s[a..b]`/`s.high`/`for c in s` are all byte-faithful supported; only `s[i]=c`/
> `s.add` (immutability) and `toLower`/`toUpper` (no Z3 case-fold) stay unsupported.

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| S | S0-ADR | author ADR-0006 — **byte-faithful** string model (Corey-locked) | SHIPPED | (this cycle) |
| S | S1 | type-bridge: string → full Z3 String walker (chars ≤0xFF); iekStr* stubs | SHIPPED | (this cycle) |
| S | S2 | string literal lifts; byte-faithful (`"é".len == 2`) | SHIPPED | (this cycle) |
| S | S3 | s.len, s[i], s[a..b], s.high byte-faithful supported (+≤0xFF char constraint); s[i] char-bridged; for-c-in-s honestly classified unsupported (unbounded iteration) | SHIPPED | (this cycle) |
| S | S4 | find, contains, startsWith, endsWith (real nim-z3 contains/startsWith/endsWith/indexOf; `sub in s`→iekStrContains) | SHIPPED | (this cycle) |
| S | S5 | replace, replaceAll, split, join; maxSplitParts (replace first-occ; replaceAll version-gated→seZ3VersionMissing; split special-cases concrete/empty-sep, general→seZ3StringIncomplete; join over concrete seq[string]) | SHIPPED | (this cycle) |
| S | S6a | regex_parser.nim standalone | SHIPPED | (this cycle) |
| S | S6b | walker integration: iekStrMatch/iekStrFindRe | SHIPPED | match SAT/UNSAT; findRe deferred (no Z3 indexOf/regex); replaceRe gated |
| S | S7a | bytes(s) trivial byte-view (identity over ≤0xFF chars) | SHIPPED | byte-faithful identity svSeq[BV8]; seqLen EQUAL len(s); at→toCode→BV8 bridge; concrete-literal length only (symbolic → seBytesSymbolicLength); >maxBytesEncodingLen(32) → seBytesLengthTooLarge; seBytesBeyondBMP unreachable |
| S | S7b | Z3-string-theory regression smoke (L+F+S1–S7a) | SHIPPED | hermetic cross-op smoke `tsymex_phase15_S7b_smoke.nim` (8 tests, c+cpp): multi-op SUTs (len+index+contains+startsWith / len+slice+endsWith+find), concrete split+join+bytes in one SUT, regex match + string-equality, withSymexSettings on a string SUT, ≤0xFF free-`s.len==1` single-byte round-trip, walker version still "5". Broad regression subset all green, no hangs. determinism.md gains a STRING section. Finding: bool-returning string *helper* procs don't inline (sxUnknown) — conditions inlined per S-cluster convention |
| S | S8 | `&` string concatenation → Z3_mk_seq_concat | SHIPPED | `iekStrConcat` (StrOpKinds stub since S1) wired: parser intercepts `&` in the `nnkInfix` arm BEFORE `binopForInfix` when BOTH operands `classifyType` as `itString` (`s&t`, `s&"lit"`, `"lit"&s`); chained `a&b&c` is left-assoc nested binary nodes so operand recursion handles it; the seq/other-`&` path is untouched (non-itString operands fall through to `binopForInfix`). runtime `lower`: `SymVal(svString, concat(l.str, r.str))` (nim-z3 `concat` = `Z3_mk_seq_concat`; both operands lower to svString, a literal via the `iekStrLit`→`mkString` path). `probeProto` svString arm. No parser-`&` was ever wired for seq concat, so additive. Test `tsymex_phase15_S8_concat.nim` (5 tests: literal→"foobar", var concat→b=="llo", chained "abc", additive len, UNSAT contradiction) green c+cpp 5/5; regression S1–S7b + phase5_seq + F2 clean, no hangs. Walker version unchanged at "5" |
| S | S9 | toLower/toUpper → seUnsupportedStringOp | SHIPPED | case-folding ops classified unsupported (ADR-0006: no Z3 native case-fold; regex-range approx deferred to Phase 16). dsl_parser intercepts `toLower`/`toUpper` (std/unicode) + `toLowerAscii`/`toUpperAscii` (std/strutils) on an `itString` receiver BEFORE `getStdlibModelFor`, routing to `iekStrUnsupported` → classified `seUnsupportedStringOp` (sxUnknown, Invariant 3 — never silent UNSAT/crash). Reuses the existing `iekStrUnsupported`/`SymexUnsupportedStringOpError` mechanism — no new IR kind, no new error kind. Explicit guard (not the `getStdlibModelFor` else-fallthrough) keeps classification intentional + carries the real op name into the diagnostic. Test `tsymex_phase15_S9_caseconv.nim` (5 tests: 4 case-conv ops → sxUnknown+seUnsupportedStringOp, plus plain `s=="abc"` still sxSat) green c+cpp 5/5; regression S1–S5,S8,S7b clean, no hangs. Walker version unchanged at "5" |
| S | S10a | $int/parseInt digits-path; explicit unsoundness window | SHIPPED | digits-path only (S10b raises-path still deferred to post-E1). New error kind `seParseIntPreE` (**sevHint**, NOT sevError — a path carrying it stays sxSat, exercising the sxSat+non-error severity contract). dsl_parser: `$n` (`nnkPrefix` `$`) on an itInt → `iekIntToStr` (Z3 `Z3_mk_int_to_str`, nim-z3 `toStr` on Z3Int; operand coerced via `toZ3Int` since int params are BVs); `parseInt(s)` on an itString → `iekStrToInt`. runtime: `iekStrToInt` = `ite(startsWith(s,"-"), -toInt(substr(s,1,len-1)), toInt(s))` (nim-z3 `startsWith`=`Z3_mk_seq_prefix`, NOT the RFC's `prefixOf`; `toInt`=`Z3_mk_str_to_int` returns **−1** for non-digit, NOT "unconstrained" as the RFC premised — verified `strings.nim:126`). Digits gate on the NEGATIVE branch only (`isNeg ⇒ negInner>=0`) via a `parseIntGateConstraints` threadvar drained into every `trySolve` check; positive branch is already faithful (returns true digits or −1). seParseIntPreE sevHint emitted whenever parseInt is lowered on a not-provably-digit string (conservative HINT over-emission), surfaced on sxSat results via `parseIntPreEHints` threadvar. Test `tsymex_phase15_S10a_strconv.nim` (4 tests: `$n=="42"`→n=42; `parseInt(s)==42`→s="42"; `parseInt("-42")`→ −42; non-digit `parseInt(s)==-1`→sxSat+seParseIntPreE hint) green c+cpp 4/4. str.to_int + prefix ops do NOT hang under ≤0xFF. Regression S1–S5,S8,S9,S7b,phase1_arith,F2 clean. Walker version unchanged at "5" |
| S | S10b | parseInt raises-path fork + $float/parseFloat classified | SHIPPED | **Closes the S10a pre-E1 unsoundness window; Cluster S now FULLY complete.** runtime `iekStrToInt`: in addition to S10a's digits ITE + negative gate, the lowering now emits a RAISE PREDICATE `(not isNeg) and (posVal < 0)` (the non-digit, non-`-`-prefixed case where Nim's `parseInt` RAISES `ValueError`) onto a new `parseIntRaiseConds` threadvar. **Expression-level raise plumbing:** `parseInt(s)` is an EXPRESSION but can raise, and `lower` has no WalkCtx/Path/routeRaise — so the predicate is surfaced via the threadvar and DRAINED by the enclosing statement walk via a new `drainParseIntRaises(p, w)` helper, wired into the `isLet`/`isAssign`/`isIf`(per-branch cond)/`isAssert` arms (each resets the threadvar immediately before its `lower`/`lowerBool` so predicates never leak across paths). The helper FORKS: a RAISES sub-path (constrained by the predicate, handed to E3's `routeRaise(…, "ValueError", …)` — surfaces `sxRaised{ValueError}` at the SUT boundary or transfers into a surrounding `except`, then terminates) + a DIGITS continuation (constrained by the negated predicate(s), carries S10a's int value forward). Reused E3's `routeRaise` unchanged (no new raise machinery). **`seParseIntPreE` emission REMOVED** — the `parseIntPreEHints` threadvar + its reset/surface in runSymexImpl are gone; the enum variant is retained (cache/enum stability) but no longer emitted. **$float/parseFloat:** dsl_parser intercepts `$f` (itFloat32/itFloat64 operand, in the `nnkPrefix` `$` arm) and `parseFloat(s)` (itString, in the `nnkCall` arm after parseInt) → `mkStrOp(iekStrUnsupported, "$float"/"parseFloat")` → classified `seUnsupportedStringOp` → sxUnknown (Z3 String theory has no float↔string conversion; S9 mechanism). **S10a's test UPDATED** — its 4th case asserted non-digit `parseInt(s)==-1`→sxSat+`seParseIntPreE` hint; now `let n = parseInt(s)` under `tRaisedExn("ValueError")`→`sxRaised`, no hint. **No walker version bump** (Cluster S's bump was S11→"6"; stays "6"). Test `tsymex_phase15_S10b_strconv.nim` (4 tests: non-digit→sxRaised{ValueError}; `parseInt("-42")`→sxSat/-42; `$f`→sxUnknown+seUnsupportedStringOp; `parseFloat(s)`→sxUnknown+seUnsupportedStringOp) green c+cpp 4/4. Regression (S10a updated, S5_strops, S3_strindex, S9_caseconv, S11_mutation, E2b_raise, E3_try, E6_defect, phase1_arith, F8_smoke) all green, no hangs. **Cluster S COMPLETE.** |
| S | S11 | string mutation classified; walker `"5"→"6"` | SHIPPED | **CLOSES Cluster S.** Immutable-string mutations classified `seUnsupportedStringOp` → sxUnknown (ADR-0006, Invariant 3 — never silent UNSAT/crash). dsl_parser: (1) `s[i] = c` detected in the `nnkAsgn` arm when the LHS `nnkBracketExpr` receiver is `itString` → `mkAssign(s, mkStrOp(iekStrUnsupported,"string mutation",[recv,idx,val]))`; the index-assign path IS reachable in the SUT grammar (local `var s: string` + index assign parses cleanly). (2) `s.add(c)`/`s.add(otherStr)` intercepted in the statement-`nnkCall` arm BEFORE the itSeq `add` arm when receiver is `itString` → `mkStrOp(iekStrUnsupported,"string add",…)`. Both reuse the S9/S3 idiom (residual `lower` arm raises `SymexUnsupportedStringOpError` → runSymex boundary maps to seUnsupportedStringOp). No new IR kind, no new error kind. **Walker version bumped `"5"→"6"`** (single-sourced `canonicalize.nim:symexWalkerVersion`; confirmed no duplicate). Stale F8/S7b version-pin assertions advanced "5"→"6" (expected consequence of the bump). determinism.md: unsupported-op table updated + Cluster-S op-table-complete note. Test `tsymex_phase15_S11_mutation.nim` (5 tests: s[i]=c, s.add(c), s.add("x") → sxUnknown+seUnsupportedStringOp; plain read → sxSat; version=="6") green c+cpp 5/5. Broad regression under v6 (cache keys orphaned → everything re-solves) all green, no hangs: S1–S10a, F2, F6, F8, phase1_arith, phase5_seq, phase14_multivariant_walker/disc_promotion/frontier_pruning. (S10b — parseInt raises-path — remains deferred to post-E1; it will carry its own bump.) |

## Cluster H — heap preparation

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| H | H1 | Path refactor (heaps/heapDepth/allocCounters) + ADR-0010 | SHIPPED | |

## Cluster E — exceptions

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| E | E1 | IR extension + walker handler-stack plumbing | SHIPPED | **Foundational Cluster-E cycle — purely STRUCTURAL** (IR + parser + handler-stack scaffolding; walker STUBS both new kinds with a classified error, no exception semantics — those land E2b+). types.nim: `IRStmtKind` += `isRaise`/`isTry`; new `ExceptHandler*` object (`typeIds: seq[string]` (empty = bare `except:`), `body: IRStmt`); IRStmt fields (isRaise: `raiseTypeId`/`raiseMsg`/`raiseIsReraise`; isTry: `tryBody`/`tryHandlers`/`tryFinally`); ctors `mkRaise`/`mkReraise`/`mkTry`; `SymexErrorKind` += `eeRaiseUnimplemented`/`eeTryUnimplemented`. render: `raise(ValueError,"x")` / `raise()` (bare) / `try{…}except[ValueError=>…][finally=>…]`. dsl_parser: `nnkRaiseStmt` (newException form → typed AST `RaiseStmt[StmtListExpr[Empty,ObjConstr[Par[RefTy[Sym T]],ExprColonExpr[msg,…]]]]`, type unwrapped through Par/RefTy/PtrTy; bare `raise` (`RaiseStmt[Empty]`) → mkReraise) + `nnkTryStmt` (`nnkExceptBranch` Type-nodes → ExceptHandler.typeIds, last child = handler body; `nnkFinally` → tryFinally); emitStmt branches for both. runtime: `HandlerFrame`/`ExnRecord` types; `WalkerStatics` += `exnTable`/`userExnHierarchy` (empty until E4a); `CallFrameCtx` += `handlerStack`/`inFlightExn`; `WalkCtx` += `frameStack`; `pushFrame`/`popFrame` helpers wired into the isCall descent arm (per-frame handler stack); walk stubs raise `SymexRaiseUnimplementedError`/`SymexTryUnimplementedError` → runSymex boundary maps to sxUnknown + `eeRaiseUnimplemented`/`eeTryUnimplemented` (sevError, Invariant 3 — never silent/crash). **Z4's WalkerStatics/CallFrameCtx split already existed** (matched the RFC; only field-population was net-new). **Exhaustiveness ripple = 5 arms in 3 files** (render+canonicalize already exhaustive: types.render, canonicalize, runtime.collectSetLitMembers, runtime.collectTableLitKeys, abstraction.collectBan, scan.scanStmt — emitStmt/abstraction.collectAssertRanges/scan's 2nd dispatch had `else`). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E1_ir.nim` (4 tests) green c+cpp 4/4. Regression (phase1_arith/let/assert, phase3_recursion (call-descent push/pop), phase4_tuple, phase5_seq, S3_strindex, S11_mutation, F8_smoke, phase11_walker) all green, no hangs. **S10b (parseInt raises-path) now UNBLOCKED** (can land after any E-cycle). |
| E | E2a | structural sxRaised cascade (stub arms, cache key, sfRaised) | SHIPPED | **Mechanical structural cascade — `sxRaised` fully wired, NO handler/propagation semantics (those land E2b).** Z4 already provided `WalkCtx.found: seq[RawResult]`; Z3e already provided `cacheKeyRaised(typeId)` (`:raised:<typeId>`) — both reused, not re-added (Invariant 6). **Added:** `sxRaised` to `SymexStatusKind` (types.nim) + `RawResult` variant branch (`raisedTypeId`/`isDefect`/`raisedMsg: Option`/`raisedWitness` — last three inert until E2b/E6) (runtime.nim); `sfRaised` to `SymexFindingStatus` (engine/types.nim); `stkRaisedExn{typeFilter}` to `SymexTargetKind` + `tRaisedExn()` ctor (smt/types.nim, NOT engine/types). Walker `isRaise` arm: REPLACED E1's classified-error stub with a STRUCTURAL per-raise-path `sxRaised` emission into `w.found` (no Z3, no handler match, no witness, no propagation); `shouldStop` stop-set now {sxSat, sxRaised}; `isTry` arm stays the E1 `eeTryUnimplemented` stub. **Multi-`sxRaised` cache protocol:** the general example-DB has NO key-prefix scan, so the RFC's `loadAll(:raised:*)` is realised as `saveSymexRaisedImpl`/`loadSymexRaisedImpl` (symex.nim) — per-type sentinel under `cacheKeyRaised(typeId)` PLUS an index slot (`:raised`) enumerating the persisted type ids (each as a `bytesChoice`), so a two-raise SUT (ValueError, IOError) round-trips both findings without Z3. `toFindingStatus(sxRaised)==sfRaised`; `saveSymexVerdictImpl` `of sfRaised: return` (raised goes through the dedicated path); `symexFindAllWitnesses` cold-path saves raised + a 4th cache cascade level loads it. **Exhaustiveness ripple = 11 compiler-required arms across 4 files**: SymexStatusKind — `SymexResult[T]` variant (types), `symexFind`'s `case raw.status`, 5 walker `trySolve`-dispatch sites (unreachable `discard`), `symexFindAllWitnesses` cold-path `case raw.status` (runtime+symex); SymexTargetKind — describeTarget, canonicalize, 4 macro-time `case target.kind` rebuild/cover sites (symex+canonicalize). **E1 test UPDATED** (its 4th case asserted the now-removed `eeRaiseUnimplemented` stub → now asserts `sxRaised`+`raisedTypeId=="ValueError"`). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E2a_cascade.nim` (4 tests) green c+cpp 4/4. Regression (phase13 verdict/cache round-trip ×5, phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. |
| E | E2b | real walk(isRaise) semantics + InternalVerdict | SHIPPED | **Real raise-path semantics + the private `InternalVerdict` boundary.** The walker is ACCUMULATOR-based (`walk` returns `seq[Path]`, side-effects findings into `w.found: seq[RawResult]`) — NO `WalkResult`/walk-return refactor; `InternalVerdict` is a LOCALIZED helper for the raise path, not a wholesale verdict rename. **runtime.nim:** private `InternalVerdictKind`(`ivSat`/`ivUnsat`/`ivUnknown`/`ivRaised`) + `InternalVerdict` union (`ivSat`→`satWitness`; `ivRaised`→`raisedTypeId`/`raisedMsg: Option`/`raisedWitness` — fields renamed off the `sxSat` `witness` to dodge Nim's no-repeated-variant-field rule). `toPublic(iv): RawResult` is the SINGLE conversion (Invariant 9), called once per finding inside the `isRaise` arm before `w.found.add`. **Real `walk(isRaise)`:** REPLACES E2a's structural stub — evaluates `stmt.raiseMsg` via `evalRaiseMsg` (string-literal `iekStrLit`→`some(sval)`, nil/non-literal→`none`), then for each non-uncertain path SOLVES the path with the existing `trySolve` (SAME extraction as the `ivSat`/`sxSat` path) and emits `toPublic(ivRaised(typeId,msg,witness))` into `w.found`. **Target-gated** (the real behavior change): the raise surfaces a finding ONLY when `w.target` is `stkAssertionViolation` (a reachable raise is a violation) or `stkRaisedExn` (matching `typeFilter`, empty=any); under an `stkLabel`/other search the raise just terminates the path (returns `@[]`) → a post-raise label is now correctly `sxUnsat`. **Bare raise** (`raiseIsReraise`): in-flight exn (`w.frame.inFlightExn`) → re-raise its `ExnRecord` (typeId+msg); empty handler stack + no in-flight → `SymexRaiseOutsideHandlerError` → boundary `eeRaiseOutsideHandler` (sevError, Invariant 3); non-empty handler stack w/o recorded in-flight → `sawUnknown` (handler-stack re-raise is E3). **isTry STAYS the E1 `eeTryUnimplemented` stub** (E3 does try/except). **types.nim:** `eeRaiseOutsideHandler` added to `SymexErrorKind`; `SymexResult[T].sxRaised` arm gains `raisedWitness*: T` (distinct name from `sxSat.witness`). **symex.nim:** `symexFind` macro `sxRaised` arm binds `witId`=`raw.raisedWitness` and reconstructs the public witness tuple via the shared `witnessTup` (same reader as `sxSat`). **E1 test UPDATED** (its 4th case asserted E2a's untargeted `sxRaised` under `tLabel` → now asserts `tLabel`→`sxUnsat` + `tRaisedExn("ValueError")`→`sxRaised`). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E2b_raise.nim` (5 tests, isExact+isOptimised) green c+cpp 5/5. Regression (E1_ir (updated), E2a_cascade, phase13 satsuffix/unsat_roundtrip/layer1_wire, phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. |
| E | E3 | try/except matching by type + inter-proc propagation stub | SHIPPED | **The control-flow core: `walk(isTry)` + handler-aware raise routing + inter-proc propagation.** The walker stays ACCUMULATOR-based (no walk-return refactor); E3 adds a shared `routeRaise(p, typeId, msg, w): seq[Path]` primitive + per-frame `caught`/`escaped` channels. **runtime.nim:** **`walk(isTry)`** (replaces the E1 `eeTryUnimplemented` stub) pushes a `HandlerFrame{handlers, finallyBlock}` onto `w.frame.handlerStack` at `myDepth = handlerStack.len`, walks `tryBody`, pops to `myDepth`, then CLAIMS the `caught` entries tagged with `depth == myDepth` (clearing them) and merges them with the body's normal fall-through — this is what makes a caught raise EXIT the try rather than resume the body after the raise site. **`routeRaise`** (the single handler-matching primitive, called inline by `isRaise` and by the `isCall` arm for inter-proc): searches `w.frame.handlerStack` top-down for the first `ExceptHandler` whose `typeIds` contains `typeId` (EXACT-STRING membership — E4 supersedes with `isSubtypeOf`; empty `typeIds` = bare `except:` catch-all). MATCH → truncates the stack below the matched frame, sets `inFlightExn` (so a bare re-raise in the handler propagates outward), walks the handler body, restores stack/inFlight, records the handler continuations on `w.frame.caught` tagged by the catching depth, and returns `@[]` (the straight-line raise path terminates). NO MATCH + callee frame (`frameStack.len > 0`) → records an `EscapedRaise{path, typeId, msg}` on `w.frame.escaped` and returns `@[]`. NO MATCH + root frame → E2b boundary behavior (target-gated `trySolve`→`toPublic(ivRaised)`). Uncertain paths → `sawUnknown`. **`isRaise`** now resolves typeId/msg (re-raise via `inFlightExn` unchanged from E2b) then delegates each path to `routeRaise`. **Inter-proc:** the `isCall` arm captures the callee frame's `escaped` BEFORE `popFrame`, then after the pop re-routes each through the CALLER's handler stack (`routeRaise` on a caller-env fork of the raise-site path) — a `try` around a call catches the callee's raise; the caught continuations join the call survivors. A callee that escaped a raise is NOT cached (`calleeEscaped.len == 0` guard — its summary is incomplete). **types.nim:** unchanged (E3 is pure runtime). **`tryFinally` DEFERRED to E5** — stubbed: walked on the normal fall-through only (no finally in the E3 tests → no-op). **Exact-string matching confirmed** by the negative test (case 5): `except CatchableError:` does NOT catch `ValueError`. **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E3_try.nim` (10 tests: 5 RFC cases × exact/optimised+sub-checks) green c+cpp 10/10. Regression (E1_ir, E2a_cascade, E2b_raise, phase3_recursion (call-descent — inter-proc rides this), phase3_mutual, phase1_arith/assert, phase11_walker, S11_mutation, F8_smoke, phase13_satsuffix) all green, no hangs. |
| E | E4 | exception type hierarchy (subtype catch, static ExnTypeTable) | SHIPPED | **Subtype catch via a static `ExnTypeTable`** — replaces E3's exact-string handler match with `isSubtypeOf`. **New file `src/proptest/smt/exn_hierarchy.nim`:** compile-time `const exnTypeTable: Table[string, seq[string]]` (each exn type name → its FULL ancestor chain, nearest parent first, toward the `Exception` root) encoding Nim's REAL system hierarchy (verified against Nim 2.2.10 `lib/system.nim` + `lib/system/exceptions.nim`): `Exception`(root) → `{Defect, CatchableError}`; `CatchableError` → `{ValueError, IOError, OSError}`; **`KeyError` is-a `ValueError`** (NOT directly `CatchableError`); `Defect` → `{IndexDefect, FieldDefect, AssertionDefect, RangeDefect, OutOfMemDefect, StackOverflowDefect}`. The real type is `OutOfMemDefect`; `OutOfMemoryDefect` (RFC/checklist spelling) is included as an alias chain. `isSubtypeOf(raised, ht, exnTable, userExnHierarchy)` = `ht == raised or ht in ancestorsOf(raised)`; `ancestorsOf` resolves the static chain, else walks `userExnHierarchy` (child→parent, E4a-populated, EMPTY in E4) splicing in the static chain of the first standard ancestor. `isDefect(exnTable, typeId, userExnHierarchy=…)` = `typeId == "Defect" or "Defect" in ancestorsOf(...)` — true for any known Defect subtype and (dkOther fallback) for an unknown type whose `userExnHierarchy` chain traces to `Defect`. `isKnownExnType` = resolvable in either table. **runtime.nim:** `WalkerStatics.exnTable` retyped `Table[string,string]`→`Table[string, seq[string]]` and populated from `exnTypeTable` in the WalkCtx ctor; `routeRaise`'s exact-string `typeId in h.typeIds` swapped for a per-`ht` `isSubtypeOf` loop (bare `except:` still catches all). **Unknown raised type** (not in either table) → records `eeUnknownExnType{sevWarning, msg: typeId}` on a new `unknownExnWarnings` threadvar (reset at runSymex entry, dedup'd by type name, drained into `RawResult.errors` on EVERY verdict branch — sevWarning never halts, Invariant 7) and is matched ONLY against a bare `except:` (conservative — no silent false-negative, Invariant 3). **types.nim:** `eeUnknownExnType` added to `SymexErrorKind`. **user-Defect dkOther (test 3):** the static membership/`isDefect` logic ships in E4; the DYNAMIC capture that fills `userExnHierarchy` from a SUT's `getImpl` is DEFERRED to E4a, so test 3 supplies the `{MyDefect: Defect}` chain the way E4a will — proving the dkOther fallback resolves once the parent is known; the public `SymexResult` carries no `isDefect` field, so the finding's defect-ness is asserted via observable `sxRaised{raisedTypeId: "MyDefect"}`. **E3's test 5 (2 cases) UPDATED** — they asserted E3's transitional exact-string negative (`except CatchableError:` does NOT catch `ValueError`); E4 reverses this, so they now assert CatchableError CATCHES ValueError (handler body reached → `sxSat`; nothing escapes → `sxUnsat`), keeping the E3 file green under E4 (same pattern E2a/E2b used on E1's test). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E4_hierarchy.nim` (4 tests) green c+cpp 4/4. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try (updated), phase1_arith/assert, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **Next: E4a** (dynamic user-exn hierarchy via `getImpl` ancestor walk → `userExnHierarchy`). |
| E | E4a | dynamic user-exception hierarchy (getImpl walk) | SHIPPED | **Closes the user-subtype soundness gap left by E4.** E4 shipped the static `ExnTypeTable` + the `isSubtypeOf`/`ancestorsOf` logic that ALREADY consults `userExnHierarchy` as a fallback — but `userExnHierarchy` was EMPTY (no parser pass filled it). E4a adds the parse-time capture so a SUT that defines `type MyError = object of ValueError`, raises `MyError`, and catches `except ValueError:` is correctly caught (was silently `sxUnsat`/`sxRaised{MyError}` before). **dsl_parser.nim:** new `collectUserExnAncestors(typeSym, ctx)` helper — `typeSym.getImpl` → `nnkTypeDef[Sym, Empty, ObjectTy[…, OfInherit[Sym Parent], …]]` (RefTy/PtrTy-unwrapped); SCANS the ObjectTy's children for the `nnkOfInherit` node (it is NOT at a fixed index — the pragma slot shifts it; the OfInherit's sibling Empty means `of RootObj` = end of chain), records `child→parent` into `ctx.userExnHierarchy`, recurses on the parent until reaching a type in the static `exnTypeTable` (ValueError/IOError/Defect/…) or a no-parent terminus (depth-cap + already-recorded cycle guard). **RECONCILIATION beyond the RFC:** the RFC says walk only `nnkExceptBranch` handler types — INSUFFICIENT for its own test 1 (raises `MyError`, catches `ValueError`; the `MyError→ValueError` link only appears at the RAISE site). So E4a calls `collectUserExnAncestors` at BOTH the `raise newException(<T>,…)` type node AND each `nnkExceptBranch` handler type node. `ParseCtx` gains `userExnHierarchy: Table[string,string]`; `parseProc` emits it (`emitStrStrTable`) into `ParseResult.userExnHierarchyNimNode`; `symexFind`'s `quote do` threads it into `SymexProgram.userExnHierarchy` (new field on `SymexProgram`, types.nim). **runtime.nim:** `WalkCtx` ctor now passes `userExnHierarchy: prog.userExnHierarchy` into `WalkerStatics` (was empty in E4); `isSubtypeOf` was already wired in E4 to bridge a user child→parent chain into the static table once it reaches a known base — so a user type whose chain reaches `Defect` makes `isDefect` true (the E4-DEFERRED dkOther is now DYNAMIC, no hand-supplied chain). Unknown types (in neither table) still emit the E4 `eeUnknownExnType{sevWarning}` + bare-except-only match (Invariant 3). **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E4a_userexn.nim` (2 tests) green c+cpp 2/2. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, phase1_arith, phase3_recursion, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **Next: E5** (finally semantics). |
| E | E5 | finally semantics | SHIPPED | **Completes `walk(isTry)` — the `finally` block now runs on BOTH the normal AND the raised exit paths (E3 stubbed it to the normal fall-through only).** Extends E3's depth-tagged channel mechanism with a new per-frame `pendingRaise` channel (mirror of `caught`): in `routeRaise`, a raise that matches NO `except` arm but is guarded by an enclosing `finallyBlock` is deferred to that try's depth (scan handler stack top-down for the deepest non-nil `finallyBlock`) instead of escaping/surfacing — so the finally runs BEFORE the raise propagates. The owning `isTry` arm claims `pendingRaise` at `myDepth` and composes per Nim semantics: (a) NORMAL exits — walk finally; finally fall-through survives, a finally-raise is routed by `routeRaise` (replaces); (b) RAISED exits — set `inFlightExn` to the original exn for the finally's duration, walk finally on the raised path; the fall-through survivors RE-RAISE the original (re-routed outward via `routeRaise`), while sub-paths where the finally itself raised had that new exn already routed (REPLACES). `inFlightExn` lifecycle: set while a raised continuation runs its finally; restored after. **Path-explosion tamed:** finally runs ONCE per exit continuation (once on the merged normal set, once per raised continuation), and the handler stack is popped to `myDepth` before the finally walk so a raise inside the finally routes to the NEXT-OUTER try (never re-enters this finally — no loop). **Test 3 (ptr-deref heap-write visibility through finally) DEFERRED to Cluster R:** the `p[]=7`/`q[]=99` SUT is logical-heap (deref/assign) semantics — `path.heaps` exists (H1) but is INERT until Cluster R, so the engine cannot yet PRODUCE ptr-write witnesses; faking it would be unsound. Tests 1 & 2 (pure control flow) ship as the E5 deliverable; the finally path-state threading is structurally in place and will exercise once Cluster R lands. **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E5_finally.nim` (5 tests + 1 deferred skip) green c+cpp 5/5. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, E4a_userexn, phase3_recursion, phase1_arith, phase11_walker, S11_mutation, F8_smoke) all green, no hangs. **Next: E6** (Defect modeling — isDefect; defectExclusions; OQ4). |
| E | E6 | Defect modeling (isDefect; defectExclusions; OQ4) | SHIPPED | **Models Nim `Defect` raises as `sxRaised{isDefect: true}` (OQ4 / ADR-0007 closed-default policy).** **What already existed (reused, NOT re-added — Invariant 6):** `DefectKind` enum + `SymexSettings.defectExclusions: set[DefectKind]` (default `{dkOutOfMemoryDefect, dkStackOverflowDefect}`) both shipped in Z3a (smt/types.nim, NOT engine/types.nim as RFC §E6's GREEN says — the prompt hedge is confirmed: the enum lives in smt/types and carries an EXTRA `dkRangeDefect` variant beyond the RFC list); `RawResult.sxRaised.isDefect: bool` shipped in E2a (init false); `exn_hierarchy.isDefect(exnTable, typeId, userExnHierarchy)` shipped in E4. **E6 added:** (1) `SymexFinding.defectTypeId: string` (engine/types.nim) for display; (2) `typeIdToDefectKind(typeId): DefectKind` (runtime.nim) mapping a raised type to its kind for the exclusion test — both `OutOfMemDefect`/`OutOfMemoryDefect` spellings → `dkOutOfMemoryDefect`, user defects → `dkOther` (so they are excludable only all-or-none; documented on the enum); (3) `InternalVerdict.ivRaised.raisedIsDefect` + `toPublic` populates `RawResult.isDefect`; (4) the routeRaise boundary now computes `raisedIsDefect = isDefect(...)` and a `defectExcluded` flag — **defectExclusions filter:** an excluded defect's DefectKind ∈ `settings.defectExclusions` is SUPPRESSED (no finding, regardless of target); a NON-excluded defect ALWAYS surfaces as `sxRaised{isDefect:true}` EVEN under a label/non-raise search (a reachable contract violation is never silently dropped); a non-defect `CatchableError` keeps E2b's target-gating. **assert→implicit-AssertionDefect raise (PARSER, not walker):** a raw `assert cond, msg` lowers (after semcheck) to gensym scaffolding (`const loc…`/`bind`/`mixin`) + `PragmaBlock[Pragma, IfStmt[ElifBranch[not (cond), Call failedAssertImpl]]]`; new `findAssertFailsCond`/`callsFailedAssertImpl` (dsl_parser.nim) recognise this expansion at the StmtList/PragmaBlock head and lower it to `mkIf(@[mkBranch(<not cond>, mkRaise("AssertionDefect", nil))])` — so the assert-FAILS branch raises an implicit `AssertionDefect`. The `symexAssert(...)` MARKER (→ `mkAssert`/`isAssert`) and its `tAssertionViolation` semantics are UNCHANGED. (5) Auto-discovery: new `irHasAssertDefect`/`scanAssertDefect` (scan.nim) detect the implicit `AssertionDefect` raise so `symexFindAllWitnesses` adds a `tRaisedExn("AssertionDefect")` target (+ `tRaisedExn` exclusion arm), and the sxRaised recording path sets `f.defectTypeId` when `raw.isDefect`. **Existing `tAssertionViolation` (phase1_assert via `symexAssert`), `stkIndexError` (phase4_tuple), `stkFieldDefect` (phase11_fielddefect) targets CONFIRMED UNCHANGED** — they produce sxSat under their own semantics; E6 only adds the sxRaised-Defect path. **OQ4 CLOSED.** **No walker version bump** (E-cluster bumps at E7; stays "6"). Test `tsymex_phase15_E6_defect.nim` (4 tests) green c+cpp 4/4. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, E4a_userexn, E5_finally, phase1_assert, phase4_tuple, phase11_fielddefect, phase1_arith, phase7_assertcovered, phase11_walker, phase13_layer1_wire, F8_smoke) all green, no hangs. **Next: E7** (regression smoke vs Cluster S + multi-frame re-raise; walker version "6"→"7"). |
| E | E7 | regression smoke; walker `"6"→"7"` | SHIPPED | **CLOSES Cluster E (complete through E7; E8 remains, additive under "7").** Hermetic E-cluster regression smoke exercising the FULL exception machinery (E1–E6) TOGETHER in one in-process file to catch state-threading bugs from the multi-file `WalkCtx` edits, + the close-out walker version bump. `tsymex_phase15_E7_smoke.nim` (12 tests) green c+cpp 12/12: inter-proc raise caught by the caller's handler (E3) + nothing-escapes dual; finally re-raises-original / finally-raises-replaces (E5); **multi-frame re-raise** (nested try; inner `except IOError:` no-match, bare `raise` pops through the inner frame to the OUTER `except ValueError:` via `inFlightExn`); subtype catch (`except CatchableError:` ⊇ `ValueError`, E4) + user exn caught by stdlib base (E4a) + nothing-escapes dual; `assert`-false → `sxRaised{AssertionDefect, isDefect:true}` (E6) + the Report-surface `sfRaised` defect entry; semantically-complete multi-finding `sxRaised` cache round-trip (two-raise SUT solved via the real walker, persisted via `saveSymexRaisedImpl`, reloaded from a fresh DB-only state via `loadSymexRaisedImpl` — both findings reconstruct, no Z3); the walker-version pin. **NO production-code change beyond the bump** — the composition smoke found no state-threading regression; `WalkCtx`/`w.frame` zeroing is correct across the combined machinery. **Walker version `"6"→"7"`** in `canonicalize.nim:symexWalkerVersion` (single source — Invariant 6, no duplicate); three prior version-pin tests (F8_smoke, S7b_smoke, S11_mutation, all pinned "6") advanced to "7". **Broad regression sweep** (ALL E tests; Cluster S sample S1/S3/S5/S6b/S7a/S9/S10a/S10b/S11; F2/F6/F8_smoke; H1; phase1_arith/assert, phase3_recursion, phase4_tuple, phase11_walker, phase11_fielddefect, phase13_layer1_wire, phase7_assertcovered) all green c, NO hangs; E7+F8_smoke+S11_mutation+S7b_smoke+E6_defect re-run under v7 green. **Next: E8.** |
| E | E8 | getCurrentException/Msg (uses svUninterpRef from Z3b) | SHIPPED | **CLOSES Cluster E (complete).** The two no-arg exception-query magic intrinsics, recognised by callee symbol name in `parseExpr`'s `nnkCall` dispatch (intercepted BEFORE the user-proc `ensureProcRegistered` fall-through, which would otherwise try to parse their stdlib bodies) → new IR kinds `iekGetCurrentExn`/`iekGetCurrentExnMsg` (no payload). **Prerequisite VERIFIED, no fix needed:** E3/E5 already set `w.frame.inFlightExn = some(...)` for the FULL duration of a matched handler-body walk (`routeRaise` match arm: set before `walk(h.body)`, restore after) — not just the re-raise path — so `getCurrentExceptionMsg()` reads the live in-flight msg correctly. `lower` is a pure `Env→SymVal` with no `WalkCtx`/frame access, so the in-flight type/msg are mirrored into two threadvars (`currentInFlightTypeId`/`currentInFlightMsg`) via a `setInFlightThreadvars` helper called at every `w.frame.inFlightExn` set/restore site (handler-body entry + finally raised-exit). `getCurrentExceptionMsg()` → `svString(mkString(inFlightMsg.get("")))` ("" for zero-arg object-construction raise with no msg field). `getCurrentException()` → fresh uninterpreted-sort constant (`mkUninterpretedSort("Exn_"&typeId)` + `Z3_mk_const`, erased to `Z3AnyAst`) wrapped in `svUninterpRef(uninterpAst, sortName:"Exn_"&typeId, typeTag:typeId)` — the real Z3b field names. Out-of-handler call (`inFlightTypeId.isNone`) → raise `SymexNotInHandlerError` → runSymex boundary → sxUnknown + `eeNotInHandler` (sevError, NEW kind; Invariant 3 — never a panic). `extractFromSymVal(svUninterpRef)` arm emits `eeUninterpRefExtraction` (sevHint, pre-existing kind) into `extractionErrors`. `discard getCurrent*()` is now lowered (bound to a synthetic sink `let`) so the effectful handler-context check + ref-tag recording fire. Exhaustiveness ripple: `iekGetCurrentExn*` arms added to types.render, canonicalize, abstraction.collectVarRefs + tryEvalInterval, dsl_parser.emitExpr, runtime.lower + probeProto. **NO walker version bump — stays "7"** (Cluster E bumped at E7; E8 additive). Test `tsymex_phase15_E8_getcurrentexn.nim` (4 tests) green c+cpp 4/4. Regression (E1_ir, E2a_cascade, E2b_raise, E3_try, E4_hierarchy, E4a_userexn, E5_finally, E6_defect, E7_smoke, phase1_arith, phase11_walker, S11_mutation, F8_smoke) all green c, no hangs — E3/E5 (the inFlightExn-threadvar-mirror at-risk pair) green. **Cluster E COMPLETE. Next: G0-ADR (Cluster G — generics).** |

## Cluster G — generics

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| G | G0-ADR | author ADR-0008-generic-instantiation.md | SHIPPED | ADR-0008 confirmed on disk (Accepted, 2026-06-06; matches preamble D1-D7). §F Cluster G reconciliation written. **MAJOR DRIFT FLAGGED:** generics ALREADY symex end-to-end via parse-time monomorphization (`ensureProcRegistered`→`gatherTypeSubst`→`monomorphize`→`parseCalleeImpl`, `dsl_parser.nim:1618/1588/1574/1645`; proven by `tsymex_rectify_generics.nim`). NO `isGenericCall` IR exists or is needed — RFC's G1a/b/c net-new IR + walk-time instCache is **redundant**; recommend repurposing G1a-c to formalize/harden the existing monomorphization (fix bare-name `ctx.procs` collision = Feas-H5, add cap). `ge*` kinds already in `types.nim:575`; `maxInstantiationsPerProc` NOT in SymexSettings (RFC wrong, net-new); `mkUninterpretedSort` exists+used (`sort.nim:113`, `runtime.nim:1757`); no `he*`→`ge*` rename needed. **Next: G1a.** |
| G | G1a | ~~IR: isGenericCall, mkGenericCall, dispatch stubs~~ → REPURPOSED: generic instantiation key (fix bare-name `ctx.procs` collision) | SHIPPED | No `isGenericCall` IR (locked decision). Replaced the bare-`name` `ctx.procs`/`parsing` key with an ADR-0008 D2 instantiation key `name#<symBodyHash-or-lineInfo>#<sorted T=Type tuple>` via a shared `instKeyFor`/`bodyHashPart` (`dsl_parser.nim`). `ensureProcRegistered` now RETURNS the key; all three call-emission sites pass it as the `mkCall` callee name so registration AND walker dispatch (`w.procs[stmt.callee]`) share ONE key. Non-generic procs keep the bare name (behavior identical). RED→GREEN: `szof[T](x):int = sizeof(T)` at int8+int64 collided (sxUnsat→sxSat). `tests/tsymex_phase15_G1a_instkey.nim` (4/4 c+cpp). Regression 9/9 (rectify_generics, phase3_recursion/mutual/summarization, phase4_tuple, phase1_arith, E3_try, S11_mutation, F8_smoke). Walker version stays "7". **Next: G1b/G1c (cap + `maxInstantiationsPerProc`).** |
| G | G1b | ~~parser: gatherTypeSubst, parseCalleeImpl, emitGenericCall~~ | folded | →G1a (gatherTypeSubst/parseCalleeImpl/monomorphize shipped pre-G; instKey + dispatch landed at G1a) |
| G | G1c | ~~walker dispatch + instantiation cache~~ → REPURPOSED: generic instantiation CAP (folds G2; dispatch+cache already shipped at G1a) | SHIPPED | No walk-time instCache (G1a's instKey'd `ctx.procs` IS the per-walker cache). NET-NEW `SymexSettings.maxInstantiationsPerProc` (default 64; RFC's "already in Z3" claim was false) + default + `+` merge ripple. Per-BASE-proc instantiation counting in `ensureProcRegistered` (base identity = the generic's DEFINITION site `lineInfoObj` file:line:col — STABLE across instantiations and module-disambiguating; `symBodyHash` could NOT key it, being per-instantiation). On overflow the over-cap instantiation is NOT registered → its `mkCall` key is absent from `w.procs` → the (G1c-hardened) missing-callee walker arm binds a fresh unconstrained retSym + marks paths uncertain → sxUnknown. `geInstantiationCapped` (sevError, reused from Z3a) accumulated into `ctx.parseErrors` → emitted via `ParseResult`/`SymexProgram.parseErrors` → drained into `RawResult.errors` by `runSymexImpl` (forces sxUnknown — Invariant 3, never silent). Cap value threads settings→parser: macros pass `settings.maxInstantiationsPerProc` into `parseProc`→`newParseCtx`→`ParseCtx`. `withSymexSettings` sets the new field (verified). `tsymex_phase15_G1c_instcap.nim` 2/2 c+cpp. Regression 8/8 (G1a_instkey, rectify_generics, phase3_recursion/summarization/mutual, F8_smoke, S11_mutation, phase1_arith). Walker version stays "7". **Next: G3 (type-substitution path through classifyType; auto return).** |
| G | G2 | ~~instantiation cache~~ | folded | →G1a (instKey'd `ctx.procs` IS the cache) |
| G | G3 | type-substitution path through classifyType; auto return | SHIPPED | Audit + guards. **Already worked:** the classifyType text-match fallthrough (`dsl_typebridge.nim:343`) already maps a substitution-derived `float64`/`string`/`bool`/`int` SYM to the right IRType and already ERRORS cleanly (never a silent default — Invariant 3), so the centerpiece's PARAM classification was sound pre-G3. **Had to FIX (gaps the float/sink RED surfaced):** (1) **walk-time float/string proc-RETURN** — the `isReturn` retConstraint raised "composite return not yet wired" for floats and all five call-return retSym allocations used `bvVar` (asserts `itInt`), so a value-returning generic at `float64` CRASHED before reaching its target; added `retBindEq` (NaN-safe structural eq for float, native for int/bool/string) + `freshRetSym` (routes through the type-aware `allocateSym`, threads string byte-range/init constraints onto the post-call survivor paths and into the call-cache `pcDelta`) and wired all five retSym sites through them (`runtime.nim`); (2) **`sink T` through generics** — the typed generic AST presents `sink T` as `nnkCommand[sink,T]` (NOT the `sink[T]` bracket Z3c handled) and `gatherTypeSubst` only matched a BARE `nnkIdent` formal, so `T` never bound and the body's `T` stayed un-monomorphised → classifyType errored on `T`; fixed `gatherTypeSubst.unwrapGenericTy` to strip `var`/`sink`/`lent` (command+bracket) before name-matching, and taught `classifyType` to strip the `nnkCommand` sink/lent form BEFORE `getTypeInst` (the substituted command carries no type). **Guard ADDED:** auto-return `nnkEmpty` guard in `parseCalleeImpl` — if a monomorphised proc that DECLARED a return type resolves it to `nnkEmpty`, `error("symex G3: type-substitution produced nnkEmpty retTy …")` instead of silently treating it as void (defensive; Nim resolves `auto` pre-getImpl). **Float64 instantiation symex's correctly** (Cluster F bridge reached THROUGH a generic call → sxSat float witness, not sxUnknown). String proc-RETURN value extraction deferred (pre-existing unwired path, not a generic concern — out of G3 scope; degrades soundly to sxUnknown). `tests/tsymex_phase15_g3_type_subst.nim` 2/2 c+cpp (float + sink). Regression 10/10 (G1a_instkey, G1c_instcap, rectify_generics, F2_float_literals, F5_float_conv, F8_smoke, phase3_recursion, phase4_tuple, S3_strindex, phase1_arith). Walker version stays "7". **Next: G4 (distinct T fresh sort).** |
| G | G4 | distinct T as fresh uninterpreted sort + inject/eject/bijectivity | SHIPPED | New `IRTypeKind` `itDistinct{distinctName, distinctBase}` (constructor `tDistinct`; exhaustiveness ripple chased across `==`/`$`/canonicalize/emitIRType/classifyType/allocateSym/tyOf/iteSV/coerceIntLit/symValHash/extractFromSymVal/extractLeaf/defaultZero/runSymexImpl param-loop — all via bounded compiles). `classifyType` recognises `type Foo = distinct Bar` (the `nnkDistinctTy` in `getImpl`) → `itDistinct(name=Foo, base=classify(Bar))`, walled off BEFORE the object/enum/alias unwrap paths. **SymVal:** NEW `svDistinct{distinctAst: Z3AnyAst, distinctName, distinctBaseSym: ref SymVal}` (NOT a reuse of E8's `svUninterpRef` — that carries no base; G4 needs the ejected base for the witness chain + conversions). **distinctSorts placement:** the LIVE cache is a per-run threadvar `currentDistinctSorts: Table[string, DistinctSortEntry{sort, inject, eject}]` (reset at `runSymexImpl` entry — `allocateSym` has no `WalkCtx` access, exactly E8's in-flight-exn threadvar idiom); `WalkerStatics.distinctSorts: Table[string, Z3Sort[stUninterpreted]]` is ADDED per ADR-0008 D4 and MIRRORED from the threadvar post-walk. **inject/eject:** two uninterpreted func-decls per distinct type via raw `Z3_mk_func_decl` (the phantom `Z3FuncDecl[ArgsTup,Ret]` can't be used — sorts are runtime-known), declared once per (name, run); `inject_T: Base→Distinct`, `eject_T: Distinct→Base`. **Round-trip / "bijectivity":** for the DECIDABLE base fragment {int, BV, bool} the round-trip is a GROUND per-occurrence pin `eject(dConst) == baseSym` — NOT a universal quantifier. **CRITICAL HANG FINDING:** the universal `∀x. eject(inject(x))==x` form HANGS (180s, MBQI loop); even WITH triggers it returns `unknown`; and even a GROUND reverse `inject(baseSym)==dConst` ALSO hangs (uninterpreted-fn-over-BV). The eject-pin alone is the decidable model (QF_UFBV, sxSat/sxUnsat) and suffices for the DoD; inject is declared but never applied at alloc. For {float32, float64, string} base the round-trip is SKIPPED and `geDistinctBijectivitySkipped` (sevHint, REUSED from Z3a) is emitted (drained into `RawResult.errors` on every verdict branch, dedup'd — like E4's `unknownExnWarnings`). **CONFIRMED int-base does NOT hang.** Z3 4.15 footguns fixed en route: the uninterpreted sort needs an explicit `Z3_inc_ref(Z3_sort_to_ast(...))` (else GC corrupts it → `Z3_UNKNOWN_SORT` → SIGSEGV); `Z3_mk_app` args + func-decl domains must be HEAP seqs not stack arrays; intermediate app results must be `wrap`-ped before re-application. **emitTyAndReader** `itDistinct` arm renders the eject-chain: `extractFromSymVal(svDistinct)` extracts the boxed base at the SAME path, the emitter wraps it `DistinctName(baseReader)` (Breadth-CRIT-1 — no silent empty reader). **`ejectBase`** (recursive, for nested chains) auto-ejects a `svDistinct` operand to its ground base in every binop compare/arith + unop site, so `float64(m)`/`int(u)` (parser passes the bare distinct var through) lower correctly. **geDistinctBarrier** ADDED to `SymexErrorKind` (net-new, sevError) — implicit distinct↔base coercion is rejected by the Nim semchecker before symex sees it (D5-style "trust the semchecker"), so the kind exists for test-injectable/defensive use, not a synthetic emission site. `tests/tsymex_phase15_g4_distinct_sort.nim` 4/4 c+cpp. Regression 10/10 (G1a_instkey, G1c_instcap, g3_type_subst, rectify_generics, phase4_tuple, phase5_seq, F2_float_literals, S3_strindex, phase1_arith, E8_getcurrentexn). Walker version stays "7" (Cluster G bumps at G10). **Next: G5 (distinct borrow semantics).** |
| G | G5 | distinct borrow semantics | SHIPPED | A `{.borrow.}` operator on a `distinct T` threads the op through the BASE type — but, per the G4 HANG finding, NOT via a Z3 `inject_T(eject_T(a) op eject_T(b))` function-application chain (uninterpreted-fn-over-BV / MBQI → hang). Instead it operates on the G4 BOXED base SymVal. **Parser (`dsl_parser.nim`):** `hasBorrowPragma`/`borrowInfoFor` classify an operator SYMBOL as a borrow shim (an `nnkPragma` child holding `ident"borrow"`; the typed AST presents `m1 + m2` as an `nnkInfix` whose `n[0]` is the borrow proc sym, body = bare `Sym` = the base op — so it must NOT be body-parsed). The borrow's return type (`impl[3][0]`) classifies `itDistinct` → arithmetic (re-box), else → comparison (bool). The `nnkInfix` arm intercepts a borrow sym and emits a NEW `iekBorrowOp` IR (`mkBorrowOp(baseOp, lhs, rhs, returnsDistinct, distinctName)`) — exhaustiveness ripple chased across emitExpr/render/canonicalize/abstraction (collectVarRefs, tryEvalInterval none-group)/probeProto. **Runtime (`runtime.nim`):** the `iekBorrowOp` lower arm EJECTS both operands (`ejectBase` → G4 `distinctBaseSym`), applies the base op (`arithInt`/`arithFloat`/BV or `cmpInt`/`cmpFloat`/`cmpString`/BV), and for arithmetic RE-BOXES via new `reboxDistinct` (a fresh opaque const of the distinct sort from `currentDistinctSorts` + the computed base boxed underneath; name uniquified by the `currentBorrowReboxCounter` threadvar, reset at `runSymexImpl` entry) — comparison returns the raw svBool. **`Meters(10.0)`** is a distinct construction the parser already passes through to its base float literal (G4). **geDistinctBarrier (Invariant 3):** in `ensureProcRegistered`, a NON-borrow proc with an empty body (`impl[6]==nnkEmpty`, e.g. an `{.importc.}` magic) taking a distinct-typed param emits `geDistinctBarrier` (sevError, REUSED from G4) into `ctx.parseErrors` and is NOT registered — the missing-callee walk arm degrades the path + the sevError forces sxUnknown (never silent). **CONFIRMED no hang** (operates on the boxed base, never applies the Z3 inject fn). `tests/tsymex_phase15_g5_distinct_borrow.nim` 3/3 c+cpp (borrowed `+` arithmetic reaches target with base-sum witness > 10.0; borrowed `<` raw Z3 bool; non-borrowed bodyless distinct op → geDistinctBarrier/sxUnknown). Regression 9/9 (g4_distinct_sort, g3_type_subst, G1a_instkey, rectify_generics, F2_float_literals, F3_float_arith, F4_float_compare, phase4_tuple, phase1_arith). Walker version stays "7" (Cluster G bumps at G10). **Next: G6 (concepts).** |
| G | G6 | concept constraints: trust boundary + compound | SHIPPED | `ProcSig.conceptConstraints` + parse-time stdlib-concept membership check (SomeNumber/Integer/Float/Ordinal/Un/SignedInt); stdlib→validate (geConceptViolation, reused), user concepts→trust semchecker. No isGenericCall node — negative test injects non-conforming pair into `conformsToStdlibConcept`. Positive concept-generic monomorphizes+symexes (clampPos[T:SomeNumber]@int sxSat). g6 3/3 c+cpp. Commit 153b92c. |
| G | G7 | static[T] params as instantiation-key components | SHIPPED | `static[T]` generic params (`proc foo[N: static int]`) as part of the instantiation identity. **AST findings (probed):** the static-param CONSTRAINT is `nnkCommand[Ident "static", T]` (NOT `nnkStaticTy` as RFC §G7 guessed); the static VALUE is NOT a call arg (`foo[3]` loses its brackets in the typed `Call`) — Nim's semchecker has ALREADY baked it into the BODY (`x[N-1]`→`x[2]`, `x>N`→`x>3`, AND `static bool` `B`→`IntLit 0/1`). The un-substituted residue is a FORMAL TYPE that names N as an array dimension (`array[N,int]`), which `classifyType` can't size. Two instantiation callee Syms are DISTINCT and `symBodyHash` ALREADY differs. **Parser (`dsl_parser.nim`):** `staticParamNames`/`genericParamsNode` detect static params; `gatherTypeSubst` binds a static-array dimension N to its literal from the ARG's `array[range[lo..hi],_]` getType (→`newLit(hi-lo+1)`) so `monomorphize` rewrites `array[N,int]`→`array[3,int]`; `instKeyFor` includes the bound value in the type-tuple, and for a SCALAR static param (empty typeSubst) appends `#<bodyHash>#static` to force a per-instantiation-distinct, non-bare key (avoiding the G1a bare-name collision). `parseCalleeImpl` single-expr-RHS path now wraps a lifted preamble (array index) instead of asserting it away. **Typebridge:** `classifyType` matches a monomorphized synthesized `array[<IntLit>, T]` on the RAW node BEFORE `getTypeInst` ("node has no type" otherwise). **Runtime (`runtime.nim`):** `coerceToBoolSV` + a widened bool `==`/`!=` arm coerce a `static bool` literal that arrives as `IntLit 0/1` (`value != 0`) so `(pred) == B` compares bool-to-bool. **REAL key shape vs RFC:** RFC §G7's idealized `"foo#int;static=3"` is NOT the real key (the real key carries a per-instantiation `bodyHash`: `name#<bodyHash>#N=3` for array dims, `name#<bodyHash>#static` for scalars) — the TEST asserts BEHAVIOR (distinct dispatch + per-instantiation literal: `x[2]` vs `x[4]`), not the string. No `cacheHitsFor` accessor exists; distinctness asserted via behavior. g7 2/2 c+cpp. Regression 9/9 (G1a_instkey, G1c_instcap, g3_type_subst, g4_distinct_sort, g6_concept_constraint, rectify_generics, phase4_tuple, phase4_array, phase1_arith). Walker version stays "7" (Cluster G bumps at G10). **Next: G8 (multi-parameter generics).** |
| G | G8 | multi-parameter generics | SHIPPED | Multi-parameter generics (`proc foo[T, U](a: T, b: U)`). **Mostly AUDIT + REGRESSION-GUARD (per RFC §G8).** `gatherTypeSubst` already iterates the formal params binding ONE entry per generic-named param from the matching call-site arg's `getType` (`argIx` advances once per formal name — NO conflation, NO off-by-one), and G1a's `instKeyFor` sorts the concrete-type tuple BY formal-param NAME (order-independent), so multi-param ALREADY worked end-to-end. The RED test pins it: (1) `foo[T,U]` at `T=int,U=string` → `a > 0` (int arith) AND `b == "ok"` (string eq) resolve INDEPENDENTLY in one proc, witness `(1, "ok")`; (2) `bar[T,U]` at `T=bool,U=int` → witness `(true, 6)` (no param-order dependency); (3) the SAME `pick[T,U]` instantiated at reversed tuples (`int,string` vs `string,int`) dispatches to CORRECTLY-typed bodies simultaneously — the G1a sorted key keeps the two tuples DISTINCT (no collapse). **BUG FIXED (surfaced by the RED test, orthogonal to multi-param):** a call whose FIRST arg is an `itString` was unconditionally claimed by the Cluster-S `itString`-receiver call-routing guard (`dsl_parser.nim`), so an ordinary USER PROC whose first parameter is `string` (`proc foo(a: string, …)`) was mis-classified `seUnsupportedStringOp` → sxUnknown. Fix: when the string-receiver model is `smkUnregistered` AND the callee resolves to a real user `nnkProcDef`, fall THROUGH to the user-proc call path instead of emitting `iekStrUnsupported` (a genuinely-unsupported stdlib string call with no user impl still routes to `iekStrUnsupported`, Invariant 3). No structural change to the generic/multi-param machinery itself. g8 3/3 c+cpp. Regression 10/10 (G1a_instkey, G1c_instcap, g3_type_subst, g4_distinct_sort, g6_concept_constraint, g7_static_param, rectify_generics, S3_strindex, phase1_arith, phase1_bool) + extra S-cluster (S1/S4/S5) all green. Walker version stays "7" (Cluster G bumps at G10). **Next: G10 (Cluster G regression smoke vs E + walker version bump 7→8; G9 folded into G1c).** |
| G | G9 | ~~concept stdlib table~~ | folded | →G1c |
| G | G10 | regression smoke vs E; walker `"7"→"8"` | SHIPPED | **Cluster G COMPLETE.** Hermetic in-process G-cluster smoke (`tests/tsymex_phase15_g10_smoke.nim`, 7/7 c+cpp) composing the G1a-G8 machinery TOGETHER to catch state-threading bugs from the multi-file edits: a multi-param generic (G8) at int+string with T/U independent; the SAME `szof[T]=sizeof(T)` at int8+int64 in one SUT (G1a collision fix); a `distinct float64` with a borrowed `+`/`<` (G4+G5); a `static[int]` `array[N,int]` generic at N=3+N=5 (G7); a `T: SomeNumber` concept-constrained generic at int + the `conformsToStdlibConcept` membership table (G6); the instantiation cap via `withSymexSettings(maxInstantiationsPerProc=2)` → `geInstantiationCapped`/sxUnknown (G1c). Composition smoke found NO state-threading regression. **Walker version bumped `"7"→"8"`** single-sourced in `canonicalize.nim:symexWalkerVersion` (Invariant 6 — confirmed no duplicate); the bump orphans every `"7"`-era cache key so the broad regression re-solves. Four prior version-pin tests (F8_smoke, S7b_smoke, S11_mutation, E7_smoke — all pinned `"7"`) advanced to `"8"`. Broad regression sweep all green c, no hangs: ALL G tests (G1a_instkey, G1c_instcap, g3-g8), rectify_generics, Cluster E (E3_try, E7_smoke), Cluster S (S3_strindex, S5_strops, S9_caseconv, S11_mutation), Cluster F (F2, F6, F8_smoke), H1, earlier phases (phase1_arith, phase3_recursion, phase4_tuple, phase4_array, phase11_walker, phase13_unsat_roundtrip). No production-code change beyond the bump. **Next: C0-ADR (Cluster C — closures).** |

## Cluster C — closures and procs-as-values

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| C | C0-ADR | author ADR-0009-closure-encoding.md + closures.md skeleton | SHIPPED | |
| C | C1 | IR + parser: iekLambda, iekClosureCall, svClosure stub | SHIPPED | Net-new `iekLambda`/`iekClosureCall` IRExprKind (+`iek`/`is`/`it` prefix comment block, M2) + node shapes/ctors (`mkLambda`/`mkClosureCall`)/render/canonicalize/emitExpr; `svClosure` SVKind stub `(closureSite, closureEnv: ref svTuple)`. Parser: `nnkLambda`/expr-position `nnkProcDef`/`nnkFuncDef` → `iekLambda` (free-var capture by scope-stack diff — value-symbol refs `{nskParam,nskLet,nskVar,nskForVar}` minus params++body-locals; body at `body(n)` routine index); proc-valued-VARIABLE call (`getImpl` not a routine def + `getTypeInst==nnkProcTy`) → `iekClosureCall`; `nnkIteratorDef` → ceNotImplemented stub (closure iterators deferred); proc-typed `let` binding placeholder-typed. `symBodyHash`-on-lambda **does NOT apply** (lambda is a nameless NODE, not a proc symbol) → **lineInfo fallback** (`file:line:col` hash) per ADR-0008 D2 / ADR-0009 D3 caveat; `declOrder` from `ParseCtx.lambdaCounter`. Walker STUBS both via `SymexClosureUnimplementedError` → caught at runSymex boundary → `ceNotImplemented`/sevError/sxUnknown (Invariant 3, no semantics). Net-new `sortOfTuple` (flattened per-leaf Z3 sorts of env svTuple, D5) + `c1ClosurePoCApply` Z3_mk_app PoC (declare funcSym over runtime sorts + apply heap-seq args — round-trips, de-risks C2b). Dual exhaustiveness ripple: IRExprKind = 6 arms (render, canonicalize, emitExpr, probeProto, lower-stub, abstraction tryEvalInterval+collectVarRefs); SVKind = 5 arms (tyOf, iteSV, coerceIntLit, extractLeaf, symValHash). `tests/tsymex_phase15_C1_ir.nim` 7/7 c+cpp; regression 11 files green (phase1_arith/let, phase3_recursion, phase4_tuple, phase5_seq, phase11_walker, g4_distinct_sort, E3_try, S11_mutation, F8_smoke, g10_smoke) no HANG. Walker version stays "8" (Cluster C bumps at C6). **Next: C2a.** |
| C | C2a | walker: closure construction (env snapshot) | SHIPPED | `lower(iekLambda)` now CONSTRUCTS the `svClosure` (replacing the C1 `ceNotImplemented` stub): (1) snapshots the captured locals — looks each `lambdaCaptures` name up in the CURRENT env, collects the SymVals → an `svTuple` `closureEnv` (env-snapshot record; **no body descent**); (2) get-or-creates the per-site uninterpreted `funcSym` over runtime-known sorts (domain = flattened env-leaf sorts via `sortOfTuple` ++ param sorts; range = `lambdaRetTy` sort) via raw `Z3_mk_func_decl` + `incRefFD` (G4/C1 raw-FFI discipline, heap domain seq), MEMOIZED in the **net-new `currentClosureSyms` threadvar** (key `ClosureSymKey = ((siteHash, declOrder), envSortId, paramsSortTupleId)`, the env/param sort fingerprints via `Z3_get_sort_id` so a re-monomorphized same-site closure gets a distinct funcSym) — the threadvar (not a WalkerStatics field directly) because `lower` has no `WalkCtx`, mirroring the G4 `currentDistinctSorts` idiom; mirrored into the **net-new `WalkerStatics.closureSyms`** field after the walk; (3) assembles `svClosure{closureSite, closureEnv, closureRawFD}`. **Net-new `svClosure.closureRawFD: RawZ3FuncDecl`** field (the per-site funcSym handle; nil in the C1 stub). `extractFromSymVal(svClosure)` → classified `ceNotImplemented`/sevError (closure as a top-level SUT RESULT is unsupported — classified, not silent; Invariant 3). `symex.nim emitTyAndReader`: a proc/closure param/result type (`classifyType(nnkProcTy)` → `tUninterp("__closure")`) renders a `proc` placeholder + a compile-time `{.warning.}` (Invariant 3). **`iekClosureCall` arm STAYS `ceNotImplemented`** (C2b wires application). `tests/tsymex_phase15_C2a_closure_capture.nim` 3/3 c+cpp (via the `c2aClosureProbe`/`c2aClosureProbeRelowered` test hooks — they lower an `iekLambda` against a hand-built env at the `let f = …` binding point and return an introspectable probe of the constructed `svClosure`, keeping `Env`/`lower`/`symValHash` encapsulated). Regression 10 files green (C1_ir, phase4_tuple, phase1_let, phase1_arith, phase3_recursion, g4_distinct_sort, E3_try, S11_mutation, F8_smoke, g10_smoke) no HANG. Walker version stays "8" (Cluster C bumps at C6). **Next: C2b.** |
| C | C2b | walker: closure call dispatch + multi-return-path axiom | pending | |
| C | C3 | top-level procs as values (unit-env encoding) | pending | |
| C | C4 | DSL HOFs: filter/map/fold over seq[T] | pending | |
| C | C5 | closure equality (nominal-for-site + structural-for-env) | pending | |
| C | C6 | regression smoke vs G; walker `"8"→"9"` | pending | |

## Cluster R — ref/ptr aliasing (logical heap)

| cluster | cycle | title | status | commit |
|---------|-------|-------|--------|--------|
| R | R1a | IR + SVKind variants + exhaustive dispatch stubs | pending | |
| R | R1 | ref sort introduction — refSorts, nilConsts, allocRefSort, isDeref | pending | |
| R | R1b | inter-procedural heap threading | pending | |
| R | R2 | new T semantics — allocCounters, freshness cap | pending | |
| R | R3 | p[] read — select(heap_T, p) | pending | |
| R | R4 | p[] = v write — store(heap_T, p, v) | pending | |
| R | R5 | nil handling — nil_T, nil-fork → sxRaised(NilAccessDefect) | pending | |
| R | R6 | ref object field access | pending | |
| R | R7 | ref equality + alias chain | pending | |
| R | R8 | ptr T family + pointer arithmetic (hePtrArith) | pending | |
| R | R8b | var ref T param handling | pending | |
| R | R9 | recursive ref structures — heapDepth; heDepthExhausted | pending | |
| R | R10 | maxHeapDepth setting — cache-key participation | pending | |
| R | R11 | cast[ptr T](addr x) — sxUnknown(heUnsafeCast) | pending | |
| R | R11b | cross-cluster regression sweep + witness-format-v3.md | pending | |
| R | R12 | walker `"9"→"10"`, rendering `"2"→"3"`, heap-snapshot witness | pending | |
| R | R13 | closures capturing ref T; ptr T + try/finally composition | pending | |

## Documentation index

ADRs and design docs introduced or referenced by Phase 15 (closes Des-LOW-L2):

- `RFC-phase15-language-fragments.md` — the RFC (design source of truth).
- `RFC-phase15-reconciliation.md` — **authoritative override layer** (read first).
- `ADR-0005-float-nan-inf.md` — float NaN/Inf semantics (on disk; F0-ADR).
- `ADR-0006-string-codepoint-indexing.md` — **byte-faithful** string model
  (S0-ADR; filename historical — title is now byte-faithful, Corey-locked).
- `ADR-0007-exception-flow.md` — exception flow model (Z4).
- `ADR-0008-generic-instantiation.md` — generic instantiation (on disk; G0-ADR).
- `ADR-0009-closure-encoding.md` + `closures.md` — closure encoding (C0-ADR).
- `ADR-0010-logical-heap.md` — logical heap model (H1).
- `witness-format-v3.md` — heap-snapshot witness format (R11b).
- `determinism.md` — updated by S7b, S11, R10 (string/heap determinism notes).
