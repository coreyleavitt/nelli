# Enables the per-query Z3 statistics instrumentation this probe reads.
# Nim auto-reads <testfile>.nim.cfg, so this define is active under the
# standard `scripts/dt.sh`/`scripts/dt-bounded.sh` harness with no extra
# arguments, on both backends.
-d:symexQueryStats
