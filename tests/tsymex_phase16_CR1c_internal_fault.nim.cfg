# CR-1c fault injection (RFC-chapulin-hardening, Cluster 2 — Crash-totality).
# Nim auto-reads `<testfile>.nim.cfg`, so this define is active for BOTH
# backends under the standard `scripts/dt-bounded.sh <c|cpp> ...` harness with
# NO changes to that script. It is compiled OUT of every other test/build —
# zero production-path overhead — because no other file's `.nim.cfg` sets it.
-d:symexTestInjectWalkerFault
