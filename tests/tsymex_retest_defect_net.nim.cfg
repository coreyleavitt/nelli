# Enables the CR-1c/round-3 synthetic fault-injection hook for THIS file
# only (Nim auto-reads <testfile>.nim.cfg) — the `__inject_walker_defect__`
# sentinel raises an AssertionDefect to exercise the outermost Defect net.
-d:symexTestInjectWalkerFault
