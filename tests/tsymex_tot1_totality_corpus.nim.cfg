# TOT-1 fault injection (RFC-chapulin-hardening, §Totality harness).
# One corpus row (Surface 3 / CR-1c) exercises the last-resort `runSymex`
# internal-fault catch via the same synthetic injection hook as
# `tsymex_phase16_CR1c_internal_fault.nim`. Nim auto-reads `<testfile>.nim.cfg`,
# so this define is active for BOTH backends under the standard
# `scripts/dt-bounded.sh <c|cpp> ...` harness with no script changes, and is
# compiled OUT of every other test/build.
-d:symexTestInjectWalkerFault
